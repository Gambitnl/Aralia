Original prompt: Set a goal to make the start of the game playable. Think of core Concepts that should work first and the set the goal to run the game and try

## 2026-08-13 Tactical Sandbox CS12 Elevation & Range canonical 3D contract

- Replaced the presentation-only fixture with selectors for 0/10/20 ft elevation, exact 30 ft versus 35 ft range, finite-height line-of-sight blockers, open/blocked ascent, controlled descent, stable Resolve/Replay, and native Reset.
- Added one shared elevation ruler used by ordinary target validation, flying/grounded sight origins, A* path cost, reachable movement, and final path payment. Horizontal travel plus real vertical feet is canonical; no high-ground attack bonus was added.
- Expected-first focused failures were captured, then the production gate passed 54/54 and the combined adapter/engine gate passed 67/67. No broad check, Git lifecycle, or server lifecycle action was performed.
- Controlled ascent and descent each cost 15 ft for one horizontal square plus a 10 ft height change. Falls and loss of aerial support remain owned by their existing production impact path rather than this walking fixture.
- The single retained-host attempt targeted `127.0.0.1:5201` with the official game client, but PowerShell rejected the composed command at its quote terminator before navigation. The no-retry bound was honored, so fresh rendered 2D/3D, camera, control, and console-delta proof remains unclaimed.

## 2026-08-13 Tactical Sandbox CS10 Conditions remediation - source-owned paired lifecycle (partial rendered closeout)

- Replaced the four independent condition toggles with one expected-first lifecycle surface: Owned Condition, Ownership / Lifecycle Case, Resolve Condition Event, and Replay Same Condition Event. Reset restores both actors, fresh economy, no scenario-owned pair, and one unrelated Poisoned pair that every event must preserve.
- The shared paired-condition helper now distinguishes same-name effects by source/spell and source actor. Independent owners stack; one owner replaces only itself; exact removal and explicit source-loss cleanup preserve other owners; identical replay returns the original character; target turn-end expiry removes only the expiring owned badge and condition while refreshing movement once.
- The production turn manager keeps its established public expiry function but delegates the paired mutation to the source-aware helper. Conditions controls expose Restrained movement 0, Prone crawling through the real grid hook, and Incapacitated Action/Reaction blocking through production affordability checks.
- Focused runtime, adapter, registry, mounted-host, repeat-save, shove, and turn-manager proof passed. The final combined focused gate passed 4 files including the full mounted host; the narrower canonical-neighbor gate passed 28/28 across 5 files.
- The exactly-one retained-host attempt used the untouched server at `127.0.0.1:5201`. Conditions mounted, but the proof locator `/Owned Condition/i` matched both select boxes and strict-mode stopped before 2D capture, control execution, 3D launch, camera gesture, or console delta. The bound forbade retry, so fresh rendered acceptance remains unproven and this closeout is PARTIAL rather than PASS.
- No branch, worktree, stage, commit, push, cleanup, or server lifecycle action was performed. Disposable proof tooling remains ignored under `.agent/scratch/cs10-conditions-proof/`.

## 2026-08-13 Tactical Sandbox CS37 remediation - atomic turns and forced movement (complete checkpoint)

- Repaired the partial CS37 follow-up transaction. A compelled attack now validates the live turn owner and the target's Action before constructing RNG or replacing actors; the first legal attack spends that Action, and an immediate repeat rejects with `ROLL: not made` and unchanged state. Compelled Duel application likewise rejects off-turn before Bonus Action, slot, save, or effect work.
- Forced movement now runs through the production `MovementCommand` as a source-owned five-foot effect. It validates exact distance, bounds, complete-footprint blocking, and occupancy before mutation; legal movement changes only the compelled target from `8,5` to `9,5`, while its Action and movement remain ready and `0/30`. Willing movement separately uses the canonical taunt leash and preserves the target's movement on rejection.
- The same four-control surface now exposes attacks, willing movement, legal/blocked/off-board/occupied/too-far forced movement, missing/downed/Incapacitated sources, expiry, exact removal, and Reset. Live 2D/3D facts and logs show turn owner, position, Action, movement, roll/no-roll, restriction state, and rejection reason.
- Final focused runtime/control/catalog/mounted proof passes 57/57; the cumulative registered-control plus mounted-host gate passes 341/341 across 41 files. Dependency synchronization succeeded for the three indexed source paths; test paths are not indexed. Scoped ESLint has zero errors and fourteen preserved host warnings. Broad TSC remains red on shared-tree debt, with no diagnostic in the CS37 control or its tests after the local inference repair.
- Reused the untouched responsive Vite server at `127.0.0.1:5201`. The official game client mounted CS37 in 2D with no error file. Inspected ignored captures prove the live forced-move result and the reset 3D board; zoom, orbit, and pan produced four distinct canvas hashes, and console/page error delta is zero. Disposable evidence remains under ignored `.agent/scratch/cs37-remediation-proof/`.
- No real CS37 cross-scenario boundary remains, so `GLOBAL_GAPS.md` was not changed and `GG-58` was not reused. Agora used exact child session `019ff429-12de-7690-887d-a79a8c279a06`, handle `cs37-remediation-019ff429`, and Pixel Coder. No branch, worktree, stage, commit, push, cleanup, deletion, or server start/restart/stop was performed; lock release and retirement follow this receipt.

## 2026-08-13 Tactical Sandbox CS36 remediation - production Hellish Rebuke event bridge (complete checkpoint)

- Closed `GG-55`. `DamageCommand` now emits one typed, stable-id post-HP event after applying the triggering hit exactly once. The production ability system claims the id before prompting, discovers owned Hellish Rebuke, presents the canonical accept/decline modal, and resolves derived damage through a depth-first queue so nested or competing reactions have deterministic order.
- The resolver observes but never reapplies triggering damage. It preserves post-hit damage/down/incapacitation eligibility, sixty-foot range, line of sight, exact Reaction plus spell-slot ownership, canonical Dexterity full/half damage, Fire resistance/immunity, and attacker downing. Re-delivering the same event id is an explicit no-op with no prompt, HP change, or resource spend.
- Reworked the registered CS36 adapter around four visible controls plus Reset: Trigger Case, Retaliation Result, Resolve Attack, and Replay Stable Event. Accept, decline, save success/half, empty slot, out-of-range, Total Cover, duplicate delivery, resource/HP changes, and exact logs are mounted against the normal attack/damage path rather than a sandbox-only result transaction.
- Focused production proof passes 37/37. The registered adapter/registry/catalog/full mounted gate passes 35/35, including all nineteen mounted Tactical Sandbox cases and CS36 3D launch. Dependency synchronization succeeded for all touched exported runtime/control paths. Scoped ESLint reports zero errors with established warnings; broad TSC remains red on preserved shared-tree debt and reported no CS36-specific diagnostic.
- The single bounded retained-server probe found both approved ports unavailable (`5198` and `5197` returned HTTP `000`). No server was started, restarted, stopped, or retried. Therefore this remediation build has no fresh 2D/3D screenshot, camera-interaction, or console-error-delta claim; the precise remaining proof boundary is browser-only, while source, focused, registered, and mounted acceptance are green.
- Agora used exact child session `019ff3fe-6b83-7e91-ab41-4888e2fafc43`, handle `cs36-remediation`, and Violet Mage with no lock conflict. No branch, worktree, stage, commit, push, cleanup, deletion, or server mutation was performed; scoped review, lock release, and retirement follow this receipt.

## 2026-08-13 Tactical Sandbox CS35 remediation - canonical scheduled damage and saves (complete checkpoint)

- Closed `GG-54`. Scheduled and recurring damage now uses the canonical resistance/immunity, temporary-HP, downing, and repeat-save transaction instead of direct HP subtraction. Searing Smite deals its authored Fire damage first, then resolves the captured Constitution save: success removes only its owned schedule and source-linked status/condition/active-effect mirrors; failure preserves them.
- Made schedule processing deterministic and idempotent per schedule/round/phase, preserved authored order on duplicate live-record publication, retained one-shot Acid timing, pruned missing targets while preserving non-concentration source loss, and enforced the exclusive ten-round Searing Smite boundary. Retained-host proof exposed and this lane repaired one same-transaction stale-roster edge where round cleanup could restore pre-Acid HP.
- Replaced the old unsupported-boundary control with deterministic defense, save-outcome, and lifecycle selectors plus Acid-only removal and Reset. The mounted 2D/3D readout now exposes temporary HP, defenses, save mode/DC, source presence, owner/target, round/actor, trigger, damage, expiry, HP, and logs from live engine state.
- Focused production/adapter/turn-manager proof passes 26/26 after the final scheduler repair. The cumulative scenario-control, factory, registry, catalog, scheduler, turn-manager, and mounted-host gate passes 371/371 across 44 files. Dependency synchronization succeeded for every edited exported path; scoped ESLint reports zero errors. The bounded typecheck delta remains red on 44 preserved shared-tree diagnostics and reports no CS35 engine, adapter, or test diagnostic.
- Reused the untouched responsive retained host at `127.0.0.1:3000`. The unmodified official develop-web-game client mounted the registered scenario and emitted no error file. One corrected bounded supplemental pass proved normal Fire then once-only Acid, resistance, immunity, temporary HP, lethal downing, save success/failure, natural expiry without an extra tick, source preservation, target pruning, Acid-only removal, repeated Reset, and 3D zoom/orbit/pan. Inspected 2D and 3D captures are legible; console, page, and request error delta is zero. Disposable proof remains only under ignored `.agent/scratch/cs35-gg54-fix-proof/`.
- Agora used exact child session `019ff3d0-155e-7980-9a87-d9acd671a3e5`, handle `codex-cs35-fix-019ff3d0`, and Cyberman. MemPalace automatically quarantined one stale HNSW segment and the single narrow retry succeeded; the quarantine was preserved and no mine or broad refresh was run. No branch, worktree, stage, commit, push, cleanup, deletion, or server start/restart/stop was performed; lock release and retirement follow this receipt.

## 2026-08-13 Tactical Sandbox CS34 remediation - production group turns (complete checkpoint)

- Closed `GG-53` with an explicit production group-turn contract. Shared-policy summons now join their caster's deterministic initiative group; one active member advances through authored member order before the group completes, while ordinary actors retain singleton groups and their existing turn behavior.
- The group owns scheduling only. Action, movement, Reaction, and start/end effects remain member-owned. Incapacitated living members retain their timing boundary, missing/dead members are skipped, active removal advances once without reinitializing combat, and repeated End Turn/removal requests are idempotent.
- Clarified throughout code, rules text, controls, and tests that Dexterity, initiative bonus, then authored order is Aralia's deterministic house tie policy, not a canonical 5e tie-break ladder. Alternate input reorders the tied singleton/group slots deterministically, and repeated Reset restores the authored captain-first group.
- Focused utility/hook/control/tracker/mounted proof passes 47/47. The cumulative Tactical Sandbox and canonical-neighbor gate passes 351/351 assertions across 46 files. Dependency synchronization succeeded for all changed exported runtime and preview paths, and scoped whitespace checking is clean.
- The unmodified official develop-web-game client and one final corrected supplemental run used the retained untouched server at `127.0.0.1:5201`. Ignored 1600-by-1000 proof covers baseline group truth, Incapacitated member economy/effect timing, group completion, active removal, repeat no-op, house-policy reorder, repeated Reset, and 3D after zoom/orbit/pan. Representative 2D and 3D captures were opened and inspected; controls, group/member labels, rules, actors, resources, and logs remain legible. Console/page errors are 0 baseline, 0 total, and 0 post-interaction delta; 61 established spell-data/software-WebGL warnings remain, including tracked `GG-51`.
- Disposable proof lives only under ignored `.agent/scratch/cs34-gg53-remediation-proof/`. Agora used exact child session `019ff3a9-1cc1-7e60-88ce-86b9f2eda074`, handle `codex-initiative-ties-16`, and Mechanical Maze Knight. No branch, worktree, stage, commit, push, cleanup, deletion, or server change was performed; final scoped task completion, lock release, and retirement follow this receipt.

## 2026-08-13 Tactical Sandbox CS38 Companion Reactions production remediation

- Closed `GG-56` in the ordinary damage transaction. `DamageCommand` now opens a pre-HP allied-protector reaction window, uses stable per-target hit/claim ids, discovers every eligible owned companion, orders responders by initiative then actor id, supports explicit decline or exact selection, and reduces damage before HP. Duplicate delivery cannot prompt, reduce HP, or spend a Reaction twice.
- Canonical Interception remains in the narrow production resolver: distinct allied target, hostile attacker, positive hit damage, 5-foot range, Interception feature, weapon or shield, non-Incapacitated protector, ready Reaction, and protector-to-attacker line of sight. Revalidation occurs at payment. Only the selected protector spends its Reaction; the owner, protected ally, and unselected protector retain theirs, and only that protector's own turn restores it.
- CS38 now mounts owner, two owned companions, protected ally, and hostile attacker. Four scenario controls plus native Reset expose qualifying and duplicate hits, every rejection gate, accept/decline, deterministic secondary selection, normal damage delivery, actor-local reset, ownership, qualification/rejection reason, HP delta, stable claim behavior, and all independent Reaction ledgers in 2D and 3D.
- Focused production/control proof passes 41/41. The cumulative Tactical Sandbox/combat gate passes 393/393 across 143 suites. Catalog/registry pass 5/5, and the exact mounted CS38 host test passes; in the combined 19-test host file, only the unrelated Falling and Flying tests hit their existing 5-second timeout. Dependency synchronization succeeded for every touched exported command/reaction/control path. Scoped ESLint has zero errors and seven established command-file warnings. The bounded typecheck-delta command remained silent and was terminated without a result; no broad debt was pursued.
- The retained untouched port-5201 server returned fresh CS38 host/catalog modules. The unmodified official game client produced mounted 2D text state and a readable control capture with no emitted error artifact. Inspected ignored supplemental captures prove primary acceptance (`30 -> 25`, owner ready, primary spent, secondary ready), explicit secondary selection (`30 -> 25`, only secondary spent), independent live readouts, Reset, and 3D launch with its rule/readout UI legible. The 3D board itself rendered black in the captured page, and the bounded command was stopped before zoom/orbit/pan frames or `proof.json`; therefore camera operation, 3D scene readability, and the supplemental console/page-error delta are explicitly unproven and were not retried.
- Disposable proof is confined to confirmed-ignored `.agent/scratch/companion-reactions-remediation-proof/`. No new product gap was added. Agora used exact session `019ff443-8f1c-7d13-bd49-22255c40cd4b`, handle `cs38-remediate-22255c40cd4b`, and Pixel Coder. The requested host/catalog locks were released early for the sequential Rules lane; all remaining locks are released and the presence retired after this receipt. No branch, worktree, stage, commit, push, cleanup, deletion, or server start/restart/stop was performed.

## 2026-08-12 Tactical Sandbox owner 20 - Companion Reactions (complete checkpoint)

- Registered `companion_reactions` as scenario 38 with a deterministic Ranger Owner, owned Guardian Companion, Protected Ally, and Hostile Attacker. The shared 2D/3D readout exposes summon ownership and shared-initiative policy, canonical Interception facts, qualification or rejection, incoming/prevented/applied damage, ally HP, and both actors' independent Reaction ledgers.
- Added a narrow production companion-protection transaction because ordinary damage resolution cannot yet discover allied responders. It reads canonical Interception feat data, verifies four distinct actors, `summonMetadata.casterId` ownership, allegiance, range, line of sight, incapacitation, shield/weapon equipment, and the companion's Reaction before payment; then it spends only that Reaction, rolls `1d10 + proficiency bonus`, and applies the remainder through the shared HP/downed resolver.
- Added four controls plus Reset Board: Reaction Case selects qualifying, beyond 5 feet, Incapacitated, Reaction spent, or non-hostile; Resolve Attack + Interception executes a deterministic 14 Slashing hit with a fixed 6 + proficiency 3 reduction; Start Owner Turn resets only the owner's economy; Start Companion Turn restores only the companion's Reaction. The legal case changes ally HP 30 -> 25 and leaves the owner's Reaction ready; every invalid case rejects before HP change or new payment.
- Focused production/control/mounted proof passes 29/29 assertions. Catalog/registry/host proof passes 19/19. The cumulative Tactical Sandbox and canonical-neighbor gate passes 302/302 assertions across 46 files. Dependency synchronization succeeded for all touched exported runtime/control/registry/catalog/host paths. Scoped ESLint reports zero errors and fourteen established warnings in the shared host; typecheck debt remains 55 diagnostics before and after, with only six line-shifted legacy spell-slot diagnostics and no CS38 resolver/control diagnostic. Scoped whitespace checking is clean.
- A bounded fresh verification server at port 5201 was necessary because retained servers did not contain CS38; its live catalog module contains `companion_reactions`, and it remains running. The unmodified official develop-web-game client mounted CS38 at 1600 by 1000, published canonical four-actor 2D state, and emitted no error file. Supplemental ignored proof covers the legal transaction, every rejection, owner-only and companion-only reset, Reset Board, and 3D before/after zoom, orbit, and pan. Every representative capture was opened and inspected; actors, terrain, labels, controls, rule facts, HP, and Reaction state remain readable. Canvas hashes differ after each camera operation. Console/page errors are 0 baseline, 0 total, and 0 post-interaction delta; 89 established spell-data/software-WebGL warnings remain, including the tracked `GG-51` renderer warning.
- Routed exactly one genuine boundary as `GG-56`: the ordinary damage pipeline still needs allied-responder discovery, player choice, and deterministic ordering before it can offer the tested Interception transaction. The scenario reports that boundary and does not fabricate automatic UI-only integration. Disposable captures live only under ignored `.agent/scratch/companion-reactions-proof/`; `git check-ignore` was verified before capture. Agora used exact child session `019ff443-8f1c-7d13-bd49-22255c40cd4b` with handle `cs38-22255c40cd4b` and Black Knightling; final lock release and retirement follow this receipt. No branch, worktree, stage, commit, push, cleanup, deletion, or server stop was performed.

## 2026-08-12 Tactical Sandbox owner 19 - Taunt & Forced Targeting (complete checkpoint)

- Registered `taunt_forced_targeting` as scenario 37 with a deterministic Challenge Knight, Goaded Raider, Protected Ally, thirty-foot Compelled Duel lane, alternate-target lane, and one authored sight blocker. The shared 2D/3D readout exposes canonical Bonus Action, level-1 slot, concentration, Wisdom save, range, sight, hostility gate, condition source/duration, attack eligibility, and reasoned logs.
- Reused canonical Compelled Duel data and the existing production taunt contract for willing-movement leashes, authored break events, weapon attacks, and spell attacks. Added one narrow lifecycle repair: expired markers and missing, downed, Unconscious, or Incapacitated sources now stop attack/movement penalties immediately and reconcile only the matching taunt/concentration record.
- Added four controls plus Reset Board: target case selects failed save, successful save, forced-target immunity, out of range, Total Cover, or non-hostile; Attempt Compelled Taunt validates before payment and then resolves the canonical save; attack/cleanup case selects the taunter, another creature, source downing, expiry, or exact removal; Resolve Follow-up executes the live modifier or cleanup. A legal failed save spends Bonus Action and slot, attacks against the taunter remain normal, attacks against others use deterministic disadvantage, a successful save spends the valid cast without an effect, and invalid gates spend nothing.
- Focused runtime/control/catalog/registry proof passes 30/30 assertions. The mounted host acceptance passes application, normal and disadvantaged attacks, Total Cover rejection, source-down cleanup, Reset, and 3D launch. The cumulative Tactical Sandbox campaign passes 279/279 assertions across 41 files. A broader factory sample passed 47/52; its five failures are pre-existing hit-path fixtures missing `turnState.currentTurn`, outside CS37, while the spell-attack and taunt utility paths pass 44/44.
- Dependency synchronization succeeded for all eight edited indexed source files. Scoped ESLint reports zero errors and seventeen established warnings. TSD passes. Scoped whitespace checking is clean, and filtered TSC output contains no CS37 scenario, taunt constraint, or new host-slot diagnostic; broad TSC remains red on preserved cumulative shared debt.
- Design Preview step files are intentionally ignored by the repository's existing `.gitignore` rule and were inspected explicitly. No proof capture or server was created because the dispatcher accepted the focused, mounted, and cumulative evidence and requested immediate sequential handoff. No console/error-delta claim is made beyond the mounted test evidence.
- No new product or cross-project gap remains in CS37: the hard targeting penalty already existed, and this lane closed its source-loss and expiry boundary rather than recording a UI-only substitute. Agora used exact child session `019ff429-12de-7690-887d-a79a8c279a06` with handle `cs37-019ff429` and Pixel Coder. No branch, worktree, stage, commit, push, cleanup, deletion, capture, or server action was performed; final lock release and retirement follow this receipt.

## 2026-08-12 Tactical Sandbox owner 18 - Reactive Damage & Retaliation (complete checkpoint)

- Registered `reactive_damage_retaliation` as scenario 36 without altering the seventeen existing Core Rules scenarios. The mounted sixteen-by-twelve board exposes a Blade Initiate attacker, Infernal Adept retaliator, sixty-foot reaction lane, sight wall, HP, Reaction, level-1 slot, Fire defenses, exact ordering, and logs in both renderers.
- Added a narrow production Hellish Rebuke resolver because canonical spell data exists but the combat runtime does not yet turn resolved damage into a reaction-spell opportunity. It applies triggering damage first, rejects duplicate event ids, checks post-hit down/incapacitation plus range and line of sight, pays the shared Reaction/slot ledger, then uses canonical save, scaling, damage-defense, HP, and downing helpers.
- Added three controls plus Reset Board: Trigger Case covers hit, miss, no damage, beyond range, Total Cover, spent Reaction, Incapacitated, and hit-downing; Attacker Defense covers normal, Fire Resistance, Fire Immunity, and fragile HP; Resolve Hit + Rebuke executes and replays one event id to prove once-only delivery.
- Focused production/control/host proof passes 30/30 assertions across three files; the mounted registry/catalog/search selection passes 36/36 across five files. The cumulative 43-file campaign and canonical-neighbor run passes 299/300 assertions: every CS36 and cumulative scenario assertion is green, while one pre-existing `useActionExecutor.reactiveDamage` mock still expects the older four-argument `handleDamage` call instead of the runtime's five arguments. That neighboring file was preserved outside this lock.
- Dependency synchronization succeeded for every edited indexed source file. Scoped ESLint reports zero errors with seven established ignored-file warnings, and scoped whitespace checking is clean. Broad TSC remains red on shared pre-existing diagnostics, but its filtered output contains no CS36 file or edited-host diagnostic.
- The unmodified official develop-web-game client mounted the scenario, published canonical 2D state through `render_game_to_text`, and emitted no error file. The retained 5197/5198 verification servers were responsive but held a stale 35-scenario no-watch graph, so they were preserved untouched and a bounded fresh no-watch server at 5199 supplied the 36th-scenario proof; no server was stopped.
- Ignored 1600-by-1000 rendered proof covers baseline, accepted response and duplicate suppression, miss, zero damage, out-of-range, Total Cover, spent Reaction, Incapacitated, hit-downing, Fire Resistance, Fire Immunity, Reset, attacker downing, and 3D. Representative captures were opened and inspected. Zoom, orbit, and pan each produced a distinct canvas hash while the same HP/resource/log state remained visible; console/page errors are 0 baseline, 0 total, and 0 post-interaction delta.
- Routed exactly one honest boundary as `GG-55`: the damage pipeline still needs a stable resolved-event-to-reaction-choice bridge and a nested-reaction ordering contract before ordinary combat can offer Hellish Rebuke. The scenario labels that boundary and calls only the tested production transaction, so no automatic-retaliation UI truth is fabricated.
- Disposable proof lives only under ignored `.agent/scratch/reactive-damage-retaliation-proof/`. Agora used exact child session `019ff3fe-6b83-7e91-ab41-4888e2fafc43` with handle `cs36-reactive-damage` and Violet Mage. No branch, worktree, stage, commit, push, cleanup, deletion, or server stop was performed; final lock release and retirement follow this receipt.

## 2026-08-12 Tactical Sandbox owner 17 - Damage Over Time & Scheduled Effects (complete checkpoint)

- Registered `damage_over_time_scheduled_effects` as scenario 35 and mounted a deterministic sixteen-by-twelve board with an Ember Arcanist schedule owner and a 60 HP Marked Guard target. The live strip and `render_game_to_text` expose round, active actor, owner/source/target, authored queue order, next trigger, dice and damage type, frequency, expiry, target HP, and recent combat logs in both 2D and 3D.
- Reused canonical Searing Smite recurring status data for 1d6 Fire at the start of the target's turn and canonical Melf's Acid Arrow delayed data for one 2d4 Acid tick at the end of the target's turn. A narrow injected dice seam makes the preview deterministic while production retains the normal roller. Scheduled-effect registration is idempotent by record id, so Reset or repeated registration cannot double-fire a tick.
- The real turn-manager proof advances 60 -> 56 at the target-turn start, 56 -> 51 at the target-turn end while consuming Acid, then 51 -> 47 at the next target-turn start with no second Acid tick. Removing Acid before its phase holds HP at 56, preserves Searing Smite, and later turns do not restore the removed record. Reset restores 60 HP and exactly two authored schedules.
- Fixed the production conversion seam that previously dropped recurring damage carried by a status-condition effect. It now emits the recurring damage payload without reapplying or refreshing the base condition. The remaining resistance, immunity, and Searing Smite recurring-save path is explicitly unsupported rather than simulated; routed once as `GG-54` after proof.
- Final verification passes 281/281 assertions across 41 files, covering every cumulative scenario-control suite, the catalog, mounted host, combat engine, real turn manager, and effect converter. Dependency synchronization succeeded. Scoped ESLint reports zero errors with established warnings. Typecheck debt remains 55 diagnostics before and after; its six shifted lines are older incomplete spell-slot fixtures in the shared ignored host, outside CS35.
- The unmodified official develop-web-game client completed on the retained verification servers and emitted canonical state with no error file. Ignored 1600-by-1000 supplemental proof covers baseline, exact start/end phases, independent once/every-time ordering, Reset, pre-phase removal, the precise defense/save boundary, and 3D before/after a `-420` wheel zoom, 150-by-70 right-drag orbit, and 110-by-55 middle-drag pan. Representative captures were opened and inspected; schedules, HP, cues, controls, logs, actors, and 3D terrain remain legible. Console/page errors are 0 baseline, 0 total, and 0 post-interaction delta. The 61 warnings are the existing spell-data/software-WebGL class, including 52 instances of tracked `GG-51`.
- Disposable proof lives only under ignored `.agent/scratch/damage-over-time-scheduled-effects-proof/`. Initial external-port loss and a stale no-watch module graph were bounded under existing `WF-G59`; fresh mounted proof succeeded, so no duplicate workflow gap was added. Agora used exact child session `019ff3d0-155e-7980-9a87-d9acd671a3e5` with handle `codex-cs35-019ff3d0` and Cyberman. No branch, worktree, stage, commit, push, cleanup, deletion, or server stop was performed.

## 2026-08-12 Tactical Sandbox owner 16 - Initiative Ties & Shared Turns (implementation checkpoint)

- Registered `initiative_ties_shared_turns` as scenario 34 (expanded scenario 28) and mounted a deterministic sixteen-by-twelve board with two tied initiative-15 rivals, a same-side shared-initiative echo, and an initiative-11 guard. The live 2D/3D readout shows total initiative, Dexterity and initiative-bonus tie facts, active actor, shared owner/follower relationship, round, Action, Reaction, movement, own-turn effect state, and exact combat logs.
- Centralized the production initiative contract in `initiativeUtils`: d20 plus Dexterity modifier plus authored initiative bonus; equal totals sort by Dexterity, then bonus, then stable authored order. Shared-policy summons remain immediately after their caster while every character id appears exactly once. `useTurnOrder` and `useTurnManager` now reuse that order and roller instead of maintaining separate sort/roll rules.
- Added two scenario controls plus the native End Turn and Reset Board controls. The tie-break selector changes the rival from DEX 14 to DEX 18 and rebuilds the live manager deterministically. The runtime-boundary action reports that shared initiative currently executes as consecutive owner then follower turns and explicitly refuses to fake a simultaneous multi-actor economy or effect clock. Reset restores the authored captain-first order and spent ledgers.
- End Turn proof advances Captain -> Shared Echo -> Rival -> Late Guard -> Captain in round 2 exactly once each. Only the ending actor's Own-Turn Marker expires, and only the next actor's Action, Reaction, movement, and free-interaction ledger resets. The mounted-host test also covers deterministic reorder, precise boundary logging, Reset Board, and 3D launch.
- The focused initiative seam passes 28/28 across six files. The cumulative campaign/canonical-neighbor selection represents 269 assertions across 43 files; all pass when the mounted host uses a 10-second test timeout. At the repository default 5 seconds, the new initiative host test passes, while two older 3D neighbor cases exceeded the timeout under the combined run; the same host file passes 10/10 with the bounded timeout. Scoped ESLint reports zero errors, dependency synchronization succeeded, and scoped whitespace checking found no errors. Broad TSC remains red on cumulative pre-existing campaign/type debt outside the new initiative files, including incomplete spell-slot fixtures; it was not broadened into this lane.
- The unmodified official develop-web-game client completed against the already-warm CS32 server at `127.0.0.1:5197`, published the correct captain-first canonical state, and emitted no error file. Ignored supplemental 1600-by-1000 proof covers the Shared Echo reset boundary, one complete round, DEX-18 reorder, explicit unsupported-runtime log, Reset Board, and 3D before/after a `-420` wheel zoom, 150-by-70 right-drag orbit, and 110-by-55 middle-drag pan. Representative captures were opened and inspected; actors, initiative facts, resources, controls, and logs remain legible. Console/page errors are 0 baseline, 0 total, and 0 post-interaction delta; 3D retains the known spell-data/software-WebGL warning class, including the existing `GG-51` blit warning.
- Disposable proof lives only under ignored `.agent/scratch/initiative-ties-shared-turns-proof/`. Routed exactly one new cross-project boundary as `GG-53`: a true simultaneous multi-actor group-turn runtime needs an explicit ownership/timing contract before implementation. Agora used exact child session `019ff3a9-1cc1-7e60-88ce-86b9f2eda074` with handle `codex-initiative-ties-16` and Mechanical Maze Knight; final task completion, lock release, and retirement follow this receipt.

## 2026-08-12 Tactical Sandbox owner 15 - Flying & Aerial Movement (implementation checkpoint)

- Registered `flying_aerial_movement` as scenario 33 (expanded scenario 27) and mounted a deterministic sixteen-by-twelve board with an Aerial Scout, Ground Warden, difficult mud, a ten-foot low obstacle, a raised landing, occupied/blocked/off-board/insufficient-speed destinations, and persistent amber-source/cyan-path/sky-destination cues.
- Added one narrow production aerial resolver because existing movement and pathfinding are horizontal and ground-bound. It reads canonical `extraMovementSpeeds.fly`, persists altitude, calculates 3D cost as horizontal plus vertical distance, ignores ground difficulty and low obstacles only when legal airspace clears them, reuses complete-footprint placement and bounds checks, and applies movement atomically through shared action-economy helpers.
- Added three deterministic controls. Fly Route moves 25 feet horizontally and climbs 10 feet, spending exactly 35/40 Fly Speed. Invalid Destination rejects occupied, blocked, off-board, and insufficient-speed choices without partial position, altitude, movement, landing, or cues. Lose Flying Support proves the canonical non-hovering zero-Fly-Speed fall trigger and reports the exact missing integrated landing/fall-event runtime boundary without faking a fall, damage, Prone, or payment. Reset Board restores the authored baseline.
- Added 2D altitude badges and physically elevated 3D actors with aerial altitude nameplates. `render_game_to_text` exposes persisted altitude beside speed, position, resources, and logs in both render modes.
- Focused mounted-host/renderer proof passes 26/26 across five files. The cumulative owner-1-through-owner-15 campaign and canonical-neighbor gate passes 127/127 across twenty files, including every campaign scenario-control suite, the new aerial utility, shared size/footprint movement, catalog, and registry. `npx tsc --noEmit --pretty false` is green; dependency headers are synchronized; scoped ESLint has zero errors.
- The unmodified official develop-web-game client completed against CS32's already-running isolated real-app server at port 5197 and published the 1600-by-1000 baseline plus canonical state. Ignored acceptance proof covers baseline, legal movement, all four rejections, the precise support-loss boundary, Reset Board, and 3D before/after a `-420` wheel zoom, 150-by-70 right-drag orbit, and 110-by-55 middle-drag pan. All representative captures were opened and inspected; altitude badges, route cues, actors, raised terrain, controls, exact logs, and elevated 3D placement remain legible. Console/page errors are 0 baseline, 0 total, and 0 post-interaction delta.
- Disposable proof lives only under ignored `.agent/scratch/flying-aerial-movement-proof/`. The earlier shared-port and ordinary Vite attempts reproduced existing `WF-G59`; the successful warm-server run supplied mounted visual proof, so no duplicate workflow gap was added. Existing `GG-51` and `GG-52` were not duplicated. Agora used exact session `019ff37d-3add-73f0-ad80-deda1f5cb51e` with handle `codex-aerial-owner15-019ff37d` and Aka Pilot; final lock release and retirement follow this receipt.

## 2026-08-12 Tactical Sandbox owner 14 - Falling & Ground Impact (implementation checkpoint)

- Registered `falling_ground_impact` as scenario 32 and mounted a deterministic sixteen-by-twelve cliff board with a 30-foot source platform, 25-foot safe terrace, open ground-impact landing, occupied landing guard, blocked wall landing, off-board boundary, and persistent source/landing/impact cues.
- Added four deterministic controls. Short Drop resolves 5 feet as canonical `0d6`, full HP, and Standing. Ground Impact derives `3d6` from the shared fall helper, rolls pinned faces `[3,4,5] = 12` through shared dice, applies canonical HP loss and both Prone mirrors, and lands exactly at `8,5`. The invalid selector rejects occupied, blocked, and off-board landings through shared full-footprint placement before any effect. Feather Fall reads its structured reaction/descent/prevention data and reports the precise unsupported executable-runtime boundary without spending Reaction or level-1 slot or faking mitigation.
- Reused `calculateFallDamage`, `rollDamage`, `applyDamageAndCheckDowned`, `applyRuntimeStatusCondition`, `validateCharacterPlacement`, real tile elevation, and canonical Feather Fall JSON. No new production primitive or duplicate rules truth was added.
- Focused mounted proof passes 20/20 across the scenario control, catalog, and host. The cumulative campaign/canonical-neighbor gate passes 259/259 across every scenario-control suite plus catalog, mounted host, fall physics, HP/downed transitions, and complete-footprint placement. `npx tsc --noEmit --pretty false` is green; dependency headers are synchronized; scoped ESLint has zero errors; `git diff --check` has no scoped whitespace errors.
- The byte-identical official develop-web-game client completed on the warmed real-app server and published the elevated baseline state. Supplemental ignored 1600-by-1000 proof covers baseline, safe drop, damaging impact, precise Feather Fall rejection, all three invalid landings, Reset Board, and 3D before/after a `-420` wheel zoom, 150-by-70 right-drag orbit, and 110-by-55 middle-drag pan. All eleven screenshots were opened and inspected; the platform, both actors, landing/impact lights, Prone marker, controls, exact result logs, and resource state remain legible. Console/page errors are 0 baseline and 0 delta.
- Disposable proof lives only under ignored `.agent/scratch/falling-ground-impact-proof/`. No new product or cross-project gap was found; existing `WF-G59`, `GG-51`, and `GG-52` were not duplicated. Agora used exact session `019ff359-880f-7c00-8725-4957950b9490` with handle `codex-falling14-019ff359` and Violet Mage; final lock release and retirement follow this receipt.

## 2026-08-12 Tactical Sandbox owner 13 - Teleportation & Occupied Spaces (implementation checkpoint)

- Registered `teleportation_occupied_spaces` as scenario 31 (expanded scenario 25) and added a deterministic 16-by-12 board with a Large Misty Step caster, an occupied far-footprint square, a blocked far-footprint square, a board-edge overflow, an opaque sight wall, difficult terrain, and a low intervening obstacle.
- Added a narrow production teleport resolver because ordinary movement fallback can choose another cell and validates only its movement route. The resolver requires the exact chosen visible destination, validates the complete creature footprint against bounds, blockers, and occupants, then pays the canonical Bonus Action and level-2 slot before moving the caster directly.
- Added three controls: legal free-space teleport; occupied 2-by-2 footprint rejection; and a selector for blocked, out-of-bounds, hidden, or beyond-30-foot destinations. Success leaves movement and the adjacent Space Warden Reaction untouched, reports zero traversed tiles and opportunity attacks, and adds persistent source/destination afterimages. Every rejection preserves position, resources, reactions, and lights. Reset Board restores the exact authored baseline.
- Added resolver, scenario-control, registration/catalog, and mounted-host proof. The focused host gate passes 26/26 assertions across five files. The cumulative campaign and canonical-neighbor gate passes 236/236 assertions across 38 files, including every scenario-control suite plus movement, targeting, self-teleport, and neighboring spell resolvers.
- Dependency headers are synchronized for every changed runtime path. Scoped ESLint reports zero errors, and `git diff --check` reports no whitespace errors; only unrelated line-ending warnings remain. No broad typecheck or lint run was used.
- The unmodified official web-game client completed from an ignored byte-identical local copy after the isolated Vite server warmed. The first shared-port attempt reproduced the existing `504 Outdated Optimize Dep`, and a separate locked 3D edit briefly produced an esbuild error; neither file was touched, and the final isolated pass was clean.
- Ignored 1600-by-1000 proof covers baseline, legal payment and placement, all five rejection reasons, Reset Board, and 3D before/after a `-420` wheel zoom plus a 150-by-70-pixel right-button orbit. All ten screenshots were inspected. Source/destination cues, both actors, footprints, controls, position/resource results, and the reasoned log remain visible; console/page errors are 0 baseline and 0 delta.
- Disposable proof lives only under ignored `.agent/scratch/teleportation-occupied-spaces-proof/`. No new product or cross-project gap was found, and the existing official-client workflow gap was not duplicated.

## 2026-08-12 Tactical Sandbox owner 12 — Sustain Actions & Ongoing Control (implementation checkpoint)

- Registered `sustain_actions_ongoing_control` and added a two-actor Witch Bolt board whose reset baseline shows the concentration-owned arc on the first later turn, 48/60 target HP after fixed initial damage, nine rounds remaining, a spent level-1 slot, fresh Action/Bonus Action, and unrelated Mage Armor.
- Added a narrow production Witch Bolt ongoing resolver because the existing sustain tracker records prompts but does not pay the live resource, apply repeat damage, evaluate range/Total Cover/concentration/duration endings, or clean owner-linked state. The resolver reads the canonical 2024 spell JSON and delegates to shared action economy, attack, damage, HP, footprint range, and line-of-sight helpers.
- Added four deterministic controls: initial Action/slot/2d12 cast and link; later-turn Bonus Action/1d12 automatic damage; optional skip with no damage or cleanup; and one boundary selector for clear state, 65-foot range break, Total Cover, concentration loss, or one-minute expiry. Cleanup removes only Witch Bolt status/active-effect/concentration records and preserves Mage Armor and unrelated actors.
- Added focused primitive, scenario-control, catalog/registry, and mounted-host proof. Dependency headers are synchronized for every changed runtime path. Scoped ESLint reports zero errors; the shared ignored host retains 15 established warnings.
- First focused Vitest pass is green: 30/30 assertions across the new production resolver, scenario controls, catalog, complete registry, and mounted host. The host proof exercised linked reset state, later-turn Bonus Action damage, Total Cover selective cleanup, and Reset Board restoration through `render_game_to_text`.
- The cumulative owner-1-through-owner-12 campaign run is green: 125/125 assertions across 19 files, including all eleven predecessor scenario controls, the new resolver/control/host seams, the existing sustain tracker, shared action economy, and shared line of sight.
- The unmodified official web-game client reproduced existing workflow gap `WF-G59` because its external location cannot resolve the repository Playwright package. Its byte-identical copy beneath ignored scratch completed and emitted the canonical text state; the supplemental 1600-by-1000 full-viewport loop then exercised all controls, Reset Board, and both 2D/3D views. Every screenshot was inspected. After a `-420` wheel dolly and a 150-by-70-pixel right-button orbit, both actors, concentration/debuff cues, duration, resources, controls, HP, and reasoned log remain visible. Console/page errors are 0 baseline and 0 delta.
- Disposable proof lives only under ignored `.agent/scratch/sustain-actions-ongoing-control-proof/`. No new product or cross-project gap was found, and `WF-G59` was not duplicated.

## 2026-08-12 Tactical Sandbox owner 11 — Repeat Saves & Condition Expiry (implementation checkpoint)

- Registered `repeat_saves_condition_expiry` and added a two-actor Hold Person board with visible source ownership, DC 15, Paralyzed, one remaining round, speed 0, concentration, and unrelated Blessed/Mage Armor state.
- Added three deterministic actions: fail the canonical end-turn Wisdom repeat save and retain penalties; meet the DC and remove source-linked Hold Person records; or advance the canonical turn-start duration boundary without another save.
- Extracted shared repeat-save cleanup and mirrored status/condition duration ticking. The combat engine now removes the structured condition and restores movement after a successful repeat save instead of deleting only its legacy status row.
- Added focused utility, scenario, mounted-host, catalog/registry, and turn-manager coverage. The first focused run passed 30/32 assertions and caught that the shared test character's class leaked Wisdom proficiency into the authored +0 save; the fixture now clears class and character save proficiencies explicitly. The final cumulative campaign run passes 96/96 across 18 files, and the post-initiative-fix focused rerun passes 15/15.
- The first rendered reset pass caught a production-only initiative race: when the target randomly opened combat, its one remaining round expired during reset initialization. The authored board now guarantees the caster opens, so launch/reset retain the inspectable baseline while the explicit expiry action still uses canonical turn-start cleanup.
- Required dependency headers are synchronized. Scoped ESLint reports zero errors and 38 established warnings in the shared host/hooks; `git diff --check` is clean. No broad typecheck or lint run was used.
- Ignored 1600-by-1000 proof covers baseline, failed save, successful save, duration expiry, reset, and 3D before/after a `-420` wheel zoom plus a 150-by-70-pixel right-button orbit. Both actors, cues, controls, penalties, and reasoned logs remain visible; baseline console/page errors and the post-interaction error delta are all zero.
- No product gap remains in this slice. Registered workflow gap `WF-G59`: the installed official web-game client cannot resolve workspace Playwright from its global path and its automatic canvas capture selects only the Tactical Sandbox backdrop; the byte-identical ignored local copy supplied official state/error evidence, while a full-viewport harness supplied rendered acceptance evidence.

## 2026-08-11 Tactical Sandbox owner 10 — Dispel Magic & Effect Cleanup

- Added the `dispel_magic_cleanup` catalog/sidebar lane and a three-actor board: an Intelligence +4 Abjurer with one level-3 slot, a self-Blessed Guardian, and a self-invisible level-4 Veiled Scout. Both targets also carry unrelated Mage Armor so cleanup selectivity stays visible.
- Added a narrow production `resolveDispelMagic` mechanic because canonical 2024 spell data described the rule but no executable resolver existed. It validates ongoing spell ownership, rejects instantaneous aftermath, pays the live Action/slot cost, uses the shared spellcasting-ability check against DC 10 + spell level, and removes only matching status, condition, active-effect, light, and concentration links.
- Added four deterministic actions: automatic lower-level Bless cleanup, successful level-4 Greater Invisibility check, failed level-4 check that retains the spell after payment, and pre-payment Fireball-aftermath rejection. Token names and `render_game_to_text` expose slots, Action state, concentration, status, condition, and active-effect results.
- Focused `/test-ts` verification passes 68/68 across the resolver, scenario controls, mounted host, catalog/registry, and all nine cumulative campaign neighbors. Required dependency headers are synchronized. Scoped ESLint, including normally ignored Design Preview paths, reports zero errors and 15 established warnings in the shared host.
- The unmodified official web-game client and the 1600 by 1000 acceptance pass wrote proof only beneath ignored `.agent/scratch/dispel-magic-cleanup-proof/`. Visual inspection confirms all four 2D outcomes and a 3D launch where all three actors, effect badges, controls, resource totals, and the rejection log remain visible after a `-420` wheel zoom and 150-by-70-pixel right-button orbit. The text probe matches the rendered state; console/page errors are 0 at baseline and 0 delta.
- No unresolved product or cross-project gap was found. The scenario adds the missing bounded Dispel Magic resolver and leaves unrelated Mage Armor intact instead of broadening into general spell-duration maintenance.

## Tactical Sandbox 3D reliability lane

- Diagnosed the 16x12 Cover dungeon disappearance as a shared-unit contract gap: `resolveHorizon` ended fog at 6.8 world units while the camera starts about 15 units from its target and legally zooms to 20.
- Centralized the camera zoom ceiling in the horizon setup and added a 1.5x camera-to-fog safety ratio. Authored open-biome distances remain unchanged; dungeon/cave fog still begins at its close authored distance and retains its dark color and lighting.
- Made the LIGHT visibility label explicitly fixed-size screen-space UI. The light rings, radii, point light, movement, and visibility mechanics are unchanged.
- Focused pure tests cover 16x12 and 80x60 enclosed-map budgets, open-biome preservation, and the no-distance-scaling marker contract (16/16 focused tests green; 17/17 with the BattleMap3D integration test).
- Rendered proof passed for Cover at close, maximum overview, panned, and explicit right-drag orbit poses; Darkvision at initial and maximum-overview+panned poses; and the expanded Line of Sight scenario at initial and maximum-overview+panned poses. The isolated orbit run recorded zero console errors; the shared browser session recorded one unrelated `Invalid class or race: fighter, dwarf` sandbox-generation error.

## 2026-08-04 skeleton pivot slice 4 (creature skeletons) — Tasks 1–2 landed, Task 3 pending eyeball gate

- **What:** plan-driven creatures (gait `'plan'`) now get a real `THREE.Bone` hierarchy and a rigid-weight skinned body, opt-in via the existing `bodyTech: 'skinned'` switch. New pure module `src/systems/entities3d/three/planSkeleton.ts` (`buildPlanSkeleton` + `createPlanPoseSink`) mirrors slice-1's skeletonBuilder shape for compiled `PlanSpec`s: rest pose captured by stepping a real `PlanDriver` at dt=0 (the parity trick — the bind pose can never drift from the driver), one bone per spine segment (`spine.N`) + per chain link (`<chainId>.N`) + terminal balls (`head<i>`/`<chainId>.foot`/`<chainId>.palm`), parented root→spine→chains (tauric `parentId` chains root on their parent chain's tip via a topological sort) → terminals. Decorative emissions (snouts, cilia, toes, fingers, auto S-necks, rings, collars) stay on the anchor path — the sink ignores them or forwards them to a decorative delegate so nothing renders today is dropped in skinned mode.
- **Task 2 (skinned body through the stack):** `buildPlanBindGeometry` + `createSkinnedPlan` in `skinnedBody.ts` (cylinders + joint spheres + terminal balls, plus rigid `BoxGeometry` slabs for box-spine creatures like the Gelatinous Cube; each vertex owned 100% by one bone; 2 draw calls; `PLAN_TRIANGLE_BUDGET`-respecting). `assembleEntity.ts` admits `bodyTech:'skinned'` for `gait:'plan'` (guard widened; species gaits still throw) and passes the segment renderer's sink as the decorative delegate. Creature SMOOTH joint weights remain deferred (loud throw, no silent fallback). Formed heads emit no ball — their `head<i>` bone borrows the driver's live socket for the bind pose (Task 3 owns parenting them).
- **Design decision (Remy-approved 2026-08-04):** only body skins get bones; decorations stay on the per-frame anchor path (rings/cilia are effects, not flesh — skinning them is larger blast radius for no visual payoff).
- **Proof:** `npx vitest run src/systems/entities3d/__tests__/planSkeleton.test.ts src/systems/entities3d/__tests__/skinnedPlan.test.ts` → 11/11 (bind-pose parity 1e-6, 120-step drift 1e-6, tube≡seg, unknown-id throw, determinism; rigid weights sum=1 / 1 influence / in-range, 2 draw calls under budget, pose parity, decorative forwarding, quad-guard). Neighbor suites green: assemble/skinnedBody/segmentCollar/perfBudget/crowdBake/skeletonBuilder/planDriver → 68/68. No git commits (plan constraint: tasks end at tests-green).
- **Pending (Task 3):** head-parenting + A/B preview surface + eyeball captures + Remy gate. Open gap: formed heads emit no ball so their `head<i>` bone is never written by the pure pose sink — parenting sculpted heads to bones needs the assembler to write those bones from `driver.headSockets()` each frame (outside the sink). Requires a live browser for A/B captures and the human eyeball gate; not done in this session.


## 2026-07-28 entity generator: reference-image pass — measured proportions + wing pleat

- **Method change:** instead of iterating on vibes, pulled Monika Zagrobelna's dragon anatomy tutorial plates (the canonical "How to Draw Dragons" reference — therapsid skeleton `mz-3`, muscle plates `mz-4`/`mz-7/8`, wing plates `mz-wing-*`, finished poses `mz-final-2`, all in `.agent/scratch/ref/`) and MEASURED them: torso:leg ≈ 1.45:1 (we were at 2:1 after the dachshund fix — still doggy), neck ≈ 0.75× torso, tail ≈ 1.2× torso and meaty, hind haunch dominates the front leg, neck leaves the chest at ~40–45° not 57°, folded wings collapse their span.
- **Template changes (dragon):** torso 2.0h → 1.8h, leg reach 1.02h → 1.12h; mass [1.42, 0.76, 1.2] (deeper keel, tighter waist, bigger hip anchor); hind leg link r 0.72 → 0.85 (the haunch carries the animal — mz-4); neck 0.53h → 1.0h in FOUR links (0.72→0.34 taper; the wave-5 cut had overshot — reference necks are long, the "garden hose" sin was uniform radius, not length); tail links +~12% longer and meatier (base r 0.5 → 0.62); head snout lengthScale 1.25 → 1.4. Driver neck angle 57° → 45° (`gaits.ts`, affects all plan necks — dragon is the only production template with one).
- **Wing pleat (the big idle win):** rigid rotation can't collapse a folded wing's span, but SCALE can — `wingL/wingR.scale.x = 1 - fold*0.5` in the assembler pleats the membrane to half span at idle, so the folded wing reads as a strip on the flank (ref mz-final-2) instead of a full-size slab. fold=0 walking → scale 1.0, byte-untouched gait.
- Proof: 664/664 entities3d tests (74 files — a third worktree joined discovery), tsc filtered clean, headers synced. Captures: `pleat-side90.png` (the money shot — compare `ref/mz-3.jpg`), `pleat-front.png` (folded wings = narrow chevron, no more box slab), `pleatwalk-{a,b}.png` (wings spread mid-beat), iteration trail `refpass-*.png`.
- Honest residuals: neck-chest shelf reduced but still visible; the neck is a straight 4-link run — the reference S-curve needs per-link pose offsets the driver doesn't have yet; pleat is uniform compression, not true finger-folding (spar tips visibly bunch at close range); beast template still runs its own unreferenced proportions; celestial/fiend/fairy inherit the fold+pleat (untested renders); wing CHAINS (plan-language) still don't fold.

## 2026-07-28 entity generator: "dachshund" proportion fix — torso:leg ratio, wing-fold tune

- **Trigger:** user, after the wave-5 body-plan renders: "the dragon still looks like a long dachshund." Correct — the mass profile had lumped up a torso whose underlying ratio was the problem: `lengthFt = 2.8h` on `reach = 0.95h` legs is a ~3:1 torso-to-leg ratio, literally dachshund territory (big cats / classic dragons sit ~1.3–1.6:1). Leg attach points at u=0.14/0.86 of the long body compounded it (legs ~2h apart).
- **Fix:** dragon template torso `2.8h → 2.0h`, leg reach `0.95h → 1.02h` (≈2:1 with a taller stance; the tail still supplies the signature length). Idle wing fold retuned twice against renders: straight-down drape (1.05 rad, curtained the legs) → backward sweep (hid the haunches) → final steep 1.32 dihedral + 0.5 y-sweep, so folded membranes hang VERTICAL along the flank like a parked bird wing. Walking fold=0 → gait behavior byte-untouched.
- Proof: 416/416 entities3d tests (47 files), tsc filtered clean, dep headers synced. Captures `.agent/scratch/doxie{,2,3,4}-side90.png` (iteration trail), `doxie4-front.png`, `doxie4walk-{a,b}.png` (walking seed-3: wings open mid-beat — fold disengages). One process fumble: shoot-wing-flap.mjs writes CWD-relative, two frames briefly landed in the git-tracked root; moved to scratch and check-ignore verified.
- Honest residuals: neck-chest shelf still visible; the folded wing is a static vertical hang — real folded wings also shrink span, which rigid rotation can't fake; beast template keeps its own 2.3h torso (wolf reads better than the dragon did, but the same ratio argument applies if the user flags it); celestial/fiend/fairy idle fold still untested on render.

## 2026-07-27 (late) entity generator: dragon body-plan rework — spine massing, hocks, wing fold

- **Trigger: brutal honesty.** The user's side-view screenshot of forge seed 1 exposed what my front-only renders had hidden all session: hot-dog torso (one radius snout-to-rump), garden-hose neck (constant radius, ~0.84h long), suitcase-handle wings (span keyed off height while the body is 2.8x height), bent-wire legs (vertical segment lists, no digitigrade hock). I admitted the verification failure — every earlier "prettier" claim rested on a single camera angle — and reworked the actual body plan. Multi-angle rendering (front / three-quarter / TRUE 90° side) is now my standing proof obligation.
- **One spine profile.** The torso radius curve existed as two duplicated copies (compilePlan.hullRadiusAt + gaits.buildBody) — the reason the tube and the hull could never drift apart but also could never gain shape. Extracted to `textPlan/spineProfile.ts` (`spineRadiusAt(spec, u)`), now with optional `spine.mass: [chest, waist, hips]` — three-lobe knots (u=0.22/0.55/0.8, cosine interp, tips 0.82/0.88). Plans WITHOUT mass keep the historical taper+bulge byte-identical; the dragon rolls `mass: [1.35, 0.78, 1.12]` = deep chest, tucked waist, heavy haunches. Both consumers delegate to the helper; the rigid-seg fallback (crowd bake) now honors bulge too — deliberate crowd-silhouette change, tests green.
- **Legs and neck.** Dragon legs rebuilt via `quadLegs(reach, 0.55, 0.72)` (heavier haunches); plan driver hind legs (attach>0.5) flip the V-bend z so hocks bend BACKWARD (digitigrade) instead of all four knees bending the same way. Neck 0.84h hose → 0.53h in three links (r 0.66→0.52→0.40, base-thick); head 1.1→1.28 sizeScale; PlanDriver 'back' anchor moved from mid-body (waist) to u≈0.3 (shoulders) for horizontal stance.
- **Wings: bigger, and they FOLD.** Dragon membrane wings gained `params.scale: 2.2` (span keyed off height but body is 2.8x height — scale-1 was the suitcase handle). New `wingFold` on GaitDriver/BaseDriver: `groundedWingFold() = clamp(1 - speedFactor*3.5)` set at the three groundedWingBeat call sites; the assembler maps fold to dihedral (`flap*(1-fold*0.55) + fold*1.05`, mirrored signs so both tips LOWER) plus a backward y-sweep. At idle the wings drape down along the body like a cloak; at any walk speed fold→0 and the prior beat behavior returns byte-identical (walking wing test untouched). Flyers stay fold-0.
- Proof: 416/416 entities3d tests (47 files), tsc filtered clean on all 8 touched files, dep headers re-synced. Captures in `.agent/scratch/`: `bodyplan-side90.png` (true profile: chest/waist/haunch + digitigrade hock + thick-based neck) vs the user's damning `user-brutal-check.png`; `bodyplan-front.png` (wings spread); `fold-side90.png` (idle cloak drape); `foldwalk-forge1-{a,b}.png` (walking beat). Reusable capture scripts: `.agent/scratch/shoot-forge1-angles.mjs`, `shoot-forge1-side.mjs`.
- Honest residuals: idle wing tips drape near the ground between the legs — may clip on short-legged winged bipeds (celestial/fiend/fairy now fold too; untested renders); slight shelf where neck meets chest; neck angle still fairly upright (~60°); plan-language wing CHAINS (Emberwing fixture) don't fold — parts only; crowd-bake silhouettes changed via the seg-fallback bulge (deterministic tests green, no in-game pixel check); mid-session forge broke ~1 min on another agent's in-flight `savingThrowUtils.ts` esbuild overlay — self-healed, untouched.

## 2026-07-27 (eve) entity generator: beauty pass — proportions, wing harmony, eyes

- **Giraffe-dragon fix.** User flagged forge seed 1 (Huge Dragon) as ugly: the wave-3 per-link length jitter stacked multiplicatively, so seed 1 rolled long on all 3 neck links + short on both leg links (~2x neck-vs-leg silhouette drift — analytical bound; 200-seed probe confirmed post-fix). `varyPlan` now renormalizes each chain so its TOTAL length lands within ±8% of the template (segmentation still jitters per link). Post-fix probe: neck-vs-leg ratio bounded 0.88–1.14x across 200 seeds. Pinned by a 200-seed budget test in `anatomyVariety.test.ts`.
- **Wings belong to the body now.** Membrane wings were one hardcoded near-black mauve (`#5a4458`) with near-black spars on every creature — "cardboard" on a red dragon. Now derived from the palette: membrane = skin x0.55, spars = skin x0.32, feathers = skin lerped 72% to white (`parts/wingParts.ts`; explicit `params.colorHex` still overrides).
- **Eyes de-googled.** Whites shrunk twice (0.32hr → 0.27 → 0.24 planned / 0.25 legacy) and warmed to `#f4f1e6`; pupils slightly larger within the eye. Plus a luminance contrast guard — gold-on-gold dragons (forge-7) had invisible pupils; now falls back to dark `#1c1c22` when iris and skin luminance are within 0.12 (`three/assembleEntity.ts`).
- Proof: 416/416 entities3d tests (47 files, incl. the second `sharp-saha-8201b1` worktree vitest discovers — that explains the earlier 25→47 file jump). Captures: `.agent/scratch/beauty-*.png` (wing harmony + budgets) and `.agent/scratch/polish-*.png` (eye polish), vs baselines `inspect-forge1-a.png` (the ugly giraffe roll) and `before-*.png`. One flaky single-test failure in the first post-change run; clean on both reruns (unknown, likely cross-worktree cache contention — watching it).
- Honest residuals: s7's head-tilted-back idle pose hides its face from the forge camera, so the pupil guard is unit-proven but not pixel-proven on that seed; the wolf beast still stands meerkat-upright at idle (stance, pre-existing); dragon necks remain long BY DESIGN (template "signature long neck") — budgets only stop the compounding; assembleEntity.ts developed transient OS write locks from other agents' esbuild watchers mid-session (retries landed every edit; nothing killed).

## 2026-07-27 entity generator: beast head redesign + seeded anatomy individuality

- **Beast head form rebuilt.** The `beast` sculpted head (dragon, beast, fiend, monstrosity) was a literal `BoxGeometry` crate whose 1.05r front half-depth swallowed the assembler-seated eyes (0.72r forward) — the flagship dragon had no visible face. Now a composed skull: shallow icosahedron cranium (0.78r), pointed octahedron muzzle wedge below the eye line, brow pair above it, jaw + teeth kept. `three/headForms.ts` documents the face-plane rule so future forms can't re-bury eyes; pinned by geometry tests.
- **Generic monsters are no longer clones.** Same-size creatures of one type compiled to byte-identical anatomy (only ±8% frame jitter and 1-of-5 skin tones varied). A third independent seed stream `anatomy` now drives `varyPlan` (`creaturePlans.ts`): jitter on every chain link length/radius, spine bulge/taper, head size, and snout; per-type identity picks (35% of dragons roll the `serpent` head form, monstrosities roll goat/slit/round pupils); and seeded garnish swaps via `CREATURE_PART_VARIANTS` (dragons/fiends roll straight/curved/ram horns, beasts roll pointed/long ears). Humanoid race identities are untouched — the swaps only run in the creature path; the stream-free `planForCreature` call returns the historical fixed anatomy byte-identical, so fixtures/dev tools/tests are unmoved. Frame and palette draws did not shift (independent seedPath streams).
- Proof: 415/415 entities3d tests across 47 files (+10 new: headForms composition/face-plane, anatomyVariety determinism/individuality/registry/stability/tiefling-guard). Baseline captures `.agent/scratch/before-{dragon-s3,dragon-s7,...}.png` show the crate head and two seeds stamping identical dragons; after-captures `.agent/scratch/after-{dragon-s3,dragon-s7,dragon-s11,beast-s3,fiend-s3}.png` (same forge seeds) show rounded skulls with visible eyes, and seed-varied individuals (s3 dark curved horns vs s7 white ram horns vs s11 green skin + spiked horns; wolf-eared beast; bat-winged fiend with slit pupils).
- Honest residuals: the `serpent` dragon roll reuses the existing serpent form (no new geometry); horn/ear variants are whole-part swaps, not parametric sculpts; seeds 3 and 7 both rolled the same 1-of-5 gold skin tone (palette-coincidence, deterministic per seed, not a bug); tsc-filtered check repeatedly blocked on Bash approval expiry — suite green via vitest transform across all 415 tests, tsc still pending a successful run.

## 2026-07-26 entity generator: grounded wing beat (dragon/celestial/fiend wings move)

- Fixed the handover's headline parked-polish item: winged grounded creatures (Dragon, Celestial, Fiend, fairy, aarakocra) held their wings frozen mid-spread because only the flyer gait ever set `driver.flap`. Biped and quad/hexapod drivers now emit a gentle speed-scaled wing beat (`groundedWingBeat()` in `three/gaits.ts` — the exact formula the plan driver already used, so both dragon paths move alike). Hopper stays flap-0: no winged hopper profiles exist anywhere.
- Found a second, worse instance of the same bug while testing: plan-driven creatures whose wings are garnish PARTS (the Emberwing dragon fixture — "chain wings render as bare sticks on big bodies") had flap pinned at 0 because the plan driver only checked wing CHAINS. Closed with a `winged` hint on `createGaitDriver`, computed from `blueprint.parts` by both production callers (`assembleEntity`, `crowdBake`). The assembler already no-ops flap on parts without wingL/wingR groups, so the beat is harmless on wingless bodies.
- Proof: 237/237 entities3d tests (+3 new: beat envelope + hopper zero, idle-vs-walk amplitude, end-to-end walking Huge dragon with wingL sweeping ≈1.4 rad mirrored on wingR); tsc clean on the four touched files; dependency headers re-synced (gaits.ts has no header — sync tool skips it). Rendered proof at the forge (`design.html?step=entityforge&mode=creature&type=Dragon&size=Huge&walk=1&wire=0`): two frames 520 ms apart (half the beat period) show the membranes at clearly different dihedral angles, 21.5% of the wing-band pixels changed (`.agent/scratch/wing-flap-{a,b}.png`, gitignored).
- Honest residuals: the beat is continuous (~1 Hz sway) even at idle — reads "alive", but a fold-when-resting pose is a future art call, not a bug. Crowd walkers still snap between the 8 baked keyframes, and `resolutionScale`/`fieldUpdateHz` remain accepted-but-ignored options on the five surfaces (both still parked in the handover).

## 2026-07-18 Q3 forest density + boulder formations (GOAL #39/#46)

- Forests now have clearings and thickets instead of a uniform tree sprinkle. The generator's obstacle pass runs a smooth low-frequency noise field: low areas get almost nothing, high areas get up to ~2.5× the base density, and the overall obstacle count stays close to the old budget. Tree tiles surrounded by other tree tiles also grow a second smaller sapling (visual only), so dense pockets read as layered undergrowth. The field shapes every biome's scatter, so desert rocks and swamp mangroves cluster naturally too.
- Boulders no longer look like lumpy dice. Each is now a three-lobe stone formation — a dominant block with a shoulder slab and a toe stone — with hard noise facets, a shared flat bottom so it sits settled into the ground, and horizontal strata color bands like layered sediment. Honest residual: the strata bands are subtle at full tactical zoom; they read best within ~15 units.
- Proof at pinned seed 424242, pose 30,55,40: q3-before-forest.png (even stipple) → q3-after-forest.png (open clearings, dense groves); q3-before-desert.png (round lumps) → q3-after-closeup-desert.png (angular layered stone).
- Verify: tsc clean on the three touched files; BattleMap suites 122/125 — the 3 failures are pre-existing stale label expectations against another session's in-flight elevation-labeling work, reproduced identically on the stashed baseline (GAPS #31).
- Rig repair rode along: the app-phase battle map demo no longer exists, so shoot.mjs now drives misc/design.html?step=battlemap directly (BASE_URL override, CDP screenshot fallback for the hanging page.screenshot, select-based biome switching, page-coordinate canvas clips). Session friction recorded as GAPS #32 (localhost vs 127.0.0.1 vite hang, wedged server, orphaned headless Chromium).

## 2026-07-18 Q1 3D combat postfx — SSAO fixed, real color grade (GOAL #51/#53/#60, gap #1)

- Fixed the long-broken SSAO by swapping to N8AO: the old effect needed a NormalPass whose depth-stencil copy throws a GL error every frame on our three r170 stack. N8AO computes its own normals from depth, so the error spam is gone (all capture runs clean) and the scene finally has contact shading — trees, rocks, and walls sit in the ground instead of floating on it.
- Found why the game looked like raw 3D instead of a graded frame: the postprocessing composer silently turns OFF the renderer's ACES tone mapping, so our Canvas setting never applied. Tone mapping now runs inside the effect stack. Bloom was also retuned (lower threshold, soft mipmap blur, per-biome strength) so torch and crystal pools glow in cave/dungeon.
- Proof at pinned seed 424242, pose 30,55,40: q1-before vs q1-after forest/desert captures and native crops — hazy flat frame → deep canopy shade, sun-lit contrast, grounded props. All 5 biomes swept; cave/dungeon/swamp keep their dark mood.
- Honest residuals: daylight dust motes in forest/desert are still faint even with bloom working — the fix is brighter motes, not postfx (GOAL #57 stays PARTIAL, narrowed). Cave/dungeon overview zoom-out is still fog-swallowed after a camera-range clamp; needs distance-scaled fog (gap #21 stays open with findings). 60fps gate could not be measured headless (software renderer); the stack uses half-resolution AO and performance mode, and the live check rides Remy's playthrough gate.
- Verify: 4 BattleMap3D vitest suites green (mocks extended for the new effects), tsc clean on touched files. New dependency: n8ao.

## 2026-07-18 Q5 map utilization — spawn engagement window (GOAL #65)

- Found the root cause of "most of the battlefield is scenic backdrop": the documented 48×36 spawn engagement window only existed in dead code. The live tactical spawn selector used the full 120×90 map, so the closest opposing units started 60-96 tiles apart (11-16 move turns of pure walking) and teammates were strung 30+ tiles along a map edge.
- Fix in `useBattleMapGeneration.ts`: all four deployment shapes now center inside a real 72×72 window (clamped on small maps, so test fixtures and extracted crops are unchanged). Deleted the 160-line dead helper. Closest-unit gap is now 38-50 tiles (~7-9 turns) with the team-centroid separation contract intact at all 5 pinned seeds. 16/16 spawn tests green; tsc clean on the touched file.
- Proof: q5pose-before/after-desert.png and -forest.png (SEED 424242, POSE 34,58,20) — before: the active unit alone in frame; after: the party as a visible formation with the move overlay. GOAL #65 PARTIAL → OK for spawn geometry; tighter pacing and mid-field objectives are written up as a Remy call in TRACKER task 85.
- Session friction fixed along the way: wedged 5174 vite servers (twice), stale storageState origin, Ollama down, and compositor screenshots starving under box-wide CPU saturation — added an rAF-readback fallback in a scratch copy of shoot.mjs (`.agent/scratch/q5-shoot.mjs`, recipe from tools/vistest/shoot.ts) and ran captures on a private port 5199 server to stop racing other agents for 5174.

## 2026-07-17 full Character Creator design preview

- Confirmed the Design Preview hub had style-study pages for individual character-creation steps, but no page that mounted the complete production Character Creator.
- Added `design.html?step=charactercreator` as a live full-flow preview. It uses the real production component, keeps preview drafts under a separate local-storage key, and contains completion/Main Menu callbacks so the standalone preview cannot launch or mutate a game session.
- Added a Design Preview-only `Export Sheet` action. It downloads a self-contained, printable HTML character sheet with readable identity, abilities, training, spells, and appearance sections plus the complete current choice record for conditional and future fields.
- Proof completed: the focused Character Creator suite passes 9/9; touched-file ESLint reports zero errors; dependency headers and sync-check pass; desktop, Auto-Fill-to-review, export-confirmation, exported-sheet, and 390x844 browser captures show the live component with no captured console errors. The downloaded HTML was re-opened and verified to contain identity, abilities, race, class, name, and the complete choice record. Broad TypeScript still fails on the repository's existing cross-domain debt, with no error naming the touched Character Creator or Design Preview files.

## 2026-07-16 truthful and readable tactical elevation

- Direct user confusion exposed a semantic defect in the first elevation HUD: River Crossing displayed 22-50 ft while claiming the values were above a local low point. The source crop actually spans 28 ft, but its wider-world offset had never been removed from presentation.
- The battle map now derives its lowest sampled tile as map height 0 ft. Hovering separately labels the difference versus the selected/active creature and the tile's height above that map zero; directional badges replace bare plus/minus signs, and the contour legend says each line is a 5 ft rise.
- A 390x844 visual pass rejected the first revision because its explanation overlapped the zoom controls. Compact layouts now place the height panel above the zoom strip; desktop keeps the lower corner treatment.
- Permanent `combat-world-river-elevation` proof now requires a real zero-foot tile, non-flat relief, explicit comparison/map-zero/contour language, and no raw `Elev:` labels. Focused tests pass 27/27; touched lint has zero errors. G14 remains active only for unauthored cliff/slope, climb/fall, high-ground, and elevation-aware line-of-sight mechanics.

## 2026-07-16 WebGL 3D hostile-opening aftermath parity

- The resolved hostile-opening return now reaches WebGL 3D through one strict selector. It renders the exact saved terrain imprints, ecological traces, abandoned/disturbed activity site, heavy combat churn, and only the Goblin/Wolf outcomes that remain physically present; withdrawals never become static substitutes or initiative actors.
- Static remains were rejected as generic olive capsules. The accepted pass uses separate anatomy: the Wolf has a long quadruped chest, muzzle, ears, four splayed legs, and tail; the Goblin has a cloth torso, oversized green head/ears, four-limb spread, dark boots, and a detached salvage pack. Saved flattening, traffic, furrow, disturbance, and body zones suppress grass locally so the scene history is not buried by procedural blades.
- The first framebuffer was a severe false positive: a generic contrast probe accepted empty olive terrain because the deterministic camera aimed at `y=0` while the saved focus sampled `11.194`. The camera now supports explicit target height; the permanent scenario requires the ground-height fact, named Goblin/Wolf/site/disturbance roots, real pixel range, and different readbacks after camera movement.
- Ability mode no longer carries valid-movement fill underneath its own targeting layer. That removed the cyan wash which hid the Goblin/site cluster, while preserving faint grid and dedicated targeting decals.
- Harsh accepted-frame verdict: immediate 2D/WebGL outcome parity is now real and the two species are distinct, but this is not a living ecology. Grass is still globally uniform away from saved damage, churn remains diagrammatic, the grid dominates, active `CharacterActor` silhouettes are generic, WebGPU lacks the layer, and no later scavenger, weather, faction, or predator-pressure phase exists.
- Unusual next monster directions: make bodies and sites ecological clocks rather than static props; let carrion insects, silent birds, displaced herbivores, scavenger succession, faction retrieval, rain-washed transfer, and scent corridors evolve from saved time/weather. Let salvaged world props physically alter monster silhouettes and let feared predators create negative-space clearings before they are seen.
- Verification: 17/17 focused selector/3D parity/Vistest registry tests; permanent desktop framebuffer capture and ignored 1353x1272 constrained capture; full-page constrained browser review; Vistest named-mesh, pixel, and camera-motion gates; touched lint zero errors. Broad TypeScript still fails on the separate repository debt set and emitted no diagnostic in the touched aftermath files.
- Elevation review created G14: raw values such as `2`, `3`, or `4` are internal units, not player language. The planned replacement is selected-relative feet/levels plus contour bands, shaded relief, slope/cliff cues, and a waterline reference; raw units stay debugger-only.

## 2026-07-16 exact opening-combat outcome and adversarial aftermath proof

- Combat teardown now settles the exact saved hostile-opening scene before clearing combat state. Every final source enemy must match the receipt; the immutable result stores final HP, tactical/world position, downed/withdrew/holding-ground status, battle outcome, site condition, and combat disturbance. Missing, unrelated, mixed, or conflicting identities fail closed.
- A resolved receipt cannot restart the original fight. The explicit return projector removes withdrawals and keeps downed or holding creatures as physical map facts with no initiative, HP, targeting, or fallback enemy generation.
- The deterministic Opening Aftermath recipe proves one Goblin and the Wolf downed, two Goblins withdrawn, an abandoned disturbed cache, and heavy saved churn. Species-diverse outcomes deliberately replaced the weaker first-two-Goblins fixture; the Wolf uses a lateral collapse so it reads as a quadruped without diagnostics.
- Visual rejection log: enemy bodies left in initiative; one oversized brown puddle; bodies crushed into unreadable debris; a stale approach marker beside the party; movement overlay dominating the ecology; attack intent panel covering the site; two same-species bodies proving too little; and an upright Wolf that looked like a gray seed. The final 1600x1000 and 1353x1272 renders fix those failures, but are not flattering completion evidence.
- Harsh remaining verdict: the churn still looks diagrammatic, the Goblin competes with cache debris, the source terrain remains dark and checkerboard-heavy, and the constrained command/roster rails clip lower content. The scene needs consequences stranger than corpses: scavenger succession, faction retrieval, drag-off paths, insect or bird silence, rain-washed blood transfer, disturbed scent geometry, and avoidance halos that become saved world facts rather than decorative decals.
- Verification: 72/72 focused tests across seven files; permanent Vistest gate requires exact resolved facts, two physical bodies, no enemy roster roles, heavy disturbance, and abandoned site state; real-browser attack/cancel probe reports two bodies and zero console/page errors. Canonical Vistest capture is 1600x1000; the canonical runner now supports explicit `--width`/`--height` for the ignored 1353x1272 proof.
- Still open under G12-G13: broader WorldForge creature lifecycle, pursuit destinations, reinforcement and regrouping, pre-contact/weather/avoidance history, darkness readability, richer species/faction aftermath, and 3D body/terrain/outcome parity.

## 2026-07-16 source-authored occupation imprints and quieter movement range

- Hostile-opening v2 scenes now persist four exact terrain-memory facts in tactical cells and WorldForge meters: flattened ground beneath the cache, a repeated cache-to-contact traffic run, dragged-salvage furrows, and a nearby refuse scatter. Early v2 receipts gain those imprints without moving their existing bodies, traces, or activity site; the reducer upgrades the same receipt id in place.
- The 2D battle map renders those source facts below actors and loose objects. Abstract wind/leaf field signs were replaced by alternating physical tracks, a territorial soil scrape, and broken stems; the claimed cache now sits inside disturbed ground and material debris rather than on neutral terrain.
- Reachable movement keeps exact per-cell interaction and a crisp outer perimeter, but the interior tint drops from 20% to 7%. The legend now reflects that boundary-led treatment instead of advertising a solid green fill.
- Visual pass 1 was rejected because the new ground layer read as a black puddle with ruler-straight spokes and the track trail remained an interface connector. Later passes softened and textured the ground, curved traffic/furrows, removed the connector, and raised footprint contrast. Accepted proof: permanent `.agent/vistest/captures/combat-world-hostile-opening.png` at 1600x1000 and ignored `.agent/scratch/world-battle-ecology-final-1353x1272.png`.
- Verification: 55/55 focused scene/reducer/scenario/tile/render/Vistest tests, including explicit fail-closed proof for partial terrain memory; permanent capture readiness requires all four imprint kinds and the territorial scrape after `saved-replay`; ESLint has 0 errors and 14 established warnings; broad TypeScript still exits on separate repository debt with no diagnostic in the exact touched-file filter; Plan Map validation is clean at 160 topics / 522 features; Battle Map project audit and sync-check pass.
- Harsh remaining verdict: darkness still suppresses some subtle evidence, the current terrain memory is a frozen contact tableau rather than a changed ecosystem, pre-contact/weather/avoidance history remains absent, combat outcomes cannot alter the group or site, and 3D consumes neither body nor terrain-imprint facts. G12-G13 remain active.

## 2026-07-16 source-authored monster bodies and visible turn overflow

- Opening bodies now freeze posture, carried profile, and normalized facing beside exact world-meter position in the saved scene. Legacy receipts remain loadable; newly authored and replayed scenes retain the physical body facts through combat source identity.
- The 2D map no longer draws new opening monsters as role-ring portrait pins. It composes top-down miniatures from the source facts: upright salvage pack, crouched long tool, crouched buckler, rear load, low scout, and scenting beast silhouettes.
- Same-species proof is no longer label-dependent: the canonical three Goblins have visibly different occupied shapes and carried edges, while the Wolf reads horizontally as an animal body. Portrait art remains only a small head detail.
- Visual pass 1 was rejected because bodies were distinct but too dark and the turn strip still hid later actors. Pass 2 increased body/gear contrast and added explicit earlier/later controls that keep the current actor centered.
- Accepted visual proof: permanent 1600x1000 `combat-world-hostile-opening` capture and ignored 1353x1272 constrained capture. Both keep all four source bodies separated; the constrained layout retains body readability and a navigable turn strip.
- Verification: 102/102 focused tests across 14 files; permanent Vistest readiness requires four distinct postures and pack/tool/buckler geometry after `saved-replay`; touched lint has zero errors; broad TypeScript still exits on separate repository debt with no touched-file diagnostic.
- Harsh remaining verdict: the cache still looks placed rather than inhabited, spoor remains symbolic, checkerboard terrain dominates the scene, combat outcomes do not alter bodies/site, and 3D actors ignore the new body-state contract. G12-G13 stay active.

## 2026-07-16 persistent hostile ecology and saved replay

- Opening scenes now use a v2 receipt that freezes a claimed activity site, physical contents, age-banded traces, exact monster bodies, and the player crop anchor in WorldForge meters. Re-entry replays that receipt exactly or fails closed when geometry, roster, or identity drifts; matching v1 saves upgrade in place.
- The mounted production provider finds the latest matching saved scene. The Hostile Opening lab authors once, then deliberately re-enters through that production replay path and labels the result `saved-replay`; the permanent Vistest capture refuses an authored-only imitation.
- A strict replay test caught a real defect: disturbed vegetation had been authored on the blocking vegetation asset. The clue now occupies adjacent traversable ground, and replay failures identify the exact rejected ecological fact and reason.
- Visual iterations rejected a floating package icon, a dark and unreadable debris pile, role-badge clutter, and a constrained establishing frame that clipped the contact group. The accepted proof uses a physical crate/bedding/remains composition, fresh/recent/weathered trace opacity, badge-free role posture/crop/ring treatment, and a HUD-safe camera offset.
- Harsh verdict: the scene still is not visually convincing. Repeated Goblin portraits, pin-sized circular bodies, weak facing/watch direction, a context-poor cache, cryptic spoor, clipped turn order, and checkerboard terrain make it read as a tactical diagram instead of a lived monster territory.
- Unusual next directions must become source facts: downwind scent corridors, conspicuously untouched avoidance zones, carried salvage changing silhouettes, unilateral mud/blood transfer, silent wildlife ahead of a predator, lookout-height shadows, and feeding/resting poses that alter occupied space.
- Verification: 101/101 focused tests across 14 files; permanent 1600x1000 Vistest capture; adversarial 1353x1272 browser capture; ESLint 0 errors and 41 established warnings; broad TypeScript still fails on separate debt with no touched-file diagnostic.
- Still open: reconcile deaths, escape, pursuit, and reinforcements back into WorldForge; author pre-contact route history; integrate ecology into terrain; produce genuine same-species silhouettes without labels or colored outlines.

## 2026-07-16 opening threat entity scene and monster ecology

- Hostile openings now author one deterministic, save-backed WorldForge contact scene with exact monster world/tactical positions, a shared approach, contact-lead/left-screen/right-screen/scent-flanker roles, and source-positioned tracks, scent, and disturbed vegetation.
- Combat records that receipt before transition, binds source identities to the bestiary roster in stable order, consumes exact positions verbatim, and rejects stale/mismatched scene rosters instead of inventing a tactical formation.
- Screenshot pass 1 was rejected: Goblins were cloned green stamps, the Wolf was too dark, role letters looked like debug punctuation, clues looked like unrelated pins, and active-hero camera framing crowded the formation against the right edge.
- The accepted 1600x1000 and 1353x1272 passes use a brighter beast emblem, maneuver-ring/icon role choreography, source-role labels in roster and turn order, a connected track trail, distinct non-magical field-sign silhouettes, and a one-time establishing camera shot over party, monsters, and evidence.
- Permanent Vistest scenario `combat-world-hostile-opening` now blocks capture until the exact source-scene diagnostics, four role badges, four roster roles, ecological traces, and connected track trail are all rendered; its canonical 1600x1000 capture passes.
- Harsh review still rejects the presentation as complete: same-species faces repeat, badge clusters crowd the tokens, field-sign glyphs require interpretation, and checkerboard terrain dominates the physical story. The next pass should express monster intent through posture, watch arcs, wind, evidence age, and a persistent claimed-object/feeding/lair fact rather than another overlay color.
- Current proof: the final focused projection/provider/action/setup/reducer/scenario/UI/Vistest matrix passes 98/98 across 14 files; touched-file lint has 0 errors and 41 established warnings. Broad TypeScript still fails on separately tracked repository debt, with no remaining diagnostic in this slice's touched contracts.
- Still open: reconcile death/escape/pursuit/reinforcement outcomes back into WorldForge; author pre-contact route/evidence age; add persistent lair/feeding/burrow/trap or wounded-member facts; create true same-species silhouette/posture variation rather than relying only on shared portraits plus role overlays.

## 2026-07-15 generated-state patrol combat bridge

- The live GroundWorld can now emit a real `state-confrontation` when the player stands inside a generated settlement, that exact controlling-state standing is hostile, and WorldForge stationed a usable land regiment there.
- Event identity is deterministic by world seed, source cell, burg, state, and game day. A save-backed receipt prevents the same event from immediately replaying when 3D exploration remounts after combat; older saves load with an empty receipt list.
- State patrols have their own `settlement-state-patrol` encounter context, source faction, marker, and deployment label instead of masquerading as a local watch arrest. They still use the proven current-position party anchor and town-side interception formation.
- The World Battle Lab now includes `legium-state-patrol`, with a visibly labeled hostile-standing fixture and Vistest scenario `combat-world-state-patrol`.
- Screenshot pass 1 found two audit defects: the panel called a valid defending force a GAP because the live input was fixture-backed, and the tactical facts called the visible state-patrol frame None. Pass 2 separates force validity from live-input proof and reports State patrol explicitly; all three party tokens and four source defenders remain readable at 100%.
- Accepted proof: `.agent/scratch/state-patrol-pass2/combat-world-state-patrol.png` (ignored scratch path).
- Verification: focused union 58/58, legacy world-state load 2/2, Vistest registry 10/10, touched-file ESLint 0 errors (baseline warnings only), `git diff --check` clean, and touched-file TypeScript diagnostic filter clean. Broad `tsc -b` still exits 1 on unrelated repository debt.
- Open Plan Map decision: retain one patrol per settlement per day until combat outcomes can authoritatively change world standing/cooldowns, then consider an outcome-driven cadence.

## 2026-07-15 3D vegetation culling repair

- Ground-mode bushes could disappear while the camera panned because their worker-loaded instance matrices changed after Three.js had lazily cached the batch's aggregate bounding sphere at its initial transforms.
- The shared vegetation matrix writer now recomputes the instanced mesh bounding sphere only when a new scatter payload actually rewrites matrices. This preserves per-chunk frustum culling and the existing cache fast path.
- Focused tests pass 2/2. Live Playwright proof at world seed 42 / cell 476 covers both +/-72-degree orbit and +/-80 m horizontal pan with the low bush layer remaining populated and no console or page errors.

## 2026-07-10 playable-start pass

- Active objective: identify the intended opening loop, run it from the current checkout, repair concrete blockers, and exercise the opening flow end to end.
- Existing intended loop found: Main Menu -> Character Creation -> Start Point Selection -> generated opening situation -> peaceful conversation or hostile skill/attack resolution -> exploration/combat.
- Ollama is reachable at `localhost:11434`; current live verification can use the real generation path.
- Durable prior handover reports hostile openings as live-verified and leaves peaceful-opening verification plus repeated pre-fight dialogue after combat as open observations.
- Next: launch the current Vite app, play from the menu, inspect screenshots and browser errors, then classify any blocker before editing runtime code.

### Runtime proof and repair

- Peaceful run completed through the normal player path: Begin Legend -> Auto-Fill character -> Begin Adventure -> choose starting town -> generated opening conversation.
- The player action "I step between them and ask Therion what survival costs here" received the generated reply: "Survival costs lives, and you have no idea how many I've lost already."
- Ending the peaceful opening completed the generated quest and returned to exploration.
- Visual proof found the collapsed Agent sim and Town history developer inspectors covering the conversation Send button. Enter worked, but pointer submission was obstructed.
- `src/components/layout/GameModals.tsx` now hides those two dev inspectors while `activeConversation` owns the interaction area. They reappear after the conversation ends, so the developer tools are preserved.
- A fresh hostile QA run reached a tense generated opening with four suggested replies, free text, and an Attack action. DOM proof showed neither developer inspector mounted during that conversation.
- Focused verification: `npx vitest run src/components/layout/__tests__/GameModals.test.tsx --maxWorkers=2` passed 28/28.

### Remaining gaps observed

- Optional opening-scene illustration generation returned HTTP 500 in both runs. The player-facing text loop handled this honestly with "Scene illustration unavailable" and remained playable.
- Full-page Playwright screenshots can hang on the live 3D/WebGL surface. Element-scoped capture succeeded for the peaceful conversation and exposed the overlap defect; DOM snapshots were used for the hostile run.
- The hostile run reached the choice/attack surface, but browser automation became unresponsive while the live world scene was continuously rendering, so this pass did not supersede the prior nine-run live proof for dice/combat resolution.

## 2026-07-11 whole-game audit W01 continuation

- A real persisted run resumed through Continue Journey and displayed the expected named welcome transition for Emongotua Knom.
- After transition, the save restored the prior exploration log, generated world, player context, transport choices, and the World Map overlay that had been open when the save was written.
- The map rendered correctly and was visually inspected. Once the live map had rendered for several seconds, the browser controller stalled on all title-bar interactions, including Maximize and element-rectangle reads. This matches the existing WebGL automation limitation and is not registered as a product defect without independent interactive proof.
- Next W01 proof: abandon confirmation, checkpoint history presentation, IndexedDB migration/corruption recovery, and manual character-creation branches.

### Checkpoint history and shared window repair

- Live eligible exploration produced a real `1 Minute Checkpoint` after 63 seconds, proving the scheduler, save service, metadata index, and Resume Journey UI together.
- Resume Journey now separates Waystones (checkpoints), Echoes (rapid autosaves), and Chronicles (manual records). Load IDs and delete confirmation are covered by focused tests; the live deletion flow was cancelled with Keep Memory so the proof checkpoint remains available.
- Independent Playwright identified why Resume Journey Close was inert: the touch-sized top-right resize zone intercepted pointer events over the shared Close button. WindowFrame now layers title actions above resize zones. Unit, independent browser, and live close proofs pass.
- Next W01 proof: longer-tier retention, IndexedDB migration/corruption recovery, abandon confirmation, and manual character-creation branches.

### IndexedDB-first recovery proof

- The older Tiered Autosave GAP-005 wording was partly stale: one-time migration, migration-flag, and emergency-save recovery tests already existed.
- Five missing initialized-storage cases now prove primary IDB writes, IDB precedence over stale local data, valid local fallback when the IDB record is absent, corrupt IDB rejection, and deletion from both stores plus metadata.
- The fallback case exposed inaccurate diagnostics: load logs named IndexedDB based on availability even when localStorage supplied the payload. SaveLoad G8 now tracks and reports the actual source, with assertions for both paths.
- The focused save/load service suite passes 42/42. Tiered Autosave has no remaining open owner gaps, although W01 still owes longer elapsed-tier retention proof before the campaign row is fully verified.

### Active-run abandonment proof

- Returning from exploration exposed Abandon Run only while an in-memory party existed and showed a clear warning that unsaved progress would be lost.
- Cancel restored the Abandon Run action without clearing state. A second pass confirmed the action, removed Return/Abandon from the menu, and left persistent Continue/Resume recovery available.
- A focused MainMenu regression now proves the warning is non-destructive, Cancel is safe, and only explicit Confirm invokes the abandon callback. The complete MainMenu suite passes 13/13.
- The previously observed one-minute checkpoint was not present when this new browser run began, so this pass does not attribute its absence to Abandon Run. Longer-tier/cross-session checkpoint retention remains deliberately unverified.

### All checkpoint horizons

- The checkpoint hook now has deterministic proof through a full hour for every service-defined horizon: 1, 5, 15, 30, and 60 minutes.
- Each horizon fires at the expected repeated cadence and always uses its stable slot key, proving bounded overwrite behavior rather than unbounded save accumulation.
- A state update halfway to the first checkpoint did not reset the timer, and the eventual snapshot contained the latest state. Disabled autosave and leaving exploration still cancel all checkpoint work.
- Together with the live one-minute Waystone creation and rendered history proof, the W01 Autosave tiers row is now verified. Browser-profile lifetime behavior remains part of broader resilience testing rather than this timer contract.

## 2026-07-11 combat-map shell continuation

- The design preview and playable combat screen now share independent roster and command-rail visibility controls.
- Collapsing the command rail keeps a compact turn strip beside the battlefield with the active actor, action/bonus/reaction state, movement remaining, a restore-panel control, and End Turn.
- The battlemap planmap shell node records the resolved turn-strip policy and asks whether map-focus mode should stay status-only or gain a compact favorite-ability affordance.
- Rendered proof now covers an enemy turn, a player turn, the wide one-row HUD, the narrow two-row HUD, and restoring the command rail. Both widths keep the strip inside the battlefield without horizontal or vertical overflow.
- Focused verification passes: CombatView responsive tests (5/5), compact-strip lint, planmap derivation tests, JSON parsing, dependency sync, and the repository sync check.
- Rail visibility now uses one shared, schema-validated preference. First use and invalid saved data show both rails; deliberate choices survive later combats; storage failures still update the current screen without crashing.
- Live proof hid the command rail, reloaded into the saved map-focus layout with the compact turn HUD intact, restored commands, and reloaded into the restored full layout with no console errors.
- Persistence verification passes: shared hook tests (4/4) and CombatView responsive tests (6/6), including a full CombatView unmount/remount.
- Desktop roster and command rails now have bounded, remembered widths. Mouse dragging, arrow keys, Home/End, per-rail double-click reset, and the toolbar's full panel reset share one layout contract.
- Responsive caps reserve the battlefield's share at desktop widths; below the desktop breakpoint the rails remain stacked and both resize handles disappear.
- Live proof dragged the roster from 230px to 294px without grid overflow, reloaded with 294px restored, resized commands from the keyboard, reset both widths to 230/300px, and found no errors on a clean page load.
- Resize verification passes: handle interactions (2/2), layout/persistence hook (7/7), and CombatView responsive behavior (7/7).
- Next: the owner can steer whether map-focus mode stays status-only or gains a favorite-ability affordance.

### Shared combat targeting intent

- The design-preview and playable combat shells now share one targeting HUD across 2D, 3D, and popped-out map modes. It names the armed ability, action cost, required target, range, caster, follow-up, and description without changing combat resolution.
- Targeting now has an explicit icon cancel action and a universal Escape path. Escape deliberately yields to focused inputs, textareas, selects, and content-editable prompts so combat cancellation cannot discard in-progress text interaction.
- Rendered proof armed Unarmed Strike in 2D, verified the HUD clears the zoom controls, switched to 3D without losing the armed intent, and confirmed the HUD remains mounted within the compact 390px composition.
- Focused verification passes: intent HUD tests (2/2), CombatView responsive tests (8/8), touched-file lint with no errors, dependency sync for all three shared consumers, and the repository sync check.
- Next targeting decision: invalid target clicks currently leave the ability armed and show the validation reason. The owner can keep that forgiving retry behavior or make invalid attempts cancel; automatic nearest-target snapping is not recommended because it can attack a creature the player did not choose.

### Armed ability command feedback

- The command tile that started targeting now remains visibly pressed with a crosshair and high-contrast ring, in both embedded and popped-out ability palettes.
- Pressing that same tile again cancels targeting. The map HUD cancel button and Escape remain available, so cancellation is reachable from both the command origin and the target surface.
- The large hover tooltip unmounts while its tile is armed because the map HUD now owns those details; this prevents two floating panels from covering the battlefield after a click.
- Both playable combat and the design-preview battle map return stacked layouts to the battlefield after a command is armed, keeping the target surface and intent HUD together on compact screens.
- The existing Plan Map targeting node records this as a completed interaction contract. Invalid-target retry versus immediate cancellation remains the only open targeting decision in this pass.

### Truthful map command toolbar

- The 2D map's Move / Attack toolbar was present in the DOM but painted underneath terrain tiles because its CSS-variable z-index resolved to `auto`. It now uses the shared numeric combat-overlay layer, and live hit-testing lands on the command button rather than the terrain beneath it.
- The validation-reason banner uses the same explicit combat layer and begins below the command cluster, so an invalid target explanation cannot disappear behind the board or overlap the controls it is explaining.
- Quick Attack no longer assumes `abilities[0]` is an attack. It preserves authored loadout order while selecting the first affordable direct main-Action attack, skipping spells, movement, area/bonus attacks, cooldowns, and depleted uses; it disables when no truthful shortcut exists.
- Move and Attack now use familiar icons and accessible pressed states. Quick Attack announces the exact attack it will arm, and pressing the armed shortcut again cancels targeting consistently with the ability palette.
- Focused verification passes 12/12 across the new selection and toolbar regressions plus existing 2D parity, visibility, and object-interaction coverage. Live 2D proof confirms z-index 400, topmost hit ownership, exact `Attack with Unarmed Strike` naming, and pressed-to-cancel behavior.
- Next command decision: keep the shortcut on the first ready direct attack in authored loadout order, remember the character's last explicitly used direct attack, or let the player configure a favorite. Remembering the last explicit direct attack is recommended; automatic highest-damage selection would make the command less predictable.

### Manual creator backtracking

- The resumed W01 pass reproduced a contradictory draft after Human -> Age -> Back: the sidebar retained Human while the detail pane and primary action reset to Fallen Aasimar.
- `RaceSelection.tsx` now initializes its viewed race from the parent creator draft. A focused regression passes 1/1, and live reload/backtracking restores both Human indicators consistently.
- Character Creator G24 is resolved. Continue the representative Human martial completion, then a spellcaster and a race with additional required choices before closing the manual-creation inventory row.

### Manual creator representative matrix

- Human/Soldier/Fighter completed with recommended point buy, Perception, replacement class skills, Defense, three weapon masteries, Savage Attacker, Tough, and a named start-town handoff.
- Fallen Aasimar/Sage/Wizard completed with three class cantrips, six spellbook entries, racial Light, configured Magic Initiate Intelligence/Wizard choices, duplicate filtering, and a named start-town handoff.
- Changeling/Criminal/Rogue completed with required Deception/Insight/Small choices preserved through Back, combined provenance badges, four replacement Rogue skills, two masteries, Alert, visible final size review, and a named start-town handoff.
- Character Creator G25-G28 are resolved. The manual creation inventory row is verified; next W01 work is game-over and not-found recovery.

## 2026-07-11 W02 dense-world and travel continuity

- The user-reported rounded `1 FPS` was reproduced on the populated opening/town path: a cold full-cast scene ran at 8.12 FPS, and a competing active 3D tab reduced delivered frames to 0.85 FPS.
- Player/cast/generated-body fields now use bounded resolution and update cadence; overview residents are nearest-first and capped; distant building shells skip inert frame work; normal-height play uses the near shadow cascade and aerial views restore the far cascade; ground DPR is bounded.
- Cold full-cast proof improved to 33.12 FPS with the intended three live generated bodies, town geometry, near shadows, and aerial far shadows intact. Focused World3D/entity coverage passes 23/23. World3D W3D-G30 is resolved.
- Cell discovery now persists exact IDs and dedupes. Live `[2497,2499,2498]` survived an explicit save and full Continue reload; later hostile routes added exact 2761 and 2371. Travel G7 is genuinely resolved after correcting its earlier read-only false closure.
- Direct cell-native travel opened Wolf combat at 2761. A one-day underprovisioned route toward 2370 stopped at the exact time-affordable cell 2371 after 1396.11 of 1670.54 minutes and opened two-Bandit combat. Travel G18/G20 are resolved.
- Ferry mode rejects water-lane endpoints without changing the player cell, accepts land/port endpoints, and no longer snaps identity ashore. Foot-only parties expose only walking; a mounted party adds horse travel. Travel G19/G21 are resolved.
- The Classic SVG atlas now supports named focus, real-neighbor arrow navigation, Enter/Space activation, and guarded pointer/touch pan-versus-pick handling. The 29-test AtlasSvgView suite and a live keyboard-only route selection/commit resolve Worldforge WF-G12.
- The hostile encounter also exposed missing Elara/Kaelen placeholder portraits. Canonical data now omits them, and both companion render boundaries filter the two retired paths from legacy saves while preserving real future URLs. Eleven focused tests and a live stale-Elara injection produced no avatar request or console error; Companions G9 is resolved.
- The owner registries, whole-game proof ledger, coverage inventory, tracker, and Planmap now agree. The focused W02 atlas/travel matrix currently passes 98/98; project-doc audit validates the touched Travel, Worldforge, Companions, World3D, and audit registries.
- Next W02 continuity targets are cardinal/out-of-bounds movement, legacy Submap reachability, exploration commands, and exact 2D-to-3D-to-2D geography return.

### Long Rest action continuity

- The live Long Rest modal described an eight-hour recovery but confirmed by dispatching the reducer directly, bypassing planar checks, overnight simulation, journal rollover, messages, and the explicit time advance owned by `handleLongRest`.
- Confirmation now travels through the same `processAction` pipeline as other Action Pane commands. Modal-authored racial choices survive that route and are merged with any planar-rest denials in the one mechanical rest action.
- Focused ActionPane, GameModals, and resource-action coverage passes 48/48. Live confirmation advanced the clock from 10:40 to 18:40, emitted the settle/world/awake messages, closed the modal once, and produced zero console errors.
- Action Pane G1 is registered, resolved, and linked in the whole-game audit and Planmap.

## 2026-07-14 world-derived battlefield north star

- The battle-map goal is now explicitly world-first: combat terrain, roads, water, vegetation, structures, props, occupants, and encounter framing should be a tactical projection of the same WorldForge facts that define the explored location. The context-free battle-map generator remains available only as an explicit developer sandbox.
- A first visual debugging harness is available at `misc/design.html?step=battlemaplab`. It deterministically rebuilds a real boreal cell and a generated settlement through the existing off-thread WorldForge worker, extracts the production five-foot referee grid, mounts the full combat shell, and shows source-world counts beside tactical counts and parity findings.
- `BattleMapData` can now retain WorldForge provenance. Both the lab and production fight-in-place handoffs attach World -> Region -> Local -> Ground -> Tactical lineage plus the seed, optional atlas cell, and world-meter crop anchor.
- The painted renderer keeps continuous texture, terrain treatment, lighting, and tactical readability, but WorldForge-provenance maps no longer receive a synthetic road, shoreline bushes/reeds, lily pads, rare landmarks, or random object-like tile scatter. Explicit GroundWorld-derived tile decorations still render normally.
- Deterministic projection and painter-policy coverage pass. The first lab scenarios expose real bridge gaps instead of disguising them: WorldForge roads do not yet have combat terrain/movement semantics, and visible world props/features do not yet publish spell-targetable object facts.
- The Plan Map now carries the scenario-lab goal, its first completed slice, the road reactivation, and three owner decisions: first proving-ground scenario, the discrete-object truth boundary, and production parity-gate strictness.
- A concurrent canonical visual-test-harness registry/runner is landing under `src/devtools/vistest` and `tools/vistest`. The battle-map lab remains the specialized semantic debugger and is explicitly routed into that shared registry instead of growing a second general-purpose capture framework.

### Road ambush tactical slice

- The earlier road-semantics gap is now bridged for regional routes and town streets. A source route segment crossing the local window survives even when both endpoints lie outside that window, and each intersected five-foot cell retains road role, run index, and authored width.
- Roads are layered surfaces over base terrain. Natural obstacles are cleared from source road footprints, structures and explicit props still override them, and water remains blocked until a source ford or bridge proves a crossing.
- The painter now draws only source-backed road cells. The canonical seed-42, cell-373 Road Ambush fixture contains one regional route, zero town streets, 469 road cells, and 469 traversable road cells.
- `BattleMapData.encounterContext` now carries the selected route anchor and normalized heading. The live combat setup realizes that context as a party traveling on the route with enemies split across off-road flanks and biased toward source cover; maps without a frame preserve procedural formations or use the true center of their extracted dimensions.
- The World Battle Lab is the rendered proof surface for this work. It now defaults to Road Ambush, fits the full board for review, reports the encounter frame and route counts, and shows a passing source-derived framing check beside the real combat UI.
- Focused verification passes 44/44 tests across extraction, projection, deployment, and painting. The canonical visual-test registry adds 10/10 passing contract tests plus a successful `combat-world-road-ambush` capture. Touched-file ESLint has zero errors, Plan Map validation is clean, and both 1920x1080 headed proof and 1600x1000 canonical capture show the diagonal route, three party tokens on-route, and four enemy tokens on both flanks. The only headed-browser console error is the existing missing `favicon.ico` request.
- Remaining bridge work is explicit: source props/features still lack `TargetableMapObject` facts; roads need an owner decision on movement cost; the visible road edge can be smoothed without changing exact referee cells; and river crossings, settlement edges, occupants, and encounter-world deltas remain future scenarios.

### Real river-crossing tactical slice

- Region generation now owns a durable road/river crossing receipt after both final centerlines are smoothed and clipped. The receipt identifies the route, river, exact point, orientation, span, width, and bridge-versus-ford decision so downstream views do not infer independent crossings from overlapping paint.
- Ground projects the same receipt into source-indexed crossing facts and a physical masonry bridge deck consumed by World3D. Tactical extraction retains water as the base terrain, suppresses unbridged road-over-water overlap, makes authored bridges normal-cost and authored fords difficult-cost, and publishes exact crossing provenance on every affected referee cell.
- The canonical River Crossing recipe is seed 42, atlas cell 853: one 44-foot regional highway, one approximately 193-foot river, one bridge, 479 passable water-backed bridge cells, and no town geometry. Its encounter context deploys the party and enemies deterministically on opposite walkable banks.
- The World Battle Lab accepts `scenario=river-bridge-crossing`, reports source bridges/fords and tactical crossing/passability counts, exposes a capture-visible `7 pass / 0 warn / 1 gap` parity summary, and is registered as `combat-world-river-crossing` in the canonical visual-test runner.
- Screenshot critique drove four revisions rather than accepting green structural tests: the 120x90 crop became an inspectable 80x60 board; redundant diagnostics were condensed; a stepped tan cell strip became one oriented, flat masonry span consistent with World3D decks; and enemy camera/inspect controls moved out of the name area.
- Focused verification passed the Region, Ground bridge, real scenario, deployment, painter, roster-camera, and visual-registry suites. The canonical 1600x1000 capture is stored only under ignored `.agent/scratch/vistest/`; touched-file ESLint has zero errors. The repository-wide TypeScript check still reports established unrelated debt, while a touched-file filter is clean.
- The remaining visible parity gap is deliberate and actionable: WorldForge props/features still do not publish `TargetableMapObject` facts. Open owner choices are whether fords stay deterministic difficult terrain or gain Athletics risk, whether destroyed bridges persist as world deltas that invalidate traversal, and whether targetable source props should be the next canonical scenario before settlement edges.

### Source-object tactical parity slice

- GroundWorld natural features and authored props now project into deterministic `TargetableMapObject` facts with source kind, source id, world-meter position, tactical footprint, display name, and the mechanics the source can actually prove. The canonical Legium crop contains 98 natural-feature anchors and 298 prop footprints; source precedence leaves 51 feature targets visible while all 298 prop footprints receive tactical targets.
- Spell targeting is conservative at incomplete fact boundaries. General object targeting can use the projected props, but filters requiring a loose object or a known weight reject the 298 props whose catalog does not yet author attachment and weight instead of treating unknown as eligible.
- The World Battle Lab now includes an independently toggled object-facts overlay, source-versus-tactical counts, incomplete-fact warnings, and the full combat shell. Deterministic WorldForge initiative removes random active-turn drift from repeated captures, the command rail initializes at Turn Order, and the diagnostics layout moves below the board before it would compress the battlefield beyond useful inspection.
- Screenshot review caught and corrected three defects that structural tests did not: a 1353-pixel viewport devoted most of the frame to empty diagnostic width, the command rail opened with Turn Order clipped, and the critical incomplete-object warning sat below the initial viewport. Final 1600x1000 and 1353x1272 proofs are stored only under ignored `.agent/scratch/vistest/`.
- Focused verification passes 77/77 tests across extraction, object registration, spell filtering, initiative injection, combat rendering, scenario parity, and visual-registry contracts. A 7,316-test repository run initially found two older targeting fixtures that called objects loose without authoring `isFixedToSurface: false`; those fixtures now state the fact they intend to test. The remaining full-run failures are unrelated roadmap test-environment, raw-button debt, TownPlan golden-count drift, and loaded-suite timeout issues. Touched-file ESLint has zero errors, Plan Map validation is clean, and the repository-wide TypeScript check remains an established debt surface rather than proof for this slice.
- Recommended next scenario is a settlement edge with occupants so the harness has to prove structures, population provenance, encounter framing, and combat UI together. Before weight- or mobility-restricted object spells can claim parity, the prop catalog should explicitly author fixed/loose state and weight; stale natural features should be reconciled in shared Local/Ground facts rather than hidden only in Tactical.

### Settlement-edge occupant tactical slice

- The canonical `legium-settlement-edge` recipe now anchors an 80x60 tactical crop on Legium's real western gatehouse at 17:15. Forty-five source buildings and one of three source gatehouses touch the crop; the normalized gate-to-town heading drives party deployment outside the boundary and defender deployment inside it.
- The same live fractional-clock schedule used by the 3D ground scene now crosses the fight-in-place handoff. The fixture retains all 94 in-crop resident identities, including four moving residents, groups them into 38 visible tactical cells without deleting people, and freezes that snapshot for the encounter.
- Residents remain explicit ambient world facts rather than counterfeit player/enemy combatants. Their cells are reserved during source-aware and fallback deployment. Meter-to-grid rounding initially put 43 residents on walls or blocking props; screenshot diagnostics exposed that false state, and extraction now deterministically snaps only the tactical marker to the nearest walkable cell while retaining exact source meters.
- The World Battle Lab gives resident and object facts independent layer controls with scenario-specific defaults. A zoom-compensated gate receipt marks the exact source anchor and inward direction, resident parity and clock counts stay above the fold, and the canonical visual registry adds `combat-world-settlement-edge` with an explicit URL instead of relying on preset order.
- Screenshot review iterated across 1600x1000 and 1353x1272 captures: unrelated cyan/amber pins no longer bury residents, blocked placements fell from 43 to zero, the gate reason for the crop became visible, and the final receipt was lifted off the deployed tokens. The only browser console error remains the existing missing `favicon.ico` request.
- Focused verification passes 68/68 tests across Ground extraction, real scenario projection, source-aware deployment, resident/source-anchor DOM rendering, the 3D handoff, and visual-registry contracts; the post-snap Ground and scenario subset passes 38/38. Touched-file ESLint has zero errors. Repository and narrowed dependency-graph TypeScript checks remain blocked by established unrelated combat/spell and Vite-plugin debt, with no reported error in the files changed for this slice.
- Next steering choices are whether settlement defenders must come directly from generated guard/faction/hostility facts, and whether residents become inspect-only identity/activity markers before full neutral fleeing, harm, allegiance, and rescue semantics exist. Source-derived defenders and inspect-only residents are recommended.

### WorldForge regiment defender tactical slice

- Generated settlement-defense facts now cross the atlas-to-Ground boundary. Legium is retained as Turino's walled capital with a citadel, state alert 4.25, the 4,565-troop 1st (Legium) Regiment, and its separate 15-vessel fleet; the land regiment's exact 2,299 archers, 1,882 infantry, 323 cavalry, and 61 artillery remain inspectable.
- The named `gate-patrol-alert-sample-v1` policy makes the scale reduction explicit. Rounded state alert produces a four-actor budget, source proportions produce two archers and two infantry, and the standard Scout/Guard bestiary bridge supplies real combat mechanics. Every actor carries stable burg/state/regiment/unit provenance.
- Cavalry and artillery remain visible exclusions because mounted actors and siege-engine objects do not yet have honest tactical semantics. The scenario also labels hostility as recipe-authored: WorldForge proves the Turino force exists, but does not yet prove a hostile player-to-faction relationship or encounter trigger.
- Screenshot pass 1 was rejected despite passing data contracts. Every Turino defender inherited the generic enemy-orc emblem, and the initiative strip shortened all four to the identical label `Turino`, contradicting the source faction and erasing military role at the primary glance surface.
- Screenshot pass 2 replaces the false species portrait with a neutral regiment weapon emblem and uses `Archer 1`, `Archer 2`, `Infantry 1`, and `Infantry 2` in the turn strip while preserving full faction-bearing names in the roster and accessible labels. The diagnostics rail shows state, stationed troops, selected regiment, alert, tactical sample, exclusions, and the hostility gap above resident/object detail.
- Rendered review passes at 1600x1000 and 1353x1272. The narrower layout moves diagnostics below the battle shell rather than compressing the 80x60 board; no material overlap, clipping, unreadable role label, or source-marker/token collision remains. The only browser console error is the existing `/favicon.ico` 404.
- Next steering choices: require an explicit encounter trigger or player-to-faction relation before production can mark a generated regiment hostile; preserve residents as inspect-only ambient facts until neutral behavior exists; and build mounted/siege semantics before projecting cavalry or artillery tokens.

### Settlement defender hostility authority slice

- Generated military presence is no longer sufficient to create enemy actors. The `explicit-trigger-plus-matching-relation-v1` rule requires a current watch/state confrontation plus either a witnessed crime in the matching canonical settlement location (`cell_829` for Legium) or a hostile player standing with the matching generated-state faction (`worldforge-state:14` for Turino).
- Missing triggers, hostile standing without a current confrontation, and wanted records from another atlas cell all return `withhold-combat`. The source regiment and patrol projection remain inspectable context, but `createWorldDefenderCombatants` refuses to promote them into the enemy team.
- Settlement-defense facts now expose the burg's canonical atlas cell directly, joining the WorldForge source record to the same cell-native identity used by player location and crime state instead of inferring location from a tactical token or display name.
- The deterministic Settlement Edge visual scenario opts into a clearly labeled witnessed-assault fixture so the real combat UI can exercise a hostile watch confrontation. Diagnostics show the trigger, player-state relation, verdict, authority rule, and a separate gap stating that production fight-in-place does not yet pass live crime/faction state; the fixture is not a fallback.
- Screenshot pass 1 was rejected because the Defending Force panel advertised a green `PASS` while its live player-state input still showed `GAP`, allowing the primary hierarchy to conceal the production hole. The panel summary now aggregates source, hostility-authority, and live-input checks and correctly reports `GAP`.
- Screenshot pass 2 passes at 1600x1000 and 1353x1272. The desktop rail keeps the full hostility receipt readable above resident facts; the narrower layout moves diagnostics below the unobscured combat shell while retaining the gap in both the parity summary and force header.
- Focused verification passes 79/79 across settlement source facts, hostility resolution, patrol projection, real bestiary combatants, WorldForge scenario parity, source-aware deployment, object/resident rendering, initiative identity, and Vistest registry contracts. The canonical Vistest capture waits for the hostile verdict, visual-harness trigger, matching witnessed crime, and real Turino actor roster before taking evidence.
- Next steering choices: wire the production fight-in-place caller to current `GameState` crime and generated-state standing inputs; preserve residents as inspect-only ambient facts until neutral behavior exists; and build mounted/siege semantics before projecting cavalry or artillery tokens.

### Live GameState settlement-watch handoff

- Wanted-watch dialogue now asks the mounted WorldForge GroundWorld to prepare combat at the player's exact current meters. The provider extracts the live terrain and occupant snapshot, matches the confrontation to the generated settlement defense, supplies current witnessed-crime records and the matching `worldforge-state:<id>` standing, and preserves the regiment-derived combatants through the normal encounter launcher.
- A new `settlement-watch` encounter frame distinguishes an arrest at the player's current position from the harness's gate-approach recipe. The lead party member stays on the nearest legal center cell, companions gather behind, and the watch intercepts from the settlement side without occupying ambient resident cells.
- The generic two-Guard encounter remains only for static authored towns or interactions with no applicable generated settlement. A mounted generated settlement that withholds hostility or lacks a defense bridge surfaces that gap instead of silently substituting unrelated enemies.
- The deterministic lab adds `scenario=legium-watch-interception` and canonical Vistest id `combat-world-live-watch`. Its crime evidence remains explicitly labeled `visual-harness`; the diagnostics correctly retain a live-input GAP even though the recipe exercises the production framing and deployment path.
- Screenshot pass 1 was rejected because 248 violet resident markers and 24% whole-map fit hid the engagement. Pass 2 removed resident clutter but still reduced the actors to tiny marks. Pass 3 opens the watch recipe at 100%, centers the active party, lifts the source receipt, and visibly shows all four Turino defenders approaching without overlap or clipping. Final paired captures also confirm the resident-focused Settlement Edge recipe did not regress.
- Focused verification passes 60/60 across the production projection, runtime provider lifecycle, wanted-watch routing, prepared combatant preservation, deployment, real scenario harness, World3D loader, and canonical Vistest registry. Touched-file ESLint has zero errors; its warnings are established raw-button/legacy World3D/type debt. Repository TypeScript still exits on unrelated baseline diagnostics, with no changed-file diagnostic after the provider-union fix.
- Remaining steering choices are unchanged: keep residents inspect-only until neutral combat semantics exist, and implement mounted/siege rules before cavalry or artillery become tactical representatives.

### Production battlefield authority boundary and encounter inventory

- Production `CombatView` no longer treats an absent extracted map as permission to call the legacy arena generator. `generateWorldBattleSetup` requires real `BattleMapData`; procedural construction is isolated behind `generateProceduralSandboxBattleSetup` and the explicit `?dev_combat=1` / BattleMapDemo developer surfaces.
- Encounters that provide actors but no WorldForge location now fail closed in a dedicated `BattlefieldSourceGap` state. The combat board and initiative do not mount, combat AI receives no actors, and the only command returns safely to the world. The `?dev_combat_source_gap=1` fixture exists solely to prove this boundary.
- Rendered inspection passed at 1600x1000 and 390x844: the missing WorldForge projection and withheld procedural fallback are readable above the fold, no combat affordance leaks through, the return action remains reachable, and there is no clipping or overlap. Captures remain under ignored `.agent/scratch/`; console noise is limited to the established favicon 404, missing local AI credential warnings, and generic dev-fixture monster warnings.
- The focused authority matrix passes 32/32 across world/procedural setup separation, source-gap rendering and safe return, encounter actor preparation, and reducer map preservation.
- `docs/projects/battle-map/WORLDFORGE_SOURCE_INVENTORY.md` now audits every known tactical entry path. Ground hostile proximity, generated-settlement wanted watch, and generated-state patrols are migrated. Land travel, hostile openings, static authored-town watch, sea encounters, and location-free encounter simulation are explicitly withheld rather than silently receiving terrain. Procedural combat, the source-gap fixture, fight-in-place probes, and the World Battle Lab are separately labeled developer paths.
- The next highest-value migration is the land-travel ambush: its destination atlas cell and deterministic actor receipt already exist, while the Road Ambush lab recipe already proves the desired source-route crop and deployment. Production still needs to assemble that exact destination through World -> Region -> Local -> Ground -> Tactical and attach the travel-event receipt before combat transition.

### WorldForge-backed land-travel ambush handoff

- Committed land-travel encounters now retain their encounter kind and exact route-cell receipt. On arrival, `App` rebuilds the destination GroundWorld from the saved world seed, game time, and WorldForge deltas through the complete Stage-B worker result before it starts combat.
- `createTravelAmbushBattlefield` selects the nearest real road in that destination cell, extracts the existing 80x60 road-ambush projection, verifies exact cell and road-anchor provenance, and appends the travel-event route receipt to the generation path. Missing seed, destination, complete GroundWorld, or source road returns an explicit source gap; none of those cases receives a center crop or procedural arena.
- The one-shot WorldForge loader now resolves only the complete Stage-B world, disposes its worker after success or failure, and forwards worker crashes to active callers before teardown. Focused lifecycle tests cover completion, source rejection, and worker failure.
- `?dev_travel_ambush=1` is a deterministic source-backed trigger, not a second implementation. It sends seed 42, destination cell 373, route cells 112/201/373, and three Bandits through the same loader, projector, encounter action, reducer, and production `CombatView` used by committed travel.
- Rendered review at 1600x1000 and 1353x1272 shows the diagonal source road as the encounter spine, the party gathered on-route, and three enemies split across source terrain on both flanks. The desktop composition stays balanced; the narrower/taller viewport retains readable rails, board controls, token separation, and the route-heading receipt without overlap or clipping. Its larger open foreground is source-authored terrain rather than a synthetic fill. Browser console output has no application/runtime error; only the established missing `favicon.ico` request and local AI-key warnings remain.
- Focused verification passes 61/61 across complete GroundWorld loading, travel projection/refusal, land and sea travel metadata routing, encounter setup, combat source boundaries, responsive rendering, and reducer persistence. Touched-file ESLint has zero errors; the new hook dependency warning was removed by passing only the canonical seed/time/delta fields. The repository-wide TypeScript check still exits on the separate known debt surface, while its output contains no diagnostic for the touched battlefield/travel files.
- The production inventory now counts road-backed land travel as the fourth migrated class and keeps roadless land travel visibly withheld. Hostile openings, static authored-town watch, sea encounters, and location-free encounter simulation remain the next audited source gaps.

### Player-readable tactical elevation

- River Crossing exposed 4,800 raw floating-point `elevation` values ranging from `22.843...` to `51.063...`. Audit traced the real contract: the tactical patch stores WorldForge relief metres divided by `0.3`, while WebGL multiplies by `0.3` to recover vertical metres. One shared config constant now drives extraction, 3D terrain, and the 2D player conversion.
- Tile labels no longer show renderer units. They state rounded whole feet above, below, or level with the active creature and retain rounded relief above the generated local low point. Hovering any tile leaves an unscaled terrain HUD visible outside the fitted board; raw values remain absent from player titles and accessibility labels.
- The ground painter adds smoothed directional hillshade and interpolated five-foot contour segments. Relief renders beneath source roads, water, bridge decks, props, bodies, fog, and interaction overlays, preserving tactical and WorldForge hierarchy instead of drawing a debug layer over combat.
- Visual pass 1 was rejected because contours were too quiet and the deterministic probe selected an off-edge tile. Pass 2 increases contour/hillshade contrast and chooses a visible interior raised tile. Accepted 1600x1000 and ignored 1353x1272 River Crossing captures keep the bridge dominant, the readout legible, and controls/legend unobstructed.
- Focused elevation, tile, painter, and Vistest suites pass 32/32. A real-browser proof reports 4,800 semantic tiles, zero raw `Elev:` titles, the five-foot contour legend, a raised relative readout, and zero console/page errors. Touched ESLint has zero errors; repository TypeScript still exits on the established unrelated debt set.
- G14 remains active rather than overstated: source-backed cliff edges, slope direction/risk, climb cost, and elevation-aware line of sight still need referee semantics and a steep deterministic WorldForge proof scenario.

### Static authored-town watch source-gap closure

- The legacy wanted-watch fallback no longer creates two generic Guards when no mounted generated-settlement provider applies. It starts a structured authored-town source gap containing the encounter, requested authored location, and missing WorldForge cell, settlement-site, and tactical-projection facts, with no monsters, combatants, source identities, or map.
- `handleStartBattleMapEncounter` treats a declared source gap as a strict state contract and rejects any payload that also carries actors or terrain. `CombatView` presents the exact reason, explicitly reports `Enemy roster: Not fabricated`, withholds the board and combat rails, gives combat AI an empty actor list, and exposes only safe return.
- Permanent Vistest `combat-world-authored-town-watch-gap` sends the fixture through the real application action, reducer, and production `CombatView`, not a component-only mock. Its readiness gate requires the exact source-gap code, all missing facts, no Turn Order, and no End Turn.
- Focused verification passes 51/51 across launcher, action, rendered boundary, and Vistest registry contracts. Adversarial 1600x1000 and 390x844 screenshots keep every fact readable and the return action above the fold with no board, roster, combat affordance, overlap, or clipping.
- G9 is complete under its explicit alternative: authored towns are still unmapped and therefore unsupported, but the application no longer invents a roster or arena.

### Remaining unsupported production launcher closure

- `useSeaEncounter` still consumes each hostile voyage event exactly once, but it now sends only `sea-encounter-no-worldforge-battlefield`. The proposed naval roster never becomes combat state; the diagnosis names the absent sea surface, vessel deck, relative headings, weather, and boarding context.
- EncounterModal still lets players build AI-generated, custom, and bestiary proposals, but `Simulate Battle` now sends only `location-free-simulation-no-worldforge-location` until a canonical cell, crop anchor, and encounter-to-location receipt are explicitly selected. The current game location is not silently assumed.
- Both launchers share production reason builders with deterministic application fixtures and cross the normal action, reducer, and `CombatView`. The strict source-gap boundary continues to reject any payload that combines a diagnosis with monsters, combatants, source identities, or terrain.
- Permanent Vistests `combat-world-sea-encounter-gap` and `combat-world-location-free-encounter-gap` require exact reason codes and omitted facts, `Enemy roster: Not fabricated`, no proposed enemy names, no Turn Order, and no End Turn before capture.
- Focused verification passes 46/46. Adversarial 1600x1000 and 390x844 captures for both states retain readable hierarchy, every missing fact, and safe return above the fold without a board, combat rail, overlap, or clipping.
- G10 and G11 close as deliberately unsupported rather than falsely migrated. The production encounter inventory now has no open or unaudited launcher class: each known class is source-backed or has deterministic fail-closed proof. The broader goal remains active for G12-G14 monster-world lifecycle, renderer parity, and steep-terrain semantics.

### Elevation comprehension rework after direct user confusion

- The prior River Crossing HUD was truthful but still failed comprehension: it showed a tile-relative value and a map-zero value as separate prose rows, leaving the player to infer why one tile could be both `19 ft` and `8 ft higher`.
- The inspector now places `This tile`, the selected creature, and `Map floor 0 ft` on one visible ruler, then states the relative result. Relative feet are calculated from the two displayed whole-foot heights so rounding can never make the shown subtraction disagree.
- The first new visual pass was rejected because translucent water contaminated the panel and the explanation line was too faint. The accepted pass uses an opaque background, stronger type, and the shorter persistent legend `Elevation: floor 0 ft | contour step 5 ft`.
- Focused elevation/tile/Vistest coverage passes 29/29. Permanent `combat-world-river-elevation` captures pass at 1600x1000 and 1353x1272 with no panel/control/legend collision; direct 989x1032 inspection also remains coherent. The browser console reports zero errors.
- A separate 390x844 audit invalidated the earlier mobile claim: the workbench and lab headers reduce the map-bearing combat grid to an approximately 90px nested scroller. The elevation panel itself fits its width, but the whole harness cannot provide honest mobile visual proof. This is now G15 and needs a compact full-canvas lab mode.

#### Pause handoff - 2026-07-16

- Paused at the user's request after the elevation comprehension slice was implemented, documented, and visually accepted. Do not redo this slice on resume.
- Proven now: 29/29 focused tests; permanent `combat-world-river-elevation` Vistest passes at 1600x1000 and 1353x1272; direct 989x1032 inspection passes; browser console has zero errors; touched ESLint has zero errors and seven established raw-button warnings; Plan Map validation, living-project audit, sync-check, and diff-check pass.
- The full repository TypeScript check still fails on the separately routed debt set. A second narrowed run intended to prove zero diagnostics in the touched elevation/Vistest files was explicitly terminated when the pause request arrived, so that narrow proof remains the first verification step on resume.
- Current goal remains active. G14 still owns source-authored cliff/slope mechanics, G15 now owns the collapsed 390px World Battle Lab viewport, and the previously planned G13 active-WebGL monster body-state parity slice has not been implemented in this elevation turn.

### Terrain-aware 3D camera spawn

- The World Battle Lab 3D camera had retained its original flat-board assumptions: it looked at `y = 0` and spawned at a fixed `y = 10`, even though WorldForge terrain can be substantially above that plane. Elevated scenarios could therefore begin with the target, and sometimes the camera itself, inside the ground mesh.
- Both WebGL and WebGPU now sample the shared terrain heightfield at the map anchor and at the physical spawn coordinate, then start ten world units above the higher surface. Camera focus, cinematic framing, and deterministic capture poses use that same sampler, so later camera actions cannot reintroduce the below-ground aim point.
- Focused BattleMap3D/WebGPU tests pass 4/4. A live Legium Watch 3D first-frame inspection is visibly above the terrain and road surface; the only browser console error is the existing missing favicon request. Touched-file ESLint has no errors and `git diff --check` passes; its 20 warnings are established legacy warnings in these renderer/test files.

### Q4 terrain shader: close-up detail, blocked rim, material variety (2026-07-18)

- Work packet Q4 of the 3D combat-map visual phase 3 (Agora task 8450f35a) landed three shader slices in `src/components/BattleMap/terrain/TerrainMesh.tsx`, program cache key v7 → v8.
- GOAL #26 (close-up detail): a high-frequency albedo octave and a third micro-normal octave fade in under 8 world units of camera distance, so leaning in shows real grain; both are free at tactical zoom. Now OK.
- GOAL #29 (walkable vs blocked): rock and wall tiles get a dark carved seam plus a bright inner lip at every border facing walkable ground; interior blocked-blocked edges stay untouched. Verified on desert rock plates; kept PARTIAL because cave/dungeon wall captures were unusable — a wall-biome eyeball is still owed.
- GOAL #30 (material variety): wider rock mineral-zone contrast, rare heather sweeps in grassland, and sharper wet-dry soil zoning, all at macro frequency so task 84's organic borders survive. Now OK.
- Proof: seed-pinned A/B at SEED 424242, POSE 30,55,40 and 12,60,20 across forest/desert/cave — q4-before-*/q4-after-*.png plus native-res crops in `.agent/3d-visual-quality/captures/`. Scoped suites green (terrain 4/4, BattleMap3D/elevation/fog 16/16); tsc has no errors in the touched file (repo-wide failures are pre-existing debt from other campaigns and reproduce with this diff reverted).

## 2026-07-18 — Q2: 3D damage numbers legible at tactical zoom (GOAL #16 → OK)

- Root cause: the 3D floating combat numbers used drei `Html distanceFactor={10}`, which shrank the 13px text to about 4.6px at the standard 30-unit tactical camera — invisible over foliage. The 2D map's numbers are a fixed 24px, so 3D silently lost parity.
- Fix in `src/components/BattleMap/vfx/VFXSystem.tsx`: constant screen-size numbers (no distance scaling, matching the 2D convention), 16px font (22px crits), hard black outline on all four sides, slower rise, and hold-then-fade — full brightness for the first 40% of the 1.5s life instead of fading from the first frame.
- Proof: seed-pinned A/B (SEED 424242, POSE 30,55,40) — q2-dmg-before5-forest.png (empty air where the DOM said numbers were) → q2-dmg-after-forest.png (MISS / -7 / +6 clearly readable). New rig support: `DAMAGE=1` for shoot.mjs plus a dev-only `spawnDamage()` hook in BattleMapDemo that drives the real turn-manager path, since no headless route executes abilities deterministically.
- Verify: BattleMap3D parity/visibility suites green; the one 2D parity failure is the pre-existing stale-label debt task 87 documented. tsc: none of the tree's 505 in-flight errors touch these files.

## 2026-07-18 — Interior occupants walk between stations (Agora 7b7c68f4)

- Replaced the living-interiors v1 hour-boundary snap with continuous travel during the first half of each new schedule hour, matching the existing street-agent rhythm. Residents face their destination and use the generated body's walk gait while moving, then return to idle at the station.
- OUT-to-home and home-to-OUT boundaries deliberately keep their prior appear/disappear behavior because the interior schedule has no door or exterior route. The street-agent layer remains the authority for outdoor travel; no wall-crossing fallback was invented.
- Focused proof: 17/17 InteriorOccupants and InteriorHourContext Vitest checks pass, touched-file ESLint is clean, and dependency headers were synchronized. A real seeded household render moved occupant 0 from meal to hearthside across 2.155 m; the 19:15 frame was the exact midpoint, and the start/mid/end captures were visually inspected under `.agent/scratch/codex-q-7b7c68f4/`.
- Residual: station-to-station movement is a straight interpolation because the baked interior packet has no room-door or stair graph. Adding true indoor pathfinding is a separate expansion slice; this implementation leaves the schedule and placement contracts open for it.

## 2026-07-19 — Deterministic dungeon architecture forms (Agora 37027b62)

- The existing `DungeonPlan` bitmap remains the sole walkability, collision, route, and object-identity authority. Its wall cells now produce boundary-oriented runs with deterministic height variation, while plan doors produce real half-torus arches and side piers without changing a door cell or consuming a random stream.
- Crypt adds restrained vertical supports, cavern adds overlapping curved rock mass, and frost adds depth-influenced walls and deterministic ice fins. These are permanent theme architecture batches in the existing preview renderer, not optional dressing or a parallel procedural representation.
- Headed seed-42 before/after comparisons and close entrance views are stored under ignored `.agent/scratch/proof/dungeon-architecture/forms/`. The mounted canonical seed-42 product fixture walked 37 cells to its treasure room and persisted one claim after the renderer change.
- Focused verification passes 46/46 across the scene model, generator, lifecycle, gameplay, and mounted entry prompt. Touched ESLint is clean and dependency metadata is synchronized. Repository `tsc -b` still exits on the broad unrelated debt backlog; its reported diagnostics do not identify the touched dungeon files.
- This pass deliberately stops before room-purpose readability, multi-level descent, broader dungeon gameplay, and performance profiling. Cavern curves remain a visual shell over the authoritative grid because replacing grid collision with curved collision would require a separate data-model redesign.

## 2026-07-21 — Canonical world-map completion audit

- Reconciled the `world-map-quality` Plan Map topic with all seven original Agora tasks and six strict-audit repair tasks. Design Preview, World Generation, live `MapPane`, and Atlas explorer now share the canonical `getBridgeAtlas` + `AtlasSvgView` L0; the retired canvas phase fails closed while Region, Local, Ground, and return-to-Local remain operational.
- The start-town listbox is bounded to 150 rendered choices per page. Search and paging retain all 888 canonical towns, announce result/page changes, and synchronize selection so a filtered-out town cannot be confirmed. The phone Design Preview verifier no longer stretches over the map.
- The live-game harness is cold-start and fail-closed: it waits for the enabled visible UI button without coupling the click to SPA URL normalization, then proves real PLAYING ancestry, responsive containment, a populated SVG, zero canvas descendants, seed-scoped preferences, and a current source fingerprint without depending on historical scratch evidence.
- Current headed proof covers 1440 x 900 and 390 x 844 entry routes plus World → Region → Local → Ground → Local. The final current-tree performance receipt records a 145 ms longest task, zero tasks over 200 ms, 16.7 ms median and 16.8 ms p95 steady pan/zoom; structured-clone output remains byte-identical and rebuilt-quadtree nearest-neighbor parity is directly tested.
- Proof is disposable and ignored under `.agent/scratch/proof/world-map-quality/`. Expected console noise is limited to denied external fonts, missing optional API configuration, favicon 404, and the intentional retired-route warning; repeated Three.js non-indexed-geometry warnings were removed.

## 2026-07-23 — Identity-preserving WebGPU building batching

- The WebGPU probe no longer creates one Three.js mesh and one box geometry for every generated wall, furnishing and facade component. Streamed boxes are grouped into per-colour `InstancedMesh` batches backed by one unit cube, while each instance retains its authored position, rotation, dimensions, colour and stable building/part identity. Roofs remain individual generated meshes, so building form and silhouette are not standardized.
- A controlled runtime inspection reduced the loaded scene from 88,788 objects and 87,442 unique geometry objects to 2,726 objects and 1,077 unique geometries. JavaScript heap use fell from about 2.0 GB to 345 MB. Three focused batching tests pass; touched-file ESLint has zero errors; the dependency header is synchronized. Repository `tsc -b` still exits on unrelated scripts/dev-hub project-list debt and reports no touched-file diagnostic.
- The controlled automation tab remained clamped to roughly one `requestAnimationFrame` callback per second even with every scene child hidden, so it cannot provide an honest foreground FPS result. The installed in-app Browser plugin is missing its required `scripts/browser-client.mjs`; reload and measure the visible Codex browser tab after that control surface is repaired.

## 2026-08-09 — World 3D performance diagnosis and bounded fixes

- A mounted-scene census reconciled the renderer's 16.91 million submitted triangles with 12.97 million mounted main-pass triangles plus 3.98 million potential shadow triangles. Dense vegetation was the triangle cost; hundreds of individually rendered multi-part ground props were the draw-call, geometry, material, program, and heap cost.
- Market stalls, fences, wells, carts, woodpiles, troughs, and crate stacks now preserve their exact primitive transforms in shared instanced batches. The live town view stayed visually intact while draw calls fell from 1,941 to about 1,147, compiled programs from 151 to 72, and the measured JavaScript heap from about 675 MB to about 503 MB in the immediate like-for-like post-change reading.
- Ground bushes keep N8AO contact shading but no longer replay their dense crowns through the shadow map. This removed about 1.86 million submitted triangles. Walking-scale understory is now limited to the five-by-five chunk window around the player, preserving nearby ferns, saplings, and logs while withholding distant sub-pixel floor detail.
- The performance panel now explains scene composition instead of stopping at aggregate renderer counters. It reports pass amplification, mounted main and potential shadow triangles, mesh and instance counts, the five heaviest named families, and a marked baseline with live frame, GPU, CPU, draw, and triangle deltas. Pasted live and recorded reports carry the same scene-family diagnosis.
- Focused performance-session and World3D lifecycle coverage passes 24 tests. Touched ESLint has zero errors; existing warnings remain in the World3D shell and custom performance-panel buttons. A live 995×774 rendered check confirmed the town, terrain, trees, local floor detail, labels, and expanded panel remain visible.
- Remaining: trees are about 68–70% of mounted scene triangles and sites still account for most individual meshes. Any tree LOD or site-shell batching pass needs its own close-range and skyline visual proof; this bounded pass did not reduce art-directed tree geometry or replace building forms.

## 2026-08-09 — World 3D utility-window consolidation

- Locale map, Agent sim, and Town history now launch from the existing top-right Controls menu. Their old always-visible lower-corner launchers are gone, leaving the 3D scene and performance panel unobstructed until a tool is requested.
- Agent sim and Town history use the shared WindowFrame shell, including named-dialog semantics, drag, resize, reset, maximize/restore, close controls, and independent persisted geometry. Their simulation and chronicle content remain unchanged inside scrollable window bodies.
- Focused Controls and inspector coverage passes 10 tests. Touched ESLint has zero errors; existing raw-button and World3D shell warnings remain. Rendered checks in the live 3D town confirmed all three Controls entries and both WindowFrame presentations.

## 2026-08-10 — Design Preview Character Sheet navigation

- Renamed the primary `Character` workbench group to `Character Creator`. Renamed the gameplay `Equipment` destination to `Character Sheet` because it now exposes the complete sheet rather than only its equipment overview.
- The Live Character Sheet preview now switches between the production Overview, Skills, Details, Crafting, and Journal views. Crafting receives an isolated reducer-backed game context, so opening its nested production panels does not touch a running or saved game.
- Focused Vitest passes 4/4 across the existing equipment behavior and the new five-tab contract. Full TypeScript checking passes. The ignored Design Preview paths report no scoped ESLint errors, with existing warnings only.
- Rendered proof exercised all five tabs, opened the production Gathering panel, verified the final Journal spread, and found no new page errors. Disposable proof is under `.agent/scratch/character-sheet-tabs/`.

## 2026-08-10 — Character Sheet equipment icon parity

- Fixed portable item SVG paths resolving beneath the current nested document route in equipped mannequin slots. Backpack rows and equipped slots now share one base-aware item asset resolver, and a replacement item gets a fresh image-load attempt if the prior slot image failed.
- Focused visual resolver and mannequin coverage passes 19/19. Scoped ESLint has zero errors and retains 13 established warnings. Repository TypeScript checking still stops on five unrelated diagnostics in spell, 3D entity, and action-economy test files; none identify the touched Character Sheet or visual resolver files.
- Rendered proof equipped Leather Cap and compared both DOM image sources: backpack and Head slot each request `/Aralia/assets/icons/general/armor/leather_cap.svg`. The official web-game client capture was visually inspected under ignored `.agent/scratch/character-sheet-icons/proof/`.

## 2026-08-11 — Tactical Sandbox Grapple & Escape

- Registered `grapple_escape` under States & Objects without changing any existing scenario. Its authored board opens with a grappler and target adjacent inside a visible five-foot sand reach ring, with Grappled represented in both combat condition mirrors and the target's production movement pool set to 0.
- Four disjoint controls exercise the shared combat path: apply Grappled, voluntarily release it, spend the target's action on a deterministic Dexterity (Acrobatics) escape check, and apply Incapacitated to the grappler before canonical maintenance automatically releases the hold. The scenario keeps no browser-only grapple state.
- Focused verification passes 18/18 across the shared ability-check roller, grapple application/removal/escape/maintenance mechanics, scenario controls, catalog, and registry. Dependency headers were synchronized for every changed exported utility and scenario path.
- Rendered 2D proof shows the four controls, adjacent actors, reach cue, Grappled marker, 0-foot movement log, restored movement after release, the successful `d20 18, total 23 vs DC 13` escape, and Incapacitated auto-release. Rendered 3D proof keeps the board, both actors, status/reach cues, controls, and log visible after a 160-pixel wheel dolly and a 75-by-40-pixel right-button orbit. Console errors stayed at 0 throughout.
- Chromium emitted repeated `glBlitFramebuffer` depth/stencil alias warnings only after 3D launch. The scene still rendered and remained interactive; cross-project follow-up is recorded as `GG-51` in `docs/projects/GLOBAL_GAPS.md`.

## 2026-08-11 — Tactical Sandbox Shove & Knock Prone

- Registered `shove_prone` under Targeting & Space while preserving all prior scenario ids and the cumulative `grapple_escape` lane. Its deterministic board keeps a Medium shover and target adjacent on a marked push lane, with a visible destination, side blocker, and hazard cue.
- Four controls resolve a successful Strength save, a failed-save five-foot push, a failed-save Prone choice, and the blocked-destination or Huge-target edge cases. They use the shared saving-throw roller, `MovementCommand`, and paired runtime Prone state; no browser-only shove result is stored.
- Focused verification passes 50/50 across deterministic save injection, canonical shove mechanics, scenario controls, catalog/registry, and neighboring forced-movement and grapple controls. Required dependency headers were synchronized for changed exported utility and scenario paths.
- Official and supplemental Playwright proof was written only beneath ignored `.agent/scratch/shove_prone/` and visually inspected at 1600 by 1000. Both 2D and 3D launch; after a 160-pixel wheel dolly and a 75-by-40-pixel right-button orbit, actors, lane/blocker, Prone cue, controls, and the reasoned combat log remain visible. Console and page error delta stayed at 0. No new project gap was found.

## 2026-08-11 — Tactical Sandbox Critical Hits & Natural 1s

- Registered `critical_hits` under Core Rules while preserving every prior catalog entry and both cumulative campaign scenarios. The board publishes a level-5 Dexterity-14 Longbow Archer (`+5`, `1d8+3`) plus passive AC 30, AC 18, and AC 5 targets at 40 HP.
- Four disjoint controls call shared attack resolution, damage dice, modifier/proficiency math, and HP transition helpers. Natural 20 totals 25 against AC 30 and critically hits for deterministic `2d8 + 3 = 13`; natural 1 totals 6 against AC 5 and automatically misses; ordinary totals 18 and 17 respectively hit and miss AC 18. No browser-only hit or damage truth was added.
- Focused `/test-ts` verification passes 34/34 across the new controls, catalog registry, canonical attack/damage helpers, and neighboring Grapple/Shove campaign controls. Changed runtime dependency headers are synchronized; scoped lint reports zero errors with 15 established warnings in the ignored scenario host.
- Official and supplemental Playwright proof was written only beneath ignored `.agent/scratch/critical_hits/` and visually inspected at 1600 by 1000. Both 2D and 3D launch; after a 160-pixel wheel dolly and a 75-by-40-pixel right-button orbit, the archer, three targets, attack cues, controls, and reasoned critical log remain visible. Console and page error baseline and delta are both 0. No new project gap was found.

## 2026-08-11 — Tactical Sandbox Healing & Temporary HP

- Registered `healing_temp_hp` under Core Rules while preserving every existing scenario and the cumulative Grapple, Shove, and Critical Hits campaign state. Its deterministic board keeps a Field Cleric beside a Wounded Ally at 12/24 HP on a short green support lane.
- Four disjoint controls prove 12 → 20 ordinary healing, a 20-point heal capped at 24/24, an 8 → rejected 5 → replacement 12 temporary-HP sequence, and 15 damage consuming 12 temporary HP before reducing 24 current HP to 21. The scenario calls the same shared healing, temporary-HP, damage, and downed-state helpers used by production commands; no browser-only health total exists.
- The missing non-stacking primitive now lives in `deathSaveUtils.ts`. Both `HealingCommand` and `DefensiveCommand` call it, preserving the larger pool and its provenance while a larger replacement takes ownership. Focused verification passes 57/57 across canonical commands/helpers, renderer cues, scenario controls, catalog/registry/host, and Grapple/Shove/Critical neighbors; the post-layout neighbor rerun passes 23/23. Dependency headers are synchronized, and scoped lint reports zero errors with 17 established warnings.
- The exact official web-game client implementation and supplemental headed Playwright proof were written only beneath ignored `.agent/scratch/healing_temp_hp/` and visually inspected at 1600 by 1000. In 2D the HP arc, cyan `+12` cue, four controls, and exact log transitions remain visible. In 3D both actors, the Wounded Ally `12/24 + 12 temp` nameplate, cyan `+12` cue, controls, and log remain visible after a 160-pixel wheel dolly and a 75-by-40-pixel right-button orbit. Console and page error baseline and delta are both 0.
- No unresolved product or cross-project gap was found. The reusable renderer addition exposes canonical temporary HP separately from current/max HP in both 2D and 3D instead of relying on scenario-only labels.

## 2026-08-11 — Tactical Sandbox Saving Throws & Half Damage

- Registered `saving_throws_half_damage` under Core Rules while preserving every existing scenario and the four completed campaign lanes. Its deterministic board keeps a level-5 Intelligence-18 Pyromancer (`DC 15`, `4d6 = 15`) beside a Slow Guard (`DEX -1`, `30 HP`) and Agile Scout (`DEX +3`, `30 HP`) inside a visible fireburst area.
- Four disjoint controls call shared spell-DC, saving-throw, damage-dice, save-outcome, resistance, and HP-transition helpers. They prove a failed `9 vs DC 15` save taking the full 15 damage, a successful `17 vs DC 15` save taking 7 after rounded-down half damage, an exact `15 vs DC 15` success, and production ordering where save then Fire resistance reduces `15 → 7 → 3`. No browser-only save or damage truth was added.
- Focused `/test-ts` verification passes 75/75 across the new controls, mounted host, catalog/registry, canonical saving throws/resistance/HP, and Grapple/Shove/Critical/Healing campaign neighbors. Dependency headers are synchronized; scoped lint with ignored Design Preview files enabled reports zero errors and 15 established host warnings.
- The exact official web-game client and supplemental headed Playwright proof were written only beneath ignored `.agent/scratch/saving_throws_half_damage/` and visually inspected at 1600 by 1000. In 2D the three actors, 13 FIRE markers, four controls, HP outcomes, and reasoned log remain visible. In 3D all three actors, fireburst VFX, controls, `27/30` HP outcome, and resistance log remain visible after a 160-pixel wheel dolly and a 75-by-40-pixel right-button orbit. Console and page error baseline and delta are both 0.
- Evasion was not found in the canonical runtime paths inspected, so the scenario does not invent it. The implemented resistance-ordering interaction and exact-DC boundary satisfy this lane without opening a new project gap.

## 2026-08-11 — Tactical Sandbox Multiattack & Attack Riders

- Registered `multiattack_riders` under Core Rules while preserving every existing scenario and the five completed campaign lanes. Its deterministic board keeps a Large Venom Drake beside an Iron Guard and Venom Ward on visible attack lanes with poison cues.
- Four disjoint controls spend one canonical Action for an authored Bite and Claw sequence, while each attack keeps its own target, d20 roll, Armor Class comparison, and HP transition. They prove independent hit/miss results, a target-bound Venom Rider that triggers only after its Bite hits, split targets, repeat-use rejection after the Action is spent, and Poison immunity reducing only the rider's damage to 0.
- The narrow shared `resolveMultiattackSequence` primitive composes existing action-economy, attack-roll, damage-roll, resistance/immunity, HP-transition, and `AttackRiderSystem` mechanics. Focused verification passes 86/86 across the primitive, scenario controls, mounted host, catalog/registry, canonical mechanics, and all five campaign neighbors. Dependency headers are synchronized; scoped lint reports zero errors and 15 established host warnings.
- The exact official web-game client and supplemental Playwright proof were written only beneath ignored `.agent/scratch/multiattack_riders/` and visually inspected at 1600 by 1000. In 2D the attacker, both targets, attack/poison cues, controls, HP outcomes, and reasoned log remain visible. In 3D the Large Dragon, targets, board cues, controls, immunity outcome, and log remain visible after a 160-pixel wheel dolly and a 75-by-40-pixel right-button orbit. Console and page error baseline and delta are both 0. The existing production monster-ability gap—automatic `multiattackCount`/`subAttackIds` N-roll dispatch—remains documented in `docs/architecture/MONSTER_DATA_PIPELINE.md`; this scenario does not duplicate or falsely close it.

- Acceptance closure repaired three deterministic transaction boundaries: repeat controls now reject without silently refreshing the Action, a missing later target rejects before payment or partial damage, and base plus rider damage crosses 0 HP as one hit rather than adding an immediate death-save failure. Current focused proof passes 14/14; the wider CS24, mounted-host, production-rider, action-economy, and campaign-neighbor run passes 333/338, with all five failures confined to the pre-existing dirty `DamageCommand.ts` turn-state path outside this lane. The one allowed fresh browser pass mounted readable 2D state and matching `render_game_to_text` with no recorded console or page errors, then stopped when the proof script's exact Reset locator omitted the button's emoji; no retry was made, so fresh control and 3D gesture evidence remains unproven. Production N-roll dispatch is now explicitly routed as `GG-58` rather than being claimed by this isolated sandbox transaction.

## 2026-08-11 — Tactical Sandbox Reach & Creature Size

- Registered `reach_creature_size` under Targeting & Space while preserving every existing scenario and all six completed campaign lanes. Its deterministic board keeps a Large lancer, Medium target, and Small scout on visible rock, sand, and grass footprint/reach bands, plus a separate gate whose open anchor hides one blocked 2-by-2 footprint square.
- Four disjoint controls prove the same target is invalid at 5-foot reach and valid at 10-foot reach, with canonical nearest-footprint distance 10 feet versus 12.5-foot token-center separation. The valid control resolves `d20 12 + 6 = 18` against AC 15 and applies 7 damage; shrinking the same anchor to Medium makes the target 15 feet away and invalid again; resetting to 5 feet restores the authored state.
- Added the narrow shared `validateCharacterPlacement` primitive beside canonical size and footprint-distance helpers. It checks every occupied square against map bounds, movement-blocking terrain, and other living footprints. The scenario's Large gate test rejects anchor `13,4` because its far square `14,5` is blocked, leaving the actor at `6,4`.
- Focused `/test-ts` verification passes 48/48 across shared size geometry, the new control module, mounted host, catalog/registry, and Grapple/Shove/Critical/Healing/Saving Throws/Multiattack campaign neighbors. Dependency headers are synchronized; scoped ESLint reports zero errors and 17 established warnings in the shared host/core utility.
- The exact official web-game client run and supplemental headed Playwright proof were written only beneath ignored `.agent/scratch/reach_creature_size/` and visually inspected at 1600 by 1000. Both 2D and 3D launch; after a 160-pixel wheel dolly and 75-by-40-pixel right-button orbit, all three actors, footprint/reach bands, controls, valid-hit outcome, and reasoned log remain visible. Console and page error baseline and delta are 0. The known `GG-51` WebGL blit warnings reproduced, and footprint-aware Opportunity Attack boundaries are routed separately as `GG-52` rather than expanding this lane.

## 2026-08-11 — Tactical Sandbox Spell Slots & Upcasting

- Registered `spell_slots_upcasting` under Core Rules while preserving every existing scenario and all seven completed campaign lanes. Its authored board keeps a level-7 Evoker and 60-HP Fireball Target on a visible fire lane, with level-3 and level-4 slot counters plus Action state in the caster nameplate.
- Four disjoint controls use live Fireball JSON and shared spell-cost, slot, Action, scaling, saving-throw, dice, and HP helpers. They prove a level-3 cast spends only L3 and deals deterministic `8d6 = 32`, a level-4 cast spends only L4 and scales to `9d6 = 36`, a missing level-5 slot rejects before any resource or effect, and reset restores slots, Action, HP, and positions.
- Focused `/test-ts` verification passes 87/87 across the new controls, canonical action-economy and Fireball scaling paths, catalog/registry/mounted host, and all seven Grapple/Shove/Critical/Healing/Saving Throws/Multiattack/Reach campaign neighbors. Dependency headers are synchronized; scoped lint reports zero errors with 15 established host warnings.
- The exact official web-game client and supplemental headed Playwright proof were written only beneath ignored `.agent/scratch/spell_slots_upcasting/` and visually inspected at 1600 by 1000. In 2D the caster and target tokens, four controls, base/upcast/rejection outcomes, exact slot counters, HP, and logs remain visible. In 3D both actors, Fireball cue, target `24/60` HP, controls, and the `8d6 → 9d6; L3 1/1; L4 0/1` log remain visible after a 160-pixel wheel dolly and 75-by-40-pixel right-button orbit. The text probe matches the rendered state; console and page error baseline and delta are both 0.
- No new product or cross-project gap was found. The known `GG-51` WebGL depth/stencil blit warning reproduced during 3D launch and remains the existing routed renderer follow-up rather than a scenario blocker.

## 2026-08-11 — Tactical Sandbox Counterspell & Nested Reactions

- Registered `counterspell_nested_reactions` under Core Rules while preserving every existing scenario and all eight completed campaign lanes. The authored board uses three persistent mages plus Fireball and blue Counterspell cues; slots, Action/Reaction state, HP, and the text probe all read from the same combat characters.
- Four disjoint controls pay live Fireball and Counterspell costs through shared action-economy helpers, resolve the repository's 2024 Counterspell Constitution save, apply canonical Fireball upcasting/save/damage/HP, reject unavailable responses, and expose last-in-first-out nested resolution. Existing `useAbilitySystem` already supports one bounded counter-Counterspell, so no new core stack primitive was added.
- Focused `/test-ts` verification passes 123/123 across the new controls, all eight cumulative campaign neighbors, mounted host/catalog/registry, canonical Counterspell nesting and visibility, action economy, saving throws, Fireball scaling/damage, and HP transitions. Dependency headers are synchronized; scoped lint reports zero errors and 15 established host warnings. The existing nested-hook test retains its known React `act(...)` warning.
- The unmodified official web-game client and supplemental headed Playwright proof were written only beneath ignored `.agent/scratch/counterspell_nested_reactions/` and visually inspected at 1600 by 1000. All four controls and Reset Board pass in 2D. In 3D, all three actors, fire/blue spell cues, controls, `24/60` HP result, resource totals, and reasoned nested log remain visible after a `-160` wheel dolly and `75`-by-`40` right-button orbit. Text state matches the rendering; console/page errors are 0 baseline and 0 delta.
- No new project gap was found. The known `GG-51` WebGL depth/stencil blit warning reproduced after 3D launch; one Chromium SwiftShader deprecation warning is specific to the proof browser flags. Baseline warnings were four existing missing-spell-context notices for Alarm and Animal Friendship.

## 2026-08-12 — Tactical Sandbox scenario-specific collapse

- Grouped each scenario's Rules & Verification and Test Controls inside one bordered Scenario Specific section without changing the scenario catalog or mechanics.
- Both detail boxes now collapse independently through accessible, shared controls. Reopening one leaves the other unchanged, while parent-owned scenario control values persist across collapse and reopen.
- Focused `/test-ts` verification passes 9/9 across the shared component and mounted Tactical Sandbox host. Export dependency headers were synchronized, and scoped lint reports zero errors with 17 established host/control warnings.
- The official web-game client completed its three-step 2D state capture. Supplemental full-viewport proof visually confirmed expanded, rules-only collapsed, both-collapsed, and rules-only reopened states; the console error delta was 0. The sole baseline error was the existing Vite HMR websocket connection failure.
- No new product or cross-project gap was found.

## 2026-08-12 — Grapple presentation and maintenance fixes

- Replaced the arbitrary ordinary-grapple round timer with maintained state. Shared Monster Info and Combatant Info now label both Grappled mirrors as `until escape/release`, show the canonical Speed 0, attack-disadvantage, drag/carry, and ending rules, and explicitly state that Grappled does not also apply Restrained.
- Routed Tactical Sandbox character updates through the shared grapple-maintenance reconciler. Moving the Grappler from `(6,5)` to `(4,5)` through the real 2D map now clears both Grappled mirrors and restores the Escape Target's movement from 0 to 30 feet.
- Focused `/test-ts` verification passes 27/27 across grapple mechanics, scenario controls, the shared inspector, and the complete mounted Tactical Sandbox host. Export dependency headers are synchronized.
- Official and supplemental Playwright proof under ignored `.agent/scratch/` exercised all four controls, reset/repeat behavior, 2D movement release, Monster Info, and 3D zoom/orbit/pan. Screenshots were visually inspected; console and page error delta remained 0. Existing `GG-51` WebGL warnings reproduced in 3D; remaining attack-disadvantage and drag/carry enforcement is recorded as `GG-57`.

## 2026-08-12 — Design Preview world map renderer comparison

- Replaced the Worldforge launcher-only preview with a side-by-side review surface. The retired `AtlasMapView` canvas and maintained `AtlasSvgView` receive the same `getBridgeAtlas` object for each applied seed; explicit labels preserve the no-player-route retirement boundary.
- Added shared seed controls, a reference color lens, canonical layer access, equal measured map panels, the existing canonical runtime and Ground links, and the Atlas to Region to Local to Ground context. Exact-route proof rejected a lazy-load experiment because the shared server could leave its chunk pending; the reliable direct mount is preserved.
- Focused `/test-ts` verification passes 7/7 across the comparison and duplicate-canvas retirement boundary. Scoped ESLint passes the new comparison with zero warnings or errors; repository-established warnings remain in the older preview shell and reference renderer.
- The official web-game client completed a final 1600 by 1000 capture of the exact `localhost:3000` Design Preview route with one retired canvas and one maintained SVG mounted. Supplemental headed proof under ignored `.agent/scratch/proof/world-map-comparison/` verified equal 772-pixel desktop cards with 770 by 520 map surfaces, synchronized seed 42 culture views, and a 390 by 844 stacked layout. Browser console and page error counts were both 0.
- Refreshed the living world-map architecture document and linked the Plan Map feature through Agora tasks `a39013a4-9a27-4313-9e34-869b50c17a0a` and `41f085cf-f2fe-4af9-88ea-4d79925b94a1`. The old `phase=worldforge` player route remains closed.

## 2026-08-13 — Grapple movement and deterministic reset follow-up

- Moved roster-wide Grappled maintenance into the production turn-manager character transition. Ordinary 2D/3D map actions and forced position updates now preserve in-reach holds, release both paired mirrors beyond reach, restore target movement immediately, and publish one reasoned release log without repeat cleanup.
- Pinned only `grapple_escape` to authored initiative totals (`Grappler 18`, `Escape Target 12`), so selection and repeated Reset Board calls always begin on the Grappler while normal combat retains the production initiative roller.
- Focused `/test-ts` evidence passes 29/29 across the real turn manager, grapple utility, scenario controls, and complete mounted Tactical Sandbox host. A larger parallel batch passed 41/45; its four unrelated late-host cases exceeded the established five-second timeout, while the same mounted host passed 17/17 in isolation. Dependency headers are synchronized; scoped lint reports zero errors and established warnings only.
- Rendered proof on the existing port-5201 server completed and was visually inspected for 2D repeated Reset, the former out-of-reach map-movement failure, and all four controls/repeat behavior. The capped run stalled while entering 3D, and the official client also timed out during navigation, so fresh 3D camera and console/page-error delta evidence is blocked by the saturated verification server. No server was started, stopped, or retried after the cap.
- No new gap was found; existing `GG-51` and `GG-57` remain unchanged.

## 2026-08-13 — Tactical Sandbox CS20 Shove regression closure

- Shove now rejects off-turn or exhausted requests before any roll, effect, or cost, and a legal attempt spends the live shover's one finite Attack-action unit in canonical character state. Reset Board restores that resource through ordinary scenario initialization.
- The setup selector exposes Strength and Dexterity defense with visibly different canonical modifiers. Blocked movement still consumes the resolved attempt, while reach and size failures remain atomic and do not spend it.
- Shove-applied Prone is now an explicit until-removed paired status/condition. Repeated turn starts preserve it; the existing Stand Up execution path removes both mirrors after spending half Speed without spending the Action.
- Cumulative focused verification passes 70/70 across action economy, Grapple preservation, Shove runtime/adapter, condition ticking, catalog/registry, and the mounted scenario host. Scoped ESLint has zero errors (17 established warnings in the large host/type files), dependency headers are synchronized, and `git diff --check` passes.
- Official plus exhaustive mounted Playwright proof under ignored `.agent/scratch/shove_prone_fix/` covers every CS20 control, reset/repeat, off-turn no-op, both save choices, push, Prone across turns, size/collision rejection, and 3D wheel/right-drag/middle-drag camera gestures. Nine inspected screenshots and `render_game_to_text` agree; page/console error baseline and final are 0, while 34 warnings reproduce known spell-data and WebGL/SwiftShader noise including GG-51. No new product gap was found.

## 2026-08-13 — Tactical Sandbox CS21 Critical Hits production remediation

- CS21 now submits every deterministic attack through the production ability/action transaction. The live turn owner and finite Action budget are checked before rolling or paying, so repeat and off-turn attempts cannot roll, damage, trigger riders, or spend resources; Reset Board restores only the authored CS21 baseline.
- Natural 20, natural 1, and ordinary AC outcomes remain canonical. A critical doubles the longbow and Flame Mark rider dice but not the flat modifier, then the existing resistance, HP, downing, and Unconscious paths resolve the defended target.
- The 3D camera contract now uses right-drag orbit, middle-drag pan, and wheel zoom. A shared pointer guard prevents an unmodified left drag from also committing an actor move.
- Final scoped verification passes 30/30 across action economy, production ownership/rejection, the CS21 adapter and mounted host, critical riders/defenses/downing/reset, and camera-input regression. The cumulative campaign gate passed 67/67 and the neighboring mounted host passed 19/19; dependency headers are synchronized and `git diff --check` passes.
- Ignored `.agent/scratch/cs21-remediation/` proof was visually inspected. The completed 2D matrix covers all four controls, repeat/off-turn rejection, and Reset. The bounded 3D pass shows all actors, terrain, controls, rule facts, critical/downing state, and reasoned log after wheel zoom, right-drag orbit, and middle-drag pan; left-drag leaves the actor at `(4,5)`. Console and page error delta are 0. The final darkvision-only fixture adjustment is focused-test verified but was not browser-rerun under the operator's stop bound.
- No new product or cross-project gap was found. Existing `GG-51` WebGL warnings remain unchanged.

## 2026-08-13 — Tactical Sandbox CS22 Healing & Temporary HP production remediation

- CS22 now operates on the mounted actor roster instead of rebuilding the ally at 12/24 before each control. Healing Word, Cure Wounds, and the temporary-HP training transaction validate the canonical target plus live turn owner before atomically paying the shared Action or Bonus Action and one of the healer's three finite level-1 slots.
- The production resolver rejects off-turn, exhausted, empty-slot, missing-actor, invalid-amount, and canonical Undead-target requests without changing HP, temporary HP, or resources. Successful healing remains capped at maximum HP and uses the shared downed transition to clear death saves and Unconscious without removing unrelated effects.
- The live cumulative route is 12 → 20 → 24 current HP, one full round refreshes only the healer's turn economy, then temporary-HP offers resolve 8 → keep 8 over 5 → replace with 12. Fifteen hazard damage consumes the 12-point buffer before reducing 24 current HP to 21. Reset restores only the authored two-actor fixture, 12/24 ally state, healer-first initiative, fresh economy, and 3/3 slots.
- Focused production, adapter, and real mounted verification passes 17/17. The cumulative CS19–CS22 and canonical targeting, action-economy, death-state, grapple, and shove gate passes 95/95. Dependency headers are synchronized; scoped ESLint reports zero errors and 13 established host warnings; scoped whitespace checking passes.
- The official develop-web-game client passed against the retained port-5201 host and published `tempHP`, source, slots, turn owner, Action/Bonus state, HP, and logs through `render_game_to_text`. Ignored supplemental proof covers independent reset cases, repeat/off-turn no-ops, the full live cumulative route, and Reset; console/page errors are 0 baseline, 0 final, and 0 delta.
- The single bounded 3D attempt launched with both actors, terrain, controls, rule facts, +12 temporary-HP cue, and the reasoned ward log visible, then captured a modest wheel zoom. It did not complete right-drag orbit or middle-drag pan before the operator's stop bound, so those two gestures are unproven in this remediation and were not retried. No product gap was found or added; existing camera/WebGL coverage remains unchanged.

## 2026-08-13 — Tactical Sandbox CS23 Saving Throws acceptance closure

- The expected-first audit confirmed the existing CS23 transaction already follows production order: spell DC and saving throw, rounded-down save outcome, resistance/immunity, then the shared HP/downing transition. Its deterministic Test Controls are isolated rule probes rather than turn-owned spell casts, so they intentionally preserve the live caster's Action and slots; Evasion remains excluded because no reusable production resolution is supported.
- Added two missing mounted boundaries without replacing the green resolver. `Save + Fire immunity` proves `15 → 7 → 0`, while `Down: 10 - 1 vs DC 15` authors a 15-HP player target and proves canonical `0 HP`, `0S/0F`, and Unconscious. Repeating downing rebuilds only the authored CS23 actors and does not add a death-save failure; Reset restores their HP, defenses, death saves, conditions, and unchanged caster resources, clears prior outcome logs, then lets the turn manager publish fresh baseline turn lines.
- Focused post-sync verification passes 15/15 across the adapter, mounted host, catalog, and registry. The wider production and cumulative CS19–CS23 gate passes 134/134 across `DamageCommand`, saving throws, defenses, death-state transitions, five scenario adapters, three mounted campaign lanes, the full mounted host, catalog/registry, and search. Dependency headers are synchronized; scoped ESLint reports zero errors and 13 established host warnings.
- The unmodified official develop-web-game client and the bounded exhaustive session used only the retained port-5201 host and confirmed-ignored `.agent/scratch/saving_throws_half_damage/`. Five inspected captures prove the 2D immunity/downing outcomes and the 3D board after wheel `-160`, right-drag `+75,+40`, and middle-drag `+45,+20`; all three actors, fire cues, controls, HP/status outcome, and reasoned log remain readable. `render_game_to_text` agrees with the rendered state. Console/page errors are 0 baseline, 0 final, and 0 delta.
- No new gap was found after searching existing gap IDs. The 35 retained warnings are established missing-spell-context fixture messages plus the existing GG-51 WebGL/SwiftShader warning family, not a CS23 functional error.

## 2026-08-13 — Tactical Sandbox CS25 Reach & Creature Size GG-52 closure

- Repaired the production Opportunity Attack boundary in `OpportunityAttackSystem`. Each voluntary movement step now projects the mover at its old and new anchors and compares the complete attacker and mover footprints through canonical nearest occupied-square distance. Normal 5-foot and extended 10-foot reach now agree with ordinary melee targeting for Small, Medium, and Large combinations; forced movement and teleport event kinds fail closed before discovery.
- Preserved the existing Disengage, ready Reaction, visibility, incapacitation, hostile-team, Summon Flyby, Sentinel, War Caster, and attack-resolution paths. The mounted action executor now claims a stable movement action id before an asynchronous reaction choice, so duplicate delivery of one event cannot prompt twice, spend twice, or resolve two Opportunity Attacks while state updates are pending.
- Extended CS25 without removing its four original controls. `Move along the reach edge` proves Large-vs-Large movement stays at `5 ft → 5 ft` with zero trigger and a ready Reaction; `Leave the footprint reach` proves the same authored anchors cross `5 ft → 10 ft`, discover one production trigger, and pay the Reaction. Repeating converges on the same state, while Reset Board restores the original Large-vs-Medium targeting fixture.
- The cumulative `/test-ts`-style Vitest gate passes 320/320 across 43 files: every scenario-control suite, catalog/registry, complete mounted host, production Opportunity Attack resolver, and real action executor. Dependency headers are synchronized for the resolver, hook, and CS25 adapter. No broad lint or typecheck was run.
- The unmodified official develop-web-game client completed against the retained port-5201 server. The corrected final supplemental attempt completed all six controls, repeat, Reset Board, 2D, 3D launch, wheel `-420` zoom, 150-by-70 right-drag orbit, and 110-by-55 middle-drag pan. The text probe and rendered combat log agree on the paid `5 ft → 10 ft` exit. Representative captures were visually inspected under confirmed-ignored `.agent/scratch/cs25-gg52-proof/`.
- Console errors and page errors were 0 at baseline and 0 after all interactions. Warnings rose from 4 to 46 only after 3D launch: one expected SwiftShader fallback notice plus 41 repetitions of the existing `GG-51` depth/stencil `glBlitFramebuffer` warning. `GG-52` is closed with no remaining footprint scope; unrelated reaction features and renderer warning `GG-51` remain on their existing boundaries.

## 2026-08-13 — Tactical Sandbox CS26 Spell Slots & Upcasting live-state remediation

- Removed the cast-time fixture rebuild. CS26 now prepares its two authored actors only when the scenario mounts or Reset runs; every cast submits the mounted caster, target, map, positions, effects, turn owner, Action, HP, and exact slot inventory to one shared atomic damage-spell transaction.
- The production transaction validates actor presence, spell eligibility, supported save-based direct-damage shape, requested slot integrity, cantrip/non-upcastable boundaries, base level, live turn owner, canonical target/range/sight, Action affordability, and exact slot availability before rolling or paying. A legal cast pays one Action and exactly one requested slot, then preserves canonical scaling, save, resistance/immunity, HP, and downing paths. Every rejection returns the original roster without a roll, effect, or resource change.
- Six visible controls now prove base level 3, upcast level 4, absent level 5, below-base level 2, forbidden slot-upcast Fire Bolt, and scoped Reset. Focused production, adapter, and mounted-host verification passes 25/25. The cumulative campaign and canonical-mechanics gate passes 403/403 across 52 files. Dependency headers are synchronized; scoped ESLint reports zero errors and 13 established host warnings; scoped whitespace and red-flag checks pass.
- The unmodified official develop-web-game client confirmed the retained port-5201 2D baseline. One bounded exhaustive Playwright session under confirmed-ignored `.agent/scratch/cs26-remediation/` exercised every CS26 control, an immediate repeat, off-turn rejection, Reset Board, scenario Reset, 2D, 3D launch, wheel zoom, right-drag orbit, and middle-drag pan. Three representative captures were visually inspected. Rendered Action, slots, target HP, turn, positions, and reasoned logs agree with `render_game_to_text`: level 3 produces `L3 0/1`, Action spent, and `60 → 28`; its immediate level-4 repeat preserves that state; a fresh level-4 cast produces `L4 0/1` and `60 → 24`; invalid requests and Reset preserve or restore the exact expected ledger.
- Console and page errors remained 0 at baseline and 0 after all interactions. Four established missing-spell-context warnings were present at baseline; 38 additional warnings appeared only after 3D launch (one SwiftShader fallback notice and the existing `GG-51` WebGL depth/stencil blit warning family). No new product or cross-project gap remains.

## 2026-08-13 — Tactical Sandbox CS27 rejection remediation

- Added visible unseen-caster, 65-foot, and voluntary-decline Counterspell controls. Each uses the production visibility/range boundary, preserves Aegis Mage's ready Reaction and level-3 slot, pays only Ember Mage's original Action and level-4 slot, and resolves the original Fireball exactly once for `60 → 24` HP.
- Corrected the live interruption range boundary to convert five-foot grid cells into feet. The canonical 60-foot edge remains eligible and the next cell at 65 feet rejects before a reaction prompt or payment.
- Added pure and mounted proof for all three branches, missing authored source/target rejection, deterministic repeat state, and scoped Reset. The first focused pass exposed a mounted mock that hid the production pure helpers; the mock now preserves those real exports, and the remediated mounted case passes.
- Focused production, adapter, registry/catalog, and mounted-host verification passes 105/105. The cumulative campaign scenario-control and complete mounted-host gate passes 308/308 across 41 files. Dependency headers are synchronized; scoped ESLint reports zero errors, with 16 established warnings confined to the large production hook; scoped whitespace and red-flag checks pass.
- The unmodified official develop-web-game client passed against the retained port-5201 host. Confirmed-ignored exhaustive proof under `.agent/scratch/counterspell_nested_reactions/remediation/` exercised all seven controls twice, Reset Board, 2D, 3D launch, wheel `-260` zoom, 120-by-60 right-drag orbit, and 90-by-45 middle-drag pan. Ten captures were visually inspected; actor positions, Action/Reaction/slot state, `60 → 24` HP, status cues, and reasoned logs agree with `render_game_to_text`.
- Console and page errors remained 0 at baseline and 0 after all interactions. Four established missing-spell-context warnings were present at baseline; 36 warnings appeared only after 3D launch (one SwiftShader fallback notice and 35 existing `GG-51` depth/stencil blit warnings). Production Counterspell has no event-ID receipt or missing-actor event branch, so this remediation proves repeat-safe mounted transactions and the adapter's missing authored source/target guard without inventing a new event model. No new product gap remains.

## 2026-08-13 — Tactical Sandbox CS32 Falling & Ground Impact remediation

- Added one production fall transaction with an explicit live-event receipt. Legal landings preserve the established threshold and cap (`<10 ft = 0d6`, one `d6` per complete 10 feet, maximum `20d6`), route bludgeoning damage through shared defenses, temporary HP, HP, downing, death-state, and paired Prone helpers, and reject living occupied footprints, blocked terrain, or off-board footprints before any state or resource change.
- Feather Fall now uses the canonical spell record and shared line-of-sight, range, ownership, Reaction, and level-1 slot rules. Acceptance pays once and prevents landing damage and Prone; decline, unowned, spent-Reaction, empty-slot, unselected-target, and replay paths do not make an additional payment, while gravity proceeds for eligible legal landings.
- The four visible scenario controls deterministically prove a 5-foot safe fall, 30-foot damage, the 200-foot `20d6 = 79` cap, resistance, immunity, temporary HP, downing, all three invalid endpoints, every Feather Fall choice/rejection, repeat safety, and Reset. Focused production and adapter verification passes 36/36, the final mounted host passes 19/19, and the cumulative scenario-control/canonical-mechanics gate passes 393/393 across 46 files. Dependency headers are synchronized; CS32 has no scoped TypeScript error, scoped lint has zero errors, and established host/type warnings remain unchanged.
- The unmodified official develop-web-game client and the single bounded retained-host pass used only port 5201 and confirmed-ignored `.agent/scratch/cs32-remediation/`. All 10 fall cases and six Feather Fall cases passed canonical text-state assertions. Six inspected captures prove baseline and damaging/Prone 2D, accepted Feather Fall payment/protection, 3D launch, wheel zoom, right-drag orbit, and middle-drag pan with actors, terrain, impact/light cues, controls, resources, and reasoned logs legible. Console/page errors are 0 baseline, 0 final, and 0 delta.
- No new product or cross-project gap was found. Legal occupied-ground behavior is the existing placement rule: any living creature footprint blocks the landing, while already-downed actors do not. Existing `GG-51`, `GG-52`, and other campaign gap records were not duplicated or changed.

## 2026-08-13 — Tactical Sandbox CS33 Flying & Aerial Movement remediation

- The canonical aerial resolver now validates every route waypoint and the actor's complete footprint against map bounds, ground/elevation intersection, sealed airspace, authored blocker tops, ceilings, and vertically overlapping flying creatures. It sums every horizontal grid step plus every climb and descent, rejects unaffordable routes before mutation, and shares live movement used across walk/fly mode switches without letting a slower mode borrow a faster mode's ceiling.
- Ordinary airborne actors now author `movementMode: fly` from the normal battle-map Move path. The action executor delegates that Move to the aerial resolver, replaces preview cost with resolved 3D cost, spends once, preserves the ground Move path, and closes a paid ground endpoint through CS32's zero-damage landing transaction. Lost non-hovering support delegates to the same CS32 transaction for Feather Fall, defenses, temporary HP/HP, downing/death state, and Prone.
- CS33's mounted controls preserve live state. Three clicks spend `20 + 15 + 5 = 40 ft`; a fourth rejects with zero remaining movement. Traversed tall-blocker, ceiling, and occupied-flyer cases plus occupied/blocked/off-board/unaffordable endpoints are atomic. Controlled descent and ordinary, Feather Fall, resistance, and downing support-loss cases publish altitude, cost, HP, resources, Prone, and exact reasons; Reset restores the authored three-actor board.
- Focused production, Move-integration, adapter, fall-neighbor, and mounted tests passed individually. The one bounded cumulative gate passed 110/111; its sole failure was a stale retained DOM button reference in the mounted repeat test, corrected immediately, and that exact mounted case then passed 1/1 with a 10-second test bound. Dependency headers are synchronized. Broad TSC remains blocked only by established repository debt outside this slice after the CS33-local diagnostics were corrected.
- The one retained-host attempt was externally blocked: `http://127.0.0.1:5174` and the warmed CS32 proof port reported as `http://127.0.0.1:5197` both returned `Unable to connect to the remote server`. Per the bounded instruction, no server was started, stopped, or reconfigured and no new visual capture was claimed. This is the already-known retained-host/browser infrastructure boundary associated with `WF-G59`; no duplicate gap was added.
- Altitude-aware attack range, melee reach, and line-of-sight are not part of the current production attack pipeline, so CS33 does not invent them in the scenario adapter. That remains an explicit future production boundary rather than UI-only truth. No other product or workflow gap was added.

## 2026-08-13 — Tactical Sandbox CS03 Difficult Terrain remediation

- Extended the registered CS03 adapter with a route/destination selector and one atomic move action while preserving the original mud, bypass, and already-spent switches. The move action uses the production A* pathfinder, 5-10-5 path-cost helpers, and shared action-economy payment rather than a display-only result.
- The authored 2-5 to 8-5 fixture now proves a 45-foot direct mud route rejects, a 35-foot dry A* route resolves, ordinary ground resolves direct for 30 feet, and a second 5-foot move consumes the same remaining pool. Fifteen feet already spent leaves only 20 feet and rejects the legal detour without moving. Blocked 10-5, occupied 12-5, off-board 16-5, and repeated-destination requests are log-only atomic rejections; classification changes only the three authored mud cells after setup; Reset Board restores the deterministic start, costs, values, and route.
- The focused CS03 adapter passes 15/15. The cumulative registered-control, movement, reachability, and disjoint mounted gate passes 360/360 across 42 files. Dependency synchronization and scoped `eslint --no-ignore` pass. Typecheck delta remains red on shared-tree drift (81 current diagnostics, 52 newer than the old baseline) and reports no CS03 source or test diagnostic.
- The single retained-host attempt used only the already-running Aralia Vite listener at `127.0.0.1:5201`. It was consumed before capture because PowerShell removed the nested selector and action-JSON quotes, so the official client loaded the page but rejected both inputs. Per the no-retry/no-substitution instruction, no second browser, port, or server mutation was attempted; fresh 2D/3D camera and console-delta evidence is therefore blocked at the browser-command boundary rather than claimed.
- Agora used exact session `019febcd-081b-7043-acdb-f8b0eba812bd`, handle `cs03-remediation-019febcd`, agent id `c4111c07-a04c-4979-ba27-98589d8a6ce1`, and Mechanical Maze Knight. No branch, worktree, stage, commit, push, cleanup, deletion, server start, restart, or stop was performed. No fly, swim, or ignore-difficult-terrain exception was invented because CS03 does not advertise one and the known preview/execution parity boundary remains out of scope.

## 2026-08-13 — Tactical Sandbox CS04 Spell Concentration remediation

- Concentration replacement is now safe at the command boundary even when a caller does not prepend the spell factory's break command. Cleanup matches both caster and spell across linked statuses, conditions, map helpers, zones, effects, and summons, so accepting a new concentration spell removes only that caster's prior owned effect.
- Damage checks use final delivered damage after saves, defenses, and reactions. Zero delivered damage creates no save; 20 damage resolves DC 10 and 22 resolves DC 11. Applying an incapacitating condition through the normal status command ends the exact linked concentration once, and the downed hook skips duplicate cleanup after the command path has already resolved it.
- CS04 now exposes fresh Bless, owned Bless-to-Protection replacement, deterministic 20-damage keep, 22-damage break, Incapacitated source loss, invalid spent-Action, declined replacement, stable replay, and Reset through the mounted production ability transaction. Accepted casts spend one Action and level-1 slot; declined and repeated event ids are claimed no-ops until Reset starts a new delivery epoch.
- Focused production, adapter, registration/catalog, event-ledger, and cleanup-parity verification passes 25/25. The exact mounted CS04 host case passes 1/1 and proves replacement/resource ownership, same-spell unrelated-owner preservation, replay idempotence, both DC boundaries, source loss, exactly one additional teardown, and Reset. A later cumulative scenario-control run was stopped at the operator's bound after 78 passing tests across six files; it is not claimed as a completed gate.
- Dependency synchronization completed for all changed exported command, hook, and control modules. Scoped ESLint reports zero errors and established warnings only. Broad TypeScript returned shared-tree debt plus one CS04 full-slot-ledger diagnostic; that CS04 diagnostic was corrected, but post-fix type status is explicitly unproven because the operator directed that no more broad checks run.
- The single retained-host attempt used only port 3000 and confirmed-ignored `.agent/scratch/cs04-concentration-retained-host/`. Windows shell quoting folded the remaining client arguments into the CSS selector, so the official client stopped before selecting CS04 or capturing proof. Per the no-retry/no-substitution instruction, no second browser, host, or server action was attempted; fresh 2D/3D zoom/orbit/pan and console-delta evidence is blocked at the browser-command boundary and is not claimed.
- No new product gap was added: the normal combat ability, damage, status, and concentration paths now invoke the repaired canonical helpers. Agora used exact session `019febcd-1673-7180-bdae-f01d19d5481e`, handle `cs04-concentration-019febcd`, agent id `a4f2e3ab-49bf-4477-a544-327fbd5035b3`, and Violet Mage. No branch, worktree, stage, commit, push, cleanup, deletion, or server lifecycle action was performed.
## 2026-08-13 — Tactical Sandbox CS06 Resistance & Vulnerability remediation

- FIXED canonical damage defense ordering: immunity still returns 0, duplicate traits still count once, resistance rounds down once, and vulnerability doubles once. When both resistance and vulnerability match, the project’s canonical 2024 order now applies resistance before vulnerability, so odd 9 becomes 4 then 8 instead of being treated as cancellation.
- FIXED the normal DamageCommand boundary so an explicitly blank stable damage event id is an atomic no-op before dice, reactions, HP, downing, events, or logs. A valid stable id applies once and an identical replay preserves the first receipt.
- Preserved the original four trait switches and fixed 10-damage calibration abilities. Added a complete proof selector plus Resolve and Replay controls for normal 10, odd resistance 9 to 4, vulnerability 10 to 20, immunity 10 to 0, simultaneous 9 to 4 to 8, duplicate resistance once, mixed piercing/cold components, temporary HP before HP, player downing, one Action owner, stable replay identity, and deterministic Reset preparation.
- Focused production and adapter verification passes 67/67 across ResistanceCalculator, DamageCommand, and CS06 controls. The later cumulative scenario-control plus mounted ability-ledger command completed with only passing files in the captured output, but its aggregate summary was truncated and is not used as an acceptance count. Dependency synchronization completed for ResistanceCalculator, DamageCommand, and the CS06 adapter. Scoped ESLint has zero errors and established warnings only; `typecheck:delta` reports no new diagnostics.
- The single retained-host attempt used only `http://localhost:3000/Aralia/misc/design.html?step=scenarios`. It selected Resistance & Vulnerability with zero console errors and four baseline warnings. The 2D text snapshot proved the Player Elementalist, Skeleton Archer, Fire Elemental, ready Actions, and exact resistance/vulnerability/immunity arrays; the inspected full-page capture proved the selected scenario and all seven controls, but the board area was visually blank. The 3D click did not settle within the bounded window and the call was terminated. Per instruction there was no retry or substitute, so 3D actors, camera gesture, and post-launch console delta remain unproven.
- No simultaneous-trait ambiguity or new product gap remains: canonical project data and the 2024 rule order supply the supported precedence. Agora used exact session `019febcd-32f7-7931-8d06-e898d9a96504`, handle `cs06-resistance-019febcd`, agent id `60687efc-4ed5-4ee2-845b-70e4c0e23595`, and ManSui. No branch, worktree, stage, commit, push, unrelated cleanup, deletion, or server lifecycle action was performed.

## 2026-08-13 — Tactical Sandbox CS08 Area of Effect remediation

- FIXED a canonical directional-geometry defect: confirming a self-origin line on the caster cell now leaves the endpoint open so facing projects the authored length instead of collapsing the line to one point. Added one pure immediate area-damage spell transaction driven by canonical Fireball and Burning Hands data for shape, origin, orientation, dimensions, range, sight, saves, scaling, damage, and slot level.
- The transaction intersects complete creature footprints with exact affected cells and resolves each included creature once. It performs one shared damage roll, one save per creature, save half/full damage, Fire resistance/immunity, temporary HP, HP/downing/Unconscious, and one Action/slot payment. Blank, invalid-placement, empty-resource, and replayed stable event ids reject before RNG, damage, conditions, payment, or event claim. Fireball sight stops at its chosen origin; an internal wall does not invent outward cover propagation.
- CS08 now exposes canonical Fireball boundary/cluster/Large-target cases, east/north Burning Hands, blocked origin, internal wall, off-map origin, mixed/all save results, defenses, temporary HP, friendly downing, Resolve, stable Replay, and exact Reset. The map highlights production-calculated cells; actor names and logs expose included/excluded membership, saves, damage, HP, Action, slot, and event status in both renderers.
- Focused production, geometry, and adapter verification passes 16/16 across three files. Scoped ESLint reports zero errors; three warnings are only ignore-pattern notices for the ignored Design Preview step tree. Scoped whitespace checking passes. The bounded file-root TypeScript check and three dependency-sync helpers did not return promptly and were stopped under the operator bound; no post-sync/type claim is made.
- The single retained-host attempt used only port 3000. The unprefixed route returned the configured `/Aralia/` base handoff with zero console errors; following that configured route timed out before the scenario mounted. Per instruction there was no retry, substitute browser, or server action, so fresh CS08 2D/3D, camera gesture, and final console delta are not claimed.
- No new GLOBAL_GAPS entry was added: the scoped canonical transaction and adapter close the evaluated CS08 behavior, while the incomplete rendered proof is an execution-time host boundary. Agora used exact session/task `019febcd-4eb2-7430-b28b-d53687023ac5`, handle `cs08-area-effect-019febcd`, agent id `7d39044f-64c2-44ff-8eef-61a6142f2f95`, and King Solomon. No branch, worktree, stage, commit, push, cleanup, deletion, or server lifecycle action was performed.

## 2026-08-13 — Tactical Sandbox CS11 Stealth & Hidden remediation

- Added one canonical Hide transaction: valid cover or real obscurity is required; Dexterity (Stealth), Action payment, passive/seeded-active Perception, observer-relative detection, movement placement/economy, exact Hidden ownership, and stable event replay use production helpers and live combat state.
- Ordinary attacks now grant Advantage only from an undetected Hide-derived source and remove that exact source after publishing the current attack roll. Unrelated Hidden and Invisible state remain untouched, including on a miss.
- CS11 exposes cover/light/Darkvision, live observer Perception, Hide/observe/covered movement/exposed movement/attack steps, Resolve, stable Replay, and native exact Reset. Focused runtime, attack-command, and adapter proof passes 11/11 across three files.
- The single allowed mounted attempt used the official web-game Playwright client at `http://localhost:3000/Aralia/misc/design.html?step=scenarios&controls=integrated` with a 1500×1000 viewport. It stopped at navigation with `ERR_CONNECTION_REFUSED`; per instruction there was no retry, alternate host, or server lifecycle action. Fresh 2D/3D rendering, control/gesture, and console-delta evidence are therefore blocked and not claimed.
- Agora used session/task `019febcd-7e01-7221-898b-1031bc8e1c76`, handle `cs11-stealth-019febcd`, agent id `7eb3bf32-8123-40fb-ba26-6051595b7757`, and Red Hood. No branch, worktree, stage, commit, push, cleanup, deletion, or server lifecycle action was performed.

## 2026-08-13 — Tactical Sandbox CS14 Summons & Controlled Allies remediation

- Added one production summon placement resolver and connected exact single-summon placement to `SummoningCommand`. The scenario adapter now creates a live Summon Beast actor with caster/source/shared-initiative metadata, materialized Rend dice, independent action economy, normal Action and slot payment, real concentration ownership, exact source-loss cleanup, stable command replay identity, and deterministic Reset preparation.
- CS14 exposes legal, exact 90-foot edge, out-of-range, occupied, source-loss, and replay summon cases plus owner-to-summon turn advance, legal/out-of-range Rend commands, and stable command replay. It preserves a rival-owned summon to prove cleanup is exact rather than roster-wide.
- The single focused run passed the new resolver and CS14 adapter suites (10/10). Three `SummoningCommand` cases failed because an empty mock map was interpreted as a loaded zero-tile board; the narrow populated-map guard was corrected afterward, but no rerun was made under the one-run instruction, so post-fix command-test status is explicitly unproven.
- The single retained-host attempt targeted `http://127.0.0.1:5201/Aralia/misc/design.html?step=scenarios&controls=integrated` with the official client and a 1600×1000 viewport. The client rejected the invocation before navigation because it requires a gameplay action in addition to the scenario selector. Per instruction there was no retry, alternate browser, or server action; mounted 2D/3D, control/gesture, and console-delta proof are not claimed.
- Agora used exact session/task `019febcd-c97f-7de2-93ef-12d9cd375e35`, handle `codex-cs14-summons-0813`, agent id `b7e144fc-122c-4efb-802e-98d6a55f6048`, and Pixel Coder. No branch, worktree, stage, commit, push, cleanup, deletion, or server lifecycle action was performed.
