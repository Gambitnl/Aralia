# 3D Exploration & Combat

This branch tracks 3D traversal and combat work that belongs under `World Exploration`.

Current scope notes:
- Keep this as a direct child branch of `World Exploration`.
- `World Map` remains a separate sibling branch, not nested under this node.

