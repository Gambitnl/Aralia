## Bucket: Attack-Roll Riders

### 1. Fields implemented (v3 round)
- `lastUpdated`: bumped to 2026-04-28T21:45Z.
- Policy edge cases: none in this bucket.
- `ExecutionStep.spells` (Gap 14): none real - the only known roster, `frostbite`, now lives on its specific edge case.
- `count: null` snapshots (Gap 12): added on `incoming_rider_on_attacks_vs_target`, `duration_based_rider`, and `bonus_penalty_rider` as subbucket-level pending inventory snapshots.
- Closure-slug normalization: already normalized.
- Other v2 fields: first-time bucket, no v2 predecessor; added `countValue`, history, and `frostbite` spell attribution where the current tracker already supports them.

### 2. Verification
- Dashboard row reads honestly: yes - bucket kind and gates are correct; timestamp was bumped fresh after the source sweep.
- Matrix click-through lands on my map: source-only/deep-link verified - direct deep link opens the Attack-Roll Riders execution map on the running local Atlas; the available browser tool exposed navigation/snapshot but no click action for the matrix row.
- Execution-map counts match tracker doc: yes - `frostbite` remains the only quantified inventory; the other rider families stay pending inventory.
- Policy edge cases now read neutral instead of rose: N/A - no policy edge cases in this bucket.
- Subbucket-level rosters render correctly (if added): N/A - no `ExecutionStep.spells` roster was added.
- `?` history bubbles render correctly (if added): yes - the three pending rider families render gray `?` progression bubbles with pending-recount tooltips.

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none.
- Existing entries I added notes to: none.
- Resolved gaps (12/13/14) that don't actually feel resolved: all good.

### 4. Doc / UI friction
- The report template frames `count: null` around edge-case handles, but this mechanics-model bucket needed the same pending-count affordance at subbucket level. The Atlas type and renderer already support that, so no new gap was needed.
- The first-time dispatch was clear enough to use without a v2 audit trail.

### 5. Confidence
Ready - the V3 model fits this bucket, with no new gaps; the real remaining work is the already-tracked inventory of rider spells beyond `frostbite`.
