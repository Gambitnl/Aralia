## Bucket: Structured Markdown

### 1. Fields implemented (v3 round)
- `lastUpdated`: bumped to `2026-04-28T21:45Z`
- Policy edge cases: N/A - this closure bucket has no policy edge cases.
- `ExecutionStep.spells` (Gap 14): none real - the open work is already represented by the two canonical-only stub subbuckets.
- `count: null` snapshots (Gap 12): none real - the live counts are known.
- Closure-slug normalization: already normalized.
- Other v2 fields: first-time bucket report; no `v2.md` predecessor. V3 verification reopened the bucket because the checkout still has `47` `CANONICAL-ONLY-REFERENCE` markers.

### 2. Verification
- Dashboard row reads honestly: source-only yes after update - bucket now shows reopened active work instead of closed.
- Matrix click-through lands on my map: source-only - dev server was not opened in this session.
- Execution-map counts match tracker doc: yes after update - `43` level-0 stubs and `4` level-1 stubs remain.
- Policy edge cases now read neutral instead of rose: N/A - no policy edge cases.
- Subbucket-level rosters render correctly (if added): N/A - no orphan roster added.
- `?` history bubbles render correctly (if added): N/A - no null snapshots added.

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none.
- Existing entries I added notes to: none.
- Resolved gaps (12/13/14) that don't actually feel resolved: all good; this bucket's issue is stale data/state, not an Atlas model gap.

### 4. Doc / UI friction
- The dispatch prompt and `BUCKET_META.tracker` both pointed at `SPELL_MISSING_STRUCTURED_MD_TRACKER.md`, but the live tracker is under `docs/tasks/spells/structured-markdown/`.
- The tracker claimed closure from an earlier pass, but the live files still contain the old canonical-only markers. The report therefore reopens the bucket instead of treating closure as complete.

### 5. Confidence
Not ready: the V3 model fits the bucket, but the bucket itself is reopened until the `47` canonical-only spell reference files receive structured blocks and the marker scan returns to `0`.
