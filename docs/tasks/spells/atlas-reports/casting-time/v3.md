## Bucket: Casting Time

### 1. Fields implemented (v3 round)
- `lastUpdated`: bumped to `2026-04-28T10:46Z`
- Policy edge cases: 1 edge case swept; 0 now carry chip-only spells because the model-gap roster is not known yet
- `ExecutionStep.spells` (Gap 14): none real - all known rosters live on edge cases
- `count: null` snapshots (Gap 12): added on `special_casting_time_gap` policy edge case (1 snapshot)
- Closure-slug normalization: N/A - no closure steps
- Other v2 fields: tweaked the uncounted special-timing policy edge from a false `0` history to a pending-recount `null` history

### 2. Verification
- Dashboard row reads honestly: yes
- Matrix click-through lands on my map: source-only - dev server not opened in this pass
- Execution-map counts match tracker doc: partial - Phase 2 matches `7`; Phase 1 still carries the tracker/report `52` only as history because subbucket distribution is not known yet
- Policy edge cases now read neutral instead of rose: source-only yes - policy status is present and no chip-only roster is attached
- Subbucket-level rosters render correctly (if added): N/A
- `?` history bubbles render correctly (if added): source-only yes - `ProgressSnapshot.count` accepts `null` and the special-timing edge now uses it

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none
- Resolved gaps (12/13/14) that don't actually feel resolved: all good

### 4. Doc / UI friction
- none

### 5. Confidence
Ready: the Casting Time bucket fits the V3 Atlas model, with the remaining work being normal bucket work to split the 52 canonical-side mismatches into concrete subbucket counts.
