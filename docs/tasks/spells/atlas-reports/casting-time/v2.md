## Bucket: Casting Time

### 1. Fields implemented
- `kind`: unchanged from `parity`
- `lastUpdated`: bumped to `2026-04-27T15:59Z`
- `countValue`: added on 1 numeric subbucket; Phase 1 subbucket counts remain unknown until the 52-spell canonical residue is split
- `dependsOn`: none real
- `overlapsWith`: none real inside this bucket yet; overlap is currently represented by shared spell chips across edge cases
- Edge-case `count` / `history`: 22 edge cases now have count, 22 have at least a starting snapshot
- Edge-case `spells`: 21 edge cases have rosters, 0 with full segments, 21 chip-only awaiting pinpoint
- `owner` / `prs`: skipped

### 2. Verification
- Dashboard row reads honestly: yes
- Matrix click-through lands on my map: structurally yes through bucket registration; not visually verified in browser this pass
- Execution-map counts match tracker doc: partial - Phase 2 matches `7`; Phase 1 still carries the tracker/report `52` only as history because subbucket distribution is not known yet
- Edge cases all have count + at least one history snapshot: yes

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none

### 4. Doc / UI friction
- The V2 prompt and in-app onboarding say only Gap 09 remains open, but the gap registry file I read still lists older gaps under Open gaps.
- `countValue` works for known totals, but the Phase 1 `52` cannot honestly be assigned to one subbucket before the ritual/reaction/bonus-action split is counted.

### 5. Confidence
The Casting Time bucket now matches the Atlas model well enough to resume bucket work, with one known manual follow-up: split the 52 canonical-side residue into real subbucket counts.
