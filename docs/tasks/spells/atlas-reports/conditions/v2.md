## Bucket: Conditions

### 1. Fields implemented
- `kind`: set to `mechanics-model`
- `lastUpdated`: bumped to `2026-04-27T16:07Z`
- `countValue`: added on 2 spell-count subbuckets; skipped on `custom_condition_labels` because its parent count is labels, not unique spells
- `dependsOn`: none real
- `overlapsWith`: added on `runtime_condition_missing_markdown_row` -> [`custom_condition_labels`] and `custom_condition_labels` -> [`runtime_condition_missing_markdown_row`]
- Edge-case `count` / `history`: 25 edge cases now have counts and 25 have at least a starting snapshot
- Edge-case `spells`: 25 edge cases have rosters, 0 with full segments, 26 chip-only spell refs awaiting pinpoint
- `owner` / `prs`: skipped

### 2. Verification
- Dashboard row reads honestly: yes - `mechanics-model`, gates `n/a` / `todo`, active work, authored map, fresh timestamp
- Matrix click-through lands on my map: yes
- Execution-map counts match tracker doc: yes - 5 live missing rows + 1 moved, 10 markdown-only rows, 9 custom labels
- Edge cases all have count + at least one history snapshot: yes

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none

### 4. Doc / UI friction
- The prompt is still a little parity-lane flavored: for Conditions, the dashboard phase-count columns honestly show `-` because the gate report lane is still pending, while the real active counts live in the execution map.

### 5. Confidence
Conditions now matches the Atlas model well enough to resume bucket work; the remaining work is real Conditions implementation, not Atlas modeling.
