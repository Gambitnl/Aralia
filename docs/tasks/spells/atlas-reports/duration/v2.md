## Bucket: Duration

### 1. Fields implemented
- `kind`: unchanged from `parity`
- `lastUpdated`: bumped to 2026-04-27T16:07Z
- `countValue`: added on 10 numeric subbuckets
- `dependsOn`: none real
- `overlapsWith`: none real
- Edge-case `count` / `history`: 34 edge cases now have count, 34 have at least a starting snapshot
- Edge-case `spells`: 34 edge cases have rosters, 0 with full segments, 34 chip-only awaiting pinpoint
- `owner` / `prs`: skipped

### 2. Verification
- Dashboard row reads honestly: yes
- Matrix click-through lands on my map: yes
- Execution-map counts match tracker doc: yes
- Edge cases all have count + at least one history snapshot: yes

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none

### 4. Doc / UI friction
- none

### 5. Confidence
Duration now genuinely matches the Atlas v2 model and is ready to go back to working the bucket; only existing Gap 09 remains as the manual tracker-to-map drift risk.
