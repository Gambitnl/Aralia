## Bucket: Material Component

### 1. Fields implemented
- `kind`: unchanged from `parity`
- `lastUpdated`: bumped to `2026-04-27T23:44Z`
- `countValue`: added on `raw_note_shape_residue`, `alternate_source_material_shape`, `missing_runtime_material_component`, and `model_display_boundary`
- `dependsOn`: none real
- `overlapsWith`: none real
- Edge-case `count` / `history`: 10 edge cases now have count, and all 10 have at least a starting snapshot
- Edge-case `spells`: 10 edge cases have rosters, 3 with full segments and 7 chip-only awaiting deeper pinpointing
- `owner` / `prs`: skipped

### 2. Verification
- Dashboard row reads honestly: yes - the authored row values now match the live `7` canonical -> structured / `7` structured -> JSON tracker state, but this pass was source-backed rather than visually re-opened
- Matrix click-through lands on my map: no - local Atlas browser reopen was not available during this pass
- Execution-map counts match tracker doc: yes
- Edge cases all have count + at least one history snapshot: yes

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none

### 4. Doc / UI friction
- The tool browser blocks `file://` Atlas opens, so local verification depends on a running dev server rather than the static file path named in the workflow
- The Atlas prompt asks for a local open first, but when the dev server is down there is no lightweight fallback verification path beyond source-backed checks

### 5. Confidence
The bucket now fits the Atlas model cleanly and the map is back in sync with the tracker, so it is ready to return to actual Material Component bucket work.
