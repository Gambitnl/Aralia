## Bucket: Sub-Classes

### 1. Fields implemented
- `kind`: unchanged from `'parity'`
- `lastUpdated`: bumped to `2026-04-27T15:56Z`
- `countValue`: added on 7 subbuckets (P1: `missing_structured_subclasses` 55, `incomplete_structured_subclasses` 24, `malformed_structured_subclass_field` 5, `unsupported_canonical_entries` 7, `repeated_base_canonical_entries` 8, `canonical_source_shape_gap` 11; P2: `structured_to_json_residue` 0); skipped on closure subbuckets where `count` is `'-'` or `'schema move'`
- `dependsOn`: added on `Sub-Classes:structured_to_json_residue` -> `[Sub-Classes:missing_structured_subclasses, Sub-Classes:incomplete_structured_subclasses, Sub-Classes:malformed_structured_subclass_field]`
- `overlapsWith`: none real - each spell lives in exactly one P1 subbucket per the tracker's file-content split
- Edge-case `count` / `history`: 8 edge cases now have count, 2 have at least a starting snapshot (`Sub-Classes:missing_structured_subclasses:a` and `:b`); the rest inherit progression from their parent subbucket history rather than carrying their own
- Edge-case `spells`: 3 edge cases have rosters (20 spells total, all chip-only awaiting pinpoint) - `Sub-Classes:malformed_structured_subclass_field:a` (5), `:unsupported_canonical_entries:a` (7), `:repeated_base_canonical_entries:a` (8); 0 with full segments. Skipped where the inside-family split isn't enumerated (`:missing_structured_subclasses:a/:b`, `:canonical_source_shape_gap:a/:b`).
- `owner` / `prs`: skipped - no single-agent driver to record yet

### 2. Verification
- Dashboard row reads honestly: yes - `parity` kind, `active` work-status, `lastUpdated` reads fresh ("today" in gray)
- Matrix click-through lands on my map: yes
- Execution-map counts match tracker doc: drift internal to tracker - map sums to 110 (matches tracker's "Current Canonical-First Spell Split"), but tracker's "Current Status" header says 186; tracker disagrees with itself, not with the map
- Edge cases all have count + at least one history snapshot: no - only `:missing_structured_subclasses:a/:b` carry per-edge `history`; marker edge cases (`:unsupported_canonical_entries:a`, `:repeated_base_canonical_entries:a`, `:malformed_structured_subclass_field:a`) and `:canonical_source_shape_gap:a/:b` share their parent subbucket's history. All 8 carry a `count` (3 are `? spells` where the inside-family split isn't enumerated)

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none

### 4. Doc / UI friction
- Tracker-internal count drift (186 vs 110 within `SPELL_SUB_CLASSES_BUCKET_TRACKER.md`): Atlas v2 has no surface for within-tracker inconsistency; Gap 09 covers tracker<->Atlas drift but not tracker<->tracker
- Onboarding panel doesn't yet mention the v2 alpha-handle convention (`<Bucket>:<subbucket>:<handle>`); had to read the v2 prompt to learn it, not the in-app panel
- Closure-step slug normalization wasn't called out as a v2 expectation - I normalized closure labels to snake_case (`fixture_and_type_test_alignment`, `retire_sub_classes_verification`, `update_gate_checker`) on the assumption that the rule applies uniformly; if closure labels are meant to stay free-form, please flag
- Edge-case `spells` chip-only tier is great, but the diagnostics rose-flag "no segment authored" likely reads as a *defect* on policy/marker edge cases (Folded into Classes / Unsupported Entries / `None` placeholder) where there isn't a "broken bit" to segment - the marker IS the resolution. A neutral diagnostic for `status: 'policy'` edge cases ("policy edge case - no segment expected") would read better than rose
- `EdgeCase.spells` lives on edge cases only; for subbuckets like `incomplete_structured_subclasses` (24) where the parent itself enumerates a roster but no edge cases exist, there's no place to attach the chip list without manufacturing an edge case (considered and rejected as papering-over)

### 5. Confidence
Ready - the bucket genuinely matches the v2 model after `lastUpdated` bump, `spells` rosters on 3 marker edge cases, and full field fill-in across all subbuckets; TypeScript compiles clean (the only `subClasses`/`subClassesStatus` errors come from `subClassesStatus.test.ts`, which is exactly what `Sub-Classes:fixture_and_type_test_alignment:a` is tracking - not new drift); I can return to working `Sub-Classes:missing_structured_subclasses`.
