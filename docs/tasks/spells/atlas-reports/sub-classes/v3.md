## Bucket: Sub-Classes

### 1. Fields implemented (v3 round)
- `lastUpdated`: bumped to `2026-04-28T11:00Z`
- Policy edge cases: 3 swept (`Sub-Classes:unsupported_canonical_entries:a`, `:repeated_base_canonical_entries:a`, `:canonical_source_shape_gap:a`); all already carried v2 chip-only rosters or just gained null-history (see Gap 12 below). They now read neutral instead of rose-flagged per Gap 13.
- `ExecutionStep.spells` (Gap 14): added on `Sub-Classes:incomplete_structured_subclasses` (24 spells, chip-only). Replaced a seeded 10-spell placeholder roster (only `aid` matched the tracker; the other 9 were unrelated spells like `blur`/`haste`/`invisibility`) with the correct 24 from `SPELL_SUB_CLASSES_BUCKET_TRACKER.md`.
- `count: null` snapshots (Gap 12): added on `Sub-Classes:canonical_source_shape_gap:a` (1 null snapshot, "Truly empty canonical") and `:b` (1 null snapshot, "Parser residue / audit artifact") - both carry `count: '? spells'` because the inside-family split across the parent's 11 isn't enumerated yet
- Closure-slug normalization: already normalized in v2 (`fixture_and_type_test_alignment`, `retire_sub_classes_verification`, `update_gate_checker`); no v3 changes needed
- Other v2 fields: unchanged - `kind: 'parity'`, `countValue` on 7 subbuckets, `dependsOn` on `:structured_to_json_residue`, `overlapsWith` empty (no real overlaps), per-edge `history` on `:missing_structured_subclasses:a/:b`, edge-case rosters on the 3 marker edge cases

### 2. Verification
- Dashboard row reads honestly: yes - `parity` kind, `active` work-status, `lastUpdated` reads fresh ("today" gray)
- Matrix click-through lands on my map: source-only (dev server unreachable from this session)
- Execution-map counts match tracker doc: drift internal to tracker - map sums to 110 (matches tracker's "Current Canonical-First Spell Split") but tracker's "Current Status" header still says 186; tracker disagrees with itself, not with the map. Same drift flagged in v2.
- Policy edge cases now read neutral instead of rose: yes (verified at source - all 3 carry `status: 'policy'`; per Gap 13 the no-segment / no-comparison diagnostic rows now render neutral with "Policy edge case - no segment expected")
- Subbucket-level rosters render correctly (if added): source-only verified - `Sub-Classes:incomplete_structured_subclasses` carries 24 well-formed `SpellRef`s with id+level
- `?` history bubbles render correctly (if added): source-only verified - both `:canonical_source_shape_gap:a` and `:b` carry one `{ count: null, date: '2026-04-28', note: 'pending recount - ...' }` snapshot

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none
- Resolved gaps (12/13/14) that don't actually feel resolved: all good - 12/13/14 each landed exactly the shape Sub-Classes needed in v2 §4

### 4. Doc / UI friction
- The seeded `ExecutionStep.spells` example on `Sub-Classes:incomplete_structured_subclasses` carried the wrong roster (10 spells, only `aid` was actually in the tracker's 24-spell list; the rest like `blur`/`haste`/`invisibility` aren't in any Sub-Classes subbucket). Likely a v3-seeding placeholder. If similar seeded examples were dropped on other buckets, those agents should re-check against their tracker rather than trust the seed.
- Onboarding panel still doesn't mention the alpha-handle convention (`<Bucket>:<subbucket>:<handle>`); v3 prompt mentioned panel updates for `ExecutionStep.spells` / policy diagnostics / null history but the alpha-handle gap from v2 §4 wasn't called out as addressed.
- Tracker-internal count drift (186 vs 110 within `SPELL_SUB_CLASSES_BUCKET_TRACKER.md`) is still unresolved; Gap 09's CI parser would catch this only if it parses the tracker's "Current Status" header *alongside* the file-content split, which probably warrants an explicit note in the Gap 09 fix shape.

### 5. Confidence
Ready, no new gaps - the model now has a slot for every honest shape Sub-Classes carries (orphan rosters, policy markers, pending-recount edge cases), and the bucket can return to actually working `Sub-Classes:missing_structured_subclasses`.
