## Bucket: Classes

### 1. Fields implemented
- `kind`: unchanged from `parity`
- `lastUpdated`: bumped to `2026-04-27T16:01Z`
- `countValue`: added on 3 subbuckets
- `dependsOn`: added on `runtime_recheck_after_phase_1` -> `Classes:missing_derived_base_classes`, `Classes:canonical_metadata_leak`; added on `canonical_audit_derivation` -> same blockers
- `overlapsWith`: none real
- Edge-case `count` / `history`: 7 edge cases now have count, 7 have at least a starting snapshot
- Edge-case `spells`: 7 edge cases have rosters, 0 with full segments, 17 chip-only awaiting pinpoint
- `owner` / `prs`: owner set on `missing_derived_base_classes` and `canonical_metadata_leak`; PRs skipped

### 2. Verification
- Dashboard row reads honestly: yes
- Matrix click-through lands on my map: yes
- Execution-map counts match tracker doc: yes, map splits the tracker's 17 canonical dirty spells into 12 derived-class residues and 5 metadata leaks, with 0 runtime drift
- Edge cases all have count + at least one history snapshot: yes

### 3. Gaps reported
- New entries appended to `ATLAS_GAPS_REGISTRY.md`: none
- Existing entries I added notes to: none

### 4. Doc / UI friction
- none

### 5. Confidence
The Classes bucket now matches the V2 model and is ready to go back to canonical-to-structured class review work.
