# Multi-Instance And Split Targeting

Bucket id: `multi_instance_or_split_targeting`
Total findings: 28
Open findings: 0
Closed findings: 28
Deferred flavor findings: 0
Special questions: 0

Effects that create multiple beams, lights, targets, attacks, or secondary target relationships.

| finding id | status | closed reason | canonical evidence | structured state | json state |
| --- | --- | --- | --- | --- | --- |
| `dancing-lights::multi_instance_or_split_targeting` | `closed` | Closed by extending the target-instance vocabulary for light instances and recording Dancing Lights as four persistent independently positioned light instances in structured markdown and runtime targeting.instanceAllocation. Separate mode-choice, movement, and sustain-action rows remain open. | You create up to four torch-size lights within range, making them appear as torches, lanterns, or glowing orbs that hover for the duration. / Whichever form you choose, each light sheds Dim Light in a 10-foot radius. | Targeting Type: point \| Targeting Max: 1 \| Target Instance Scaling Rule: not_applicable | Runtime JSON contains related hints: maxTargets. |
| `eldritch-blast::multi_instance_or_split_targeting` | `closed` | Eldritch Blast now records one base beam, character-level tier scaling to additional beams, same-or-different target assignment, and separate per-beam attack resolution with structured Target Instance fields and runtime targeting.instanceAllocation data. | The spell creates two beams at level 5, three beams at level 11, and four beams at level 17. / You can direct the beams at the same target or at different ones. / Make a separate attack roll for each beam. | Targeting Type: multi \| Targeting Max: not_applicable \| Target Instance Scaling Rule: character_level_tiers \| Scaling Rule 1 Type: character_level_tiers \| Scaling Rule 1 Applies To: damage \| Force damage \| dice 1d10 \| trigger immediate \| Scaling Rule 1 Notes: 1 beam at level 1, 2 beams at level 5, 3 beams at level 11, 4 beams at level 17; bonusPerLevel=+1 beam | Runtime JSON contains related hints: maxTargets, thresholds, multi. |
| `green-flame-blade::multi_instance_or_split_targeting` | `closed` | Closed by adding Secondary Target structured fields and runtime effects[].secondaryTargeting data. Green-Flame Blade now records the primary-hit trigger, primary-target origin, different visible creature within 5 feet, caster choice, no repeat chain, and no fresh attack or damage roll for the secondary fire damage effect. | On a hit, the target suffers the weapon attack's normal effects, and you can cause green fire to leap from the target to a different creature of your choice that you can see within 5 feet of it. / The second creature takes fire damage equal to your spellcasting ability modifier. / At 5th level, the melee attack deals an extra 1d8 fire damage to the target on a hit, and the fire damage to the second creature increases to 1d8 + your spellcasting ability modifier. | Targeting Type: melee \| Targeting Max: 1 \| Secondary Target Trigger: primary_hit \| Secondary Target Origin: primary_target \| Secondary Target Range: 5 \| Secondary Target Range Unit: feet \| Secondary Target Valid Targets: creature \| Secondary Target Selection: caster_choice | Runtime JSON contains related hints: maxTargets, secondary. |
| `chromatic-orb::manual_damage_dice_leap_chain` | `closed` | Closed by adding Secondary Target structured fields and runtime effects[].secondaryTargeting data. Chromatic Orb now records duplicate damage dice as the chain trigger, previous-target origin, different chosen creature within 30 feet, fresh attack and damage rolls, slot-level maximum leaps, and one-target-per-casting uniqueness. | If you roll the same number on two or more of the d8s, the orb leaps to a different target of your choice within 30 feet of the target. Make an attack roll against the new target, and make a new damage roll. | Effect Type: DAMAGE; structured fields only preserve the initial ranged spell attack and damage. | Runtime JSON only has one DAMAGE effect for the initial hit. |
| `heroism::manual_slot_scaled_targets` | `closed` | Heroism now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 1. | Scaling Rule 1 records special higher-level prose; max target count remains one. | Runtime JSON stores maxTargets 1 and no target-count scaling formula. |
| `jump::manual_slot_scaled_targets` | `closed` | Jump now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 1. | Scaling Rule 1 records special higher-level prose; max target count remains one. | Runtime JSON stores maxTargets 1 and no target-count scaling formula. |
| `longstrider::manual_slot_scaled_targets` | `closed` | Longstrider now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 1. | Scaling Rule 1 records special higher-level prose; max target count remains one. | Runtime JSON stores maxTargets 1 and no target-count scaling formula. |
| `magic-missile::manual_dart_allocation_and_scaling` | `closed` | Magic Missile now records its base dart count, per-slot added darts, same-or-different target allocation, and simultaneous dart resolution with structured Target Instance fields and runtime targeting.instanceAllocation data. | You create three glowing darts of magical force. Each dart strikes a creature of your choice that you can see within range. The darts all strike simultaneously, and you can direct them to hit one creature or several. Using a Higher-Level Spell Slot. The spell creates one more dart for each spell slot level above 1. | Targeting Max: 3; Scaling Rule 1 Bonus Per Level: +1 dart (1d4+1 each). | Runtime JSON stores maxTargets 3 and damage scaling prose, but no separate dart count, per-dart allocation, simultaneity, or dart-count scaling field. |
| `blindness-deafness::manual_slot_scaled_targets` | `closed` | Blindness/Deafness now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 2. | Scaling Rule 1 stores +1 target, but max target scaling is not represented as a typed target-count scaling field. | Runtime effects carry scaling bonusPerLevel +1 target, but targeting.maxTargets remains fixed at 1. |
| `cordon-of-arrows::multi_instance_or_split_targeting` | `closed` | Cordon of Arrows now records planted ammunition as target-instance projectile data with four base pieces, +2 pieces per slot level, sequential one-piece trigger resolution, and per-piece destruction notes. Its separate ignored-creature, planted-object, and no-ammunition-ending rows remain open. | You touch up to four nonmagical Arrows or Bolts and plant them in the ground in your space. | Targeting Type: area \| Target Instance Scaling Rule: slot_level_plus_two_per_level \| Scaling Rule 1 Type: slot_level_bonus \| Scaling Rule 1 Applies To: targeting.instanceAllocation.baseCount \| damage \| Piercing damage \| dice 2d4 \| trigger on_enter_area \| One planted ammo piece strikes a creature that enters the area or ends its turn \| Scaling Rule 1 Bonus Per Level: +2 ammo pieces | Runtime JSON contains related hints: maxTargets. |
| `enhance-ability::manual_slot_scaled_targets_with_per_target_choice` | `closed` | Closed by reusing scalable targeting.maxTargets for +1 target per slot above 2 and adding targeting.perTargetChoice data. Enhance Ability now records that each selected target receives a required ability choice and higher-slot casts may assign different abilities to different targets. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 2. You can choose a different ability for each target. | Scaling Rule 1 stores the higher-level prose as special_text. | Runtime targeting.maxTargets remains 1 and there is no per-target ability choice model. |
| `enthrall::manual_creatures_of_choice_unbounded_multi_target` | `closed` | Closed by applying the schema-safe `unlimited` targeting.maxTargets sentinel: Enthrall structured markdown now records Targeting Max unlimited and runtime JSON uses maxTargets `unlimited` for any number of chosen visible creatures in range. | You weave a distracting string of words, causing creatures of your choice that you can see within range to make a Wisdom saving throw. | Targeting Type: multi; no explicit target count. | Runtime targeting type is multi but maxTargets remains 1. |
| `invisibility::manual_slot_scaled_targets` | `closed` | Invisibility now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 2. | Scaling Rule 1 stores the higher-level target count as special_text. | Runtime targeting.maxTargets remains 1 and the utility scaling has no typed +1 target value. |
| `scorching-ray::manual_ray_allocation_and_scaling` | `closed` | Scorching Ray now records its three base rays, per-slot added rays, same-or-different target assignment, and separate per-ray attack resolution with structured Target Instance fields and runtime targeting.instanceAllocation data. | You hurl three fiery rays. You can hurl them at one target within range or at several. Make a ranged spell attack for each ray. ... You create one additional ray for each spell slot level above 2. | Targeting Max: 3; Scaling Rule 1 Notes: +1 ray per slot level above 2nd. | Runtime JSON stores maxTargets 3 and a prose customFormula, but no first-class ray count, per-ray attack, allocation, or ray-count scaling. |
| `spider-climb::manual_slot_target_scaling` | `closed` | Spider Climb now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 2. | Scaling Rule 1 records special_text for one additional target per slot level above 2. | Runtime targeting has maxTargets 1 and no target-count scaling data. |
| `animate-dead::multi_instance_or_split_targeting` | `closed` | Closed the reassert-control count by adding `controlled_undead` to the target-instance vocabulary and recording Animate Dead's reassert mode as up to four existing undead, plus two per slot level above 3, in structured markdown and runtime targeting.instanceAllocation. The separate animate-versus-reassert mode, source corpse/bones eligibility, undead output, command action, control duration, and transformation mechanics remain open under other Animate Dead rows. | This use of the spell reasserts your control over up to four creatures you have animated with this spell rather than animating a new creature. | Targeting Type: single \| Target Instance Scaling Rule: slot_level_plus_two_per_level \| Scaling Rule 1 Type: slot_level_bonus \| Scaling Rule 1 Applies To: targeting.instanceAllocation.baseCount \| Scaling Rule 1 Bonus Per Level: +2 undead \| Scaling Rule 1 Notes: You animate or reassert control over two additional Undead creatures for each spell slot level above 3. Each of the creatures must come from a different corpse or pile of bones. | Runtime JSON contains related hints: maxTargets, multi. |
| `catnap::manual_slot_target_scaling` | `closed` | Catnap now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | At Higher Levels. When you cast this spell using a spell slot of 4th level or higher, you can target one additional willing creature for each slot level above 3rd. | Scaling Rule 1 records special_text target scaling. | Runtime targeting maxTargets is 3 with no target-count scaling data. |
| `fly::manual_slot_target_scaling` | `closed` | Fly now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | Using a Higher-Level Spell Slot. You can target one additional creature for each spell slot level above 3. | Scaling Rule 1 records special_text target scaling. | Runtime targeting maxTargets is 1 with no target-count scaling data. |
| `gaseous-form::manual_slot_target_scaling` | `closed` | Gaseous Form now records higher-slot target-count scaling with structured targeting.maxTargets scaling notes and runtime targeting.maxTargets slot-level thresholds. | When you cast this spell using a spell slot of 4th level or higher, you can target one additional creature for each slot level above 3rd. | Scaling Rule 1 records special_text target scaling. | Runtime targeting maxTargets is 1 with no target-count scaling data. |
| `intellect-fortress::manual_slot_target_scaling` | `closed` | Closed by adding first-class target-cluster fields and runtime targeting.targetCluster data: Intellect Fortress now records slot-level targeting.maxTargets scaling and the requirement that all higher-slot chosen targets be within 30 feet of each other. | When you cast this spell using a spell slot of 4th level or higher, you can target one additional creature for each slot level above 3rd. The creatures must be within 30 feet of each other when you target them. | Scaling Rule 1 records special_text target scaling. | Runtime targeting maxTargets is 1 with no target-count scaling or mutual-distance constraint. |
| `lightning-arrow::manual_both_damage_effects_scale` | `closed` | Closed by splitting Lightning Arrow into primary 4d8 Lightning and secondary 2d8 Lightning damage legs, adding Secondary Damage structured fields, and recording +1d8 slot-level scaling on both runtime damage effects. The separate pending-attack trigger finding remains open. | The damage for both effects of the spell increases by 1d8 for each spell slot level above 3. | Scaling Rule 1 records special_text for both effects. | Runtime has only one damage effect and no typed scaling for both primary and secondary damage legs. |
| `tiny-servant::manual_slot_additional_objects` | `closed` | Closed by reusing existing scalable targeting.maxTargets: Tiny Servant now records a multi-object target count with slot-level thresholds from 1 object at slot 3 to +2 additional objects per slot level above 3, and its structured markdown points Scaling Rule 1 at targeting.maxTargets. | When cast using a spell slot of 4th level or higher, you can animate two additional objects for each slot level above 3rd. | Scaling Rule 1 records special_text target/object scaling. | Runtime targeting maxTargets 1 and has no target scaling. |
| `druid-grove::multi_instance_or_split_targeting` | `closed` | Closed the multi-tree half by adding `animated_tree` to the target-instance vocabulary and recording Druid Grove as up to four persistent independently tracked tree guardians in structured markdown and runtime targeting.instanceAllocation. The split 5-foot square selection and ward/environment mechanics remain open under Druid Grove's terrain, choice/mode, environmental, ward, summon/control, message, and conditional-ending rows. | You can animate up to four trees in the area, causing them to uproot themselves from the ground. | Targeting Type: touch \| Target Instance Scaling Rule: not_applicable | Runtime JSON contains related hints: maxTargets. |
| `planar-ally::multi_instance_or_split_targeting` | `closed` | The detector matched requesting a specific creature, but the spell sends one planar ally; no multi-instance targeting is required. | If you know a specific creature's name, you can speak that name when you cast this spell to request that creature, though you might get a different creature anyway (DM's choice). | Targeting Type: point | Runtime JSON contains related hints: maxTargets. |
| `animal-shapes::multi_instance_or_split_targeting` | `closed` | Closed by expanding the shared targeting.maxTargets contract to accept the explicit `unlimited` sentinel and replacing Animal Shapes' runtime 999 placeholder with `unlimited`; structured markdown already recorded Targeting Max as unlimited. | Choose any number of willing creatures that you can see within range. | Targeting Type: multi \| Targeting Max: unlimited \| Valid Targets: willing_creature | Runtime JSON uses targeting.type multi, but maxTargets is a temporary numeric stand-in because the schema lacks an unlimited sentinel. |
| `mighty-fortress::multi_instance_or_split_targeting` | `closed` | Closed: four turrets, up to four doors, rooms, and one hundred servants are now captured by spatial details and utility/summoning effects. | You can place up to four stone doors in the fortress's outer wall. | Targeting Type: area | Runtime JSON contains related hints: maxTargets. |
| `storm-of-vengeance::multi_instance_or_split_targeting` | `closed` | Closed by adding a reusable Effect Schedule field family and runtime effectSchedule object. Storm of Vengeance now records later caster-turn stages for Turn 2 acid rain, Turn 3 six distinct lightning targets, Turn 4 hailstones, and Turns 5-10 freezing rain/control while keeping the existing effect packets as the mechanical payloads. | You call six bolts of lightning from the cloud to strike six different creatures or objects beneath it. | Targeting Type: point \| Secondary Damage Dice: 4d6 \| Secondary Damage Type: Acid | Runtime JSON contains related hints: maxTargets. |
| `true-polymorph::multi_instance_or_split_targeting` | `closed` | Closed: The one-target creature/object split is now represented by creature_or_object structured targeting and mode Utility Options. | The creature shape-shifts into a different creature or a nonmagical object, or the object shape-shifts into a creature (the object must be neither worn nor carried). | Targeting Type: creature_or_object \| Targeting Max: 1 | Runtime JSON contains related hints: maxTargets. |
