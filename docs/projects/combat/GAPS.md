# Combat System Gap Registry

Status: active
Last updated: 2026-06-08

## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|
| G1 | done | support_needed_now | Worker A | `src/systems/combat/` | combat docs enrichment | Event emitters and sustain tracker are singletons with no test-reset API. | `src/systems/combat/AttackEventEmitter.ts`, `src/systems/combat/MovementEventEmitter.ts`, `src/systems/combat/SustainActionSystem.ts` | Shared singleton state can leak across test runs and prevents deterministic test isolation. | Implement `setInstance` / `createFresh` test control methods on each singleton emitter/tracker and isolate in tests. | Completed and verified with 21 new isolated unit tests. |
| G2 | done | support_needed_now | Worker A | `src/systems/combat/` | combat docs enrichment | Widespread `TODO(lint-intent)` debt (`unused imports`) in combat systems. | `src/systems/combat/AttackRiderSystem.ts`, `src/systems/combat/SustainActionSystem.ts` | Unused imports reduce file signal and clarity. | Remove unused `CombatCharacter as _CombatCharacter` imports and add standard plain-English headers. | Completed and verified. |
| G3 | not_started | adjacent_follow_up | Worker A | `src/hooks/combat/` | combat docs enrichment | AI and turn loop behavior has known hard-coded assumptions and TODO markers (`max` action cap, action-type support). | `src/hooks/combat/useCombatAI.ts`, `src/hooks/combat/useCombatEngine.ts`, `src/hooks/combat/useTurnManager.ts`, `src/hooks/combat/useActionExecutor.ts` | Limits hidden assumptions can become correctness issues under non-default combat content (e.g., auto-managed allies, legendary chains, forced movement). | Add deterministic follow-up pass to confirm AI action loop assumptions against gameplay test fixtures after this docs-only slice. | Capture a focused test scenario for AI/legendary/forced movement before changing behavior. |
| G4 | not_started | in_scope_now | Worker A | `src/hooks/combat`, `src/systems/combat` | combat docs enrichment | Death-save and several rules-level details are documented as missing/incomplete in architecture notes and should not be treated as complete. | `docs/architecture/domains/combat.md`, `src/hooks/combat/useTurnManager.ts`, `src/hooks/combat/useCombatEngine.ts` | Missing/partial combat rule coverage risks hidden balance and sequencing differences in future slices. | Decide whether to route to immediate follow-up implementation or formal architectural follow-up under a dedicated rules slice. | Any implementation change touching combat math should explicitly reference this gap as active. |
| G5 | done | in_scope_now | Worker A | `src/components/Combat/` | combat docs enrichment | In `ReactionPrompt.tsx` and encounter builder there is no broad end-to-end gameplay flow test tying reaction UI prompt timing to turn-level execution. | `src/components/Combat/ReactionPrompt.tsx`, `src/components/Combat/__tests__/ReactionPrompt.test.tsx`, `src/hooks/combat/useActionExecutor.ts` | Current coverage is component-scoped and does not guarantee full flow correctness under map/AI sequencing. | Add an integration test slice after this docs pass if combat flow regressions appear. | Keep as open follow-up; mark as completed only when end-to-end path is added. |
| G6 | done | support_needed_now | Worker A | `src/utils/combat/` | combat docs enrichment | player character `armorClass` / `baseAC` are not mapped from PlayerCharacter to CombatCharacter. | `src/utils/combat/combatUtils.ts:845` | Every player character enters combat with undefined AC, bypassing normal AC checks. | Map `PlayerCharacter.armorClass` to `CombatCharacter.armorClass` and `baseAC` in the player character converter. | Completed and verified with `combatUtils_premade.test.ts` mapping checks. |
| G7 | done | support_needed_now | Worker A | `src/utils/combat/` | combat docs enrichment | Ranged weapon range is not read in `createWeaponAbility`, default is melee range (1 or 2). | `src/utils/combat/combatUtils.ts` | Equipped longbows and ranged weapons get melee range, breaking ranged combat semantics. | Parse `range:N` from `weapon.properties` when creating weapon abilities in combatUtils. | Completed and verified with `combatUtils_premade.test.ts` ranged weapon range checks. |
| G8 | done | adjacent_follow_up | Worker A | `src/utils/combat/` | combat docs enrichment | Rage resistance modifiers are ignored by ResistanceCalculator. | `src/utils/combat/resistanceUtils.ts:126` | Barbarian Rage status effect modifiers are registered but ignored during damage resolution. | Modify `applyResistances` to check `statusEffects[].modifiers.resistance[]` in addition to direct `resistances[]`. | Completed and verified with new `resistanceUtils.test.ts` status effects tests. |
| G9 | done | adjacent_follow_up | Worker A | `src/commands/factory/` | combat docs enrichment | Sneak Attack automatic trigger logic is completely absent. | `src/commands/factory/AbilityCommandFactory.ts` | Rogue Sneak Attack mechanics cannot be resolved or logged during active combat. | Implement Sneak Attack validation and trigger hooks inside `WeaponAttackCommand` and damage command resolution. | Completed and verified with new extensive `AbilityCommandFactory.test.ts` trigger tests. |
| G10 | done | support_needed_now | Worker A | `src/utils/combat/` | combat docs enrichment | Enemy / Monster `armorClass` / `baseAC` are not mapped in `createEnemyFromMonster.ts`. | `src/utils/combat/createEnemyFromMonster.ts` | Enemies enter combat with undefined AC, defaulting to AC 10 and bypassing bestiary definitions. | Map `monsterData.armorClass` in `createEnemyFromMonster.ts` fallback and standard paths. | Completed and verified with `createEnemyFromMonster.test.ts`. |
| G11 | done | adjacent_follow_up | Next Agent | `src/utils/combat/` | combat docs enrichment | Class features were not auto-generated for all classes. | `src/utils/combat/combatUtils.ts` | Barbarian Rage, Monk Flurry, Bardic Inspiration, Divine Smite, and Pact Magic were absent from the ability palette. | Added class-feature palette generation in `combatUtils.ts` for missing entries. | Added `rage`, `flurry_of_blows`, `bardic_inspiration`, `divine_smite`, and `pact_magic` to the generated ability palette with focused limited-use mapping where available. |
| G12 | done | adjacent_follow_up | Next Agent | `public/premade-characters/` | combat docs enrichment | Weaponless premade character data configuration. | `public/premade-characters/` | Verified all 13 premade character templates already define `equippedItems.MainHand` with a weapon object and no combat fixture uses premade martial fallback logic. | Keep JSON templates as-is and enforce regression coverage with focused tests in `src/utils/combat/__tests__/combatUtils_premade.test.ts` that assert fighter/martial premades always produce `attack_main`. | Verify fighter premade character possesses `attack_main` with a real weapon name (Longsword/Shortsword/Rapier/etc.) rather than unarmed strike fallback. |
| G13 | done | in_scope_now | Gemini | `src/hooks/combat/` | combat docs enrichment | Death Saving Throws system is completely absent. | `src/hooks/combat/__tests__/useTurnManager.deathSaves.test.ts` | Characters at 0 HP are simply skipped by turn ordering indefinitely instead of rolling death saves or dying. | Implement a comprehensive downed condition state and death-saving throw d20 execution loop in `advanceTurn`. | Verified with 8 comprehensive unit tests. |
| G14 | done | adjacent_follow_up | Gemini | `src/components/BattleMap/` | death saves implementation | UI panels do not render death save successes or failures. | `src/components/BattleMap/CombatCharacterInspector.tsx`, `src/components/BattleMap/InitiativeTracker.tsx` | Downed player's stabilization/death progress is invisible to the user in combat UI. | Add successes/failures indicators to the combat inspector and initiative tracker UI elements. | Verified visually in Inspector and InitiativeTracker components. All TS compilation checks pass. |
| G15 | done | support_needed_now | Gemini | `src/types/combat.ts` | death saves implementation | SavePenaltySystem has type drift with missing savePenaltyRiders property on CombatCharacter. | `src/types/combat.ts` | Causes multiple TypeScript compiler errors across combat systems and tests. | Add `savePenaltyRiders?: SavePenaltyRider[];` to the `CombatCharacter` interface in `src/types/combat.ts`. | Compiler compiles cleanly with no SavePenaltyRider errors. |
| G16 | done | adjacent_follow_up | Gemini | `src/hooks/combat/` | death saves implementation | useCombatAI lacks awareness of downed characters. | `src/hooks/combat/useCombatAI.ts`, `src/utils/combat/combatAI.ts` | Enemies might attack stabilized characters or waste attacks on unconscious characters; allies may not prioritize healing downed characters. | Refactor target selection heuristics in AI systems to respect downed states. | Tested in combatAI.test.ts with 3 new targeted AI target selection tests. All pass. |
| G17 | done | adjacent_follow_up | Gemini | `src/hooks/combat/` | T10 implementation | Conditions and active effects do not tick down at turn boundaries, leaving buffs/debuffs indefinitely. | `src/hooks/combat/useTurnManager.ts:236` | Buffs like Shield/Bless persist indefinitely without manual clearing. | Implement turn-based duration tick down and cleanup. | Verified with 6 comprehensive durations tests. |
| G18 | done | adjacent_follow_up | Gemini | `src/hooks/combat/` | combat docs enrichment | Opportunity Attack reactions pick arbitrary weapons rather than allowing tactical weapon choice. | `src/hooks/combat/useActionExecutor.ts:283` | Characters with multiple weapons cannot pick which weapon to swing on OA reaction. | Add weapon picker select/button options to OA reaction prompt. | Verified in `useActionExecutor.test.ts` with mock choice verification. |
| G19 | not_started | adjacent_follow_up | Next Agent | `src/utils/combat/` | combat scenario tester | Environmental spell zones that grant damage resistance/immunity are not dynamically applied to damage calculations. | `src/utils/combat/resistanceUtils.ts` | Standing inside a protective spell zone (like warding) does not dynamically grant resistance in `calculateDamage`. | Update `ResistanceCalculator` or spell zone processing to check if the target's tile overlaps with a resistance-granting active spell zone. | Validate that a character inside a protection zone receives halved damage. |
| G20 | not_started | adjacent_follow_up | Next Agent | `src/components/BattleMap/` | combat scenario tester | Damage resistances, vulnerabilities, and immunities are not visually exposed in the 2D/3D map overlays. | `src/components/BattleMap/BattleMap.tsx` | Players must open character sheets manually to see target weaknesses/immunities, slowing down strategic selection in the sandbox. | Add small elemental badges or tooltips to 2D BattleMap tokens and 3D VFX overlays displaying active traits. | Verify badges are visible on Fire Elemental and Skeleton tokens. |
| G21 | not_started | adjacent_follow_up | Next Agent | `src/hooks/combat/` | T10 implementation | Active effects and conditions that modify speed (e.g. Slasher, Haste) do not dynamically recalculate movement speed on start or expiration. | `src/hooks/combat/useActionEconomy.ts`, `src/hooks/combat/useTurnManager.ts` | Prevents speed debuffs and buffs from modifying movement range during active combat. | Update `resetEconomy` to check `activeEffects` and `statusEffects` for speed modifiers and apply them to `movement.total`. | Verify Warlock under Slasher effect receives speed penalty. |
| G22 | done | in_scope_now | Gemini | `src/hooks/combat/` | T10 implementation | Unconscious or downed characters (at 0 HP) do not automatically drop concentration on active spells. | `src/hooks/combat/useTurnManager.ts`, `src/commands/effects/DamageCommand.ts` | Violates fundamental concentration rules, allowing unconscious characters to maintain spells. | Implemented via `handleCharacterUpdateWrapped` in useTurnManager.ts (hook path) and 0 HP bypass in DamageCommand.ts (command path). | Verified with 9 passing tests in useTurnManager.deathSaves.test.ts including concentration drop + ally effect cleanup. |
| G23 | not_started | adjacent_follow_up | Next Agent | `src/state/reducers/combatReducer.ts` | Triage import from GG-13 (2026-06-02) | Combat log state is transient and cleared upon page reload, with no history or export functionality. | `src/state/reducers/combatReducer.ts` | Important combat events and results are lost, hindering long-term tactical review or bug reporting. | Implement local storage persistence or an export feature for the combat log. | Verify log persistence after page refresh. |
| G24 | not_started | adjacent_follow_up | Next Agent | `src/hooks/combat/` | T11 implementation (2026-06-02) | Opportunity Attack system does not support the War Caster feat option to cast a spell instead of making a weapon attack. | `src/hooks/combat/useActionExecutor.ts` | Players with War Caster feat should be able to cast a single-target spell as an Opportunity Attack reaction. | Update Opportunity Attack execution flow to prompt for reaction spells if character has War Caster feat. | Verify reaction prompt displays spells as options for player Opportunity Attacks. |
| G25 | not_started | adjacent_follow_up | Next Agent | `src/hooks/combat/` | T11 implementation (2026-06-02) | Sentinel feat stop-in-place mechanics are not implemented for Opportunity Attacks. | `src/hooks/combat/useActionExecutor.ts` | Characters with the Sentinel feat should set the target's speed to 0 for the turn when hitting them with an opportunity attack. | Add a check for Sentinel feat in Opportunity Attack hit resolution and set speed to 0. | Verify a target hit by an Opportunity Attack from a Sentinel character has their speed set to 0. |
| G26 | done | adjacent_follow_up | Codex | `src/hooks/combat/` | T11 type-error resolution (2026-06-02) | AI character turn processing does not await the asynchronous execution of `executeAbility`, potentially causing out-of-order logs, state desynchronization, or race conditions during multi-action turns. | `src/hooks/combat/useCombatAI.ts:171` | When an AI character executes an ability that triggers player reactions (like Opportunity Attacks), React state updates must block execution. If not awaited, the AI proceeds synchronously while the player is prompted. | Update `useCombatAI.ts` `performTurnLogic` to await `executeAbility` and broaden the execute callback contract to accept async completion. | `src/hooks/combat/__tests__/useCombatAI.test.ts` verifies AI action loop does not advance to a second action before `executeAbility` resolves. |
| G27 | done | adjacent_follow_up | Worker A | `src/systems/combat/reactions/` | OpportunityAttackSystem reach inspection (2026-06-02) | `OpportunityAttackSystem` now checks distinct threatened reach thresholds per weapon/ability instead of collapsing everything into one maximum reach. | `src/systems/combat/reactions/OpportunityAttackSystem.ts`, `src/systems/combat/__tests__/OpportunityAttackSystem.test.ts` | A character wielding both a 5ft reach weapon and a 10ft reach weapon now provokes at the correct boundary for each threshold, instead of waiting for the maximum reach alone. | Completed and verified with focused 5ft and 10ft threshold tests. | Verified by `OpportunityAttackSystem.test.ts` cases covering both 5ft and 10ft reach boundaries. |
| G28 | done | adjacent_follow_up | Codex | `src/hooks/combat/` | G22 implementation (2026-06-03) | `handleCharacterUpdateWrapped` reads `characters` (React prop) to find original state, but within a single synchronous batch (e.g., `initializeCombat`), the prop is stale. If two concentrating characters both drop to 0 HP in the same batch, the second cleanup pass uses the same stale `characters` array and may redundantly re-clean already-cleaned ally effects. | `src/hooks/combat/useTurnManager.ts:100,123` | Can produce redundant `onCharacterUpdate` calls for allies and double-log concentration loss messages in edge-case multi-drop scenarios. | Refactor `handleCharacterUpdateWrapped` to track already-cleaned effect IDs across calls within a batch (e.g., via a ref), or validate the ally's current effects before cleaning. | Verified with a focused same-batch regression in `useTurnManager.deathSaves.test.ts`; only one ally cleanup update fires when two concentrating characters drop together. |
| G29 | done | adjacent_follow_up | Codex | `src/hooks/combat/`, `src/commands/effects/` | G22 implementation (2026-06-03) | Concentration-break on 0 HP existed in two separate code paths: `handleCharacterUpdateWrapped` (hook callback path for scheduled/tile damage) and `DamageCommand` (command path for attack/spell damage). These paths now have a parity test to guarantee equivalent cleanup. | `src/hooks/combat/useTurnManager.ts:103-147`, `src/commands/effects/DamageCommand.ts:318-335` | If one path is updated without the other, concentration cleanup behavior can diverge silently between attack damage and scheduled/environmental damage. | Keep a cross-path integration test that verifies ally cleanup, light cleanup, and concentration-loss messaging are equivalent between the two paths. | `ConcentrationBreakPathParity` in `src/commands/effects/__tests__/ConcentrationBreakPathParity.test.ts` compares outputs across both paths for the same setup. |
| G30 | blocked | blocked_human_decision | human/product owner + combat/layout owners | `docs/projects/code-modularization-audit` CMA-G4 | Code modularization audit routing | `useAbilitySystem.ts` and `useCombatEngine.ts` are large orchestration surfaces and should not be split before command/combat ownership and App-shell boundaries are explicit. | `src/hooks/useAbilitySystem.ts`; `src/hooks/combat/engine/useCombatEngine.ts`; `src/App.tsx`; `docs/projects/code-modularization-audit/GAPS.md` CMA-G4 | These hooks bridge spells, commands, combat state, and App routing; a split can change runtime semantics. | Keep this modularization route review-gated until owner-approved test boundaries and preservation notes exist. | Split plan names `useAbilitySystem` and `useCombatEngine` tests plus App/provider invariants before code movement. |

## Global Gap Routing Notes

- GG-13 was triaged during this pass and imported into this registry as G23.

## Open / Uncertain Notes

- The architecture doc `docs/architecture/domains/combat.md` lists additional mechanical gaps (example: armor/class-feature conversions, death-save behavior). Those remain valid open observations, but they are not fully revalidated here.
- The `docs/architecture/COMBAT_MAP_ENGINE.md` and `docs/architecture/domains/battle-map.md` split show combat execution ownership vs rendering ownership; no immediate gap is claimed here beyond ownership boundaries.
