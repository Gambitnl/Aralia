# World Living Tracker

Status: complete for World-owned scope (all World gaps resolved or routed)
Last updated: 2026-06-18

## Status Vocabulary

- `not_started`
- `active`
- `waiting`
- `blocked`
- `done`
- `superseded`
- `out_of_scope`
## Active Task Queue

| ID | Status | Task | Owner | Last updated | Evidence | Next action | Next check/proof |
|---|---|---|---|---|---|---|---|
| T2 | done | Complete world-system gap pass from observed simulation/state contracts. | Codex application agent (GPT-5) | 2026-06-18 | `src/systems/world/WorldEventManager.ts`, `src/state/migrations/worldDataMigration.ts`, `src/types/world.ts`, `src/types/state.ts`, `src/components/MapPane.tsx`, `src/hooks/actions/handleMovement.ts`, `docs/projects/DECISION_BLITZ_2026-06-10.md` D2, `docs/projects/world/GAPS.md` G1-G11 | Gap pass completed: G1/G2 closed against current source/tests; G9-G11 opened for the remaining world-geography adapter blockers and owner seams. | Focused proof recorded in `AUDIT_OR_PROOF.md`; continue with T4. |
| T4 | done | Extract tile-grid world-map dependency contract for phase-out planning. | Codex application agent (GPT-5) | 2026-06-18 | `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md`, `src/components/MapPane.tsx`, `src/hooks/actions/handleMovement.ts`, `src/types/world.ts`, `src/state/appState.ts`, `src/state/migrations/worldDataMigration.ts`, `src/hooks/useGameInitialization.ts`, `src/utils/mapDataToWorldData.ts`, `docs/projects/DECISION_BLITZ_2026-06-10.md` D2 | Adapter design completed: the contract note names `WorldGeographyId`, `WorldGeographyPoint`, `WorldGeographySnapshot`, `WorldGeographyMutation`, and `WorldGeographyAdapter`, plus per-lane ownership and stop conditions. | Docs consistency and source-backed references verified; continue with T5. |
| T5 | done | Implement the read-only legacy geography adapter types and `fromMapData` helper without changing runtime consumers. | Codex application agent (GPT-5) | 2026-06-18 | `src/utils/world/worldGeographyAdapter.ts`, `src/utils/world/__tests__/worldGeographyAdapter.test.ts`, `src/utils/world/index.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` | Source adapter slice completed: stable legacy geography IDs, read-only point/snapshot projection, optional biome allow-list passability, and `WorldData` carry-through now have focused tests. | `npx vitest run src/utils/world/__tests__/worldGeographyAdapter.test.ts` passed 4 tests; dependency headers synced for `src/utils/world/worldGeographyAdapter.ts` and `src/utils/world/index.ts`. |
| T6 | done | Implement pure discovery/current-marker mutation helpers before any movement rewiring. | Codex application agent (GPT-5) | 2026-06-18 | `src/utils/world/worldGeographyAdapter.ts`, `src/utils/world/__tests__/worldGeographyAdapter.test.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` | Mutation helper slice completed: adapter can build bounded legacy discovery mutations, set/clear current markers, apply mutations immutably to copied `MapData`, and refresh a snapshot for later consumers. | `npx vitest run src/utils/world/__tests__/worldGeographyAdapter.test.ts` passed 8 tests; dependency headers synced for `src/utils/world/worldGeographyAdapter.ts` and `src/utils/world/index.ts`. |
| T7 | done | Wire movement discovery/current-marker writes through the adapter helper while preserving movement behavior. | Codex application agent (GPT-5) | 2026-06-18 | `src/utils/world/worldGeographyAdapter.ts`, `src/hooks/actions/handleMovement.ts`, `src/hooks/actions/__tests__/handleMovement.test.ts`, `src/utils/world/__tests__/worldGeographyAdapter.test.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` | Movement write slice completed: `handleMovement.ts` now routes map reveal, named-location reveal, and world-cell transition marker writes through `buildLegacyDiscoveryMutations`, `createSetCurrentMutation`, and `applyMutationsToMapData` while preserving the legacy `MOVE_PLAYER` payload. | `npx vitest run src/hooks/actions/__tests__/handleMovement.test.ts src/hooks/actions/__tests__/handleMovement_skillCheck.test.ts src/utils/world/__tests__/worldGeographyAdapter.test.ts` passed 3 files / 13 tests; dependency header synced for `src/hooks/actions/handleMovement.ts`. |
| T8 | done | Wire MapPane discovery/current-marker reads through the geography adapter snapshot without changing UI behavior. | Codex application agent (GPT-5) | 2026-06-18 | `src/components/MapPane.tsx`, `src/components/__tests__/MapPane.test.tsx`, `src/utils/world/worldGeographyAdapter.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` | MapPane read slice completed: legacy grid rendering, click payloads, POI discovery, pointer tile resolution, hovered tile readout, and player-cell lookup now consume adapter-projected tile reads while preserving the existing tile-grid UI. | `npx vitest run src/components/__tests__/MapPane.test.tsx src/utils/world/__tests__/worldGeographyAdapter.test.ts src/hooks/actions/__tests__/handleMovement.test.ts src/hooks/actions/__tests__/handleMovement_skillCheck.test.ts` passed 4 files / 14 tests; dependency header synced for `src/components/MapPane.tsx`. |
| T9 | done | Add startup/setup geography snapshot continuity proof without changing save/load or 3D entry. | Codex application agent (GPT-5) | 2026-06-18 | `src/state/__tests__/appState.worldGeographySnapshot.test.ts`, `src/state/appState.ts`, `src/state/migrations/worldDataMigration.ts`, `src/utils/world/worldGeographyAdapter.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` | Startup/load proof completed: new-game setup, dummy/dev start, dummy preview initialization, and migrated loaded-game payloads all produce `WorldGeographySnapshot` values that agree with seed, grid size, point count, current marker, and `worldData` expectations without changing save/load persistence or 3D entry. | `npx vitest run src/state/__tests__/appState.worldGeographySnapshot.test.ts` passed 1 file / 4 tests; no production helper was added because existing `fromMapData` plus migration already express the proof. |
| T10 | done | Prove passability adapter continuity before movement/pathfinding rewiring. | Codex application agent (GPT-5) | 2026-06-18 | `src/utils/world/worldGeographyAdapter.ts`, `src/utils/world/__tests__/worldGeographyAdapter.test.ts`, `src/hooks/actions/handleMovement.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md`, `docs/projects/travel/GAPS.md` G6-G10 | Passability proof completed: adapter snapshots now preserve passable and impassable biome values from canonical biome definitions or a caller-supplied allow-list, while current movement passability checks remain unchanged. Travel-owned cell-native pathfinding and Submap local-terrain routing stay routed to their owner lanes. | `npx vitest run src/utils/world/__tests__/worldGeographyAdapter.test.ts src/hooks/actions/__tests__/handleMovement.test.ts` passed 2 files / 12 tests; dependency headers synced for `src/utils/world/worldGeographyAdapter.ts` and `src/utils/world/index.ts`. |
| T11 | done | Add/prove migration persistence for the future geography contract while legacy `MapData` stays readable. | Codex application agent (GPT-5) | 2026-06-18 | `src/types/world.ts`, `src/state/migrations/worldDataMigration.ts`, `src/state/migrations/__tests__/worldDataMigration.test.ts`, `src/utils/world/worldGeographyAdapter.ts`, `src/utils/world/__tests__/worldGeographyAdapter.test.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` | Migration persistence proof completed: `MapData` now carries optional persisted `worldGeography`, `worldDataMigration` attaches it after backfilling `worldData`, and already migrated saves keep or gain their existing snapshot. Legacy `tiles`, `gridSize`, `MapTile.discovered`, and `MapTile.isPlayerCurrent` remain readable. | `npx vitest run src/state/migrations/__tests__/worldDataMigration.test.ts src/utils/world/__tests__/worldGeographyAdapter.test.ts src/state/__tests__/appState.worldGeographySnapshot.test.ts` passed 3 files / 26 tests; dependency headers synced for `src/state/migrations/worldDataMigration.ts` and `src/types/world.ts`. |
| T12 | done | Prove 3D anchor conversion from persisted world geography without retiring legacy `MapData` fields. | Codex application agent (GPT-5) | 2026-06-18 | `src/utils/world/worldGeographyAdapter.ts`, `src/utils/world/__tests__/worldGeographyAdapter.test.ts`, `src/components/MapPane.tsx`, `src/utils/mapDataToWorldData.ts`, `src/types/state.ts`, `src/types/world.ts`, `docs/projects/world/WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md`, `docs/projects/world-3d-ui/NORTH_STAR.md` | 3D anchor proof completed: `resolve3DAnchor` resolves a persisted geography point into `PlayerWorldPosition`, `PlayerGroundPosition`, the source `WorldData`, and the same geography point without adding a new direct `MapData.tiles` consumer or changing MapPane click behavior. | `npx vitest run src/utils/world/__tests__/worldGeographyAdapter.test.ts src/components/__tests__/MapPane.test.tsx src/utils/__tests__/worldCoords.test.ts src/state/migrations/__tests__/worldDataMigration.test.ts` passed 4 files / 32 tests; dependency headers synced for `src/utils/world/worldGeographyAdapter.ts` and `src/utils/world/index.ts`. |
| T13 | done | Normalize or prove mandatory world-state shape for optional world fields without broad cleanup. | Codex application agent (GPT-5) | 2026-06-18 | `src/types/state.ts`, `src/types/state.d.ts`, `src/state/initialState.ts`, `src/state/appState.ts`, `src/utils/core/factories.ts`, `src/state/__tests__/appState.worldStateShape.test.ts`, `src/state/reducers/__tests__/worldReducer.test.ts`, `docs/projects/world/GAPS.md` G4 | World-state shape proof completed: `activeRumors` and `worldHistory` are mandatory on `GameState`, fresh/factory state seeds them, and `LOAD_GAME_SUCCESS` repairs legacy saves that omit rumor/history/notification/merchant UI fields. | `npx vitest run src/state/__tests__/appState.worldStateShape.test.ts src/state/__tests__/appState.worldHistory.test.ts src/state/reducers/__tests__/worldReducer.test.ts src/systems/world/__tests__/WorldEventManager.test.ts` passed 4 files / 17 tests; dependency headers synced for `src/types/state.ts`, `src/state/initialState.ts`, `src/state/appState.ts`, and `src/utils/core/factories.ts`. |
| T14 | done | Decide and prove the narrow reducer payload contract cleanup for G5 without broad state refactoring. | Codex application agent (GPT-5) | 2026-06-18 | `src/state/reducers/worldReducer.ts`, `src/state/actionTypes.ts`, `src/types/state.ts`, `src/types/state.d.ts`, `src/state/reducers/__tests__/worldReducer.test.ts`, `docs/projects/world/GAPS.md` G5 | Reducer payload slice completed: `SET_MAP_DATA` now centers minimap focus from typed `MapData.gridSize`, `UPDATE_INSPECTED_TILE_DESCRIPTION` writes the typed tile key/description directly, and the stale payload-cast TODOs were removed without touching MapPane, Travel, Submap, deterministic event IDs, or worker/performance lanes. | Red proof: reducer test first failed with `{ x: 0, y: 0 }` for typed `MapData`; green proof: `npx vitest run src/state/reducers/__tests__/worldReducer.test.ts` passed 10 tests; dependency headers synced for `src/state/reducers/worldReducer.ts` and `src/types/state.ts`. |
| T15 | done | Audit and prove the schedule/proximity coupling boundary for G6 without absorbing sibling owner lanes. | Codex application agent (GPT-5) | 2026-06-18 | `src/hooks/actions/handleMovement.ts`, `src/hooks/actions/handleWorldEvents.ts`, `docs/projects/world/GAPS.md` G6, `docs/projects/travel/GAPS.md` G1, `docs/projects/events/GAPS.md` G3-G4, `docs/projects/memory/GAPS.md` G4, `docs/projects/town-description-system/GAPS.md` G5 | Boundary audit completed: World keeps the existing long-rest residue, memory decay, and gossip sequence; forced march is routed to Travel; scheduler/marker policy is routed to Events; action-to-memory mapping stays with Memory; proximity-triggered town-description loading is now represented as a Town Description inbound stub. No World runtime code changed. | Source/docs proof only: `handleMovement.ts`, `handleWorldEvents.ts`, and owner gap files were checked; `git diff --check` is the required structural verification for the docs-only route. |
| T16 | done | Decide and prove deterministic daily-world event IDs for G7 without broad event-loop refactoring. | Codex application agent (GPT-5) | 2026-06-18 | `src/systems/world/WorldEventManager.ts`, `src/systems/world/__tests__/WorldEventManager.test.ts`, `docs/projects/world/GAPS.md` G7 | Deterministic daily-world log proof completed: `WorldEventManager` now derives its own daily log IDs from saved game time and normalizes the returned daily log envelope so sibling subsystem wall-clock IDs do not break replayability. Event selection and sibling subsystem internals were left intact. | Red proof: focused test failed when two identical seeded daily runs used different `Date.now()` values; green proof: `npx vitest run src/systems/world/__tests__/WorldEventManager.test.ts` passed 6 tests; dependency header synced for `src/systems/world/WorldEventManager.ts`. |
| T17 | done | Decide whether G8 needs implementation, benchmark proof, or routing for worldSim load-time performance. | Codex application agent (GPT-5) | 2026-06-18 | `src/services/worldSim/index.ts`, `src/state/migrations/worldDataMigration.ts`, `docs/projects/world/GAPS.md` G8, `docs/projects/worldsim-service/GAPS.md` WSS-001 | Owner audit completed: WorldSim Service already tracks synchronous `runWorldSim(...)` blocking risk, measurement, and background scheduling decisions as WSS-001. World keeps the migration/load evidence but does not duplicate worker/deferred architecture work. | Closed as routed for World. No runtime code changed; source/doc proof is the required check. |
| T18 | done | Decide and prove the remaining G11/GG-29 owner seam for final `MapData.tiles` retirement. | Codex application agent (GPT-5) | 2026-06-18 | `docs/projects/world/GAPS.md` G11, `docs/projects/GLOBAL_GAPS.md` GG-29, `docs/projects/travel/GAPS.md` G6-G10, `docs/projects/submap/NORTH_STAR.md`, `docs/projects/submap/GAPS.md` G9-G12 | Owner seam audit completed: World's adapter-contract lane is already closed by G3/G9/G10; Travel owns cell-native movement/discovery/marker updates; Submap owns grid/Submap retirement and Enter-3D grid-coordinate decoupling; Global GG-29 now records the owner split. | Closed as routed for World. No active World-owned gap remains. |
| T3 | adjacent_follow_up | Keep world-system registry separation from town-description scope. | future agent | 2026-05-31 | `docs/projects/PROJECT_TRACKER.md`, `docs/projects/town-description-system` | Verify handoff boundary in the next world pass and any new project-local docs. | Confirm no gap in this project owns only town-content behavior. |

## Contract Preservation Table

Last updated: 2026-06-18

This table is the first proof artifact for D2/G3. It names the tile-grid contract pieces that must survive while the new world-geography contract and adapters are designed. `WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md` is the concrete T4 design, `src/utils/world/worldGeographyAdapter.ts` contains the T5 read adapter, T6 mutation helpers, T10 passability proof, and T12 `resolve3DAnchor` conversion proof, `handleMovement.ts` uses those helpers for T7 movement writes, `MapPane.tsx` uses adapter-projected marker reads for T8, `appState.worldGeographySnapshot.test.ts` proves startup/load snapshot continuity for T9, and `worldDataMigration.test.ts` proves T11 migration persistence. No old field is safe to retire until its row has all runtime consumers adapted and proven.

| Dependency | Current source | Preserved behavior | Adapter needed before retirement | Owner seam | Next proof |
|---|---|---|---|---|---|
| Movement target and bounds | `src/hooks/actions/handleMovement.ts` uses `mapData.gridSize`, `mapData.tiles`, and `Location.mapCoordinates`. | Player movement cannot leave valid world geography; wilderness/town entry resolves to a stable destination. | `WorldGeographyMovementAdapter` or equivalent that maps legacy x/y and future cell IDs to one destination contract. | World owns contract; Travel owns cell-native pathfinding/movement implementation. | Movement tests cover legacy coordinate and future cell-backed destination resolution. |
| Passability and terrain blocking | `handleMovement.ts` checks `Biome.passable`, `getSubmapTileInfo`, tile biome ids, and grid bounds. | Impassable biomes/submap terrain still block travel with the correct reason. | `WorldGeographyPassabilityAdapter` that reads passability from canonical geography and falls back to legacy tile biome ids during migration. | World owns passability contract; Submap/Travel own local-terrain and route consumers. | T10 proof covers canonical biome and allow-list passability through the adapter; future route consumers must still prove cell-native/local terrain behavior in their owner lanes. |
| Discovery and current marker state | `handleMovement.ts` writes marker changes through the adapter; `MapPane.tsx` renders discovered/current state from adapter-projected legacy tiles. | Exploration reveals the target and nearby cells; the current player position remains visible after movement/load. | Future cell discovery/current state can replace legacy flags once Travel/Submap owner lanes accept their side. | World owns data contract; Travel owns movement write path; MapPane owns rendering read path. | T8 proof covers marker rendering and click payloads; future proof should cover cell-native replacement before field retirement. |
| Startup/setup map continuity | `src/hooks/useGameInitialization.ts` generates/preserves `MapData`; `src/state/appState.ts` stores setup/load payloads. | New games and dummy/dev starts get consistent geography, seed, map state, factions, and initial location. | Startup adapter that creates the new geography contract alongside existing `MapData` until save migration is proven. | World owns contract; SaveLoad owns persistence edge; WorldSim owns generation source. | T9 proof shows new-game, dummy/dev, dummy preview, and migrated load payloads produce seeded geography snapshots without field removal. |
| Save/load/migration compatibility | `src/state/migrations/worldDataMigration.ts` backfills `MapData.worldData`; `src/utils/mapDataToWorldData.ts` resolves 3D-ready `WorldData`; `MapData.worldGeography` persists the bridge snapshot for future consumers. | Old saves still load; missing worldSim output is regenerated idempotently and not flattened. | Migration adapter that persists the new canonical geography contract while keeping legacy `MapData` readable until final retirement. | World owns migration contract; SaveLoad owns load/save integration. | T11 proof covers persisted `worldGeography` and preserved old fields; future save/load wiring can consume this field without recomputing it. |
| 3D marker and streaming anchors | `MapPane.tsx` maps world positions to grid cells; `resolveWorldDataFor3D` validates `WorldData`; World 3D reads world data for streaming; `resolve3DAnchor` converts persisted geography identity to world and ground coordinates. | Entering or exiting 3D returns to the correct world position and terrain chunk. | 3D anchor adapter that maps Azgaar/world cell IDs, legacy grid coordinates, and streamed-world meters without lossy conversion. | World owns coordinate contract; World 3D UI/System owns phase transition and chunk streaming consumers. | T12 proof covers persisted geography identity to `PlayerWorldPosition`/`PlayerGroundPosition`; final UI entry wiring remains a World 3D UI/MapPane owner seam before field retirement. |

## Task Slice Notes

- Completed T2 slice target: this pass updated the World project gap registry from observed source contracts and did not perform runtime implementation.
- Completed T4 slice target: adapter design now lives in `WORLD_GEOGRAPHY_ADAPTER_CONTRACT.md`.
- Completed T5 slice target: read-only adapter types and pure `fromMapData` helpers now live in `src/utils/world/worldGeographyAdapter.ts` with focused tests.
- Completed T6 slice target: pure discovery/current-marker mutation helpers now live in `src/utils/world/worldGeographyAdapter.ts` with focused tests.
- Completed T7 slice target: movement discovery/current-marker writes now route through the adapter helper with focused movement tests.
- Completed T8 slice target: MapPane discovery/current-marker reads now route through the adapter snapshot with focused component tests.
- Completed T9 slice target: startup/setup/load map payloads now have focused reducer/migration proof that they can produce seeded geography snapshots.
- Completed T10 slice target: passability can be represented through the geography adapter from canonical biome definitions or caller allow-lists while movement passability behavior stays unchanged.
- Completed T11 slice target: migration now persists optional `worldGeography` snapshots alongside legacy `MapData` and preserves already migrated saves.
- Completed T12 slice target: persisted geography points can resolve 3D entry anchors through `resolve3DAnchor` without changing MapPane behavior or retiring legacy `MapData` fields.
- Completed T13 slice target: world-state defaults are mandatory and load-normalized for rumors/history/notifications/merchant UI fields without broad cleanup.
- Completed T14 slice target: reducer payload handling for `SET_MAP_DATA` and `UPDATE_INSPECTED_TILE_DESCRIPTION` now uses typed action payloads and a typed `minimapFocus` state field without broad reducer cleanup.
- Completed T15 slice target: schedule/proximity coupling was source-audited and routed to Travel, Events, Memory, and Town Description owner gaps without World runtime code changes.
- Completed T16 slice target: daily-world log IDs are stable for repeated seeded runs, including logs returned from daily subsystems through the `WorldEventManager` boundary.
- Completed T17 slice target: worldSim load-time performance risk is routed to WorldSim Service WSS-001, which already owns measurement and background scheduling decisions.
- Completed T18 slice target: G11/GG-29 is routed to Travel, Submap, and the Global Gaps umbrella after World adapter-contract work closed under G3/G9/G10.
- No active World-owned task remains. Future work should start in the owner project named by the relevant routed gap unless a new World-specific gap is discovered.
- Evidence for next review pass: world event pipeline, migration path, and integration with `ADVANCE_TIME`, `START_NEW_GAME_SETUP`, `START_GAME_FOR_DUMMY`, and `LOAD_GAME_SUCCESS`.

## Next Checks

- Confirm `worldDataMigration` is still invoked from load/setup flows for legacy save compatibility.
- Confirm world event/event rumor handling has deterministic and typed boundaries for `weather` and `economy.activeEvents`.
- Confirm whether world geography outputs (`WorldData`) are consumed by 3D/world-render consumers or remain save-data only.

## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|

## Update Rules

- Update this tracker before starting a new slice.
- Update it when implementation changes the current state.
- Every active, waiting, or blocked row needs owner, last updated date, evidence or next proof, and next action.
- Record new gaps here or link the owning subsystem tracker.
- Keep raw process artifacts out unless a concise summary helps future work.
