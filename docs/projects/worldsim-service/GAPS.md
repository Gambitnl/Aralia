# Worldsim Service Gap Registry

Status: active (WSS-005 decision recorded 2026-06-10; implementation lane open)
Last updated: 2026-06-10 (WSS-005 decided — Azgaar is canonical; feature hints flow into WorldData)

Use this file for durable unresolved findings that are too important or too large to live only in the tracker and that genuinely belong to this project. Put cross-project, orphaned, or out-of-current-scope gaps in the global gap tracker instead.
## Durable Gaps

| Gap ID | Gap | Evidence | Why it is in-scope here | Next check |
|---|---|---|---|---|
| WSS-001 | Synchronous world simulation can block UI for large maps. | `src/services/worldSim/index.ts` | `runWorldSim(...)` is currently fully synchronous and called during map generation flow. | Measure generation and migration time on large seed sizes and decide if background scheduling is required. |
| WSS-002 | Migration contract for future `WorldData` schema versions is implicit. | `src/types/world.ts`, `src/state/migrations/worldDataMigration.ts`, `src/services/saveLoadService.ts` | Loader currently enforces idempotence only around `version === 2`; no published policy for staged v3+ upgrades. | Define migration policy and add explicit acceptance tests for next schema bump. |
| WSS-003 | Feature-to-render contract is not fully specified between generation and 3D visual systems. | `src/systems/world3d/chunkSampler.ts`, `src/systems/world3d/chunkWorkerCore.ts`, `src/services/worldSim/index.ts` | `chunkSampler` clips `rivers`/`roads`/`sites`, but mesh builders and renderer consumers are still partially placeholder-centric. | Confirm schema expectations for `Road`, `River`, and `Site` consumers and finalize a durable render contract. |
| WSS-004 | **[REMEDIATED 2026-06-02] Silent flat-world fallback.** When `MapData` reaches migration with neither `worldData` nor `azgaarWorld`, `migrateMapDataToWorldDataV2` backfills constant `heights = 30` (temp 15, moisture 20, templateId 'unknown'), so `runWorldSim` emits a featureless flat heightfield. **Verified live path** (not hypothetical): `generateMap` falls back to `generateLegacyMap` on any Azgaar-generation throw, and `generateLegacyMap` builds only biome `tiles` — it never sets `azgaarWorld`/`worldData`. Such a world enters 3D as a flat pancake colored by biome only. **Observability now implemented (2026-06-02):** `MapData.generation: WorldGenDiagnostics` records `source` + `reason`; `mapService` stamps `azgaar-derived` / `legacy-fallback` (with the caught error message), migration stamps `flat-backfill` (with reason) without clobbering an upstream reason; the dev DebugHUD shows the source and a highlighted reason for fallback/backfill worlds. **Still open:** the *policy* — whether to derive real heights from biome tiles or block 3D entry with a "world not fully generated" state instead of shipping a flat world. | `src/types/world.ts` (`WorldGenDiagnostics`); `src/services/mapService.ts:43-58`; `src/state/migrations/worldDataMigration.ts` (flat-backfill stamp); `src/components/World3D/DebugHUD.tsx` (display); tests: `worldDataMigration.test.ts`, `DebugHUD.worldGen.test.tsx` | Migration + generation entry paths are owned here; this is a generation-fidelity/observability defect, not a render bug. | **DONE (policy A — derive from biomes).** `heightFromBiomes` maps each cell's biome `elevation` band to height (aquatic<low<mid<high, on the 0-100 scale, SEA_LEVEL=20) with seeded per-cell jitter; deterministic per seed. Migration now calls it instead of `fill(30)`; provenance renamed from `flat-backfill` to `biome-derived` (still amber/flagged in DebugHUD as lower-fidelity). **Proof:** real `generateMap` legacy-fallback path now yields `distinct=38 min=25 max=88` (was a single constant 30). Files: `src/services/worldSim/heightFromBiomes.ts` (+test), `worldDataMigration.ts`, `src/types/world.ts`/`.d.ts`, `DebugHUD.tsx`. Tests: `heightFromBiomes.test.ts` (7), `worldDataMigration.test.ts` (8), `mapService.fallback.test.ts` (2), `DebugHUD.worldGen.test.tsx` (4). Residual fidelity gap is now only WSS-007; WSS-006 was remediated 2026-06-08 via biome-derived climate fields. |
| WSS-005 | **[DECIDED 2026-06-10 / OPEN FOR IMPLEMENTATION] 2D-3D feature divergence at the source.** The 2D Azgaar atlas and 3D world still consume different feature sources: `azgaarWorld.rivers` (boolean mask) is rendered by the atlas path while `WorldData.rivers`/`roads`/`sites` are regenerated by `runWorldSim` from terrain arrays. This is currently an explicit divergence by design, and it can break visual/source consistency for a shared seed. **Decision (2026-06-10, DECISION_BLITZ D1): Azgaar is canonical — option (A). Azgaar feature hints flow into `WorldData`; 3D and gameplay features follow the atlas; the proc-gen pipeline derives deterministically from Azgaar output. Decided in the June 2026 proc-gen campaign context (Azgaar-based submap replacement).** | `src/services/worldSim/__tests__/featureSourceTruth.test.ts` (mismatch proof on a fixed seed); `src/services/azgaarDerivedMapService.ts:502-509`; `src/services/worldSim/index.ts:63-65`; `docs/projects/DECISION_BLITZ_2026-06-10.md` D1 | The 3D stream contract is owned here; the canonical source is now decided, so this is an implementation lane. | Implement option (A): consume Azgaar feature hints into `WorldData` via the WSS-005a bridge spec, then add a fixed-seed acceptance check proving atlas and 3D consume the same canonical features. |
| WSS-005a | **Define 2D-3D feature source contract.** The current mismatch is partly structural: 2D stores `boolean` rivers while 3D uses traced polylines + derived sites/roads, so a direct equality contract does not yet exist. **Update (2026-06-10): with WSS-005 decided (Azgaar canonical), this bridge spec is the first implementation slice — the encoding must carry Azgaar feature hints into `WorldData` deterministically.** | `src/services/worldSim/__tests__/featureSourceTruth.test.ts`; `src/services/worldSim/types.ts`; `src/services/worldSim/__tests__/roads.test.ts`; `docs/projects/DECISION_BLITZ_2026-06-10.md` D1 | WSS-005 should not be closed without a shared feature encoding contract and a migration path from legacy saves. | Add a minimal bridge spec + test fixture for the canonical Azgaar-derived feature fields that both atlas and renderer consume, with a fixed-seed acceptance check. |
| WSS-006 | **[REMEDIATED 2026-06-08] Biome-fallback climate is no longer flat.** `temperatures` and `moisture` now derive from biome climate/moisture bands with seeded jitter in `climateFromBiomes`. | `src/state/migrations/worldDataMigration.ts` (`temperatures`/`moisture`), `src/services/worldSim/climateFromBiomes.ts` | Same migration-fidelity concern as WSS-004; owned by worldsim-service. | **DONE** — proof: `src/services/worldSim/__tests__/climateFromBiomes.test.ts` (4) and `src/state/migrations/__tests__/worldDataMigration.test.ts` (10) pass. |
| WSS-007 | **[REMEDIATED 2026-06-08] Biome-derived heights now use deterministic smoothing.** `heightFromBiomes` now applies a neighbor-relaxation pass after band jitter, removing cliff seams at biome borders while preserving seed determinism and broad elevation ordering. | `src/services/worldSim/heightFromBiomes.ts`; `src/state/migrations/__tests__/worldDataMigration.test.ts` | Fallback height derivation is the owning scope for biome->terrain mapping during legacy load. | **DONE** — smoothing regression added in `heightFromBiomes.test.ts`; migration tests confirm ordered bands and deterministic output. |
| WSS-008 | **[REMEDIATED 2026-06-08] First-build story output now rides the bootstrap/save lifecycle.** `WorldHistoryService.createFirstBuildHistory(...)` is attached in `useGameInitialization` for standard start and dummy start flows, `START_GAME_SUCCESS` / `START_GAME_FOR_DUMMY` carry it through `GameState`, and `saveLoadService.loadGame(...)` backfills an empty history for legacy saves that predate the payload. | `src/services/WorldHistoryService.ts`, `src/hooks/useGameInitialization.ts`, `src/state/actionTypes.ts`, `src/state/appState.ts`, `src/services/saveLoadService.ts` | First-build history is now an explicit part of game boot and persistence without changing the world geometry source-of-truth. | **DONE** - proof: `src/services/__tests__/WorldHistoryService.test.ts`, `src/services/__tests__/saveLoadService.test.ts`, `src/state/__tests__/appState.worldHistory.test.ts` (24 tests total across the focused run). |

## Open Questions

- Should `worldSim` expose a secondary fast path for legacy-only or low-end devices to avoid synchronous regeneration at load?
- Should `worldDataMigration` include checksum or generation metadata (`seed`,`templateId`,`inputHash`) to support trust-safe rehydration?
- What is the acceptable fallback behavior when a load-time regenerated world snapshot diverges from a display cache that assumes older feature density?

## Notes

- Keep ownership boundaries explicit: gameplay simulation questions belong in `docs/projects/world`, not here.

## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|
| G1 | not_started | adjacent_follow_up | future agent | docs/projects/PROJECT_CARD_SCHEMA.md | schema normalization | Replace this seeded gap row with project-specific findings if any remain after the next bounded gap sweep | docs/agent-workflows/living-project-task-protocol/templates/GAPS.md | The workflow requires durable gaps to have a consistent table shape and evidence path | Perform a bounded gap sweep and either update this row or close it as no longer applicable | Updated GAPS.md and TRACKER.md agree on the project gap state |

## Classification Reference

| Classification | Use when |
|---|---|
| `in_scope_now` | The task cannot honestly complete without it. |
| `support_needed_now` | It is not the product task, but the task cannot move without it. |
| `adjacent_follow_up` | Useful and related, but not required for this slice. |
| `out_of_scope` | It should not be part of this project/task. |
| `blocked_human_decision` | A real owner/operator choice is needed. |
| `blocked_external_state` | Waiting on PR, CI, vendor, service, environment, or another person. |

## Update Rules

- Keep each gap tied to evidence and a next proof/check.
- Link back to a global gap ID when this project imports one.
- If the current project should not own a gap, add or update the global gap tracker instead of keeping the gap here.
- Do not mark a gap done unless completion evidence is linked or summarized.
- Add dated testimony or status notes to an existing gap instead of opening duplicates.
