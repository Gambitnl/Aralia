# Creatures System Gap Registry

Status: active (G4 decision recorded 2026-06-10; docs-only closure)
Last updated: 2026-06-10

Reviewed during the 2026-06-09 review-gate pass. G2/CT-3 is resolved; CT-2 taxonomy integration is complete; G4 is now review-required; G5 corpus ownership proof is documented.
Update (2026-06-10): G4 decided — keep binary include/exclude targeting, defer hybrid semantics (DECISION_BLITZ D11); G4 is closed as a deliberate defer with docs-only closure.

Use this file for durable unresolved findings that are too important or too large to live only in the tracker and that genuinely belong to this project. Put cross-project, orphaned, or out-of-current-scope gaps in the global gap tracker instead.
## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|
| G1 | done | in_scope_now | Worker A | `docs/projects/creatures/TRACKER.md` | docs refresh pass | Creature taxonomy logic was fragmented across spell and manual combat validators. | `src/systems/creatures/CreatureTaxonomy.ts`, `src/systems/spells/targeting/TargetValidationUtils.ts`, `src/hooks/combat/useTargetValidator.ts`, and their focused tests. | Inconsistent behavior and migration risk between AI-targeting and manual combat targeting paths. | N/A. `CreatureTaxonomy` now runs in both paths. | `TargetValidationUtils.test.ts` and `useTargetValidator.test.ts` verify include/exclude behavior and legacy alias compatibility. |
| G2 | done | support_needed_now | Codex | `docs/projects/creatures/TRACKER.md` | docs refresh pass + schema review | `TargetConditionFilter` model supports overlapping legacy/new fields; integration points consume different aliases. | `src/types/spells.ts` documents both `creatureType` and `creatureTypes`, `sizes`/`size`, `alignment`/`alignments`; `src/systems/spells/validation/targetingSchemas.ts` writes plural arrays; `src/types/combat.ts` uses plural `creatureTypes` with singular `alignment`; `CreatureTaxonomy.ts` already treats `creatureTypes` as the V2 standard and `creatureType` as legacy. | Without a schema policy, future edits could bypass filter compatibility or widen logic unintentionally. | N/A. Policy resolved: new writes use plural `creatureTypes`, `sizes`, `alignments`, and `excludeCreatureTypes`; singular aliases are read-only compatibility inputs until corpus migration happens elsewhere. | Docs consistency and `git diff --check`; CT-2 can now proceed under the plural-write, legacy-read policy. |
| G3 | done | in_scope_now | Worker A | `docs/projects/creatures/TRACKER.md` | Encounter policy alignment pass (2026-06-08) | Encounter generator and AI fallback used different monster-count caps. | `src/utils/world/encounterUtils.ts` now exports `MAX_ENCOUNTER_MONSTER_COUNT` (4), and both `src/services/geminiServiceFallback.ts` and `src/services/gemini/encounters.ts` consume it. | Different encounter requests could have produced different cardinality by path, creating balance/pacing drift. | N/A (implemented). | `src/services/__tests__/geminiServiceFallback.test.ts` asserts fallback encounter total and canonical prompt cap in generateEncounter. |
| G4 | done | out_of_scope | human/product owner | `docs/projects/creatures/TRACKER.md` | `src/systems/creatures/Creatures_Ralph.md` review | Hybrid/multi-type semantics are planned but not implemented, and the runtime validator still uses a binary include/exclude contract. Decided 2026-06-10 (DECISION_BLITZ D11): keep the binary include/exclude contract; defer hybrid semantics to a later product/schema decision (docs-only closure; the campaign's entity pipeline may inform the eventual model). | `CreatureTaxonomy.ts` uses binary inclusion/exclusion; `Creatures_Ralph.md` calls out dominance/partial-effect model as a gap; `docs/projects/DECISION_BLITZ_2026-06-10.md` D11. | Existing logic misses more complex creature identity behavior and can misclassify edge-cases, but the owner explicitly chose to keep the binary contract this cycle. | None this cycle; any future hybrid work starts with a new product/schema decision rather than reopening this gap. | Explicit defer note recorded in `NORTH_STAR.md` Required Review Brief Decision (2026-06-10) and `DECISIONS.md`. |
| G5 | done | adjacent_follow_up | Codex | `docs/projects/creatures/AUDIT_OR_PROOF.md` | Creatures docs pass (source-backed corpus boundary review) | `src/data/monsters.generated.ts` is generated by `scripts/ingestMonsters.ts`, and `src/data/monsters.ts` only re-exports the corpus. | `scripts/ingestMonsters.ts`, `src/data/monsters.generated.ts`, `src/data/monsters.ts`, `docs/projects/creatures/AUDIT_OR_PROOF.md` | Manual splitting or deleting generated creature data would fight the pipeline and risk hiding the real ownership boundary. | Keep any future sharding discussion in the monster ingestion pipeline; do not manually partition the generated corpus. | `AUDIT_OR_PROOF.md` records the generator-owned boundary and keeps future expansion intact without touching the corpus. |

## Classification Reference

- `in_scope_now` - required for the current task slice.
- `support_needed_now` - not in current slice but must be completed before the slice can progress safely.
- `adjacent_follow_up` - useful future work that should be preserved but not promoted into this slice.
- `out_of_scope` - explicitly rejected for this project/task.
- `blocked_human_decision` - needs explicit owner decision.
- `blocked_external_state` - external dependency/coordination needed.

## Import / Routing Rule

- If a future task identifies cross-project ownership, move it to `docs/projects/GLOBAL_GAPS.md` with routing rationale.
- Gaps already in this registry stay here until ownership is intentionally reassigned.

## Update Rules

- Keep each gap tied to evidence and a next proof/check.
- Link back to a global gap ID when this project imports one.
- If the current project should not own a gap, add or update the global gap tracker instead of keeping the gap here.
- Do not mark a gap done unless completion evidence is linked or summarized.
- Add dated testimony or status notes to an existing gap instead of opening duplicates.
