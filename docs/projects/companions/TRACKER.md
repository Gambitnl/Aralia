# Companions System Living Tracker

Status: review-required
Last updated: 2026-06-08

## Status Vocabulary

- `not_started`
- `active`
- `waiting`
- `blocked`
- `done`
- `superseded`
- `out_of_scope`

## Active Task Queue

| ID | Status | Task | Owner | Last updated | Evidence | Next action | Next check/proof |
|---|---|---|---|---|---|---|---|
| T1 | done | Establish and enrich project docs with evidence-backed scope and gaps. | Worker A | 2026-05-31 | `docs/projects/companions/NORTH_STAR.md` | Keep docs in sync if companions code paths change. | Docs-only pass complete; no behavior change required. |
| T2 | blocked | Clarify/route in-project gaps for implementation handoff and risk control. | Worker A | 2026-06-08 | `docs/projects/companions/GAPS.md`, `src/systems/companions/Companions_Ralph.md` | G5 is resolved as a documentation contract, G8 is now a documented companion-owned banter split plan, and G9 has a focused regression; do not assign forward companion work until G6 review clears or a fresh source-backed scan finds a non-G6 gap. | Human/story owner must choose automatic, event-driven, or hysteresis romance downgrade semantics; then update G6 and add the focused relationship regression. |

## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|
| G1 | done | support_needed_now | Worker A | `docs/projects/companions/GAPS.md` | Documentation sweep | Enforce `CompanionReactionRule.requirements` in reaction evaluation. | `src/systems/companions/CompanionReactionSystem.ts`, `src/systems/companions/__tests__/CompanionReactionSystem.test.ts` | Existing rule metadata is now applied so reactions cannot bypass relationship bounds. | Keep the regression cases for min/max relationship boundaries and extend them with new rule content. | Dispatch with representative reaction rule sets and assert filtered result. |
| G2 | done | support_needed_now | Worker A | `docs/projects/companions/GAPS.md` | Documentation sweep | Reconcile relationship scale semantics (`-500..500`) across types/UI/docs. | `src/systems/companions/RelationshipManager.ts`, `src/types/companions.ts`, `src/components/ui/CompanionCard.tsx`, `src/systems/companions/__tests__/RelationshipManager.test.ts`, `src/components/ui/CompanionCard.test.tsx` | The approval clamp and the approval bar now read the same runtime contract, so the displayed scale no longer compresses the ends. | Keep the contract comments and UI math aligned with the runtime clamp. | Focused tests now cover the approval boundaries and the normalized approval marker. |
| G3 | done | support_needed_now | Worker A | `docs/projects/companions/GAPS.md` | Documentation sweep | Resolve loyalty placeholder and stale TODO import/type contracts. | `src/systems/companions/RelationshipManager.ts`, `src/systems/companions/__tests__/RelationshipManager.test.ts` | The loyalty helper now has an explicit retention-floor contract, so the companion slice no longer relies on a silent placeholder. | Keep departure/betrayal policy as a separate, later decision unless gameplay needs it now. | Regression coverage proves the floor at 10 and the safe side above it. |
| G4 | done | adjacent_follow_up | Worker A | `docs/projects/companions/GAPS.md` | Documentation sweep | Add queue/ratelimit strategy for repeated companion bubble reactions. | `src/components/ui/CompanionReaction.tsx`, `src/components/ui/CompanionReaction.test.tsx` | Concurrent reactions now queue instead of overwriting, and duplicate bursts from the same companion collapse instead of extending the display. | Keep the queue and duplicate window in place when adding new reaction sources. | Focused regression proves back-to-back reactions queue in order and immediate duplicates do not reset the timer. |
| G5 | done | support_needed_now | Worker A | `docs/projects/companions/GAPS.md` | Documentation sweep | Companion approval `source` stays as provenance-only in `UPDATE_COMPANION_APPROVAL`, and `useCompanionBanter` already has an explicit player-directed route: `generatePlayerDirectedLine` -> response window -> `playerInterrupt`/`handlePlayerIgnored`/`extendPlayerResponseDeadline`. | `src/state/reducers/companionReducer.ts`, `src/hooks/useCompanionBanter.ts`, `src/state/appState.ts` | The side-effect route is now explicit in docs and comments, so future companion work can extend it without re-deriving the branch shape. | Keep the provenance field and player-response window documented as separate contracts. | Contract notes now point at the reducer, banter hook, and migration path that carry the current behavior. |
| G6 | not_started | blocked_human_decision | Worker A | `docs/projects/companions/GAPS.md` | `src/systems/companions/Companions_Ralph.md` review | Romance state lock-in can keep a companion flagged as `romance` even after approval collapses to hostile territory. | `src/systems/companions/Companions_Ralph.md`, `src/systems/companions/RelationshipManager.ts` | Story logic needs an explicit breakup/downgrade contract before more relationship content can be safely added. | Choose automatic, event-driven, or hysteresis breakup semantics and encode them in `RelationshipManager`. | Run a regression that drops approval from romance to hostile and verifies the chosen exit policy. |
| G7 | done | adjacent_follow_up | Worker A | `docs/projects/companions/GAPS.md` | `src/systems/companions/Companions_Ralph.md` review | `RelationshipManager` now routes all generated approval/history IDs through the shared `generateId()` helper instead of calling `crypto.randomUUID()` directly. | `src/systems/companions/Companions_Ralph.md`, `src/systems/companions/RelationshipManager.ts`, `src/systems/companions/__tests__/RelationshipManager.test.ts`, `src/utils/core/idGenerator.ts` | Runtime compatibility stays centralized in the shared helper, and the regression check covers the fallback path when `crypto.randomUUID` is unavailable. | Keep the helper as the single ID-generation entrypoint. | Focused `RelationshipManager` regression now proves event IDs still generate when `crypto.randomUUID` is absent. |
| G8 | done | adjacent_follow_up | Codex | `docs/projects/code-modularization-audit` CMA-G12 | Code modularization audit routing | `useCompanionBanter.ts` remains the companion-owned orchestration shell, and its later split is now defined around trigger/session gating, line generation and pacing, player-response handling, and archive/summarize side effects. | `src/hooks/useCompanionBanter.ts`; `src/hooks/__tests__/useCompanionBanter.test.ts`; `docs/projects/dialogue/GAPS.md`; `docs/projects/conversation-panel/GAPS.md` | The shell can be extracted safely only if the companion-owned responsibilities stay intact and the dialogue/conversation-panel boundaries stay separate. | Keep the hook as the integration shell; later code passes may extract `banterSessionPolicy`, `banterLineFlow`, `banterResponseWindow`, and `banterArtifacts` helpers without pulling in romance policy or conversation-panel ownership. | Later code pass should keep `src/hooks/__tests__/useCompanionBanter.test.ts` as the integration regression and add focused helper tests for session gating, mode selection, player-response timing, and archive/summarize side effects. |
| G9 | done | adjacent_follow_up | Worker A | `docs/projects/companions/GAPS.md` | `src/hooks/useCompanionBanter.ts` review | Focused regression now proves the player-directed response-window contract (`generatePlayerDirectedLine` -> wait timer -> interrupt reset / timeout cancellation). | `src/hooks/useCompanionBanter.ts`, `src/hooks/__tests__/useCompanionBanter.test.ts` | The directed banter timer now has a narrow proof path, so UI or state-shape changes are less likely to silently break the opening window. | Keep the regression in place when changing the banter hook or response-window state. | Regression covers directed line opening, waiting/deadline state, interrupt reset, and canceled escalation timeout. |
