# Companions System Gap Registry

Status: review-required
Last updated: 2026-06-08

## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|
| G1 | done | support_needed_now | Worker A | `docs/projects/companions/TRACKER.md` | Documentation sweep | `CompanionReactionSystem` now enforces `minRelationship`/`maxRelationship` checks before reaction approval/dialogue aggregation. | `src/systems/companions/CompanionReactionSystem.ts`, `src/systems/companions/__tests__/CompanionReactionSystem.test.ts` | Relationship-bounded reactions now stay inside the intended approval window. | Keep the boundary tests in place when adding new reaction rule content. | Regression coverage exists for below-min, above-max, and in-range behavior. |
| G2 | done | support_needed_now | Worker A | `docs/projects/companions/TRACKER.md` | Documentation sweep | Relationship scale comments and UI normalization now match the runtime `-500..500` approval thresholds. | `src/types/companions.ts`, `src/systems/companions/RelationshipManager.ts`, `src/components/ui/CompanionCard.tsx`, `src/systems/companions/__tests__/RelationshipManager.test.ts`, `src/components/ui/CompanionCard.test.tsx` | Approval comments, clamp behavior, relationship levels, and the visible approval marker now share one scale. | Keep future UI math and type comments aligned with the runtime clamp. | Focused tests cover approval clamp endpoints and normalized approval marker positions. |
| G3 | done | support_needed_now | Worker A | `docs/projects/companions/TRACKER.md` | Documentation sweep | `RelationshipManager.checkLoyalty` now uses an explicit retention-floor contract (`loyalty > 10`) and focused tests; companion leave/betrayal behavior remains a separate future slice. | `src/systems/companions/RelationshipManager.ts`, `src/systems/companions/__tests__/RelationshipManager.test.ts` | The loyalty helper now answers a concrete safety question instead of acting like an undefined placeholder. | Keep leave/betrayal policy separate unless a future story decision asks for it. | Regression coverage proves the floor at 10 and the safe side above it. |
| G4 | done | adjacent_follow_up | Worker A | `docs/projects/companions/TRACKER.md` | Documentation sweep | Companion reaction bubble rendering now keeps a FIFO queue with a short duplicate window, so repeated bubbles do not overwrite the active one. | `src/components/ui/CompanionReaction.tsx`, `src/components/ui/CompanionReaction.test.tsx` | Dense message bursts stay readable, and same-companion duplicate lines do not restart the visible bubble lifetime. | Keep the queue and duplicate window in place when adding new companion reaction sources. | Focused regression now proves back-to-back reactions queue in order and immediate duplicates collapse instead of resetting the timer. |
| G5 | done | support_needed_now | Worker A | `docs/projects/companions/TRACKER.md` | Documentation sweep | Companion approval `source` stays as provenance-only in `UPDATE_COMPANION_APPROVAL`, and `useCompanionBanter` already has an explicit player-directed route: `generatePlayerDirectedLine` -> response window -> `playerInterrupt`/`handlePlayerIgnored`/`extendPlayerResponseDeadline`. | `src/state/reducers/companionReducer.ts`, `src/hooks/useCompanionBanter.ts`, `src/state/appState.ts` | The side-effect route is now explicit in docs and comments, so future companion work can extend it without re-deriving the branch shape. | Keep the provenance field and player-response window documented as separate contracts. | Contract notes now point at the reducer, banter hook, and migration path that carry the current behavior. |
| G6 | not_started | blocked_human_decision | Worker A | `docs/projects/companions/TRACKER.md` | `src/systems/companions/Companions_Ralph.md` review | Romance state lock-in can keep a companion flagged as `romance` even after approval collapses to hostile territory. | `src/systems/companions/Companions_Ralph.md`, `src/systems/companions/RelationshipManager.ts` | The system can present a companion as romantically committed while the approval state says the opposite; story logic needs an explicit breakup/downgrade contract. | Choose automatic, event-driven, or hysteresis breakup semantics and encode them in `RelationshipManager`. | Run a regression that drops approval from romance to hostile and verifies the chosen exit policy. |
| G7 | done | adjacent_follow_up | Worker A | `docs/projects/companions/TRACKER.md` | `src/systems/companions/Companions_Ralph.md` review | `RelationshipManager` now routes all generated approval/history IDs through the shared `generateId()` helper instead of calling `crypto.randomUUID()` directly. | `src/systems/companions/Companions_Ralph.md`, `src/systems/companions/RelationshipManager.ts`, `src/systems/companions/__tests__/RelationshipManager.test.ts`, `src/utils/core/idGenerator.ts` | Older runtimes or embedding contexts still rely on the centralized UUID fallback, but the companion slice now uses the shared helper everywhere it generates IDs. | Keep the helper as the single ID-generation entrypoint. | Focused `RelationshipManager` regression now proves event IDs still generate when `crypto.randomUUID` is absent. |
| G8 | done | adjacent_follow_up | Codex | `docs/projects/code-modularization-audit` CMA-G12 | Code modularization audit routing | `useCompanionBanter.ts` remains the companion-owned orchestration shell, and its later split is now defined around four preserved responsibilities: trigger/session gating, line generation and pacing, player-response handling, and archive/summarize side effects. | `src/hooks/useCompanionBanter.ts`; `src/hooks/__tests__/useCompanionBanter.test.ts`; `docs/projects/dialogue/GAPS.md`; `docs/projects/conversation-panel/GAPS.md` | The shell can be extracted safely only if the companion-owned responsibilities stay intact and the dialogue/conversation-panel boundaries stay separate. | Keep the hook as the integration shell; later code passes may extract `banterSessionPolicy`, `banterLineFlow`, `banterResponseWindow`, and `banterArtifacts` helpers without pulling in romance policy or conversation-panel ownership. | Later code pass should keep `src/hooks/__tests__/useCompanionBanter.test.ts` as the integration regression and add focused helper tests for session gating, mode selection, player-response timing, and archive/summarize side effects. |
| G9 | done | adjacent_follow_up | Worker A | `docs/projects/companions/TRACKER.md` | `src/hooks/useCompanionBanter.ts` review | Focused regression now proves the player-directed response-window contract (`generatePlayerDirectedLine` -> wait timer -> interrupt reset / timeout cancellation). | `src/hooks/useCompanionBanter.ts`, `src/hooks/__tests__/useCompanionBanter.test.ts` | The directed banter timer now has a narrow proof path, so UI or state-shape changes are less likely to silently break the opening window. | Keep the regression in place when changing the banter hook or response-window state. | Regression covers directed line opening, waiting/deadline state, interrupt reset, and canceled escalation timeout. |

## Classification Reference

| Classification | Use when |
|---|---|
| `in_scope_now` | The current project cannot complete without this gap fixed. |
| `support_needed_now` | Task cannot progress cleanly without resolving this gap. |
| `adjacent_follow_up` | Useful and related, but can be deferred until core needs are complete. |
| `out_of_scope` | Confirmed not part of this project's current contract. |
| `blocked_human_decision` | Needs product or design signoff before engineering can continue. |
| `blocked_external_state` | Depends on external vendor/tooling or unrelated owner. |
