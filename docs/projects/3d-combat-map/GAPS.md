# 3D Combat Map Gap Registry

Status: active
Last updated: 2026-06-08

## Gap Log

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|---|
| G1 | closed | in_scope_now | future_worker | `docs/projects/3d-combat-map/TRACKER.md` | Boundary audit | No explicit MVP quality gate is documented at project level for combat-only 3D delivery. Closed 2026-06-05 after `NORTH_STAR.md` gained the Dashboard Card Schema and MVP acceptance criteria; sync confirmed here 2026-06-07 (this registry was still showing `open` while TRACKER had it closed). | `docs/projects/3d-combat-map/NORTH_STAR.md`, `docs/superpowers/specs/2026-05-21-3d-combat-map-design.md`, `src/components/BattleMap/BattleMap3D.tsx`, `src/components/Combat/CombatView.tsx` | Teams can over-scope into non-combat or non-critical 3D work. | Keep acceptance criteria synchronized with future tracker edits. | Next doc sweep confirms criteria are still visible and current. |
| G2 | closed | support_needed_now | Codex | `docs/projects/3d-combat-map/TRACKER.md` | Runtime scan + NC1 browser proof | 2026-06-08 NC1 browser proof confirmed the Bloom+Vignette+ContactShadows stack does not repeat the old SSAO/NormalPass/WebGL blit errors. | `src/components/BattleMap/BattleMap3D.tsx:228-251` (removal rationale), `:437` (ContactShadows), browser Battle Map Demo 3D view at 766x1068 canvas. | Stale gap wording could have misled the next agent into re-fixing already-removed code. | N/A. Keep SSAO excluded unless a future renderer profile intentionally reintroduces it with fresh proof. | Browser 3D smoke: no repeated `GL_INVALID_OPERATION`, `glBlitFramebuffer`, `SSAO`, or `NormalPass` errors; one shader warning routed to G6. |
| G3 | open | adjacent_follow_up | future_worker | `docs/projects/3d-combat-map/TRACKER.md` | Runtime scan | `CombatView` pop-out lifecycle state and render-mode state can diverge. `renderMode` is a single `useState` (`CombatView.tsx:141`) shared by both toggles, but the pop-out container remount path (`:477-503`) is unverified. | `src/components/Combat/CombatView.tsx:141,477-503` | Visual mode can become inconsistent after pop-out interactions. | Add sync rule set for pop-out entry/exit and mode restore. | NC2 integration check in one encounter session (see `AUDIT_OR_PROOF.md`). |
| G4 | open | in_scope_now | future_worker | `docs/projects/3d-combat-map/TRACKER.md` | Runtime scan | Terrain tile raycast mapping now routes through a clamped helper so edge drift cannot drop a hit outside the playable grid, but the steep-slope selection path still needs live browser confirmation. | `src/components/BattleMap/BattleMap.tsx`, `src/components/BattleMap/terrain/TerrainMesh.tsx`, `src/components/BattleMap/terrain/terrainTileMapping.ts`, `src/components/BattleMap/terrain/__tests__/terrainTileMapping.test.ts` | Click targets can still mismatch the visual slope if the browser path diverges from the structural mapping proof. | Run a browser slope-tile click pass if feasible; otherwise keep the structural clamp proof noted and flag the remaining visual check. | One slope test case in an integration or E2E check. |
| G5 | open | adjacent_follow_up | future_worker | `docs/projects/3d-combat-map/TRACKER.md` | Relationship scan | No joint visual standard document exists across `BattleMap3D`, `World3D`, and `ThreeDModal`. | `docs/projects/world3d/NORTH_STAR.md`, `docs/projects/three-d-modal/NORTH_STAR.md`, `src/components/BattleMap/BattleMap3D.tsx` | Future style drift can confuse users when shifting between combat and exploration 3D surfaces. | Add a short policy note if shared standards are adopted; otherwise keep explicit boundaries. | Add link/decision in next project update only. |
| G6 | open | adjacent_follow_up | Codex | `docs/projects/3d-combat-map/TRACKER.md` | NC1 browser proof (2026-06-08) | The 3D map render emitted a single shader compiler warning: `THREE.WebGLProgram: Program Info Log: (643,1): warning X4000: use of potentially uninitialized variable (f_getTerrainColor)`. | Browser console during Battle Map Demo 3D view; likely terrain shader path in `src/components/BattleMap/terrain/TerrainMesh.tsx` or related terrain material code. | The warning did not blank the canvas or repeat as the old SSAO issue did, but shader warnings should not become invisible baseline noise. | Investigate terrain shader initialization in a future bounded pass; do not block NC1/post-processing closure on this. | Focused terrain shader smoke or unit-level material proof shows no compiler warning after the fix. |
| G7 | open | support_needed_now | future_worker | `docs/projects/3d-combat-map/GAPS.md` | NC2 headless attempt (2026-06-08, Claude/Opus) | NC2 (and the CombatView-hosted variant of NC1) cannot be verified because the offline autosave fixture loads the **World3D exploration surface** (no ActionPane System Menu), so the headless path to a combat encounter (Dev Menu → Generate Encounter → Simulate Battle → `CombatView`) is unreachable. Blocks the G3 proof. | `.agent/3d-visual-quality/captures/nc2-combatview.mjs` (ready harness), `storageState.json` (loads World3D phase), `AUDIT_OR_PROOF.md` F-2026-06-08-03 | Without a reachable combat fixture, G3 (pop-out lifecycle) and any CombatView-hosted 3D check stay unproven — the residual risk task-24 flagged. | Capture a 2D-exploration save fixture, add a dev hook to jump to a battle-map encounter, or script the World3D→combat trigger. | `nc2-combatview.mjs` reaches `CombatView`, records canvas-vs-ErrorBoundary-fallback + pop-out renderMode persistence. |
| G8 | closed | adjacent_follow_up | gpt-5.3-codex-spark / MCP-subagent | `docs/projects/3d-combat-map/GAPS.md` | Closeout doc-account (2026-06-08) | `NORTH_STAR.md` frontmatter declares `DECISIONS.md` and `RUNBOOK.md` under `required_docs`, and both files now exist in the project folder. | `docs/projects/3d-combat-map/NORTH_STAR.md` frontmatter `required_docs`; `docs/projects/3d-combat-map/DECISIONS.md`; `docs/projects/3d-combat-map/RUNBOOK.md` | The schema audit no longer reports this folder as missing declared docs. | N/A. | `npm run projects:audit` reports no `missingDeclaredDocs` for this folder. |

## Classification Reference

| Classification | Use when |
|---|---|
| `in_scope_now` | Directly blocks or reduces confidence in this project's core goal. |
| `support_needed_now` | Not core implementation, but it blocks confidence in the main MVP path. |
| `adjacent_follow_up` | Useful, related, but not required for current 3D-combat MVP completion. |
