# World3d Gap Registry

Status: active — W3D-G10 decision recorded 2026-06-10; implementation lane open
Last updated: 2026-06-10 (D4 decision recorded)

North Star: `docs/projects/world3d/NORTH_STAR.md`

Use this file for durable unresolved findings that are too important or too large to live only in the tracker and that genuinely belong to this project. Put cross-project, orphaned, or out-of-current-scope gaps in the global gap tracker instead.
## Scope
In-project execution gaps rooted in current implementation evidence. Resolved gaps are
retained with `done`/`superseded` status as history; do not silently delete.

## Gap Log

| Gap ID | Status | Classification | Owner | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|
| W3D-G1 | not_started | adjacent_follow_up | unassigned | 2026-05-31 scan | `World3DDemo` uses inline `handleChunkRequest`, not the worker-backed loader | `src/components/World3D/World3DDemo.tsx`, `createWorkerChunkLoader.ts` | Worker path is unverified in the real scene entry | Decide inline vs worker for the demo; if worker, wire `createWorkerChunkLoader` | Trace loaded-chunk call stack under `WORLD3D_DEMO` |
| W3D-G2 | done | in_scope_now | — | 2026-05-31 scan | `polylineClip.ts` not integrated with any builder | `polylineClip.ts` now used by `waterGeometry`/`roadGeometry` via the sampler | Was blocking river/road detail | — | Resolved by Plan 3 (sampler clips rivers/roads; builders consume them) |
| W3D-G3 | routed | blocked_human_decision | world-3d-ui | 2026-05-31 scan | `WORLD3D_DEMO` is a sandbox, not connected to gameplay/saved world-actor state | `src/App.tsx`, `useHistorySync.ts` | Without routing from real play, the 3D world ships as test-only | **Routed to `world-3d-ui`** (entry/transition owner); tracked as its Plan 4 | see `docs/projects/world-3d-ui/GAPS.md` |
| W3D-G4 | done | in_scope_now | — | 2026-05-31 scan | Loader on placeholder `ChunkGeometryArrays` while `types.ts` had richer `ChunkMeshBundle` | `chunkWorkerCore`, `useChunkStreaming`, `createWorkerChunkLoader`, `chunkWorker` all now carry `ChunkMeshBundle` | Was API-drift risk | — | Resolved by Plan 3 Task 9 (full pipeline rewire to bundle) |
| W3D-G5 | done | adjacent_follow_up | — | 2026-05-31 scan | Rich content builders (roads/sites/vegetation/water) were type-only intent | All five builders implemented + rendered | Was scope-estimation risk | — | Resolved by Plan 3 + render in `World3DScene` |
| W3D-G6 | done | adjacent_follow_up | claude | 2026-05-31 scan | No scene-lifecycle/visual assertions on `World3DScene` mount/camera beyond unit streamer tests | `World3DScene.tsx`, `__tests__/useChunkStreaming.test.tsx`, `__tests__/World3DScene.lifecycle.test.tsx` | Render-path regressions can escape unit tests | Added a focused RTL scene proof that keeps the shell visible, verifies camera/origin wiring and the first mount update, and renders loaded chunk content; the existing StrictMode streamer proof still guards the hook lifecycle. | Lifecycle test green |
| W3D-G7 | routed | blocked_human_decision | world-3d-ui | 2026-06-01 live debug | `?phase=world3d` cold-load intermittently bounces to `main_menu` | live nav this session; `optimizeDeps` change did not fix; server logs show no Vite re-optimize → app-level race | Deep link is unreliable for testing/demo | **Routed to `world-3d-ui`** (entry/transition owner) | see `docs/projects/world-3d-ui/GAPS.md` (W3DUI-5) |
| W3D-G8 | done | adjacent_follow_up | claude | 2026-06-01 live debug | Demo world was all-`plains`, so rivers/roads/towns (built + tested) weren't visible | `World3DDemo.tsx` now calls `generateMap(...).worldData` (real pipeline) and spawns on the first town | Demo undersells implemented content | — | T4: real world flows through; live screenshot shows ocean + landmass (biome variety); 81 chunks load; tsc clean; 70 tests pass. Full river/road/town visibility gated by W3D-G11 (flat relief) |
| W3D-G9 | done | adjacent_follow_up | Codex worker | 2026-06-08 T6 implement | `WorldData.lakes` polygons were not meshed; only river ribbons produced water geometry. | `src/systems/world3d/chunkSampler.ts`, `src/systems/world3d/waterGeometry.ts`, `src/systems/world3d/__tests__/chunkSampler.test.ts`, `src/systems/world3d/__tests__/waterGeometry.test.ts` | Lake polygons are now carried into chunk data and triangulated as filled water surfaces before river ribbons. | **DONE:** sample clipped lake polygons into chunk payloads and triangulate them in `buildWaterMesh`. | Focused tests prove lake payload sampling and water mesh lake-fill triangles. |
| W3D-G10 | not_started | in_scope_now | unassigned | 2026-06-08 T7 worker review | Per-LOD geometry detail cannot be safely implemented as a sampler-only change because the loader API requests chunks by `cx/cy` before `ChunkStreamer` records each chunk's LOD tier. Mixed-resolution meshes also need an explicit seam/skirt strategy. | `src/components/World3D/createWorkerChunkLoader.ts`, `src/systems/world3d/chunkStreamer.ts`, `src/systems/world3d/chunkGeometry.ts`, `src/systems/world3d/lod.ts` | A local resolution change would either ignore the computed LOD or introduce visible cracks between adjacent chunks with different mesh densities. | **Decided 2026-06-10 (Remy, DECISION_BLITZ D4):** extend the chunk-loader request contract to carry the requested LOD tier; use skirt geometry to hide mixed-resolution seams. T7 lane reopens — implement the extended loader contract + skirts. See `docs/projects/DECISION_BLITZ_2026-06-10.md` and `docs/projects/world3d/DECISIONS.md` D2. | Mixed near/mid/low chunk regression tests land with the loader/skirt implementation. |
| W3D-G11 | done | adjacent_follow_up | claude | 2026-06-01 T4 live verify | Terrain vertical relief was imperceptible: `MAX_TERRAIN_HEIGHT_M=150` over `METERS_PER_CELL=1024` gives ~0.15 height:width, so hills read as a flat plane and the thin water/road ribbons are nearly invisible from a near-horizontal camera | `config.ts` `VERTICAL_EXAGGERATION: 12` + shared pure `heightToMeters()` helper; all four Y-mappers (`chunkGeometry`, `waterGeometry`, `roadGeometry`, `vegetationScatter`) route through it in lockstep | This is the main blocker to *seeing* the rivers/roads/towns the engine already builds | — | **T8 done (2026-06-02):** exaggeration applied via single `heightToMeters` source; camera + relief-aware demo spawn lift to the ground; live screenshot shows a clear peaked ridge against the sky (was a near-flat plane). 76 world3d tests pass; tsc clean for world3d. Remaining flatness limited by sub-cell view window → W3D-G16 |
| W3D-G12 | not_started | adjacent_follow_up | unassigned | 2026-06-01 T4 live verify | Hard biome-color seams: terrain color is per-vertex nearest-neighbor biome (`sampleBiomeNearest`) with no cross-boundary blending, so biome edges are abrupt color steps | `chunkSampler.ts` `sampleBiomeNearest`, `chunkGeometry.ts` per-vertex `biomeColor` | Biome transitions look banded/unnatural at world scale | Blend biome colors across a feather band (e.g., sample neighbor biomes and lerp by distance to boundary) | Adjacent biomes show a gradient, not a hard line |
| W3D-G13 | not_started | adjacent_follow_up | unassigned | 2026-06-01 unrelated scan | The `culled` LOD tier is unreachable dead logic: `lod.ts` only returns `culled` for Chebyshev distance > `LOD_RINGS.low` (6), but `ChunkStreamer` unloads any chunk beyond `UNLOAD_RADIUS` (6) — so a chunk is removed before it can ever be tagged `culled`. Any future `culled`-tier handling will never run. | `src/systems/world3d/lod.ts` (`LOD_RINGS.low=6`), `src/systems/world3d/chunkStreamer.ts` (`UNLOAD_RADIUS=6` unload + `dist <= unloadRadius` guard) | Dead branch hides the intended "draw far chunks cheaply instead of unloading them" behavior; LOD ring config is misleading | Either raise `UNLOAD_RADIUS` above `LOD_RINGS.low` so a `culled`/billboard tier renders beyond the detailed rings, or delete the `culled` tier and document that the unload radius is the true horizon | A chunk exists in the `culled` tier and renders cheaply, OR the tier is removed and tests assert the new contract |
| W3D-G14 | not_started | adjacent_follow_up | unassigned | 2026-06-01 unrelated scan | Floating-origin never rebases: `World3DScene` fixes `sceneOrigin` at the mount `start` and never recenters. The streamer follows the camera in absolute world coords fine, but rendered scene-local coordinates grow without bound as the player pans — after enough travel the float-precision problem the floating-origin was introduced to solve returns (vertex jitter, shadow/camera math drift). | `src/components/World3D/World3DScene.tsx` (`sceneOrigin = useMemo(..., [start])`), `src/components/World3D/FreeRoamCameraController.tsx` (reports world coords via `sceneToWorld`) | Long-range traversal silently degrades rendering precision; this is the "moving floating-origin" the NORTH_STAR lists as future but has no gap row | Rebase `sceneOrigin` (and offset the camera/controls + loaded chunk positions) when the camera drifts beyond a threshold (e.g., > N chunks) from the current origin | After panning > a few km, vertex positions stay near 0 (no jitter); a unit test covers the rebase offset math |
| W3D-G15 | done | adjacent_follow_up | claude | 2026-06-02 T10 implement | Site/town boxes ignore terrain height: `SitePieces` places each box at `position={[localX, radius*0.5, localZ]}` — a few meters above scene-Y 0 — instead of on the terrain surface. With vertical exaggeration (W3D-G11) the ground at a town now sits hundreds of meters up, so the town boxes the engine builds are **buried far below the terrain** and never seen. (Related to T8: exaggeration turned a latent offset into a visible burial.) | `src/components/World3D/World3DScene.tsx` `SitePieces` (~line 108); `siteGeometry.ts` `ChunkSite` carries only `localX/localZ`, no surface Y | Towns — a headline feature — are invisible in the exaggerated world | — | **Fixed (T10, 2026-06-02):** `surfaceY` carried on `ChunkSite` (sampler bilinearly samples `world.heights` at site grid coords, applies shared `heightToMeters`); box seated at `surfaceY + radius*0.5`; chunk-scoped React key. Also discovered Voronoi footprint radii hit `~785m` (full grid cell) which put the camera inside the box (back-face culled invisible) — capped radius at `MAX_RADIUS_M=80` in `siteGeometry.ts`. Live screenshot confirms golden town box sitting on terrain. 76 tests pass. |
| W3D-G16 | not_started | adjacent_follow_up | unassigned | 2026-06-02 T8 implement | Sub-cell view window flattens relief: the streamed window is `LOAD_RADIUS(4) × CHUNK_WORLD_SIZE(128)` ≈ 512 m radius, but `METERS_PER_CELL = 1024`, so the camera sees **≤ ~1 height cell**. Within one cell the heightfield is a single bilinear-interpolated slope, so even with strong vertical exaggeration the terrain reads as one smooth ridge, never *rolling* hills, and multi-cell features (a road crossing several cells, a valley) can't fit in frame. (Related to T8: it's the deeper limiter on relief legibility that exaggeration alone can't fix.) | `config.ts` (`LOAD_RADIUS`, `CHUNK_WORLD_SIZE`, `METERS_PER_CELL`); live T8 screenshots show a single ridge | Relief/roads/rivers are only ever seen one cell at a time; shrinking `METERS_PER_CELL` would cross the world↔grid coord contract `world-3d-ui` depends on, so it needs a deliberate plan | Widen the visible window without exploding chunk count — e.g., bigger `CHUNK_WORLD_SIZE` (fewer chunks/cell) and/or a coarse far-LOD ring beyond the detailed window (ties into W3D-G10/G13) | Camera frames ≥ 3–4 cells of varied terrain; multiple hills + a road segment read at once |
| W3D-G17 | done | adjacent_follow_up | claude | 2026-06-02 T8 implement | StrictMode-killed streamer: `useChunkStreaming` pinned its `ChunkStreamer` with `useMemo` but disposed it in an effect cleanup. Under React dev StrictMode's double-mount (setup → cleanup → setup) the cleanup permanently set `disposed=true` on the pinned instance; the remounted tree reused the dead streamer, which dropped its first `MAX_CONCURRENT_LOADS` batch (loadOk frozen at 4) and never streamed — the live 3D world was silently empty (all sky/fog). | `src/components/World3D/useChunkStreaming.ts`; live: `loadOk=4`, `chunks=0`; fixed → `chunks=81` | This (not the entry bounce) was the actual reason the live scene rendered blank; blocked verifying T8 | — | **Fixed:** streamer now created *inside* the mount effect (per-mount instance, disposed by the same effect's cleanup) — the StrictMode-safe disposable pattern; added `ChunkStreamer.isDisposed()` and a StrictMode double-mount regression test. Verified live: 81 chunks load |
| W3D-G18 | done | adjacent_follow_up | claude | 2026-06-02 T8 implement | Duplicate React keys on site boxes: the live console logs "Encountered two children with the same key" repeatedly. Likely a site `id` appearing in more than one streamed chunk (sites near chunk boundaries are included in multiple chunks), and/or duplicate ids within a chunk — React may omit/duplicate site meshes. | console during `?phase=world3d`; `World3DScene` `SitePieces` keys on `s.id`; `siteGeometry.ts` | Towns can flicker/vanish or render twice; masks real site-render regressions | T10 (2026-06-02) chunk-scoped the key to `${chunk.cx}\|${chunk.cy}\|${s.id}`; **T12 (2026-06-02) eliminated the underlying duplicate by making the chunkSampler site filter half-open (W3D-G20)**. | **Resolved by T12.** Deterministic in-page replay of the demo world (seed 2026): all 58 sites land in exactly one chunk (`sitesInMultipleChunks: []`); generator assigns no duplicate IDs (`duplicateGeneratorIds: []`). |
| W3D-G19 | done | adjacent_follow_up | claude | 2026-06-08 T14 implement | Voronoi footprint radii made town boxes uselessly huge: `buildSiteMeshes` derived the box half-width from `(vertex - center) * METERS_PER_CELL`, so typical towns could reach camera-inside-box scale. | `src/systems/world3d/siteGeometry.ts`; focused `siteGeometry.test.ts` coverage | Oversized placeholder boxes no longer drive town visualization. | **DONE:** replace footprint-derived radius with a bounded kind/population visual scale (`8`-`30`m). | Focused tests pin plausible radius bounds. |
| W3D-G20 | done | adjacent_follow_up | claude | 2026-06-02 T10 implement | Boundary sites double-included: the chunkSampler site filter used `<=` on both `maxGX`/`maxGY`, so any site exactly on a chunk edge was counted in both neighbours. (T12 discovery: this was not a corner-only edge case — **every** site is on a boundary, because site positions are integer grid coords and `METERS_PER_CELL`=1024 is an exact multiple of `CHUNK_WORLD_SIZE`=128, so `(x·1024) % 128 === 0` for all sites. The `<=` bug therefore duplicated essentially every site.) | `src/systems/world3d/chunkSampler.ts` site filter; live duplicate-key console errors after T10 key fix | Duplicate site meshes waste GPU draw calls and confuse React reconciliation | — | **Resolved by T12 (2026-06-02):** filter changed to half-open `[min, max)` — `s.position.x < aabb.maxGX` and `s.position.y < aabb.maxGY`. Added 2 boundary unit tests (shared X edge → upper chunk only; shared Y edge → lower chunk only). Verified via deterministic in-page replay: 58/58 demo sites in exactly one chunk. 9 chunkSampler tests pass; tsc clean. |
| W3D-G21 | done | adjacent_follow_up | claude | 2026-06-02 T12 implement | River/road polyline clip is inclusive on both chunk edges (`x <= maxX`, `y <= maxY` in `clipSegment`), so a river or road segment running exactly along a shared chunk boundary is emitted by **both** adjacent chunks. This is the line-geometry sibling of W3D-G20: T12 made the *site* filter half-open, but `clipPolylineToChunk` was not touched. Unlike sites it produces no React-key warning (rivers/roads are keyed per chunk), so it's silent — but boundary-coincident segments are meshed and drawn twice. | `src/systems/world3d/polylineClip.ts` `clipSegment` edges (lines 52–53, `maxX - a.x` / `maxY - a.y` both `<=`); confirmed by reading during T12 | Overdraw + z-fighting along chunk seams where a river/road runs along a boundary; wasted vertices. With sites now half-open and lines still closed, the two are inconsistent. | — | **Resolved by T13 (2026-06-02):** `clipSegment` max-edge tests are now half-open (`x < maxX`, `y < maxY`) by rejecting parallel-coincident segments with `q <= 0` on the two max edges while min edges stay inclusive (`q < 0`) — mirrors the W3D-G20 site rule. 2 new unit tests (boundary-coincident polyline owned by the upper chunk only; min-edge-coincident polyline still admitted); 67 world3d tests pass; tsc clean. Verified by deterministic in-page replay (seed 2026): the demo world has **116 boundary-coincident road segments**; the old inclusive clipper double-counted **31/34** roads (total clipped length up to 2.0× original), the half-open clipper yields exactly 1.0× for all 34 (no double-drawn seams). |
| W3D-G23 | done | adjacent_follow_up | claude | 2026-06-08 T15 implement | Roads rendered at a hardcoded uniform width because `chunkSampler` built road widths as `rd.points.map(() => 0.04)`, discarding `road.type`. | `src/systems/world3d/chunkSampler.ts`; focused `chunkSampler.test.ts` coverage | Road hierarchy encoded by the generator was invisible in 3D. | **DONE:** map `road.type` to a bounded width lookup and carry widths per output road vertex. | Focused world3d tests verify different road types carry distinct widths. |
| W3D-G25 | done | adjacent_follow_up | claude | 2026-06-08 T17 implement | Vegetation scatter recomputed identical transforms for unchanged chunk payloads, introducing avoidable buffer churn. | `src/systems/world3d/vegetationScatter.ts` (`buildVegetationScatter`) | Buffer churn appears when unchanged chunks reload during window motion. | **DONE:** cache the scatter payload and reuse existing typed buffers when input chunk payload is identical. | Bounded LRU cache (256 keys), payload-aware hash key, and tests asserting reference reuse on identical chunk payloads. |
| W3D-G26 | done | adjacent_follow_up | claude | 2026-06-08 T18 implement | Cached scatter payload reuse still left full instanced-mesh matrix writes for unchanged buffers in `VegetationPiece`. | `src/components/World3D/World3DScene.tsx`, `src/components/World3D/vegetationInstanceMatrices.ts`, `src/systems/world3d/vegetationScatter.ts` | Per-frame CPU work no longer repeats for identical scatter payloads. | **DONE:** key matrix writes on `VegetationScatter.cacheKey` and skip the repeated `setMatrixAt` loop. | Focused helper test proves unchanged payloads reuse prior matrix write state. |
| W3D-G24 | done | adjacent_follow_up | claude | 2026-06-08 T16 implement | `sampleChunk` clipped every river/road against every streamed chunk even when the polyline bounding box missed the chunk. | `src/systems/world3d/chunkSampler.ts`; focused `chunkSampler.test.ts` coverage | Wasted CPU scaled with total world polyline count instead of nearby polylines. | **DONE:** add polyline bbox-vs-chunk AABB prefilter before calling `clipPolylineToChunk`. | Focused world3d tests verify off-chunk polylines are skipped while overlapping polylines still render. |
| W3D-G22 | not_started | adjacent_follow_up | unassigned | 2026-06-02 T12 implement | Site horizontal positions are quantized to integer grid cells: the T12 replay showed **all 58** demo sites sit at integer grid coordinates (`(x·METERS_PER_CELL) % CHUNK_WORLD_SIZE === 0` for every site). Consequence in the renderer: a site's chunk-local position is `gx·M − cx·S`, which for an integer-grid site is exactly `0` — so every town seats flush at its owning chunk's NW corner (`localX = localZ = 0`), never in the chunk interior, and towns snap to a coarse 1024 m lattice. The half-open rule (W3D-G20) deterministically assigns these corner sites to the higher chunk. | `chunkSampler.ts` + `siteGeometry.ts` (chunk-local placement); T12 in-page replay (`boundarySitesOnGrid: 58/58`) | Towns can't be placed sub-cell; at world scale settlements visibly snap to a grid and always hug a chunk corner, which looks artificial and can collide with chunk-seam artifacts | Decide whether sub-cell site placement is wanted; if so, carry fractional site positions from the generator (likely a `worldsim-service` concern) and verify the renderer honours them. If integer placement is intended, document it so the corner-seating isn't mistaken for a bug | Either sites render at fractional in-chunk positions, or the integer-lattice placement is documented as intended with the corner-seating noted |

## Classification Reference

| Classification | Use when |
|---|---|
| `in_scope_now` | The task cannot honestly complete without it. |
| `support_needed_now` | It is not the product task, but the task cannot move without it. |
| `adjacent_follow_up` | Useful and related, but not required for this slice. |
| `out_of_scope` | It should not be part of this project/task. |
| `blocked_human_decision` | A real owner/operator choice is needed. |
| `blocked_external_state` | Waiting on PR, CI, vendor, service, environment, or another person. |

## Update Rules

- Keep each gap tied to evidence and a next proof/check.
- Link back to a global gap ID when this project imports one.
- If the current project should not own a gap, add or update the global gap tracker instead of keeping the gap here.
- Do not mark a gap done unless completion evidence is linked or summarized.
- Add dated testimony or status notes to an existing gap instead of opening duplicates.
