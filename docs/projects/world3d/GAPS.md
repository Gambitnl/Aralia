# World 3D System Gap Registry

Status: active
Last updated: 2026-05-31

## Scope
These are in-project execution gaps rooted in current implementation evidence. This registry captures what is blocked today so the next slice can start safely.

| Gap ID | Status | Classification | Owner | Owning tracker/subsystem | Found during | Gap | Evidence/source | Why it matters | Next action | Next proof/check |
|---|---|---|---|---|---|---|---|---|---|
| W3D-G1 | not_started | in_scope_now | Worker A | `docs/projects/world3d/TRACKER.md` | Current scan | World3DDemo uses inline generator and does not use `createWorkerChunkLoader` in production path | `src/components/World3D/World3DDemo.tsx` (inline loader), `src/components/World3D/createWorkerChunkLoader.ts` | Missed worker pipeline verification in actual scene entry path | Decide whether next slice keeps inline path or switches demo to worker-backed loader for parity | Validate by tracing loaded chunk call stack under `WORLD3D_DEMO` |
| W3D-G2 | not_started | in_scope_now | Worker A | `docs/projects/world3d/TRACKER.md` | Current scan | `polylineClip.ts` is present but not integrated with any chunk render builder | `src/systems/world3d/polylineClip.ts`, `src/components/World3D/World3DScene.tsx` | Planned river/road/surface detail is blocked until this hook is consumed | Keep `polylineClip` gated with explicit builder callout in first implementation row | Add a call-site and test that clipping receives a simple in-chunk river polyline |
| W3D-G3 | not_started | in_scope_now | Worker A | `docs/projects/world3d/TRACKER.md` | Current scan | `World3DDemo` is a sandbox entry, not connected to full gameplay phase transitions or saved world actor state | `src/App.tsx`, `src/hooks/useHistorySync.ts`, `src/types/core.ts` | Prevents full 3D traversal mode from shipping as a disconnected test-only surface | Add explicit decision row: keep sandbox or route from map/player state | Confirm decision in docs + acceptance check before refactor |
| W3D-G4 | not_started | in_scope_now | Worker A | `docs/projects/world3d/TRACKER.md` | Current scan | Loader contracts are mixed: `types.ts` already includes richer `ChunkMeshBundle`, while scene and loader currently operate on placeholder `ChunkGeometryArrays` | `src/systems/world3d/types.ts`, `src/components/World3D/useChunkStreaming.ts`, `src/components/World3D/createWorkerChunkLoader.ts`, `src/components/World3D/chunkWorker.ts` | Risks API drift and partial migration during future expansion | Freeze contract and convert in slices: schema, loader, tests, scene | Create migration checklist and run the existing world3d test subset |
| W3D-G5 | not_started | adjacent_follow_up | Worker A | this project | Current scan | Rich content builders referenced by plan exist only as type/interface intent (roads, sites, vegetation, water), no active render layer currently | `docs/superpowers/plans/2026-05-31-world3d-chunk-rendering.md`, `src/components/World3D/World3DScene.tsx` | Prevents overestimating scope as if terrain and content were both production-ready | Keep as adjacent follow-up with acceptance tied to renderer output coverage | Add acceptance for each content pass in next tracker rows |
| W3D-G6 | not_started | adjacent_follow_up | Worker A | this project | Current scan | No visual/scene lifecycle assertions on `World3DScene` mount/unmount and camera focus behavior beyond unit-level streamer tests | `src/components/World3D/World3DScene.tsx`, `src/components/World3D/__tests__/useChunkStreaming.test.tsx` | Scene lifecycle regressions can appear only in render path | Plan Playwright or component-level checks for mount and camera updates | Add one non-flaky visual or RTL-based lifecycle proof before phase hardening |
