# Level 3 Spells

This directory is prepared for level 3 spell migration.

Currently, level 3 spells are in the parent directory (`public/data/spells/`) as flat files.
During migration, they will be moved here and converted to the new JSON format.

See: `docs/tasks/spell-system-overhaul/` for migration progress.
