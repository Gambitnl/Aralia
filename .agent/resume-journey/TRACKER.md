# Resume Journey Flow — Task Tracker

> Single source of truth for resume-journey tasks.
> Read `.agent/GOAL-resume-journey.md` for the standard and process rules.

## Status Key
- [ ] Not started
- [~] In progress
- [x] Complete
- [!] Blocked

## Tasks

| # | Task | Priority | Status | Notes |
|---|------|----------|--------|-------|
| 1 | End-to-end audit: save → reload → Continue Journey. Enumerate per-surface what restores, what resets, what errors. Write the symptom list into this tracker as new tasks. | P0 | [x] | Done 2026-06-11. Rig: `audit.mjs` (resume + saved-vs-live diff) + `roundtrip.mjs` (resume → switch surface → autosave → reload → resume) + dev probe `window.__araliaState` (App.tsx, canUseDevTools-gated). Evidence in `evidence/run1-*`, `evidence/rt1-*`. **What PASSES:** Continue routes to the latest slot incl. autosave; LOAD_TRANSITION → PLAYING in ~3.1s; zero console errors; location, submap coords, worldViewMode, party, gold, inventory restore exactly; full round trip (3D → Atlas → autosave → reload → Continue) preserves the surface change; atlas/exploration UI healthy after resume (rt1-resume2.png). **Symptoms found → tasks 2-5.** |
| 2 | **World3D resume shows a featureless blue wedge — saved playerWorldPos can be off-map.** The autosave carries `playerWorldPos z=-881` (valid 0..40960): `World3DWrapper.handlePositionChange` dispatches unclamped camera coords, autosave persists them, resume restores them faithfully → the chunk streamer centers outside the grid. Fixed: clamp at dispatch (wrapper) AND clamp/heal on load (appState validatedPlayerWorldPos). | P0 | [x] | Done 2026-06-11. `World3DWrapper.tsx` clamps to `gridWorldDimensions` before throttle/dispatch; `appState.ts` LOAD_GAME_SUCCESS clamps the loaded position into the world grid (heals existing bad saves). 2 new reducer tests (off-map clamped / in-bounds untouched); 12 suites green (32 tests). **Before/after on the same bad autosave:** run1-result.json (live z stayed −881) → run2-postfix-result.json (live z=0, y recomputed 32.98, zero console errors). **Honest scoping:** the view at the healed position is STILL near-featureless (run2-postfix-resumed.png) — control experiment world3d-control.png proves the World3DDemo's known-good spawn renders the same minimal plain, so the emptiness is the streamed-world surface's current fidelity (worldforge 3D-ground-mode lane, in progress by design), NOT a resume defect. Resume now restores correct, in-bounds state with zero errors. Logged as GAPS #3. |
| 3 | **Combat does not resume.** `saveLoadService.loadGame` forces `phase = PLAYING` (line ~354) and combat runtime (turn manager, initiative, battle map) is hook-local — never serialized. Autosave IS eligible during COMBAT/BATTLE_MAP_DEMO (`useAutoSave.isGameplayPhase`), so a mid-combat save quietly resumes on the exploration surface instead. | P1 | [x] | Done 2026-06-12 via Option B (pre-combat checkpoint + "combat doesn't save"). Gemini's analysis (`combat-resume-analysis-gemini.md`) verified file:line then implemented in-session at the true chokepoints (reducers kept pure): (1) `useAutoSave.isGameplayPhase` now only PLAYING+VILLAGE_VIEW — combat excluded from the rolling autosave; (2) `useAutoSave` fires a one-shot **pre-combat checkpoint** to AUTO_SAVE_SLOT on the exploration→combat phase transition (GameState is still exploration values since combat is hook-local); (3) `saveLoadService.loadGame` ~354 detects a raw COMBAT/BATTLE_MAP_DEMO phase, notifies "Resumed from pre-combat checkpoint." then heals to PLAYING; (4) `handleSaveGame` (handleSystemAndUi.ts) blocks manual save during combat with a warning — true chokepoint for the `save_game` action, covers all UI. Verified: new loader test (mid-combat save heals to PLAYING + toast) + 21 saveLoadService tests green; full hooks+services sweep 419 passed/8 skipped; tsc clean on all 3 touched files (2 pre-existing unrelated errors remain: appState.ts:773 long-rest, worldHistory test). |
| 4 | Saved-while-overlay-open view state resets by design: `isMapVisible`/`isSubmapVisible`/journal/dev-menu forced false on save AND load. Substance (subMapCoordinates etc.) restores; only the open panel is lost. | P2 | [x] | **LIVE-VALIDATED 2026-07-24.** Full round trip in the real app on :3000: Quick Start → open World Map overlay (`isMapVisible` false→true) → autosave captures the open state → reload → Continue Journey → resumes to PLAYING with `isMapVisible: true` AND the overlay `[role="dialog"]` actually rendered, zero NEW console errors. Confirms the earlier build (all 3 sanitize/heal layers + `appState.ts LOAD_GAME_SUCCESS`). Prior build detail: player-facing overlays persist at every layer that forced them closed; dev/debug surfaces stay closed by design; 3 tests + 140/140 services+state green. `task4-overlay-report.html` can be deleted. |
| 5 | Resume log noise: every load appends "Game loaded successfully." to the persistent message log (3 accumulated in rt1-resume2.png). | P2 | [x] | Done 2026-06-12 via fleet orchestration: Qoder (free Qwen3.7-Max) located the append (`useGameInitialization.ts:232` → ADD_MESSAGE → serialized `messages`) and proposed the diff read-only; orchestrator applied it (toast-only confirmation, persistent append removed) and verified: 36 suites / 192 tests green, tsc clean. Old saves keep already-accumulated lines; no new ones added. |

| 6 | **Headless rigs cannot run: "No chronicles found".** Root cause is NOT the IndexedDB migration (see the corrected note below) — a Playwright storage state is keyed by ORIGIN, and `storageState.json` is pinned to `http://localhost:5174`, so every run against another port seeded nothing and the app honestly reported no saves. | P1 | [x] | Done 2026-07-26. New `rigContext.mjs` owns the target URL (`BASE_URL` env, default `http://localhost:3000/Aralia/` — vite's own port) and rewrites the seed state's origins to that run's origin in memory; `audit.mjs`, `roundtrip.mjs`, `world3d-control.mjs` all consume it. **A/B on the same server:** mismatched port → `saved payload: null`, no Continue button (the reported symptom, reproduced); after the fix → `audit postfix` resumes to PLAYING in 4.3s with 0 console errors, `roundtrip postfix-rt3` green (`roundTripPreservedSurface: true`). Round-trip's autosave check also stopped lying: it now watches the slot timestamp move past its pre-mutation baseline instead of testing wall-clock freshness against a months-old seeded save. Uncovered and fixed GAPS #4 (auto-save starvation) on the way. All three rigs also take `HEADED=1` (plus `SLOWMO`, default 150ms) so a run can be watched live instead of only read afterwards — Remy's standing preference; headed runs show two extra console 404s, which are the browser chrome asking for `/favicon.ico` at the server root, outside the `/Aralia/` base (`/favicon.ico` 404, `/Aralia/favicon.ico` 200) — pre-existing, unrelated to resume. |

## Progress Log

| Date | Task # | Action | Result |
|------|--------|--------|--------|
| 2026-06-11 | — | Goal created; loop retargeted from 3D visual quality | Scaffolding only — no audit performed yet. |
| 2026-06-11 | 1 | End-to-end audit executed | Built `audit.mjs`/`roundtrip.mjs` + `__araliaState` dev probe. Happy path is healthier than reported: state fidelity exact, 0 console errors, ~3.1s resume, round-trip green. The real defect is task 2 (off-map saved position → wedge view on the World3D surface) — the most likely source of "resume feels broken". Tasks 2-5 enumerated with evidence. |
| 2026-07-24 | 4 | Live validation in the real app | Reached PLAYING via Quick Start (see side-fix below), opened World Map, autosaved, reloaded, Continue Journey → overlay reopened (flag true + dialog rendered), zero new errors. Task 4 CLOSED. |
| 2026-07-24 | side-fix | Unblocked reaching a live game via Groq local proxy | `CompanionGenerator.generateSoul` hardcoded `OllamaClient` and never consulted the provider router, so with Ollama down Quick Start failed 0/3 even with Groq selected. Fixed: branch on `isGroqActive()` → `routeGenerateForTask` (Groq/proxy), Ollama path unchanged. Verified live: 3× `POST /__groq/v1/chat/completions → 200 OK`, party generated. 3/3 CompanionGenerator tests green, touched-file tsc clean (1 pre-existing `skeleton.gender` error untouched). |
| 2026-07-24 | GAPS | IndexedDB save migration breaks old localStorage rigs | Saves now live in IndexedDB (`aralia_rpg_migrated_to_idb` flag); only the slot INDEX stays in localStorage. The June headless rigs seed a localStorage `storageState.json`, so "Continue Journey" now reports "No chronicles found" against them — the rigs need an IDB-seeding bridge to run headless again. Logged for the resume-journey rig owner. **CORRECTED 2026-07-26 (task 6): the diagnosis was wrong on both counts.** No IDB-seeding bridge is needed — the app migrates a seeded localStorage save into IndexedDB on startup, proven live: `Migrated saves from localStorage to IndexedDB {count: 1}`, IDB then holds `aralia_rpg_autosave`, localStorage no longer does, and Continue Journey offers the save. The real cause was the seed state's pinned ORIGIN (5174) not matching the port under test. |
| 2026-07-26 | 6 | Rigs made port-agnostic; auto-save starvation found and fixed | Reproduced the reported "No chronicles found" (mismatched port → `saved payload: null`, no Continue button), then traced it to origin-keyed storage state rather than IndexedDB — the migration is healthy. Fix: `rigContext.mjs` as the single source of the target URL + in-memory origin rewrite, consumed by all three rigs. Verifying the round trip then exposed a real product bug: the rolling auto-save never fired during play because the 1-second world clock re-armed its debounce forever (GAPS #4) — fixed in `useAutoSave.ts` with a failing-first unit test. Verify: `useAutoSave.test.tsx` 6/6; `audit postfix` and `roundtrip postfix-rt3` both green with 0 console errors; hooks+services+state sweep 1051 passed / 1 failed, and that one failure (`useBattleMapGeneration` spawn-zone fallback) plus `geminiServiceFallback` both pass in isolation (21/21) — parallel-load timeouts, not this diff. |
| 2026-06-12 | 3 | Combat-resume Option B (pre-combat checkpoint + no-combat-saves) | Implemented Gemini's recommended design in-session after verifying its file:line claims. 4 change points: useAutoSave eligibility (combat excluded), one-shot checkpoint on exploration→combat transition, loader phase-heal + checkpoint toast, manual-save block in handleSaveGame. New loader test + 419 hooks/services tests green. Combat now resumes to a clean pre-fight exploration checkpoint instead of dropping the player onto the exploration surface mid-encounter. |
| 2026-06-11 | 2 | Off-map playerWorldPos clamp (dispatch + load-heal) | `World3DWrapper.handlePositionChange` clamps to the world grid before anything persists; `appState` LOAD_GAME_SUCCESS heals existing saves (audited z=-881 → 0). A/B: run1-* → run2-postfix-* (same bad autosave; position now in-bounds, 0 errors). Round-trip gate re-run green (rt2-postfix-result.json). Control experiment (world3d-control.png) separated the remaining featureless view from resume: the World3D streamed surface renders the same minimal plain at its known-good demo spawn — that's worldforge ground-mode fidelity work, not a resume defect (GAPS #3). Pre-existing tsc error appState.ts:773 (`isLongRestModalVisible`) belongs to a concurrent session's uncommitted long-rest work — not touched. |
