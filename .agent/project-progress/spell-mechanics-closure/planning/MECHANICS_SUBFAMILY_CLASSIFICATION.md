# Mechanics Subfamily Classification

Status: planning aid
Generated: 2026-05-14T11:03:39.631Z
Source report: .agent\roadmap-local\spell-validation\spell-mechanics-discovery.json

This report groups currently open mechanics findings by candidate runtime shape. It is a planning aid only: batch owners must still read the canonical prose and confirm that every row in a subfamily is genuinely compatible before editing schema, templates, or spell data.

Open findings classified: 1240
Candidate subfamilies: 15

## Summary

| Subfamily | Findings | Closure Rule |
| --- | ---: | --- |
| Choice, Mode, Or Option Branch | 380 | The spell exposes mode selection, option-specific targeting/effects, allocation choices, or player/caster branch choices. |
| Summon, Control, Possession, Or Soul Lifecycle | 144 | Summoned, animated, controlled, possessed, or soul/container states need lifecycle data distinct from ordinary spell ending. |
| AI Arbitration Or Custom Resolution | 127 | The finding likely cannot be captured with current deterministic schema without custom or AI-arbitrated handling. |
| Vision, Light, Sound, Or Sensory | 112 | Light, sound, visibility, senses, cover, heat, or sensory emission/suppression needs runtime data. |
| Duration Progression Or Permanence | 78 | Repeated casting, full-duration concentration, elapsed days, or slot level changes duration, permanence, or concentration requirements. |
| End Cleanup: Fall, Descent, Or Prone Outcome | 72 | A spell or effect ending causes falling, controlled descent, landing prone, or another end-position consequence. |
| Delivery Failure, Cooldown, Or Availability | 57 | A spell can fail delivery, create a later casting block, enforce a cooldown, or depend on target/soul availability. |
| External Destruction Or Dispel | 53 | A named external spell, dispel effect, object destruction, movement break, or trigger consumption destroys or ends the created effect. |
| Needs Manual Subfamily | 47 | No confident keyword match; classify manually before editing. |
| Transfer, Retarget, Or Reassignment | 36 | An effect ends on one target, moves to another target, or can be reassigned while the spell remains active. |
| Sustain, Range, Cover, Or Upkeep | 34 | A later action, range requirement, total-cover condition, or upkeep failure ends the spell or effect. |
| Object Stats, Damageability, Or Capacity | 32 | Created or affected objects need hit points, AC, damageability, capacity, size limits, or structural state. |
| End Cleanup: Space Or Passage Ejection | 32 | Spell-created spaces, passages, or containers end and move their contents to an exit, anchor, nearest space, or other explicit destination. |
| Save Counter Or Recurring End | 27 | Recurring saves, counted successes/failures, branch outcomes, or escape checks end or transform an ongoing effect. |
| Illusion, Disguise, Or Reveal | 9 | Illusion/disguise behavior, reveal conditions, study actions, physical interaction, or apparent state needs runtime data. |

## Choice, Mode, Or Option Branch

The spell exposes mode selection, option-specific targeting/effects, allocation choices, or player/caster branch choices.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `chill-touch::resource_or_consumption` | `resource_or_consumption` | Actionable open: This row identifies resource-like prevention or component behavior that is not fully represented as a runtime resource rule. | Add resource/prevention fields where this is not ordinary spell components. |
| `chill-touch::object_stats_or_damageability` | `object_stats_or_damageability` | Actionable open: Object or hit-point state needs object/stat fields that are not yet available on every affected low-level spell. | Add object stat/damageability fields for target objects and created objects. |
| `dancing-lights::movement_or_repositioning` | `movement_or_repositioning` | Actionable open: Moving created lights/forms needs an owned movable effect schema rather than simple target movement. | Add movable created-effect fields for distance, leash, and action cost. |
| `druidcraft::aftermath_or_memory` | `aftermath_or_memory` | Actionable open: Aftermath knowledge or later memory effects need a follow-up/aftermath schema. | Add aftermath/memory fields for post-spell knowledge and delayed consequences. |
| `elementalism::created_object_or_structure` | `created_object_or_structure` | Actionable open: Created objects/forms need object-state fields for durability, placement, dimensions, and disappearance. | Add created object/structure bucket fields. |
| `elementalism::resource_or_consumption` | `resource_or_consumption` | Actionable open: This row identifies resource-like prevention or component behavior that is not fully represented as a runtime resource rule. | Add resource/prevention fields where this is not ordinary spell components. |
| `elementalism::aftermath_or_memory` | `aftermath_or_memory` | Actionable open: Aftermath knowledge or later memory effects need a follow-up/aftermath schema. | Add aftermath/memory fields for post-spell knowledge and delayed consequences. |
| `friends::aftermath_or_memory` | `aftermath_or_memory` | Actionable open: Aftermath knowledge or later memory effects need a follow-up/aftermath schema. | Add aftermath/memory fields for post-spell knowledge and delayed consequences. |
| `green-flame-blade::environmental_change` | `environmental_change` | Actionable open: Ignition, weather, sensory, or cosmetic environmental change needs a reusable environment/world-state bucket. | Add environment/world-state fields for ignition, weather, sensory phenomena, and lasting environmental facts. |
| `guidance::attack_or_save_modifier` | `attack_or_save_modifier` | Guidance adds 1d4 to ability checks for the chosen skill, but runtime JSON only records a generic utility description. | Add ability-check modifier fields parallel to attack-roll and save-penalty fields. |
| `lightning-lure::forced_movement` | `forced_movement` | The pull is represented, but the damage only applies if the target is within 5 feet after the pull; that post-movement gate is prose-only. | Add a post-movement damage gate field for distance-from-caster checks. |
| `mold-earth::terrain_or_surface` | `terrain_or_surface` | Mold Earth can excavate/move/deposit loose earth, make difficult terrain, or restore normal terrain; JSON has terrain/manipulation but needs review against all modes. | Split terrain manipulation modes and durations explicitly. |
| `prestidigitation::terrain_or_surface` | `terrain_or_surface` | Magic Mark places color, mark, or symbol on an object or surface for 1 hour. | Use surface-marking fields shared with Mold Earth-like effects. |
| `prestidigitation::illusion_or_disguise` | `illusion_or_disguise` | Minor Creation can create a nonmagical trinket or illusory image that fits in the caster's hand and lasts until the end of the next turn. | Add option-specific created-object/illusion fields. |
| `prestidigitation::environmental_change` | `environmental_change` | Prestidigitation can light/snuff small fires, clean/soil an object, chill/warm/flavor nonliving material, and create harmless sensory effects. | Add utility option fields for fire play, clean/soil, temperature/flavor, and harmless sensory effects. |
| `shape-water::environmental_change` | `environmental_change` | Shape Water changes water flow, color, opacity, and frozen state in the world, including a no-creatures-in-water freeze prerequisite and no-damage movement caveat. | Add environmental substance-change fields for water movement distance, no-damage caveat, color/opacity, freeze, creature exclusion, and unfreeze timing. |
| `spare-the-dying::object_stats_or_damageability` | `object_stats_or_damageability` | Spare the Dying targets a creature with exactly 0 Hit Points that is not dead; current structured markdown and JSON only say creatures/allies. | Add target eligibility fields for current HP state and death-state exclusion. |
| `thaumaturgy::attack_or_save_modifier` | `attack_or_save_modifier` | Booming Voice grants Advantage on Charisma (Intimidation) checks for 1 minute, but this ability-check modifier is prose-only. | Add ability-check modifier fields for advantage/disadvantage, ability, skill, duration, and source option. |
| `thaumaturgy::terrain_or_surface` | `terrain_or_surface` | Tremors causes harmless ground tremors for 1 minute; this gray-zone world-state effect is not structured. | Add harmless terrain/surface disturbance option fields. |
| `thaumaturgy::vision_light_sound` | `vision_light_sound` | Thaumaturgy modifies voice volume, flames, and creates a point-originating sound, but these sensory mechanics are collapsed into one description. | Add sensory utility option fields for voice volume multiplier, flame brightness/color changes, and point-originating sounds. |
| `thaumaturgy::environmental_change` | `environmental_change` | Thaumaturgy can change existing flames and open or shut unlocked doors/windows, but these world-state object changes are prose-only. | Add utility option fields for flame alteration and unlocked door/window open-or-shut movement. |
| `thorn-whip::forced_movement` | `forced_movement` | Thorn Whip's size-gated pull is partially represented, and the runtime direction was corrected to toward_caster, but the optional 'can pull' choice remains flattened. | Add forced-movement option fields for optional pull, pull-toward-caster direction, maximum distance, and Large-or-smaller size gate. |
| `vicious-mockery::vision_light_sound` | `vision_light_sound` | Vicious Mockery can target a creature the caster can see or hear; current targeting only records lineOfSight=true and does not represent hearing as an alternate perception path. | Add target acquisition fields for visible_or_audible targets. |
| `alarm::choice_or_mode` | `choice_or_mode` | Alarm can ward a door, a window, or a 20-foot cube and lets the caster choose audible or mental alarm mode, but JSON keeps those choices in prose. | Add ward target-shape fields and alarm mode choice fields. |
| `alarm::vision_light_sound` | `vision_light_sound` | The audible alarm creates a handbell sound for 10 seconds within 60 feet of the warded area, but sound duration and radius are not structured. | Add audible alarm fields for sound type, sound duration, and audible radius. |
| `alarm::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Alarm's core ward trigger, intrusion conditions, exempt creatures, audible/mental delivery, mental one-mile range, and sleep wake-up are flattened into one utility description. | Add ward/alarm fields for trigger event, warded area/object, exempt creatures, delivery mode, mental range, and wakes-sleeping-caster behavior. |
| `bane::attack_or_save_modifier` | `attack_or_save_modifier` | Bane subtracts 1d4 from attack rolls and saving throws made by failed-save targets, but structured markdown and JSON only apply a generic Bane status; the save outcome was corrected to negates_condition. | Add attack/save penalty fields for affected roll types, dice 1d4, failed-save gate, and duration while concentrated. |
| `command::manual_grovel_prone_state` | `status_or_state_change` | The Grovel option applies the Prone condition, but that state change is flattened into option text. | Add option-level status/state fields for utility control options that apply named conditions. |
| `comprehend-languages::vision_light_sound` | `vision_light_sound` | Comprehend Languages distinguishes heard language, seen signed language, and seen written language, but runtime JSON stores them in one prose description. | Add language comprehension mode fields for heard speech, seen signing, and seen writing. |
| `disguise-self::vision_light_sound` | `vision_light_sound` | Disguise Self can change apparent height by up to 1 foot and apparent weight, but these appearance constraints are only prose in a generic utility effect. | Add disguise appearance fields for included worn/carried gear, apparent height delta, apparent weight change, and same-limb-arrangement requirement. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Summon, Control, Possession, Or Soul Lifecycle

Summoned, animated, controlled, possessed, or soul/container states need lifecycle data distinct from ordinary spell ending.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `druidcraft::summon_or_controlled_entity` | `summon_or_controlled_entity` | Actionable open: Created sensory effects or minor entities need summon/created-entity fields if they become independently addressable. | Add created entity/effect bucket fields. |
| `mage-hand::manual_controlled_hand_entity` | `summon_or_controlled_entity` | Mage Hand creates a persistent controllable entity, but the entity is not modeled with actions, movement, or limits. | Add controlled utility entity fields for entity type, allowed actions, later-turn control action, movement distance, range tether, carry limit, and prohibited actions. |
| `shape-water::created_object_or_structure` | `created_object_or_structure` | Shape Water can form and animate simple water shapes for 1 hour, but the created/animated form is not represented structurally. | Add controlled or animated substance fields for simple shape creation, animation direction, and one-hour duration. |
| `find-familiar::choice_or_mode` | `choice_or_mode` | Find Familiar requires choosing a familiar form and creature type, and recasting changes the existing familiar's eligible form instead of creating another familiar. | Add summon choice fields for form choice, creature type choice, and recast form replacement. |
| `find-familiar::target_filter_or_eligibility` | `target_filter_or_eligibility` | The familiar appears and reappears only in unoccupied spaces, eligible forms are Beast CR 0 forms, and one familiar is allowed at a time. | Add summon placement and eligibility fields for unoccupied space, CR 0 Beast form eligibility, and one-active-familiar limit. |
| `find-familiar::message_or_communication` | `message_or_communication` | Find Familiar grants telepathic communication with the familiar within 100 feet, but telepathy is only a summon subfield and not connected to communication mechanics. | Add communication fields for telepathic link target and range. |
| `find-familiar::summon_or_controlled_entity` | `summon_or_controlled_entity` | The summon is represented, but independent initiative, ally status, no-attack restriction, allowed normal actions, chosen-form statistics, disappearance at 0 HP, item drop, pocket dimension, and active-instance limit are not first-class data. | Extend summon fields for initiative, ally relationship, prohibited attacks, allowed actions, form statistics, 0-HP disappearance, item drop behavior, dismissal modes, and one-active-instance limit. |
| `find-familiar::social_or_knowledge_effect` | `social_or_knowledge_effect` | The familiar is explicitly an ally to the caster and the caster's allies, but ally relationship is not represented as runtime relationship data. | Add summon relationship fields for ally-to-caster and ally-to-caster-allies. |
| `find-familiar::object_stats_or_damageability` | `object_stats_or_damageability` | The familiar uses the chosen form's statistics, disappears at 0 Hit Points, and leaves worn or carried objects behind when it disappears. | Add summon stat source, zero-HP behavior, and worn/carried item drop fields. |
| `protection-from-evil-and-good::attack_or_save_modifier` | `attack_or_save_modifier` | The specified creature types' attack disadvantage is now represented as disadvantage_on_attacks, but the target's conditional Advantage on new saves against existing possession, Charmed, or Frightened effects is not first-class data. | Add conditional save-advantage fields scoped to existing possessed/charmed/frightened effects from specified creature types. |
| `protection-from-evil-and-good::social_or_knowledge_effect` | `social_or_knowledge_effect` | The protection prevents possession by specified creature types, but possession immunity is not represented as structured or runtime data. | Add possession immunity fields scoped by source creature type. |
| `unseen-servant::target_filter_or_eligibility` | `target_filter_or_eligibility` | Unseen Servant appears in an unoccupied space on the ground, but runtime targeting does not expose unoccupied-space or ground placement constraints. | Add summon placement fields for unoccupied space and on-ground requirement. |
| `unseen-servant::terrain_or_surface` | `terrain_or_surface` | The servant must appear on the ground; runtime has no placement surface field. | Add placement surface fields for summons/created entities. |
| `unseen-servant::vision_light_sound` | `vision_light_sound` | The servant is Invisible, but invisibility is only prose inside objectDescription and not a status/visibility field. | Add summoned entity visibility/status fields for Invisible. |
| `unseen-servant::summon_or_controlled_entity` | `summon_or_controlled_entity` | Unseen Servant is represented as a summon, but size, invisibility, mindless/shapeless force traits, AC, HP, Strength, no-attack rule, task completion/wait behavior, command range ending, and 0-HP ending are not first-class fields. | Extend summon/control fields for creature/object stats, traits, allowed/prohibited actions, command behavior, and lifecycle ending rules. |
| `unseen-servant::object_stats_or_damageability` | `object_stats_or_damageability` | The servant has AC 10, 1 Hit Point, Strength 2, cannot attack, and the spell ends at 0 HP; runtime stores these only in objectDescription. | Add summon stat fields for AC, HP, ability scores, attack prohibition, and 0-HP spell ending. |
| `unseen-servant::status_or_state_change` | `status_or_state_change` | The servant is Invisible, mindless, shapeless, and Medium, but those traits/states are prose-only. | Add summoned entity trait/status fields for Invisible, mindless, shapeless, and size. |
| `calm-emotions::choice_or_mode` | `choice_or_mode` | Calm Emotions lets the caster choose one of two effects separately for each failed-save Humanoid, but structured markdown and runtime JSON flatten those per-creature choices into one utility description. | Add per-target mode choice fields for save-gated utility/status effects. |
| `calm-emotions::social_or_knowledge_effect` | `social_or_knowledge_effect` | The Indifferent attitude shift, chosen creatures it applies toward, hostile prerequisite, damage/witnessed-damage break condition, and attitude restoration are prose-only. | Add social-attitude fields for attitude state, attitude target set, prerequisite attitude, break events, and restoration behavior. |
| `find-steed::choice_or_mode` | `choice_or_mode` | Find Steed has steed appearance choice, Celestial/Fey/Fiend type choice, disappeared-steed versus different-steed recast choice, and type-specific traits/actions; runtime only stores shallow form/type prose. | Add summon choice fields for appearance, creature type, recast return/new choice, and type-gated traits/actions. |
| `find-steed::attack_or_save_modifier` | `attack_or_save_modifier` | The steed uses the caster's spell attack modifier for Otherworldly Slam and spell save DC for Fell Glare, but these attack/save formulas are only in higher-level prose. | Add summon action attack/save formula fields that can reference caster spell attack modifier and spell save DC. |
| `find-steed::target_filter_or_eligibility` | `target_filter_or_eligibility` | The summon appears in an unoccupied space, and Fey Step teleports steed and rider to an unoccupied space; unoccupied-space requirements are not first-class. | Add unoccupied-space placement fields for summons and teleport destinations. |
| `find-steed::message_or_communication` | `message_or_communication` | The steed has Telepathy 1 mile that works only with the caster; runtime telepathy range has been corrected numerically but no communication restriction field exists. | Add summon communication fields for telepathy range and allowed participants. |
| `find-steed::travel_or_planar_movement` | `travel_or_planar_movement` | Fey Step teleports the steed and rider up to 60 feet to an unoccupied space and recharges after Long Rest, but type-gated teleport action data is not represented. | Add summon action fields for type-gated teleport, rider inclusion, distance, destination, and recharge. |
| `find-steed::resource_or_consumption` | `resource_or_consumption` | Fell Glare, Fey Step, and Healing Touch each recharge after Long Rest, and Life Bond triggers from the caster regaining HP from level 1+ spells; these recharge/trigger resources are prose-only. | Add summon action recharge fields and linked healing trigger fields. |
| `find-steed::healing_or_restoration` | `healing_or_restoration` | Life Bond mirrors qualifying healing to the steed, and Celestial Healing Touch restores 2d8 plus spell level; neither healing mechanic is typed. | Add linked healing trigger fields and summon action healing fields. |
| `find-steed::object_stats_or_damageability` | `object_stats_or_damageability` | Runtime stat block now has level-2 AC/HP corrected, but the formulas AC 10 + spell level, HP 5 + 10 per spell level, Hit Dice count, fly speed at spell level 4+, and disappearance at 0 HP/caster death are not formulaic data. | Add summon stat formula fields and lifecycle disappearance fields. |
| `find-steed::status_or_state_change` | `status_or_state_change` | If the caster is Incapacitated, the steed changes turn behavior; Fiend Fell Glare can Frighten a target until the end of the caster's next turn. Both are prose-only. | Add condition-gated summon behavior and type-gated status action fields. |
| `see-invisibility::travel_or_planar_movement` | `travel_or_planar_movement` | The spell does not grant travel, but it does grant planar perception into the Ethereal Plane, which current runtime data only stores in prose. | Add planar perception fields distinct from planar travel fields. |
| `see-invisibility::manual_invisible_and_ethereal_sight` | `sensor_or_remote_perception` | See Invisibility grants specific sensory permissions and a planar perception payload, but those permissions are not typed for runtime use. | Add sensory override fields for condition visibility, seen target categories, planar perception plane, and optional presentation cue. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## AI Arbitration Or Custom Resolution

The finding likely cannot be captured with current deterministic schema without custom or AI-arbitrated handling.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `message::message_or_communication` | `message_or_communication` | Message supports one-target private whisper plus target reply, but JSON only records a prose utility description. | Add communication fields for private target-only delivery and one-reply permission. |
| `message::social_or_knowledge_effect` | `social_or_knowledge_effect` | Message can pass through solid objects only if the caster is familiar with the target and knows it is beyond the barrier. | Add communication through-barrier prerequisite fields for familiarity and known target location. |
| `mold-earth::message_or_communication` | `message_or_communication` | Mold Earth can spell out words on dirt or stone, which is a world-state communication/marking effect. | Represent as surface-marking text support rather than generic communication. |
| `mold-earth::environmental_change` | `environmental_change` | Mold Earth changes terrain state and earth placement in the world. | Track world-state earth/terrain changes explicitly. |
| `prestidigitation::resource_or_consumption` | `resource_or_consumption` | The created trinket can deal no damage and has no monetary worth, which is a created-object restriction. | Add created-object restriction fields for damage capability and monetary worth. |
| `arms-of-hadar::reaction_or_opportunity_restriction` | `reaction_or_opportunity_restriction` | Arms of Hadar suppresses all reactions until the start of the target's next turn on a failed Strength save; the status rider now uses negates_condition, but still relies on a broad Reactions Suppressed status with inappropriate default repeat-save/escape sentinels. | Use reaction restriction fields for all-reaction suppression, failed-save gate, and until-start-of-target-next-turn duration, with not_applicable repeat-save/escape fields. |
| `burning-hands::environmental_change` | `environmental_change` | The spell starts eligible flammable objects burning, which is a persistent world-state/environmental effect not represented structurally. | Add burning/ignition environmental state fields for objects. |
| `charm-person::social_or_knowledge_effect` | `social_or_knowledge_effect` | Charm Person makes the Charmed creature Friendly and later aware it was Charmed by the caster; the runtime has a generic utility description but no structured friendly-state or awareness payload. | Add social disposition fields for Friendly-to-caster and aftermath awareness fields for knows-charmed-by-caster. |
| `charm-person::aftermath_or_memory` | `aftermath_or_memory` | When Charm Person ends, the target knows it was Charmed by the caster; this aftermath knowledge is not structured. | Add aftermath awareness fields for target knowledge after spell end. |
| `comprehend-languages::terrain_or_surface` | `terrain_or_surface` | Comprehend Languages requires touching the surface that contains written language, but this prerequisite is only stored in prose. | Add written-language reading fields for surface-touch requirement and read rate. |
| `comprehend-languages::message_or_communication` | `message_or_communication` | Comprehend Languages grants literal meaning only, reads about one page per minute, and does not decode symbols or secret messages; these communication limitations are prose-only. | Add communication/language fields for literal-meaning-only, page read rate, and no-symbol-or-secret-message decoding. |
| `find-familiar::sensor_or_remote_perception` | `sensor_or_remote_perception` | The caster can remotely see and hear through the familiar and gains its special senses, but the runtime only stores sharedSenses as a coarse boolean. | Add remote perception fields for shared sight/hearing, special-sense inheritance, range, action cost, and end timing. |
| `fog-cloud::environmental_change` | `environmental_change` | Fog Cloud's fog sphere, strong-wind dispersal, and slot scaling are represented, but the Heavily Obscured visibility state is still flattened into terrainType obscuring. | Add explicit obscurity-level fields for terrain/environmental effects. |
| `hunters-mark::social_or_knowledge_effect` | `social_or_knowledge_effect` | Hunter's Mark improves finding the marked target with Perception or Survival checks, but the tracking knowledge/search benefit is prose-only. | Add tracking/search benefit fields for marked quarry effects. |
| `purify-food-and-drink::resource_or_consumption` | `resource_or_consumption` | Purify Food and Drink removes poison and rot from nonmagical food and drink in an area, but runtime JSON only stores a generic creation utility description. | Add food/drink purification fields for contaminant types, nonmagical restriction, and area scope. |
| `snare::terrain_or_surface` | `terrain_or_surface` | Snare creates a 5-foot-radius magic trap circle on the ground or floor, but runtime JSON only stores area targeting and a generic utility description. | Add trap surface fields for ground/floor placement, circle radius, and rope-to-trap transformation. |
| `cloud-of-daggers::travel_or_planar_movement` | `travel_or_planar_movement` | Cloud of Daggers lets the caster use a later-turn Magic action to teleport the Cube up to 30 feet, and creatures take damage if the Cube moves into their space; runtime now has initial/enter/end-turn damage but lacks movable-area data and an area-moves-into-target trigger. | Add movable area fields for later-turn Magic action relocation distance and damage trigger when the area moves into a creature's space. |
| `cordon-of-arrows::terrain_or_surface` | `terrain_or_surface` | The ammunition is planted in the ground in the caster's space and cannot be physically uprooted, but that terrain/object anchoring is prose-only. | Add planted/anchored object fields for ground placement and uproot prevention. |
| `cordon-of-arrows::environmental_change` | `environmental_change` | The spell changes the local battlefield by planting persistent ammunition traps, but persistent placed trap objects are not first-class. | Add placed trap/object fields for persistent world-state hazards. |
| `flaming-sphere::terrain_or_surface` | `terrain_or_surface` | The sphere rolls along the ground, can cross barriers up to 5 feet and pits up to 10 feet, but those traversal rules are only spatial notes. | Add movable terrain traversal fields for ground-only rolling, barrier height, and pit width limits. |
| `flaming-sphere::environmental_change` | `environmental_change` | Flammable unattended objects start burning when touched by the sphere, but ignition/world-state change is prose-only. | Add ignition fields for flammable unattended objects touched by an effect. |
| `gentle-repose::status_or_state_change` | `status_or_state_change` | Gentle Repose prevents decay, prevents becoming Undead, and pauses days counting against resurrection time limits; runtime stores all of this in utility prose. | Add corpse/remains preservation fields for no decay, cannot become Undead, and resurrection time-limit pause. |
| `gentle-repose::manual_corpse_or_remains_target` | `target_filter_or_eligibility` | Gentle Repose targets a corpse or remains, but runtime cannot express corpse/remains as a valid target type. | Add corpse/remains target values and not_applicable sentinel handling where target kind does not apply. |
| `heat-metal::environmental_change` | `environmental_change` | Any creature in physical contact with the heated object takes damage, but physical-contact targeting is not first-class. | Add contact-with-target-object condition fields. |
| `knock::created_object_or_structure` | `created_object_or_structure` | Knock unlocks, unsticks, or unbars one access-preventing mechanism and suppresses Arcane Lock for 10 minutes, but these object/access state changes are prose-only. | Add lock/access state fields for mundane unlock/unstick/unbar, one-lock limit, and Arcane Lock suppression duration. |
| `knock::manual_access_preventing_object_target` | `target_filter_or_eligibility` | Knock only works on objects that contain mundane or magical means preventing access, but runtime target eligibility is generic objects. | Add object target eligibility fields for access-preventing mechanisms and example categories. |
| `magic-mouth::message_or_communication` | `message_or_communication` | Magic Mouth stores a spoken message of up to 25 words, can deliver it over up to 10 minutes, uses the caster's voice and spoken volume, and may use an existing mouth-like feature; runtime only has summary prose. | Add stored message fields for max words, max delivery duration, voice/volume preservation, mouth appearance, and object-mouth anchoring. |
| `moonbeam::manual_shapeshift_reversion_and_suppression` | `status_or_state_change` | Moonbeam has a failed-save state rider for shape-shifted creatures that is not represented structurally. | Add shape-shift reversion and shape-shift suppression fields with until-leaves-area duration. |
| `nystuls-magic-aura::social_or_knowledge_effect` | `social_or_knowledge_effect` | False Aura changes the knowledge returned by Detect Magic-like effects, but there is no first-class magical-aura spoofing payload. | Add magical detection spoof fields for appearsMagical, appearsNonmagical, and school override. |
| `pyrotechnics::environmental_change` | `environmental_change` | The spell extinguishes nonmagical flame in a 5-foot cube, creates smoke in a 20-foot radius moving around corners, and smoke persists 1 minute or until strong wind disperses it; these world-state changes are prose-only. | Add flame target/extinguish fields and smoke cloud fields for radius, corner movement, duration, and wind dispersal. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Vision, Light, Sound, Or Sensory

Light, sound, visibility, senses, cover, heat, or sensory emission/suppression needs runtime data.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `create-bonfire::environmental_change` | `environmental_change` | Actionable open: Ignition, weather, sensory, or cosmetic environmental change needs a reusable environment/world-state bucket. | Add environment/world-state fields for ignition, weather, sensory phenomena, and lasting environmental facts. |
| `elementalism::environmental_change` | `environmental_change` | Actionable open: Ignition, weather, sensory, or cosmetic environmental change needs a reusable environment/world-state bucket. | Add environment/world-state fields for ignition, weather, sensory phenomena, and lasting environmental facts. |
| `fire-bolt::environmental_change` | `environmental_change` | Actionable open: Ignition, weather, sensory, or cosmetic environmental change needs a reusable environment/world-state bucket. | Add environment/world-state fields for ignition, weather, sensory phenomena, and lasting environmental facts. |
| `mind-sliver::attack_or_save_modifier` | `attack_or_save_modifier` | Mind Sliver's damage and next-save 1d4 penalty share one failed Intelligence save, but runtime JSON stores them as separate effects that can drift. | Add or reuse a bundled save-penalty-with-damage field family, parallel to bundled attack-roll riders. |
| `thaumaturgy::illusion_or_disguise` | `illusion_or_disguise` | Altered Eyes changes the caster's eye appearance for 1 minute, which is a visible body/appearance change not represented structurally. | Add appearance-change utility fields for altered body feature, duration, and cosmetic-only scope. |
| `vicious-mockery::attack_or_save_modifier` | `attack_or_save_modifier` | Vicious Mockery's damage and next-attack Disadvantage rider share one failed Wisdom save, but JSON stores damage and a generic utility effect separately. | Use the ATTACK_ROLL_MODIFIER field family for outgoing next attack disadvantage bundled with damage behind one save. |
| `bless::attack_or_save_modifier` | `attack_or_save_modifier` | Bless adds 1d4 to attack rolls and saving throws for affected targets, but structured markdown and JSON only apply a generic Blessed status. | Add attack/save bonus fields for affected roll types, dice 1d4, target duration, and concentration scope. |
| `burning-hands::target_filter_or_eligibility` | `target_filter_or_eligibility` | Burning Hands ignites flammable objects in the cone that are not worn or carried, but targeting and effects only cover creatures. | Add object ignition fields for flammable objects, area membership, and not-worn-or-carried exclusion. |
| `color-spray::attack_or_save_modifier` | `attack_or_save_modifier` | Color Spray's stale HP-pool utility data was removed and the Blinded status is now Constitution-save gated, but the status still uses generic one-round duration plus default repeat-save/escape placeholders rather than an explicit until-end-of-caster-next-turn duration with not_applicable sentinels. | Add duration endpoint fields for until_end_of_caster_next_turn and not_applicable repeat-save/escape fields for one-save status effects. |
| `command::message_or_communication` | `message_or_communication` | Command requires the caster to speak a one-word command, but runtime JSON does not make the spoken-word delivery or language/understanding assumptions explicit. | Add command/communication fields for spoken one-word delivery and any language-understanding requirements or exclusions discovered across command-like spells. |
| `detect-magic::social_or_knowledge_effect` | `social_or_knowledge_effect` | Detect Magic teaches the spell school when the detected effect was created by a spell, but this knowledge payload is prose-only. | Add knowledge payload fields for detected spell school and created-by-spell prerequisite. |
| `disguise-self::illusion_or_disguise` | `illusion_or_disguise` | Disguise Self has physical-inspection failure and Study action Intelligence (Investigation) reveal rules, but these reveal mechanics are only prose. | Add illusion/disguise reveal fields for physical interaction, Study action, Intelligence (Investigation), spell save DC, and same-limb arrangement. |
| `entangle::attack_or_save_modifier` | `attack_or_save_modifier` | Entangle's Strength save is represented, but the canonical exclusion for the caster ('other than you') is not explicit in targeting or status target filters. | Add caster-exclusion fields for area save/status applications. |
| `fog-cloud::manual_heavy_obscurement` | `vision_light_sound` | The fog imposes the Heavily Obscured visibility state, but the runtime uses a coarse obscuring terrain type. | Add obscurity-level fields for terrain and environmental visibility effects. |
| `grease::environmental_change` | `environmental_change` | Grease explicitly creates nonflammable grease, but runtime terrain has no flammability or fire-interaction field. | Add environmental surface material fields for flammable/nonflammable state where spell prose makes it mechanical. |
| `guiding-bolt::attack_or_save_modifier` | `attack_or_save_modifier` | Guiding Bolt grants Advantage to the next attack roll against the target before the end of the caster's next turn, but the runtime stores this as a generic utility description. | Add attack-roll modifier fields for incoming next-attack advantage, hit-gated application, target scope, and duration until end of caster's next turn. |
| `hellish-rebuke::manual_damaging_creature_target` | `target_filter_or_eligibility` | The spell can target only the creature that damaged the caster, and that relationship is not first-class target-filter data. | Add trigger-source target fields for reaction spells where the legal target is the triggering creature. |
| `silent-image::illusion_or_disguise` | `illusion_or_disguise` | Silent Image creates a movable image with size/form limits, natural movement appearance, physical-interaction reveal, Study action reveal, Investigation check against spell save DC, and see-through discerned state, but JSON stores this as one sensory utility description. | Add illusion fields for image kind, max cube size, movement action, appearance adjustment, physical interaction reveal, Study/Investigation reveal, and discerned-state behavior. |
| `snare::vision_light_sound` | `vision_light_sound` | The trap is nearly invisible and can be discerned with Intelligence (Investigation) against spell save DC, but runtime JSON stores this only in utility prose. | Add visibility/discernment fields for nearly invisible traps and Investigation checks. |
| `snare::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Snare triggers only when a Small, Medium, or Large creature moves onto the ground or floor in the radius. Runtime now filters sizes and records the no-restrained cleanup ending, but the ground/floor movement trigger is not fully first-class. | Add trap trigger fields for eligible sizes and movement onto the ground/floor area. |
| `snare::status_or_state_change` | `status_or_state_change` | Snare restrains and hoists the creature upside down 3 feet above the ground/floor; Restrained is represented, but the hoisted position and upside-down state are prose-only. | Add positional restraint fields for hoisted height and inverted hanging state. |
| `snare::environmental_change` | `environmental_change` | Snare transforms consumed rope into a nearly invisible magic trap circle, but that world-state change is not represented beyond utility prose. | Add trap creation/world-state fields for consumed rope disappearing and trap circle becoming magical. |
| `thunderwave::forced_movement` | `forced_movement` | Thunderwave pushes failed-save creatures and unsecured objects 10 feet away, but runtime JSON keeps both pushes in a generic utility description instead of MOVEMENT/object movement data. | Add failed-save forced movement fields for creatures and unsecured object push fields. |
| `unseen-servant::environmental_change` | `environmental_change` | Some servant tasks can alter the world state, such as cleaning, mending, lighting fires, serving food, and pouring drinks, but task effects are not enumerated. | Add task capability fields that can describe simple world-state interactions. |
| `beast-sense::sensor_or_remote_perception` | `sensor_or_remote_perception` | Beast Sense's canonical shared perception and special-senses benefit are now reflected in runtime description, but there is still no first-class shared-senses mechanic. | Add shared-senses fields for perceive-through-target, retain-own-senses, special-sense inheritance, willing Beast target, and duration. |
| `find-traps::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Find Traps detects objects/mechanisms and spells such as Alarm or Glyph of Warding that were created to cause damage or danger, but trap eligibility is prose-only. | Add trap detection fields for qualifying created danger sources, included spell traps, mechanical traps, and excluded natural hazards. |
| `flame-blade::attack_or_save_modifier` | `attack_or_save_modifier` | Flame Blade's melee spell attack and 3d6 plus spellcasting ability modifier damage are represented in prose/damage dice, but the spellcasting ability modifier addition is not first-class. | Add damage formula fields for spellcasting ability modifier additions. |
| `flaming-sphere::target_filter_or_eligibility` | `target_filter_or_eligibility` | The sphere must be created in an unoccupied space on the ground, and flammable objects must be not worn or carried to ignite; those placement/eligibility constraints are not first-class. | Add unoccupied ground placement and object not-worn-or-carried ignition eligibility fields. |
| `levitate::attack_or_save_modifier` | `attack_or_save_modifier` | The Constitution save applies only to unwilling creatures, but runtime save data does not encode that target-state condition. | Add save-applies-to fields for unwilling creature targets. |
| `magic-weapon::attack_or_save_modifier` | `attack_or_save_modifier` | Magic Weapon gives a +1/+2/+3 bonus to weapon attack and damage rolls by slot tier, but runtime stores the attack/damage bonus as prose. | Add weapon attack/damage bonus fields with tiered slot scaling. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Duration Progression Or Permanence

Repeated casting, full-duration concentration, elapsed days, or slot level changes duration, permanence, or concentration requirements.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `find-familiar::sustain_or_recast_action` | `sustain_or_recast_action` | Find Familiar uses a Bonus Action for shared senses, Magic actions for dismiss/reappear, and recasting changes the familiar's form. | Add follow-up action fields for shared senses, temporary dismissal, reappearance, permanent dismissal, and recast reshaping. |
| `animal-messenger::conditional_ending` | `conditional_ending` | If the Beast does not reach the destination before the spell ends, the message is lost and the Beast returns to the casting location; duration also scales by slot. | Add delivery failure and duration scaling fields. |
| `nystuls-magic-aura::illusion_or_disguise` | `illusion_or_disguise` | The aura/mask illusion changes how spells and magical effects treat or detect the target, and repeated daily casting for 30 days makes it last until dispelled; runtime stores only prose. | Add magical-detection illusion fields and repeated-cast permanence fields. |
| `galders-tower::manual_recast_maintenance_and_permanent_tower` | `conditional_ending` | Galder's Tower has recast maintenance and a same-location/same-configuration permanence rule that are prose-only. | Add recastMaintenance and permanenceCondition fields. |
| `glyph-of-warding::choice_or_mode` | `choice_or_mode` | Actionable open: Mode choices need first-class mode/option fields when a spell exposes several distinct operations. | Add a choice/mode bucket with option-specific targets/effects. |
| `glyph-of-warding::terrain_or_surface` | `terrain_or_surface` | Actionable open: Terrain/surface rules need stronger terrain, surface, passability, and environmental-state fields when not already covered by TERRAIN. | Add terrain/surface bucket fields. |
| `glyph-of-warding::summon_or_controlled_entity` | `summon_or_controlled_entity` | Actionable open: Summoned/controlled entities need command, initiative, stat, duration, and ownership fields. | Add summon/controlled-entity bucket fields. |
| `glyph-of-warding::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Actionable open: Wards, glyph triggers, and delayed triggered effects need trigger-condition and stored-effect fields. | Add ward/trigger bucket fields. |
| `glyph-of-warding::conditional_ending` | `conditional_ending` | Actionable open: Some ending conditions are represented in prose/options but need reusable break-condition fields. | Add conditional-ending bucket fields. |
| `glyph-of-warding::manual_stored_spell_constraints` | `ward_alarm_or_trigger` | The spell-glyph stored spell constraints are not represented. | Add storedSpell fields for max spell level, prepared requirement, cast-as-part cost, target constraints, and concentration override. |
| `plant-growth::environmental_change` | `environmental_change` | Actionable open: Environmental changes need reusable world-state fields for weather, obscurement, growth, gas, liquids, and hazards. | Add environment/world-state bucket fields. |
| `plant-growth::created_object_or_structure` | `created_object_or_structure` | Enrichment changes future harvest output, but that created-resource/world-state effect is not typed. | Add harvestYieldMultiplier fields for plant enrichment. |
| `plant-growth::resource_or_consumption` | `resource_or_consumption` | Plant Growth modifies food resources and imposes a one-Plant-Growth-per-year limit. | Add resourceYield and annualBenefitLimit fields. |
| `plant-growth::environmental_change` | `environmental_change` | Plant Growth has two environmental world-state effects: overgrowth movement cost and long-term enrichment, both prose-only. | Add overgrowth movement cost and enrichment duration/radius fields. |
| `speak-with-dead::choice_or_mode` | `choice_or_mode` | Actionable open: Mode choices need first-class mode/option fields when a spell exposes several distinct operations. | Add a choice/mode bucket with option-specific targets/effects. |
| `speak-with-dead::message_or_communication` | `message_or_communication` | Actionable open: Communication rules need sender/recipient, language, plane, and message-shape fields. | Add communication bucket fields. |
| `speak-with-dead::summon_or_controlled_entity` | `summon_or_controlled_entity` | Actionable open: Summoned/controlled entities need command, initiative, stat, duration, and ownership fields. | Add summon/controlled-entity bucket fields. |
| `speak-with-dead::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Actionable open: Wards, glyph triggers, and delayed triggered effects need trigger-condition and stored-effect fields. | Add ward/trigger bucket fields. |
| `speak-with-plants::environmental_change` | `environmental_change` | The spell exposes one-day local event history through plants and can alter plant terrain, but both are prose-only. | Add plant memory/event-history and terrain conversion fields. |
| `divination::social_or_knowledge_effect` | `social_or_knowledge_effect` | The caster asks one question about a goal, event, or activity within 7 days; the DM gives a truthful phrase or cryptic rhyme; repeated castings before a Long Rest have cumulative 25 percent no-answer chance. Runtime stores this as prose only. | Add divination/information fields for question count, lookahead window, truthful reply, reply form, changed-circumstance caveat, repeat-before-rest chance, and cumulative chance step. |
| `find-greater-steed::choice_or_mode` | `choice_or_mode` | The spirit's form, Celestial/Fey/Fiend type, and language are all caster choices; runtime stores none of them as typed options. | Add summon choice fields for formOptions, creature type override options, Intelligence floor, and language choice. |
| `find-greater-steed::sustain_or_recast_action` | `sustain_or_recast_action` | The mount can be dismissed temporarily as an action, released permanently as an action, and recast to re-summon the bonded mount with restored HP and removed conditions; runtime lacks these lifecycle actions. | Add bonded summon lifecycle fields for dismiss, release, recast restore, and temporary/permanent disappearance. |
| `find-greater-steed::message_or_communication` | `message_or_communication` | The mount gains a language if needed and has 1-mile telepathy with the caster; runtime lacks typed language and telepathy fields. | Add summoned creature language and telepathy range fields. |
| `find-greater-steed::summon_or_controlled_entity` | `summon_or_controlled_entity` | The spell creates a bonded controllable mount with form options, stat-block references, type overrides, combat control, telepathy, spell-sharing, disappearance/recast lifecycle, one-mount bond limit, and carried-object drop behavior; runtime is only a generic utility placeholder. | Add bonded mount summon fields covering form, stats, control, spell sharing, lifecycle, bond limit, and equipment handling. |
| `find-greater-steed::social_or_knowledge_effect` | `social_or_knowledge_effect` | The one-bond limit across Find Greater Steed and Find Steed is a persistent relationship constraint, not currently typed. | Add bonded summon exclusivity fields across spell families. |
| `leomunds-secret-chest::travel_or_planar_movement` | `travel_or_planar_movement` | The chest and contents move to the Ethereal Plane, can be recalled to the caster, can be sent back, and may remain findable there if the spell ends; runtime stores this planar object movement only in prose. | Add object planar-storage fields for destination plane, recall action, send-back action, return placement, and end-state. |
| `leomunds-secret-chest::conditional_ending` | `conditional_ending` | The spell has a cumulative 5 percent daily ending chance after 60 days, ends if cast again, and ends if the Tiny replica is destroyed; runtime preserves these only in prose. | Add conditional ending fields for delayed cumulative chance, recast ending, and linked-object destruction. |
| `summon-greater-demon::attack_or_save_modifier` | `attack_or_save_modifier` | At the end of each of the demon's turns it makes a Charisma save, with disadvantage if the caster says its true name; on success, control ends. Runtime has no typed recurring control save or true-name save modifier. | Add controlled-summon recurring save fields with timing, ability, success branch, and conditional disadvantage source. |
| `summon-greater-demon::sustain_or_recast_action` | `sustain_or_recast_action` | When summoned and on each later caster turn, the caster can issue a verbal command requiring no action; runtime lacks command-economy fields for controlled summons. | Add summon command fields for no-action verbal commands, command timing, command target turn, and default behavior when no command is issued. |
| `summon-greater-demon::summon_or_controlled_entity` | `summon_or_controlled_entity` | The spell summons a demon with type choice, own initiative, no-action commands, default hostile behavior, recurring control saves, uncontrolled behavior, protective-circle interaction, 0-HP disappearance, and post-concentration linger; runtime is still a generic utility placeholder. | Add full controlled-summon fields for entity choice, initiative, allegiance/control state, command economy, default behavior, control saves, uncontrolled behavior, protective circle, and disappearance rules. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## End Cleanup: Fall, Descent, Or Prone Outcome

A spell or effect ending causes falling, controlled descent, landing prone, or another end-position consequence.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `druidcraft::sensor_or_remote_perception` | `sensor_or_remote_perception` | Actionable open: Sensory prediction/detection effects need a sensor/information schema when they are more than plain description. | Add sensor/information bucket fields. |
| `druidcraft::environmental_change` | `environmental_change` | Actionable open: Ignition, weather, sensory, or cosmetic environmental change needs a reusable environment/world-state bucket. | Add environment/world-state fields for ignition, weather, sensory phenomena, and lasting environmental facts. |
| `catapult::choice_or_mode` | `choice_or_mode` | Catapult requires choosing an eligible object and a travel direction, then resolving a straight-line movement path; current data collapses this to an area line plus prose utility. | Add object projectile fields for selected object, chosen direction, straight-line travel, maximum distance, fall behavior, and stop-on-impact. |
| `catapult::attack_or_save_modifier` | `attack_or_save_modifier` | Catapult's Dexterity save is only made by a creature the moving object would strike, and failure causes the object to stop and damage both object and creature; JSON stores a generic immediate save damage effect. | Add projectile-impact save fields tied to collision, object stop behavior, and damage-to-both-target-and-object. |
| `catapult::terrain_or_surface` | `terrain_or_surface` | Catapult stops early on impact with a solid surface and then drops the object, but there is no surface collision or fall-state representation. | Add projectile collision fields for solid surface impact and fall-to-ground behavior. |
| `create-or-destroy-water::manual_create_destroy_modes` | `choice_or_mode` | Create or Destroy Water has four utility modes with different targets, environmental effects, and scaling choices. | Add utility option fields for create contained water, create rain, destroy contained water, and destroy fog, including per-mode target, volume/cube size, exposed-flame extinguishing, and slot-scaling choice. |
| `create-or-destroy-water::manual_water_rain_fog_flame_state` | `environmental_change` | The spell creates clean water, extinguishes exposed flames with rain, destroys contained water, and clears fog, but these world-state changes are not first-class runtime fields. | Add environmental change fields for water creation/destruction, rain extinguishing exposed flames, and fog destruction. |
| `tensers-floating-disk::terrain_or_surface` | `terrain_or_surface` | The disk can move across uneven terrain, stairs, and slopes, but cannot cross elevation changes of 10 feet or more; spatial details preserve measurements, but traversal permissions are prose-only. | Add object traversal fields for allowed terrain and blocked elevation changes. |
| `tensers-floating-disk::travel_or_planar_movement` | `travel_or_planar_movement` | The disk follows the caster when the caster moves more than 20 feet away and remains within 20 feet, but follow movement behavior is only summarized in objectDescription and spatial notes. | Add created-object follow fields for follow threshold, desired distance, immobile-near-caster rule, and obstacle limitations. |
| `crown-of-madness::choice_or_mode` | `choice_or_mode` | The caster mentally chooses a creature for the Charmed target to attack each turn, and choosing no creature or having no creature in reach lets the target act normally; this forced-action choice is prose-only. | Add compelled-action control fields for controller-selected attack target, fallback when no valid choice exists, and before-movement timing. |
| `crown-of-madness::status_or_state_change` | `status_or_state_change` | Charmed is represented, but the additional forced-action behavioral state is not modeled as a typed control/status rider. | Add control-state fields for forced action, timing, controller choice, and fallback behavior. |
| `find-steed::summon_or_controlled_entity` | `summon_or_controlled_entity` | Find Steed has persistent summon lifecycle, replacement on recast, controlled mount behavior while ridden, independent behavior if caster is Incapacitated, and leave-behind carried gear; runtime summon data is too shallow. | Add summon lifecycle/control fields for replacement, initiative sharing, controlled mount, incapacitated fallback behavior, disappearance causes, and carried-gear handling. |
| `levitate::movement_or_repositioning` | `movement_or_repositioning` | Levitate raises and suspends a creature or loose object and later changes altitude by up to 20 feet, but the current movement model has no vertical suspension/altitude-change type and is forced to use teleport. | Add vertical levitation/suspension movement fields, altitude delta, actor-specific action cost, and max object weight. |
| `levitate::forced_movement` | `forced_movement` | The target can move only by pushing or pulling against a fixed object or surface within reach, as if climbing; this movement restriction is prose-only. | Add movement restriction fields for requires fixed surface/object within reach and climb-like movement. |
| `levitate::terrain_or_surface` | `terrain_or_surface` | Fixed objects or surfaces within reach determine whether a levitated target can self-move, but terrain/surface dependency is not represented. | Add terrain/surface dependency fields for movement constraints. |
| `levitate::aftermath_or_memory` | `aftermath_or_memory` | The gentle float to the ground after the spell ends is an aftermath behavior rather than ordinary duration text. | Add aftermath behavior fields for movement effects. |
| `phantasmal-force::sustain_or_recast_action` | `sustain_or_recast_action` | A dangerous phantasm can deal 2d8 Psychic damage on each of the caster's turns only if the target is in the phantasm's area or within 5 feet, but runtime lacks actor timing and distance/area gating. | Add recurring illusion damage fields for caster-turn timing and target-in-area-or-within-distance condition. |
| `phantasmal-force::illusion_or_disguise` | `illusion_or_disguise` | Phantasmal Force creates a target-only mental illusion, lets the target Study it with Intelligence (Investigation) vs spell save DC, causes rationalization, and can end on successful study; these mechanics are mostly prose-only. | Add phantasm fields for target-only perception, Study action reveal check, rationalization behavior, and end-on-successful-study. |
| `phantasmal-force::created_object_or_structure` | `created_object_or_structure` | The phantasm can appear as an object, creature, bridge, or other phenomenon but is target-only and mental rather than a physical object; this representational object is not modeled. | Add mental illusion representation fields without physical object stats. |
| `phantasmal-force::social_or_knowledge_effect` | `social_or_knowledge_effect` | The target rationalizes illogical outcomes from interacting with the phantasm, a cognition/knowledge effect not represented structurally. | Add cognition/rationalization fields for mental illusions. |
| `catnap::status_or_state_change` | `status_or_state_change` | Unconscious is now represented, but gaining a short rest after the full duration and immunity to Catnap until a long rest are still prose-only. | Add rest-benefit and temporary recast-immunity fields tied to completing the full duration. |
| `fly::movement_or_repositioning` | `movement_or_repositioning` | Fly grants a Fly Speed of 60 feet and hover, but runtime stores this only in utility prose. | Add movement grant fields for fly speed and hover. |
| `glyph-of-warding::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Glyph of Warding's configurable trigger and exclusion rules are not represented. | Add wardTrigger fields for trigger events, distance, creature filters, password, and custom prose fallback. |
| `sleet-storm::terrain_or_surface` | `terrain_or_surface` | Actionable open: Terrain/surface rules need stronger terrain, surface, passability, and environmental-state fields when not already covered by TERRAIN. | Add terrain/surface bucket fields. |
| `sleet-storm::status_or_state_change` | `status_or_state_change` | Actionable open: Some state changes are not simple D&D conditions and need state-change fields. | Add state-change bucket fields for non-condition states. |
| `water-walk::terrain_or_surface` | `terrain_or_surface` | Targets can move across any liquid surface as harmless solid ground, with lava heat still damaging them, but runtime stores liquid-surface movement only as utility prose. | Add movement-surface adaptation fields for liquid-surface walking, surface examples, harmless-ground treatment, and retained heat damage exceptions. |
| `dominate-beast::summon_or_controlled_entity` | `summon_or_controlled_entity` | The caster can issue no-action commands that the target obeys on its turn, including attack, movement, and object fetching, with fallback self-protection behavior; runtime only has Charmed. | Add dominated-target command fields for no-action commands, obedience, examples, idle behavior, and reaction command cost. |
| `dominate-beast::status_or_state_change` | `status_or_state_change` | Charmed is typed, but the status carries special domination riders: command obedience, same-plane telepathic link, self-protection fallback, and on-damage repeat save. | Add status rider fields for domination-specific control behavior. |
| `watery-sphere::forced_movement` | `forced_movement` | Creatures can be ejected to chosen nearby spaces, overflow creatures fall out and land prone within 5 feet, restrained creatures move with the sphere, and spell-end fall knocks restrained creatures prone; runtime lacks forced placement and prone ejection data. | Add forced placement/ejection fields for success ejection, overflow ejection, carried movement, and spell-end prone placement. |
| `watery-sphere::terrain_or_surface` | `terrain_or_surface` | The sphere's hover height, safe descent over pits or cliffs, spell-end fall to ground, and flame extinguishing are terrain/environment interactions stored only in prose. | Add terrain interaction fields for hover height, drop-off descent, fall-to-ground, and normal-flame extinguishing radius. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Delivery Failure, Cooldown, Or Availability

A spell can fail delivery, create a later casting block, enforce a cooldown, or depend on target/soul availability.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `message::vision_light_sound` | `vision_light_sound` | Message is blocked by magical silence and by specific barrier materials/thicknesses, which are not represented structurally. | Add communication blocker fields for silence and material barriers. |
| `detect-evil-and-good::manual_creature_and_hallow_detection` | `sensor_or_remote_perception` | The spell's detected creature types, Hallow detection, location payload, and blocker materials are not represented as first-class detection fields. | Add detection fields for detected creature types, detected spell/effect names, location payload, radius, and blocker materials. |
| `detect-magic::vision_light_sound` | `vision_light_sound` | Detect Magic's faint aura visibility requires a later Magic action and only applies to visible creatures or objects bearing magic; runtime stores this in prose. | Add detection reveal fields for follow-up Magic action, visible target requirement, aura visibility, and blocker materials. |
| `detect-poison-and-disease::manual_poison_creature_contagion_detection` | `sensor_or_remote_perception` | The spell detects several poison/disease categories and their locations, but the categories and location payload are prose-only. | Add detection fields for poisons, poisonous/venomous creatures, magical contagions, location payload, radius, and blocker materials. |
| `dissonant-whispers::reaction_or_opportunity_restriction` | `reaction_or_opportunity_restriction` | Dissonant Whispers represents reaction-using forced movement away from the caster, but 'if available' and 'safest route' are only stored in prose/custom formula. | Add reaction availability and safest-route forced movement fields. |
| `arcane-lock::environmental_change` | `environmental_change` | Arcane Lock magically locks a closed door/window/gate/container/hatch, blocks nonmagical unlocking, permits designated creatures, and optionally uses a spoken password within 5 feet to unlock for 1 minute. Runtime prose has been updated, but lock behavior is not first-class data. | Add lock control fields for eligible object types, nonmagical-unlock prevention, designated opener list, password, password range, and temporary unlock duration. |
| `darkness::sensor_or_remote_perception` | `sensor_or_remote_perception` | The specific Darkvision cannot see through it rule is only described, not represented as a sense-specific blocker. | Add sense-blocker fields for named senses or perception modes. |
| `detect-thoughts::choice_or_mode` | `choice_or_mode` | Detect Thoughts has Sense Thoughts and Read Thoughts modes, and later turns can activate either mode with a Magic action; runtime stores two utility effects but no explicit mode/action selection model. | Add repeatable mode activation fields for later-turn Magic action choices. |
| `detect-thoughts::attack_or_save_modifier` | `attack_or_save_modifier` | The deeper probe Wisdom save has different outcomes: failed save reveals deeper information, successful save ends the spell. Runtime has a save but lacks a typed success-ending outcome tied to the probe. | Add save outcome fields for success_ends_spell and failed-save knowledge payloads. |
| `detect-thoughts::message_or_communication` | `message_or_communication` | The spell's targets must know languages or be telepathic for thought sensing/reading to work, but language/telepathy eligibility is not first-class. | Add mental communication/thought eligibility fields for knows-language or telepathic. |
| `detect-thoughts::conditional_ending` | `conditional_ending` | Detect Thoughts can end on successful Wisdom save against deeper probe or on a target's action Intelligence (Arcana) check while attention remains shifted; only prose carries the Arcana escape and attention condition. | Add conditional-ending and escape-check fields for successful probe save, target Arcana action, and shift-attention-away condition. |
| `locate-object::social_or_knowledge_effect` | `social_or_knowledge_effect` | Locate Object can locate a familiar specific object or nearest object of a kind, gives direction to location and movement direction if moving, and is blocked by any lead; runtime keeps this as prose. | Add object detection query fields for specific familiar object, kind/category search, 1000-foot radius, direction payload, movement direction payload, and lead blocker. |
| `prayer-of-healing::healing_or_restoration` | `healing_or_restoration` | The 2d8 healing and +1d8 scaling are represented, but Short Rest benefits and once-per-creature-until-Long-Rest lockout are prose-only. | Add restoration fields for grants_short_rest_benefits and per-target lockout until Long Rest. |
| `rope-trick::vision_light_sound` | `vision_light_sound` | Rope Trick creates an Invisible portal and gives creatures inside one-way sight through it while blocking attacks, spells, and effects. | Add portal visibility and one-way perception fields. |
| `rope-trick::travel_or_planar_movement` | `travel_or_planar_movement` | The spell opens an Invisible portal to an extradimensional space reached by climbing the rope, with capacity and effect-barrier rules. | Add extradimensional portal fields for access method, portal size, capacity, occupant size limit, and pass-through restrictions. |
| `rope-trick::manual_portal_effect_barrier` | `travel_or_planar_movement` | The extradimensional portal has an explicit pass-through blocker for attacks, spells, and effects while allowing one-way sight. | Add portal pass-through restriction fields for blocked attacks, blocked spells, blocked effects, and allowed sight. |
| `silence::vision_light_sound` | `vision_light_sound` | Silence blocks sound creation and sound passage through the sphere and makes verbal-component spellcasting impossible there; runtime keeps those as prose in a utility description. | Add sound-zone fields for no sound created, no sound passing through, and verbal component casting blocked in the area. |
| `zone-of-truth::message_or_communication` | `message_or_communication` | On a failed save, a creature cannot speak a deliberate lie while in the radius, but runtime stores the speech restriction in prose. | Add truth/speech restriction fields for deliberate-lie blocking. |
| `haste::status_or_state_change` | `status_or_state_change` | Haste has a delayed spell-end state change that needs a spell-end trigger model before it can be encoded correctly. | Add spellEndTrigger/afterEffect fields for temporary post-duration statuses and speed overrides. |
| `leomunds-tiny-hut::manual_passage_and_spell_barrier` | `ward_alarm_or_trigger` | Tiny Hut creates a passage and spell-effect barrier with an initial occupant/object whitelist. | Add barrier fields for initial occupants/objects, barred entrants, blocked spell levels, and blocked spell effects. |
| `magic-circle::status_or_state_change` | `status_or_state_change` | Magic Circle's entry restrictions, teleport/interplanar save, attack disadvantage, and charm/frighten/possess protection are not represented. | Add creatureType-gated barrier, travel blocking, attack disadvantage, and condition/source protection fields. |
| `meld-into-stone::vision_light_sound` | `vision_light_sound` | Meld into Stone hides the caster from nonmagical senses and blocks outside sight, but those sensory states are not typed. | Add nonmagical undetectability and blocked outside vision fields. |
| `nondetection::sensor_or_remote_perception` | `sensor_or_remote_perception` | Nondetection blocks Divination targeting and magical scrying sensor perception, but these protections are not typed. | Add divinationProtection and scryingSensorBlock fields. |
| `phantom-steed::choice_or_mode` | `choice_or_mode` | Actionable open: Mode choices need first-class mode/option fields when a spell exposes several distinct operations. | Add a choice/mode bucket with option-specific targets/effects. |
| `sending::message_or_communication` | `message_or_communication` | Actionable open: Communication rules need sender/recipient, language, plane, and message-shape fields. | Add communication bucket fields. |
| `sending::social_or_knowledge_effect` | `social_or_knowledge_effect` | Actionable open: Knowledge, attitude, speech, or memory effects need explicit social/knowledge fields. | Add social/knowledge bucket fields. |
| `sending::social_or_knowledge_effect` | `social_or_knowledge_effect` | Sending creates knowledge states for sender recognition, delivery failure, and blocked status. | Add communication knowledge-result fields. |
| `sending::conditional_ending` | `conditional_ending` | Sending has delivery failure and recipient-created block conditions that can cause later casts to fail. | Add delivery failure and recipient block/cooldown fields. |
| `slow::reaction_or_opportunity_restriction` | `reaction_or_opportunity_restriction` | Slow blocks all reactions, but this is prose-only. | Add reactionsBlocked field. |
| `wall-of-sand::terrain_or_surface` | `terrain_or_surface` | The wall must be on the ground, blocks line of sight but not movement, and costs 3 feet of movement for every 1 foot moved in its space; runtime has wall dimensions but no typed movement-cost multiplier or ground placement rule. | Add terrain obstruction fields for ground placement, line-of-sight blocking, movement passability, and movement-cost multiplier. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## External Destruction Or Dispel

A named external spell, dispel effect, object destruction, movement break, or trigger consumption destroys or ends the created effect.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `illusory-script::illusion_or_disguise` | `illusion_or_disguise` | Illusory Script creates an illusion over writing with designated viewers, alternate meaning/handwriting/language, dispel cleanup, and Truesight bypass rules, but JSON keeps all of that in description text. | Add written-illusion fields for substrate, designated viewers, normal-view payload, alternate script payload, allowed known language, dispel result, and Truesight bypass. |
| `illusory-script::message_or_communication` | `message_or_communication` | The spell conveys a hidden intended meaning to designated readers and can alter meaning, handwriting, and language for others. | Add written message fields for hidden message, altered message, viewer groups, and language restriction. |
| `cordon-of-arrows::object_stats_or_damageability` | `object_stats_or_damageability` | Each ammunition piece is destroyed after striking, but per-ammunition destruction is not modeled as object state. | Add per-instance object consumption/destruction fields. |
| `darkness::vision_light_sound` | `vision_light_sound` | Darkness blocks Darkvision and nonmagical light, can be blocked by opaque cover when object-originated, and dispels spell-created Bright/Dim Light of level 2 or lower; these are mostly prose-only. | Add magical darkness fields for vision blockers, nonmagical-light immunity, object-origin opaque cover blocker, and lower/equal spell-light dispel. |
| `mirror-image::attack_or_save_modifier` | `attack_or_save_modifier` | Mirror Image redirects hits using a d6 roll for each remaining duplicate, but runtime still has only a corrected prose description and no interception model. | Add defensive duplicate interception fields for trigger on hit, dice per remaining duplicate, success threshold, redirect target, and duplicate destruction. |
| `mirror-image::object_stats_or_damageability` | `object_stats_or_damageability` | Duplicates are destroyed only by redirected hits and ignore all other damage/effects; the spell ends when all three are destroyed. Runtime has no duplicate object state. | Add duplicate lifecycle fields for destruction trigger, immunity to other damage/effects, remaining count, and ends_when_all_destroyed. |
| `daylight::vision_light_sound` | `vision_light_sound` | Bright and dim radii are now represented, but object-origin Emanation, opaque-cover blocking, and dispelling overlapping Darkness from level 3 or lower are still prose-only. | Add light source mode fields, opaque cover blocker fields, and darkness dispel interaction fields. |
| `dispel-magic::choice_or_mode` | `choice_or_mode` | Dispel Magic can target a creature, object, or magical effect; runtime now allows creatures and objects, but magical effects are not a valid target category. | Add magical_effect as a target category or a spell-effect target family. |
| `dispel-magic::attack_or_save_modifier` | `attack_or_save_modifier` | For ongoing spells of level 4 or higher, Dispel Magic requires a spellcasting ability check against DC 10 plus spell level; runtime stores the check in prose only. | Add ability-check resolution fields for spellcasting ability checks with formula DCs. |
| `dispel-magic::conditional_ending` | `conditional_ending` | Ongoing spells end automatically up to the cast slot level and end on successful checks above that level; runtime has no typed dispel/ending data. | Add dispel fields for auto-ended spell level threshold and checked spell level threshold. |
| `dispel-magic::manual_magical_effect_target` | `target_filter_or_eligibility` | Dispel Magic's magical-effect target type has no runtime target category. | Add magical_effect target eligibility. |
| `glyph-of-warding::conditional_ending` | `conditional_ending` | Glyph of Warding has movement-break and trigger-consumption ending rules that are not typed. | Add conditionalEnding fields for moved distance and one-shot trigger consumption. |
| `wall-of-water::attack_or_save_modifier` | `attack_or_save_modifier` | Ranged weapon attacks that enter the wall's space have disadvantage, and fire damage is halved if the fire effect passes through the wall; both are only in utility prose. | Add wall-interaction modifier fields for ranged weapon attack disadvantage and damage-type reduction through the wall. |
| `wall-of-water::terrain_or_surface` | `terrain_or_surface` | The wall's space is difficult terrain, cold damage can freeze 5-foot-square sections, and destroyed frozen sections leave gaps that water does not refill; runtime only stores most of this in prose. | Add terrain/wall state fields for difficult terrain, freeze transformation, section size, and non-refill gaps. |
| `wall-of-water::object_stats_or_damageability` | `object_stats_or_damageability` | Each 5-foot-square frozen section has AC 5 and 15 Hit Points and is destroyed at 0 HP; runtime has no typed destructible-section stats. | Add object/section AC, HP, size, and destroyed-at-zero fields. |
| `wall-of-water::environmental_change` | `environmental_change` | Cold damage passing through the wall freezes part of it, fire damage through it is halved, and destroyed sections remain empty; these environmental interactions are only prose. | Add environmental interaction fields keyed by damage type and pass-through condition. |
| `leomunds-secret-chest::object_stats_or_damageability` | `object_stats_or_damageability` | Destroying the Tiny replica chest ends the spell, but runtime lacks typed dependency/destruction semantics for the material replica. | Add component-dependency fields for destroyable linked objects that end a spell. |
| `otilukes-resilient-sphere::forced_movement` | `forced_movement` | An enclosed creature can spend an action to roll the sphere at up to half its Speed, and other creatures can pick up and move the globe; runtime stores this movement only in prose. | Add enclosed-object movement fields for internal push action, half-speed cap, and external pickup/move. |
| `dispel-evil-and-good::attack_or_save_modifier` | `attack_or_save_modifier` | The incoming attack disadvantage from Celestials, Elementals, Fey, Fiends, and Undead is now represented, but Dismissal's Charisma save and planar send-home branch remain prose-only. | Add special-function action fields for Dismissal with Charisma save and planar movement outcome. |
| `dispel-evil-and-good::social_or_knowledge_effect` | `social_or_knowledge_effect` | Break Enchantment removes possession, Charmed, or Frightened caused by qualifying creature types; runtime lacks typed source-filtered condition/possession removal fields. | Add source-filtered removal fields for possession and conditions caused by specific creature types. |
| `dispel-evil-and-good::status_or_state_change` | `status_or_state_change` | Break Enchantment and Dismissal can remove possessed, Charmed, Frightened, or relocate a creature; only the attack-roll disadvantage is typed. | Add special-function status removal and dismissal fields. |
| `swift-quiver::conditional_ending` | `conditional_ending` | Runtime has the attack grant and ammunition creation, but lacks typed ending if the quiver leaves possession and disintegration of created ammunition when the spell ends. | Add possession-based ending and created-ammunition cleanup fields. |
| `swift-quiver::aftermath_or_memory` | `aftermath_or_memory` | Created ammunition disintegrates when the spell ends; runtime lacks typed cleanup/aftermath data. | Add created-object cleanup fields. |
| `wall-of-force::forced_movement` | `forced_movement` | If the wall cuts through a creature's space, the creature is pushed to a chosen side; runtime lacks this push effect. | Add wall-creation displacement fields. |
| `wall-of-force::created_object_or_structure` | `created_object_or_structure` | Runtime now marks the utility as creation and captures geometry, but physical pass-through blocking, damage immunity, Dispel Magic immunity, and Disintegrate destruction remain prose-only. | Add wall barrier/material immunity and spell-destruction fields. |
| `wall-of-force::travel_or_planar_movement` | `travel_or_planar_movement` | The wall extends into the Ethereal Plane and blocks ethereal travel, but this planar blocking is not typed. | Add ethereal-plane blocking fields. |
| `wall-of-stone::created_object_or_structure` | `created_object_or_structure` | The wall is created and partially sized, but nonmagical object status, permanence, dispel immunity after permanence, and collapse relationships are not typed. | Add created-structure object lifecycle fields. |
| `disintegrate::created_object_or_structure` | `created_object_or_structure` | The spell can target creations of magical force and automatically disintegrate a Large-or-smaller nonmagical object or magical-force creation, but runtime lacks an object/force-creation destruction branch. | Add object and magical-force creation disintegration fields. |
| `disintegrate::object_stats_or_damageability` | `object_stats_or_damageability` | Disintegration at 0 hit points, nonmagical worn/carried item destruction, True Resurrection/Wish revival restriction, automatic Large-or-smaller object destruction, and Huge-or-larger 10-foot Cube partial destruction remain prose-only beyond the damage description. | Add disintegration aftermath and partial-object destruction fields. |
| `drawmijs-instant-summons::conditional_ending` | `conditional_ending` | The spell ends when the object is summoned, and the runtime prose also notes Dispel Magic or similar effects on the sapphire end the spell; these ending conditions are not typed. | Add linked-token destruction/use and dispel-on-token ending fields. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Needs Manual Subfamily

No confident keyword match; classify manually before editing.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `detect-evil-and-good::manual_hallow_presence_knowledge` | `social_or_knowledge_effect` | The spell provides knowledge of whether Hallow is active and where it is, but that payload is prose-only. | Add knowledge payload fields for detected spell/effect presence and location. |
| `detect-poison-and-disease::manual_kind_identification` | `social_or_knowledge_effect` | The spell identifies the kind of poison, poisonous creature, or contagion, but that knowledge payload is prose-only. | Add knowledge payload fields for detected kind/category. |
| `fog-cloud::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing prevention, stabilization, or condition/restoration removal needs explicit restoration/prevention schema beyond generic utility text. | Add restoration, condition-removal, and healing-prevention buckets. |
| `goodberry::manual_created_berries` | `created_object_or_structure` | Goodberry creates a fixed count of ten magic berries, but count, item identity, and duration are not first-class created-object data. | Add created consumable object fields for object name, count, location, duration, and magical state. |
| `augury::manual_repeat_cast_no_answer_chance` | `resource_or_consumption` | Augury has a repeated-casting risk that accumulates until Long Rest, but it is prose-only. | Add repeated-casting risk fields for reset cadence, starting cast count, cumulative chance, and no-answer outcome. |
| `augury::manual_omen_payload_and_limits` | `social_or_knowledge_effect` | Augury's knowledge payload has a time-bound planned-action question and explicit reliability caveat that are not first-class. | Add knowledge payload fields for planned-action horizon, information source, result table, and caveats. |
| `enlarge-reduce::manual_object_not_worn_or_carried_and_unwilling_creature_save` | `target_filter_or_eligibility` | Enlarge/Reduce target eligibility differs by target kind: objects must be unattended, and only unwilling creatures get a save. | Add target-kind-specific eligibility and save-condition fields. |
| `gust-of-wind::manual_movement_cost_toward_caster` | `forced_movement` | Gust of Wind changes movement cost directionally, but the cost is prose-only. | Add movement cost multiplier fields with direction condition. |
| `magic-weapon::manual_nonmagical_weapon_target` | `target_filter_or_eligibility` | Magic Weapon's target must be a nonmagical weapon, but runtime target eligibility is generic objects. | Add object target eligibility fields for weapon category and nonmagical requirement. |
| `protection-from-poison::manual_end_poisoned_condition` | `status_or_state_change` | Protection from Poison removes Poisoned immediately, but condition removal is prose-only. | Add condition-removal fields for named condition ended on cast. |
| `shining-smite::manual_cast_after_hit_trigger` | `reaction_or_opportunity_restriction` | Shining Smite is cast after a triggering weapon or unarmed hit, but runtime timing does not preserve that trigger. | Add casting trigger fields for after-hit smite-style spells, including allowed attack sources and target binding. |
| `blinding-smite::manual_cast_after_hit_trigger` | `reaction_or_opportunity_restriction` | Blinding Smite is cast after a triggering melee weapon or unarmed hit, but runtime timing does not preserve that trigger. | Add casting trigger fields for after-hit smite-style spells, including allowed attack sources and target binding. |
| `elemental-weapon::manual_nonmagical_weapon_target` | `target_filter_or_eligibility` | Elemental Weapon specifically requires a nonmagical weapon target. | Add object subtype and nonmagical target filter fields. |
| `elemental-weapon::manual_weapon_becomes_magical` | `status_or_state_change` | The target weapon becoming magical is a state change not represented in runtime data. | Add object state fields for becomes_magic_weapon. |
| `galders-tower::environmental_change` | `environmental_change` | The tower creates an interior environmental state that protects occupants from outside weather. | Add created-environment fields for interior warmth/dryness and weather isolation. |
| `galders-tower::manual_slot_extra_stories` | `created_object_or_structure` | The higher-slot extra-story scaling is not represented as structure scaling data. | Add created-structure slot scaling fields for story count. |
| `gaseous-form::attack_or_save_modifier` | `attack_or_save_modifier` | Gaseous Form grants saving throw advantage for three abilities, but that modifier is not typed. | Add savingThrowAdvantage fields for ability lists. |
| `gaseous-form::movement_or_repositioning` | `movement_or_repositioning` | Gaseous Form replaces movement with limited flying, hover, and overlapping creature-space movement. | Add movementOverride fields for fly speed, hover, only-movement restriction, and space-occupancy permission. |
| `gaseous-form::status_or_state_change` | `status_or_state_change` | The mist form and its action/object restrictions are not represented as state data. | Add transformationState and restrictedCapabilities fields. |
| `hunger-of-hadar::status_or_state_change` | `status_or_state_change` | The Blinded condition is represented, but its precise fully-within gating is not. | Add areaMembership condition fields such as fully_within_area. |
| `hypnotic-pattern::status_or_state_change` | `status_or_state_change` | Hypnotic Pattern's Charmed and Incapacitated statuses are represented, but Speed 0 and dependent rider semantics are not. | Add status-rider movement override fields and dependent-status relationship fields. |
| `incite-greed::social_or_knowledge_effect` | `social_or_knowledge_effect` | Incite Greed's charm imposes a specific compelled behavior that is not represented by the generic Charmed condition. | Add compelledBehavior fields for allowed actions and safe approach target. |
| `incite-greed::status_or_state_change` | `status_or_state_change` | Incite Greed applies a proximity-gated movement lock that is not represented. | Add proximity-gated movement restriction fields. |
| `intellect-fortress::attack_or_save_modifier` | `attack_or_save_modifier` | Intellect Fortress grants Intelligence, Wisdom, and Charisma saving throw advantage, but this is prose-only. | Add savingThrowAdvantage fields for ability lists. |
| `leomunds-tiny-hut::environmental_change` | `environmental_change` | Tiny Hut creates a comfortable, dry interior independent of outside weather. | Add environmentalChange fields for comfortable/dry interior and weather isolation. |
| `meld-into-stone::status_or_state_change` | `status_or_state_change` | The expulsion Prone condition is not represented. | Add conditional status fields tied to expulsion. |
| `meld-into-stone::manual_movement_limits` | `movement_or_repositioning` | Meld into Stone restricts movement except for a 5-foot movement exit. | Add movementRestriction and exitCost fields. |
| `nondetection::manual_target_types_and_size` | `target_filter_or_eligibility` | Nondetection target categories are represented, but willing creature and 10-foot maximum object/place size are not. | Add target willingness and max dimension fields for object/place targets. |
| `phantom-steed::manual_created_equipment_distance_vanish` | `resource_or_consumption` | The created tack and its 10-foot separation vanish rule are not represented. | Add createdEquipment and distance-based disappearance fields. |
| `revivify::manual_dead_creature_recent_not_old_age` | `target_filter_or_eligibility` | Revivify requires a dead creature that died within the last minute and excludes old age deaths. | Add death-state target filters for max time since death and old-age exclusion. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Transfer, Retarget, Or Reassignment

An effect ends on one target, moves to another target, or can be reassigned while the spell remains active.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `chromatic-orb::choice_or_mode` | `choice_or_mode` | Chromatic Orb requires choosing one of six damage types and can leap to additional chosen targets when two or more damage dice match, but runtime JSON stores the damage types as a slash-delimited string and has no leap mechanic. | Add chosen damage type fields and chained-target leap fields for duplicate damage dice, target distance from previous target, target uniqueness, and slot-level leap cap. |
| `chromatic-orb::attack_or_save_modifier` | `attack_or_save_modifier` | Chromatic Orb's leap requires a new attack roll and new damage roll against the new target; JSON only represents the initial attack damage. | Add secondary attack fields for conditional chained attacks. |
| `hex::conditional_ending` | `conditional_ending` | If the cursed target drops to 0 Hit Points before the spell ends, the caster can move the curse to a new creature with a later Bonus Action; this transfer rule is prose-only. | Add conditional transfer fields for trigger target_drops_to_0_hp, later bonus-action retargeting, and new creature target. |
| `hex::manual_bonus_action_curse_transfer` | `sustain_or_recast_action` | Hex grants a later bonus-action curse transfer after the current cursed target drops to 0 HP, but that follow-up action is not modeled. | Add follow-up action fields for conditional retargeting after target_drops_to_0_hp. |
| `hunters-mark::conditional_ending` | `conditional_ending` | If the marked target drops to 0 Hit Points before the spell ends, the caster can move the mark to a new visible creature with a Bonus Action; duration also scales at higher slots. | Add conditional transfer fields and duration scaling fields for Hunter's Mark. |
| `sanctuary::choice_or_mode` | `choice_or_mode` | A failed save against Sanctuary forces the attacker to choose a new target or lose the attack/spell, but runtime JSON keeps that branch in description text. | Add failed-save forced-choice fields for choose_new_target_or_lose_action. |
| `sanctuary::attack_or_save_modifier` | `attack_or_save_modifier` | Sanctuary's Wisdom save trigger for creatures targeting the warded creature is represented, but the trigger does not distinguish attack rolls from damaging spells or the area-of-effect bypass. | Add ward targeting-trigger fields for attack roll, damaging spell, and area-of-effect exclusion. |
| `sanctuary::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | Sanctuary is a ward with incoming targeting triggers and area-effect bypass; current utility effects preserve some behavior but flatten trigger scope and failed-save options. | Add ward fields for protected target, incoming trigger kinds, save, failed-save options, area bypass, and end conditions. |
| `life-transference::choice_or_mode` | `choice_or_mode` | Actionable open: Mode choices need first-class mode/option fields when a spell exposes several distinct operations. | Add a choice/mode bucket with option-specific targets/effects. |
| `life-transference::object_stats_or_damageability` | `object_stats_or_damageability` | Actionable open: Object stats, structure durability, or object transformations need explicit AC/HP/passability/damageability fields. | Add object stat/damageability bucket fields. |
| `life-transference::choice_or_mode` | `choice_or_mode` | Life Transference links a chosen visible target's healing amount to twice the actual self-damage taken, but runtime only applies damage. | Add linked healing fields that can reference actual damage taken by another effect. |
| `life-transference::object_stats_or_damageability` | `object_stats_or_damageability` | Life Transference's self-damage cannot be reduced and should be bound to the caster, but runtime does not type either constraint. | Add damageReductionBypass and effectTarget/selfCost fields. |
| `life-transference::manual_healing_twice_damage_taken` | `healing_or_restoration` | Life Transference's healing effect and its twice-actual-damage formula are not represented. | Add healingFormula fields that can reference actual damage taken from a paired effect. |
| `mass-healing-word::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing, restoration, transfer, or condition removal needs specific restoration/prevention fields beyond generic utility. | Add restoration/condition-removal/healing-transfer bucket fields. |
| `motivational-speech::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing, restoration, transfer, or condition removal needs specific restoration/prevention fields beyond generic utility. | Add restoration/condition-removal/healing-transfer bucket fields. |
| `remove-curse::conditional_ending` | `conditional_ending` | Remove Curse ends ordinary curses and has a different cursed-magic-item branch that breaks attunement without ending the item curse. | Add curseRemoval and cursedMagicItem attunement-break fields. |
| `sleet-storm::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing, restoration, transfer, or condition removal needs specific restoration/prevention fields beyond generic utility. | Add restoration/condition-removal/healing-transfer bucket fields. |
| `speak-with-dead::conditional_ending` | `conditional_ending` | Speak with Dead has a 10-day retarget failure and a five-question limit. | Add priorTargetCooldown and maxQuestions fields. |
| `stinking-cloud::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing, restoration, transfer, or condition removal needs specific restoration/prevention fields beyond generic utility. | Add restoration/condition-removal/healing-transfer bucket fields. |
| `vampiric-touch::sustain_or_recast_action` | `sustain_or_recast_action` | Until the spell ends, the caster can make the melee spell attack again on each turn as a Magic action against the same or different creature, but the current runtime does not model repeat attacks as granted actions. | Add sustained repeat-attack fields for action type, cadence, attack reuse, and target-retargeting permission. |
| `geas::summon_or_controlled_entity` | `summon_or_controlled_entity` | The target is not summoned, but it is behaviorally constrained by a command and penalized for acting counter to it; runtime lacks geas/control-obligation fields. | Track this under a control-obligation field family rather than summon fields. |
| `geas::social_or_knowledge_effect` | `social_or_knowledge_effect` | The Charmed target's behavior is constrained by a socially framed command, including service/refrain choices and understanding requirements; runtime lacks typed social-control semantics. | Add social-control command fields for compelled service or abstention. |
| `geas::healing_or_restoration` | `healing_or_restoration` | Remove Curse, Greater Restoration, or Wish can end the spell; runtime lacks typed external-spell removal/ending fields. | Add removableBySpell fields listing spells that can end a spell effect. |
| `geas::status_or_state_change` | `status_or_state_change` | Runtime represents the Charmed condition but not the once-per-day 5d10 Psychic damage when the Charmed target acts directly counter to the command. | Add command-violation damage fields with once-per-day frequency. |
| `geas::conditional_ending` | `conditional_ending` | The spell ends on suicidal commands and can be ended by specific spells; runtime lacks typed conditional ending fields for both cases. | Add conditional-ending fields for forbidden command endings and external spell removal. |
| `greater-restoration::healing_or_restoration` | `healing_or_restoration` | Greater Restoration's restoration choices are flattened into prose; runtime cannot distinguish Exhaustion removal, condition removal, curse/attunement removal, ability-score restoration, or hit-point-maximum restoration. | Add restoration-removal fields for exhaustion levels, named conditions, curses including cursed-item attunement, ability-score reductions, and hit-point-maximum reductions. |
| `modify-memory::message_or_communication` | `message_or_communication` | The caster must speak the memory description and the target must understand the caster's language; runtime lacks language/comprehension fields. | Add spoken-instruction and target-language-comprehension fields. |
| `modify-memory::healing_or_restoration` | `healing_or_restoration` | Remove Curse or Greater Restoration can restore the true memory; runtime lacks typed external-spell restoration fields. | Add removable/restorable-by-spell fields for altered memories. |
| `modify-memory::aftermath_or_memory` | `aftermath_or_memory` | The modified memory takes hold when the spell ends only if the description is complete; runtime lacks typed memory aftermath/finalization data. | Add memory finalization fields. |
| `raise-dead::attack_or_save_modifier` | `attack_or_save_modifier` | The revived target takes a -4 penalty to attacks, saves, and ability checks that decreases by 1 after each long rest; runtime has no typed decaying penalty fields. | Add decaying resurrection penalty fields. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Sustain, Range, Cover, Or Upkeep

A later action, range requirement, total-cover condition, or upkeep failure ends the spell or effect.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `dancing-lights::sustain_or_recast_action` | `sustain_or_recast_action` | Actionable open: Repeat action and sustain behavior needs a stronger granted-action/sustain schema for low-level utility cantrips. | Add sustain/reposition action fields with action cost, range leash, and recurrence. |
| `mold-earth::sustain_or_recast_action` | `sustain_or_recast_action` | Mold Earth limits non-instantaneous active effects to two and allows dismissing one as an action. | Add active effect cap and dismiss action fields. |
| `shape-water::sustain_or_recast_action` | `sustain_or_recast_action` | Shape Water limits non-instantaneous effects to two active instances and lets the caster dismiss one as an action; this is not structured. | Use active effect cap and dismiss action fields shared with Mold Earth-like cantrips. |
| `expeditious-retreat::sustain_or_recast_action` | `sustain_or_recast_action` | Expeditious Retreat immediately grants Dash and then grants Dash again as a Bonus Action until the spell ends, but runtime JSON keeps this in a generic utility description with grantedActions empty. | Add granted action fields for immediate Dash and recurring bonus-action Dash while active. |
| `goodberry::healing_or_restoration` | `healing_or_restoration` | Each Goodberry restores 1 Hit Point when eaten, but the runtime JSON has no healing effect tied to berry consumption. | Add consumable healing fields for fixed HP restoration triggered by eating a created item. |
| `unseen-servant::sustain_or_recast_action` | `sustain_or_recast_action` | Once on each caster turn, a Bonus Action can mentally command the servant to move up to 15 feet and interact with an object; runtime only stores commandCost bonus_action and objectDescription prose. | Add controlled entity command fields for frequency, action cost, mental command, movement distance, and object interaction. |
| `witch-bolt::attack_or_save_modifier` | `attack_or_save_modifier` | Witch Bolt's initial range, initial damage, later-turn Bonus Action cost, and sustain scaling have been corrected, but the automatic sustain damage is not explicitly tied to the sustained arc/target relationship. | Add sustained-link damage fields for later-turn automatic damage independent of initial hit. |
| `witch-bolt::ward_alarm_or_trigger` | `ward_alarm_or_trigger` | The sustained lightning arc is a maintained link between caster and target, but runtime JSON represents it only as concentration plus a later action damage effect. | Add sustained link fields for caster-target arc effects. |
| `crown-of-madness::social_or_knowledge_effect` | `social_or_knowledge_effect` | The mental target choice and compelled attack control affect creature behavior but are flattened into a generic utility description. | Represent this under compelled-action control rather than generic social/knowledge prose. |
| `flame-blade::sustain_or_recast_action` | `sustain_or_recast_action` | If the caster lets go of the blade it disappears, and the caster can evoke it again as a Bonus Action; this object persistence/re-evoke action is prose-only. | Add conjured-object persistence fields for disappears_on_release and reevoke action cost. |
| `flaming-sphere::sustain_or_recast_action` | `sustain_or_recast_action` | The caster can move the sphere up to 30 feet as a Bonus Action, but runtime still uses on_caster_action without a typed movable-area action model. | Add movable hazard fields for action cost, move distance, movement mode, and stop-on-creature behavior. |
| `gust-of-wind::sustain_or_recast_action` | `sustain_or_recast_action` | Changing the Line direction as a later-turn Bonus Action is prose-only. | Add later-turn action fields for changing an active area direction. |
| `spiritual-weapon::sustain_or_recast_action` | `sustain_or_recast_action` | The later-turn Bonus Action can both move the force up to 20 feet and repeat the attack from the force's position; runtime now has the bonus-action sustain cost but still lacks controlled-force movement data. | Add sustain action fields that combine moving a persistent effect and making an attack from its new position. |
| `animate-dead::sustain_or_recast_action` | `sustain_or_recast_action` | A Bonus Action can mentally command controlled creatures within 60 feet, and recasting before 24 hours reasserts control; runtime only stores this as prose. | Add controlled-summon command action and recast-to-maintain-control fields. |
| `call-lightning::manual_repeat_magic_action_strike` | `sustain_or_recast_action` | Call Lightning's repeat Magic action to call another bolt is not represented. | Add sustain/reuse action fields for Magic action repeat strikes. |
| `clairvoyance::sustain_or_recast_action` | `sustain_or_recast_action` | Switching between seeing and hearing is a Bonus Action; runtime prose was corrected, but no typed action field exists. | Add sensor mode switch action fields. |
| `melfs-minute-meteors::sustain_or_recast_action` | `sustain_or_recast_action` | Actionable open: Repeat actions, recasts, and sustain behavior need stronger action-economy fields. | Add sustain/recast action bucket fields. |
| `melfs-minute-meteors::sustain_or_recast_action` | `sustain_or_recast_action` | Melfs Minute Meteors has an initial spend and repeat bonus-action spend loop that is not represented. | Add initialAndSustainAction fields for expendable spell instances. |
| `tiny-servant::sustain_or_recast_action` | `sustain_or_recast_action` | Actionable open: Repeat actions, recasts, and sustain behavior need stronger action-economy fields. | Add sustain/recast action bucket fields. |
| `tiny-servant::sustain_or_recast_action` | `sustain_or_recast_action` | Tiny Servant uses a recurring bonus action to command one or multiple servants. | Add summon command action fields including multi-servant same-command rule. |
| `grasping-vine::sustain_or_recast_action` | `sustain_or_recast_action` | The Bonus Action repeat attack is a sustained spell action from the vine's position; runtime lacks repeat action semantics. | Add sustained/repeat attack fields for bonus-action repeats from created entities. |
| `storm-sphere::choice_or_mode` | `choice_or_mode` | The caster chooses a creature within 60 feet of the sphere's center for the bonus-action lightning bolt each turn; runtime represents the bonus-action attack but lacks a typed secondary-origin target-choice field. | Add sustained secondary-origin target choice fields for effects emitted from created areas or objects. |
| `storm-sphere::sustain_or_recast_action` | `sustain_or_recast_action` | The bonus-action lightning bolt is now represented as an on_caster_action damage effect, but the schema cannot fully express that the attack originates from the sphere center and targets within 60 feet of that secondary origin. | Add sustained action fields for secondary origin, per-turn action cost, target range from origin, and attack type. |
| `bigbys-hand::sustain_or_recast_action` | `sustain_or_recast_action` | On cast and as a bonus action later, the caster can move the hand up to 60 feet and choose an effect; while grappling, a bonus action can crush the target. Runtime lacks this sustained action economy. | Add sustained created-entity action fields for move-plus-mode and grapple-crush action. |
| `danse-macabre::sustain_or_recast_action` | `sustain_or_recast_action` | The caster can use a bonus action to mentally command all created undead within 60 feet with the same command; runtime lacks command action economy and command range fields. | Add controlled-creature command fields for bonus action, mental command, same-command multi-control, and range. |
| `dawn::sustain_or_recast_action` | `sustain_or_recast_action` | If the caster is within 60 feet of the cylinder, they can move it up to 60 feet as a bonus action; runtime lacks created-area reposition action fields. | Add sustained area movement fields for controller range, action cost, and movement distance. |
| `enervation::attack_or_save_modifier` | `attack_or_save_modifier` | Runtime has the initial Dexterity save but flattens success as half damage; canonical success is 2d8 and spell ends, while failure is 4d8 and enables sustained damage. | Add save-branch damage fields with different success/failure dice and branch outcomes. |
| `enervation::sustain_or_recast_action` | `sustain_or_recast_action` | After a failed save, the caster can use an action each turn to automatically deal 4d8 Necrotic damage; the spell ends if the caster uses the action for anything else. Runtime lacks sustained action and missed-action ending fields. | Add sustained damage action fields with required action each turn and fail-to-sustain ending. |
| `enervation::conditional_ending` | `conditional_ending` | The spell ends on successful initial save, on caster using an action for anything else, when target leaves range, or when target has total cover; runtime lacks these branch and ending conditions. | Add branch-ending and sustain-ending fields for save success, action use, range loss, and total cover. |
| `far-step::sustain_or_recast_action` | `sustain_or_recast_action` | On each later turn before the spell ends, the caster can use a bonus action to teleport again; runtime has the teleport effect but lacks repeat granted-action semantics. | Add repeat teleport action fields for bonus-action reuse while active. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Object Stats, Damageability, Or Capacity

Created or affected objects need hit points, AC, damageability, capacity, size limits, or structural state.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `chill-touch::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing prevention, stabilization, or condition/restoration removal needs explicit restoration/prevention schema beyond generic utility text. | Add restoration, condition-removal, and healing-prevention buckets. |
| `mending::environmental_change` | `environmental_change` | Mending repairs object damage with a one-foot size limit and a magic-item caveat, but structured and JSON only store a generic utility description. | Add repair utility fields for break/tear repair, max damaged dimension, and magic-item-magic-not-restored caveat. |
| `toll-the-dead::object_stats_or_damageability` | `object_stats_or_damageability` | Toll the Dead switches from d8 to d12 damage when the target is missing any Hit Points; JSON only preserves this in scaling prose while damage.dice remains 1d8. | Add conditional damage dice fields based on target missing HP. |
| `armor-of-agathys::healing_or_restoration` | `healing_or_restoration` | Actionable open: Healing prevention, stabilization, or condition/restoration removal needs explicit restoration/prevention schema beyond generic utility text. | Add restoration, condition-removal, and healing-prevention buckets. |
| `cure-wounds::object_stats_or_damageability` | `object_stats_or_damageability` | Cure Wounds heals 2d8 plus the caster's spellcasting ability modifier, but runtime healing only stores dice 2d8. | Add healing formula fields for spellcasting ability modifier and slot-scaling dice. |
| `heroism::healing_or_restoration` | `healing_or_restoration` | Heroism grants recurring Temporary Hit Points, but runtime JSON does not represent them as healing or temporary HP data. | Add temporary-HP fields that support spellcasting ability modifier amounts and recurring timing. |
| `hunters-mark::attack_or_save_modifier` | `attack_or_save_modifier` | Hunter's Mark's extra Force damage is represented, but Advantage on Wisdom (Perception or Survival) checks to find the marked target is only generic utility prose. | Add ability-check modifier fields for skill-specific Advantage against a marked target. |
| `unseen-servant::movement_or_repositioning` | `movement_or_repositioning` | The servant can be commanded to move up to 15 feet, but movement is only in objectDescription prose. | Add controlled entity movement fields for commanded movement distance. |
| `unseen-servant::created_object_or_structure` | `created_object_or_structure` | The servant can interact with objects and perform simple tasks including fetching, cleaning, mending, folding, lighting fires, serving food, and pouring drinks, but allowed task categories are prose-only. | Add controlled task fields for simple humanlike tasks and object interaction. |
| `enlarge-reduce::manual_carried_items_resize_and_dropped_items_revert` | `object_stats_or_damageability` | The spell changes carried equipment size and has immediate reversion rules for dropped/thrown items, but those object-state transitions are prose-only. | Add equipment size propagation and item reversion fields. |
| `rope-trick::manual_extradimensional_space_capacity` | `created_object_or_structure` | Rope Trick creates an extradimensional space with explicit portal size, access, capacity, and occupant size limits, but no structured/runtime field preserves those parts. | Add created/extradimensional structure fields for portal aperture, capacity count, allowed occupant size, and access method. |
| `warding-bond::attack_or_save_modifier` | `attack_or_save_modifier` | The +1 AC bonus is represented, but the +1 saving throw bonus while within 60 feet is not first-class in runtime JSON. | Add saving throw bonus fields parallel to AC bonus fields. |
| `beacon-of-hope::attack_or_save_modifier` | `attack_or_save_modifier` | The spell grants Advantage on Wisdom saving throws and Death Saving Throws, but runtime stores this in prose only. | Add saving throw advantage fields scoped by save category. |
| `gaseous-form::terrain_or_surface` | `terrain_or_surface` | Actionable open: Terrain/surface rules need stronger terrain, surface, passability, and environmental-state fields when not already covered by TERRAIN. | Add terrain/surface bucket fields. |
| `haste::attack_or_save_modifier` | `attack_or_save_modifier` | Haste's +2 AC is represented, but its speed, save advantage, and limited extra action remain prose-only. | Add movement multiplier, saving throw advantage, and limited extra-action fields. |
| `mass-healing-word::resource_or_consumption` | `resource_or_consumption` | Mass Healing Word's spellcasting ability modifier addend is not typed. | Add healing ability modifier addend fields. |
| `meld-into-stone::terrain_or_surface` | `terrain_or_surface` | The stone surface/object requirement is only partially represented and lacks the body-capacity constraint. | Add surface/object material and capacity fields. |
| `motivational-speech::object_stats_or_damageability` | `object_stats_or_damageability` | Actionable open: Object stats, structure durability, or object transformations need explicit AC/HP/passability/damageability fields. | Add object stat/damageability bucket fields. |
| `motivational-speech::attack_or_save_modifier` | `attack_or_save_modifier` | Motivational Speech grants Wisdom save advantage and a conditional next attack-roll advantage after being hit, but both are prose-only. | Add save advantage and hit-triggered next attack advantage fields. |
| `motivational-speech::healing_or_restoration` | `healing_or_restoration` | Motivational Speech grants and scales temporary hit points, but runtime has no typed temporary HP effect. | Add temporary hit point amount and scaling fields. |
| `slow::object_stats_or_damageability` | `object_stats_or_damageability` | Actionable open: Object stats, structure durability, or object transformations need explicit AC/HP/passability/damageability fields. | Add object stat/damageability bucket fields. |
| `slow::attack_or_save_modifier` | `attack_or_save_modifier` | Slow has multiple debuffs that are not typed: speed halving, AC penalty, Dexterity save penalty, and one-attack limit. | Add movement multiplier, AC penalty, save penalty, and action/attack limit fields. |
| `tiny-servant::object_stats_or_damageability` | `object_stats_or_damageability` | Actionable open: Object stats, structure durability, or object transformations need explicit AC/HP/passability/damageability fields. | Add object stat/damageability bucket fields. |
| `wall-of-water::created_object_or_structure` | `created_object_or_structure` | The straight and ringed wall forms are typed, but cold-created frozen wall sections with AC, HP, destruction, and non-refill gaps are not first-class structure states. | Add destructible section fields for created or transformed wall portions. |
| `grasping-vine::status_or_state_change` | `status_or_state_change` | Grappled is typed with the correct spell-save escape DC, but the Huge-or-smaller eligibility, one-creature grapple limit, free release, and higher-level grapple capacity are not first-class. | Add grapple rider fields for size limit, capacity, free release, and capacity scaling. |
| `mordenkainens-faithful-hound::object_stats_or_damageability` | `object_stats_or_damageability` | The hound is intangible and invulnerable, but runtime lacks typed durability and physicality semantics. | Add created-entity physicality and invulnerability fields. |
| `otilukes-resilient-sphere::target_filter_or_eligibility` | `target_filter_or_eligibility` | The target must be Large or smaller, and runtime lacks this size filter. | Add target size eligibility fields for creature/object targets. |
| `bigbys-hand::object_stats_or_damageability` | `object_stats_or_damageability` | The hand has AC 20, HP equal to the caster's hit point maximum, Strength 26, Dexterity 10, does not fill its space, and the spell ends at 0 HP; runtime lacks typed object stats and damageability. | Add created-object stat and durability fields with formulas. |
| `enervation::healing_or_restoration` | `healing_or_restoration` | Whenever the spell deals damage, the caster regains hit points equal to half the Necrotic damage taken; runtime lacks linked life-drain healing fields. | Add damage-linked healing fields for half damage dealt. |
| `transmute-rock::object_stats_or_damageability` | `object_stats_or_damageability` | The restraining rock's AC 15, 25 HP, and poison/psychic immunity are not represented. | Add restraint-object stats fields. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## End Cleanup: Space Or Passage Ejection

Spell-created spaces, passages, or containers end and move their contents to an exit, anchor, nearest space, or other explicit destination.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `blink::choice_or_mode` | `choice_or_mode` | Blink lets the caster choose the return space within 10 feet, with fallback to the nearest unoccupied space; runtime stores this in prose only. | Add return-placement choice fields for visible unoccupied space within range and fallback nearest space. |
| `blink::target_filter_or_eligibility` | `target_filter_or_eligibility` | The return requires a visible unoccupied space within 10 feet or nearest unoccupied fallback; no structured/runtime placement filter exists. | Add unoccupied visible destination eligibility fields. |
| `blink::travel_or_planar_movement` | `travel_or_planar_movement` | Blink uses an end-of-turn 1d6 roll, shifts the caster to the Ethereal Plane on 4-6, ends instantly if already there, and returns the caster at start of next turn; runtime only has corrected prose. | Add recurring planar shift fields for die roll, success range, destination plane, already-on-plane ending, return timing, and return placement. |
| `meld-into-stone::target_filter_or_eligibility` | `target_filter_or_eligibility` | The spell requires a stone object or surface large enough to contain the caster and later expels to the nearest unoccupied entry-adjacent space. | Add stone object/surface capacity targeting and expulsion destination fields. |
| `banishment::choice_or_mode` | `choice_or_mode` | If an extraplanar target does not return, it is transported to a random location on a plane chosen by the DM and associated with its creature type; this destination choice is prose-only. | Add planar destination choice fields for random location, DM-chosen plane, and creature-type association. |
| `banishment::travel_or_planar_movement` | `travel_or_planar_movement` | Targets are transported to a harmless demiplane, may reappear at the original or nearest unoccupied space, and certain creature types can be sent permanently to an associated plane; runtime only stores Banished/Incapacitated statuses. | Add planar transport fields for temporary demiplane, return placement, permanent non-return, creature-type filter, and associated-plane destination. |
| `banishment::conditional_ending` | `conditional_ending` | Aberration, Celestial, Elemental, Fey, and Fiend targets do not return if the spell lasts for 1 minute; runtime has no typed duration-completion branch by creature type. | Add conditional ending fields for no-return-on-full-duration with creature-type filters. |
| `banishment::aftermath_or_memory` | `aftermath_or_memory` | When the spell ends, the target reappears in its original space or nearest unoccupied space, unless the creature-type full-duration branch prevents return; runtime lacks return placement. | Add post-spell return placement fields for original space and nearest unoccupied fallback. |
| `watery-sphere::choice_or_mode` | `choice_or_mode` | A creature that succeeds on the initial save chooses the nearest unoccupied exit space, a Large or smaller creature can choose to fail, and overflow ejection randomly chooses an already restrained creature; runtime lacks these choice/random-selection branches. | Add save-result placement choice, voluntary failure, and random overflow ejection fields. |
| `watery-sphere::attack_or_save_modifier` | `attack_or_save_modifier` | The initial and repeat Strength saves are typed, but Huge-or-larger automatic success, Large-or-smaller voluntary failure, and ram-forced saves are not first-class. | Add size-based save overrides, voluntary save failure, and ram-triggered save fields. |
| `watery-sphere::target_filter_or_eligibility` | `target_filter_or_eligibility` | The sphere can restrain up to four Medium-or-smaller creatures or one Large creature, while Huge-or-larger creatures automatically succeed; runtime lacks capacity and size-filter fields. | Add area capacity fields by target size and auto-success size filters. |
| `watery-sphere::sustain_or_recast_action` | `sustain_or_recast_action` | As an action, the caster can move the sphere up to 30 feet in a straight line and ram it into creatures to force saves; runtime lacks granted movement/ram action data. | Add sustained created-area movement fields for action cost, distance, straight-line restriction, carried restrained targets, and ram trigger. |
| `watery-sphere::movement_or_repositioning` | `movement_or_repositioning` | The sphere hovers no more than 10 feet above the ground, safely descends over drop-offs, moves restrained creatures with it, and falls when the spell ends; runtime lacks created-sphere movement and vertical constraint fields. | Add created-area movement/hover fields for height cap, drop-off descent, carried occupants, and spell-end fall. |
| `watery-sphere::conditional_ending` | `conditional_ending` | The spell-end sequence makes the sphere fall, extinguishes normal flames within 30 feet, knocks restrained creatures prone where it falls, and then makes the water vanish; runtime lacks typed spell-end cleanup sequence fields. | Add ordered spell-end cleanup fields for created areas with occupant and environmental effects. |
| `banishing-smite::attack_or_save_modifier` | `attack_or_save_modifier` | The hit-triggered Force damage, Charisma save, Banished status, and Incapacitated status are now represented, but the banishment save only occurs if the attack reduces the target to 50 Hit Points or fewer; runtime lacks a typed HP-threshold gate for the save/status branch. | Add hit-result HP-threshold gating fields for riders. |
| `banishing-smite::travel_or_planar_movement` | `travel_or_planar_movement` | The target can be transported to a harmless demiplane and later return to its original or nearest unoccupied space; runtime has Banished/Incapacitated but no typed demiplane transport or return placement data. | Add planar/demiplane transport and return placement fields. |
| `banishing-smite::conditional_ending` | `conditional_ending` | When the spell ends, the target reappears in its original space or nearest unoccupied fallback; runtime lacks typed spell-end return placement. | Add spell-end return placement fields for banishment effects. |
| `banishing-smite::aftermath_or_memory` | `aftermath_or_memory` | The spell-end return leaves the target in its original or nearest unoccupied space, but runtime lacks typed post-effect placement state. | Add aftermath placement fields for post-banishment return. |
| `passwall::choice_or_mode` | `choice_or_mode` | The caster chooses opening dimensions up to 5 feet wide, 8 feet tall, and 20 feet deep; runtime has only a coarse cube size. | Add opening dimension fields for width, height, and depth maxima. |
| `passwall::target_filter_or_eligibility` | `target_filter_or_eligibility` | The target point must be on a wooden, plaster, or stone surface, and later ejection goes to the nearest unoccupied space; runtime lacks material/surface and ejection-destination fields. | Add surface material eligibility and ejection destination fields. |
| `passwall::terrain_or_surface` | `terrain_or_surface` | The wooden/plaster/stone wall, ceiling, or floor surface requirement remains prose-only. | Add passage surface fields for allowed materials and orientations. |
| `passwall::created_object_or_structure` | `created_object_or_structure` | Runtime now marks the utility as creation, but the passwall opening's dimensions, no-instability rider, and safe ejection behavior are still prose-only. | Add passage-creation fields for dimensions, structural stability, and ejection on disappearance. |
| `teleportation-circle::choice_or_mode` | `choice_or_mode` | The destination circle must be chosen from known same-plane sigil sequences; runtime lacks destination choice and known-sigil fields. | Add teleportation-circle destination/sigil fields. |
| `teleportation-circle::target_filter_or_eligibility` | `target_filter_or_eligibility` | Arrival is within 5 feet of the destination circle or nearest unoccupied space if occupied; runtime lacks arrival-space fallback fields. | Add arrival destination and fallback occupancy fields. |
| `teleportation-circle::travel_or_planar_movement` | `travel_or_planar_movement` | The portal travel, same-plane restriction, enter-portal trigger, and arrival placement remain prose-only. | Add portal travel fields. |
| `investiture-of-stone::target_filter_or_eligibility` | `target_filter_or_eligibility` | Ejection to the nearest unoccupied space when the caster ends movement inside earth or stone is described in runtime, but there is no first-class nearest-unoccupied-space destination field. | Add forced relocation destination fields for nearest unoccupied space and invalid end-position handling. |
| `investiture-of-stone::terrain_or_surface` | `terrain_or_surface` | Earthquake ground radius, ignored earth/stone difficult terrain, and movement through solid earth/stone are represented in structured/runtime prose, but runtime lacks first-class ignore-terrain-cost and move-through-solid-material fields. | Add ignore-terrain-cost and move-through-solid-material fields. |
| `investiture-of-stone::conditional_ending` | `conditional_ending` | The ordinary 'Until the spell ends' duration is represented, but the special ending when the caster ends movement inside earth or stone is only described in prose. | Add conditional spell-ending triggers tied to invalid end positions. |
| `word-of-recall::target_filter_or_eligibility` | `target_filter_or_eligibility` | Creatures appear in nearest unoccupied spaces near a prepared sanctuary; runtime represents teleport movement and options but lacks first-class sanctuary preparation and nearest-unoccupied placement fields. | Add prepared sanctuary and nearest-unoccupied destination fields. |
| `word-of-recall::travel_or_planar_movement` | `travel_or_planar_movement` | Teleport to a previously designated sanctuary is represented as MOVEMENT teleport, but runtime lacks first-class prepared-sanctuary state and no-effect-if-unprepared behavior. | Add prepared destination and no-effect-if-missing-prerequisite fields. |

_Only the first 30 findings are shown in markdown for reviewability. See the JSON report for the full group._

## Save Counter Or Recurring End

Recurring saves, counted successes/failures, branch outcomes, or escape checks end or transform an ongoing effect.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `shocking-grasp::manual_opportunity_attack_suppression` | `reaction_or_opportunity_restriction` | The core mechanic is specifically Opportunity Attack suppression until the start of the target's next turn; the current status condition name is broader than the prose and carries inappropriate default escape/repeat-save data. | Add reaction/opportunity restriction fields for prohibited reaction subtype, hit condition, and until-start-of-target-next-turn duration, with not_applicable sentinels for escape and repeat saves. |
| `animal-friendship::social_or_knowledge_effect` | `social_or_knowledge_effect` | Animal Friendship applies the Charmed condition to a Beast for 24 hours, but the status runtime includes inappropriate default repeat-save data that is not in the canonical prose. | Add explicit not-applicable sentinels for repeat saves and escape checks on status effects that do not have them. |
| `charm-person::attack_or_save_modifier` | `attack_or_save_modifier` | Charm Person represents the Wisdom save and combat advantage modifier, but the status effect still carries inappropriate default repeat-save/escape placeholders not present in the prose. | Add not_applicable sentinels for repeat saves and escape checks on one-save status effects. |
| `searing-smite::attack_or_save_modifier` | `attack_or_save_modifier` | Searing Smite's recurring Constitution save occurs after start-of-turn fire damage and ends the spell on success; the initial hit now applies the Ignited state without an incorrect initial save, but recurring damage/save sequencing still lacks first-class fields. | Add recurring damage plus post-damage save fields with success_ends semantics. |
| `sleep::attack_or_save_modifier` | `attack_or_save_modifier` | Sleep now represents the initial Wisdom save, Incapacitated first stage, and a status-gated Unconscious second stage, but the exact branch timing remains approximated by generic repeat-save fields. | Add staged save fields for initial save, repeat save at end of next turn, success/failure branches, and escalation. |
| `web::status_or_state_change` | `status_or_state_change` | Restrained and the Strength (Athletics) escape action are represented, and the incorrect repeat save was removed; the remaining gap is the status duration while in webs or until escape. | Add status duration fields for while_in_area_or_until_escape. |
| `bestow-curse::attack_or_save_modifier` | `attack_or_save_modifier` | The initial Wisdom save is now represented, but the selected curse options include multiple d20 modifiers and a recurring Wisdom save that remain prose-only. | Add option-scoped attack/save/check modifier fields and recurring save fields. |
| `conjure-animals::attack_or_save_modifier` | `attack_or_save_modifier` | The caster gains Advantage on Strength saves near the pack, and creatures can be forced to make Dexterity saves from several once-per-turn pack proximity triggers; runtime lacks both the save advantage and the damage trigger model. | Add aura-adjacent save advantage fields and pack proximity damage trigger fields. |
| `enemies-abound::attack_or_save_modifier` | `attack_or_save_modifier` | The initial Intelligence save is now represented, but the repeat save whenever the target takes damage remains prose-only. | Add damage-triggered repeat save fields. |
| `fast-friends::attack_or_save_modifier` | `attack_or_save_modifier` | The initial Wisdom save and Charmed condition are represented, and inappropriate default repeat-save data was removed, but harm/conflict-triggered Wisdom save with advantage while fighting is not typed. | Add conditional repeat save fields for harmful/conflicting tasks and fighting advantage. |
| `fear::status_or_state_change` | `status_or_state_change` | Frightened is represented and the fake escape check was removed, but dropping held items and forced Dash-away behavior are not typed. | Add item-drop and forced behavior fields for drop held objects and Dash away by safest route. |
| `feign-death::status_or_state_change` | `status_or_state_change` | Blinded and Incapacitated are represented and fake escape/repeat saves were removed, but cataleptic false-death state and Speed 0 are not typed. | Add state fields for cataleptic/apparent death and speed set to 0. |
| `control-water::forced_movement` | `forced_movement` | Whirlpool pulls creatures in water within 25 feet 10 feet toward it, and swimming away requires an action plus Strength (Athletics) check; runtime has no typed pull or escape check for this mode. | Add environmental forced movement fields for pull distance, radius, medium requirement, and escape action/check. |
| `elemental-bane::attack_or_save_modifier` | `attack_or_save_modifier` | The target must fail a Constitution save before the duration effects apply; runtime still stores this as utility prose rather than a typed save-gated ongoing effect. | Add save-gated ongoing debuff fields for effects that are not standard conditions. |
| `conjure-elemental::attack_or_save_modifier` | `attack_or_save_modifier` | The spirit forces Dexterity saves when a visible creature enters its space or starts its turn within 5 feet, only if the spirit has no creature Restrained; runtime lacks these trigger filters and occupancy restriction. | Add created-hazard save trigger fields for enter-space, start-turn-nearby, visible target, and no-current-restrained-target gating. |
| `conjure-elemental::status_or_state_change` | `status_or_state_change` | Failed saves apply Restrained until the spell ends, with start-turn repeat saves and one-creature capacity; runtime lacks the Restrained status and linked repeat-save damage branch. | Add linked status/rider fields for restrained-by-created-spirit, capacity, repeat saves, and failed-repeat damage. |
| `contagion::attack_or_save_modifier` | `attack_or_save_modifier` | The initial Constitution save, damage, and Poisoned condition are typed, but runtime lacks the chosen-ability save disadvantage, three-success/three-failure progression, and save-to-resist condition removal. | Add linked save-disadvantage rider fields, counted repeat-save tracks, and condition-removal resistance checks. |
| `dominate-person::attack_or_save_modifier` | `attack_or_save_modifier` | The Wisdom save, Advantage when the caster or allies are fighting the target, and on-damage repeat save are still prose-only in runtime. | Add domination save fields for initial save, combat-state advantage, and on-damage repeat save. |
| `dominate-person::status_or_state_change` | `status_or_state_change` | The target gains Charmed for the duration, with repeat save on damage and domination riders; runtime lacks typed Charmed effect and status riders. | Add Charmed status plus domination rider fields. |
| `immolation::conditional_ending` | `conditional_ending` | Runtime represents successful repeat save ending, but it still lacks typed data for the flames being impossible to extinguish by nonmagical means. | Add extinguishability or ending-prevention fields for magical ongoing effects. |
| `synaptic-static::attack_or_save_modifier` | `attack_or_save_modifier` | Initial damage and repeat save are represented, but the d6 penalty to attack rolls, ability checks, and Constitution concentration saves remains only implicit in the Muddled Thoughts status. | Add multi-surface d6 penalty fields for attack rolls, ability checks, and concentration saves. |
| `wall-of-light::attack_or_save_modifier` | `attack_or_save_modifier` | The initial Constitution save and Blinded save branch are represented, but the end-turn damage branch and action-launched ranged spell attack branch remain flattened into prose. | Add separate recurring area-damage and wall-beam attack branches. |
| `ottos-irresistible-dance::attack_or_save_modifier` | `attack_or_save_modifier` | The initial Wisdom save, repeat save, attack-roll disadvantage, and Dexterity-save disadvantage are represented, but runtime lacks first-class saving-throw disadvantage and attacker-advantage-against-target fields. | Add disadvantage-on-save and advantage-to-attackers fields. |
| `power-word-pain::conditional_ending` | `conditional_ending` | The pain ends on a successful end-of-turn Constitution save, but runtime currently stores that in status aftermath/utility options rather than a typed recurring end condition. | Add recurring save end-condition fields. |
| `earthquake::manual_level8_actionable_schema_gap` | `terrain_or_surface` | Actionable open: Earthquake needs random fissure counts, variable fissure dimensions, structure height-derived collapse radius, and escape checks; current schema preserves these in options but not fully as data. | Add random-count/random-dimension, structure-collapse radius, and escape-check fields for environmental hazards. |
| `feeblemind::manual_level8_actionable_schema_gap` | `status_or_state_change` | Actionable open: Feeblemind needs ability-score set-to-1 fields, spellcasting/magic-item/language/communication lockout fields, 30-day repeat saves, and explicit ending spells; current schema preserves these as utility/status text. | Add ability-score override, capability lockout, long-interval repeat save, and ending-spell-list fields. |
| `tsunami::manual_level8_actionable_schema_gap` | `environmental_change` | Actionable open: Tsunami needs a moving-wall hazard schema with per-round height decay, damage dice decay, once-per-round damage gate, size-filtered ongoing damage, swim escape check, and height-zero ending. | Add moving-wall hazard, per-round decay, size filter, once-per-round gate, swim check, and height-ending fields. |

## Illusion, Disguise, Or Reveal

Illusion/disguise behavior, reveal conditions, study actions, physical interaction, or apparent state needs runtime data.

| Finding | Bucket | Issue | Recommended Template Change |
| --- | --- | --- | --- |
| `mold-earth::illusion_or_disguise` | `illusion_or_disguise` | Mold Earth can create shapes, colors, words, images, and patterns on dirt or stone for 1 hour. | Add surface-marking fields for text/image/pattern/color changes and duration. |
| `identify::social_or_knowledge_effect` | `social_or_knowledge_effect` | Identify reveals item properties, usage, attunement requirement, charges, active spells, creator spell name, and creature-affecting spells, but runtime JSON stores this as one information description. | Add identify-style knowledge payload fields for properties, usage, attunement, charges, active spells, creator spell, and creature active-spell list. |
| `illusory-script::social_or_knowledge_effect` | `social_or_knowledge_effect` | Illusory Script controls which creatures know the true message and which see unintelligible or altered text, but those knowledge partitions are prose-only. | Add viewer knowledge partition fields for designated creatures, other creatures, and Truesight readers. |
| `find-traps::social_or_knowledge_effect` | `social_or_knowledge_effect` | The spell reveals trap presence and general nature of the danger, but not exact location; this knowledge payload is not first-class. | Add detection payload fields for presence_only, no_location, and general_danger_nature. |
| `mirror-image::illusion_or_disguise` | `illusion_or_disguise` | The spell creates three duplicates in the caster's space that move with the caster and mimic actions, but duplicate count, following, mimicry, and indistinguishability are not structured. | Add illusion duplicate fields for count, location, follows caster, mimics actions, and indistinguishable behavior. |
| `phantasmal-force::terrain_or_surface` | `terrain_or_surface` | The phantasm can represent dangerous terrain, creatures, objects, or hazards, but the represented hazard type and area are not first-class. | Add phantasm representation fields for object/creature/phenomenon/hazard and max 10-foot Cube. |
| `dream::illusion_or_disguise` | `illusion_or_disguise` | The messenger can shape the dream environment with landscapes, objects, and images; runtime lacks typed dream illusion/environment-shaping fields. | Add dream environment shaping fields. |
| `teleportation-circle::status_or_state_change` | `status_or_state_change` | Committing a new sigil sequence to memory after study is a knowledge-state change not represented in runtime. | Add learned-sigil state fields. |
| `mental-prison::conditional_ending` | `conditional_ending` | The spell ends on successful initial save and after breach-trigger damage; runtime records the damage but lacks first-class conditional spell-ending triggers. | Add conditional spell-ending triggers for save success and breach events. |
