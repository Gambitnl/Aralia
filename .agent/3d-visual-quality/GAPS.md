# 3D Visual Quality — Gap Registry

Issues found during visual quality work that are real but out of scope.
These should be addressed in a follow-up pass, not during this visual quality sprint.

> Rule: Record immediately. Do not fix. Do not get distracted. Review at end.

## Gaps

| # | Issue | Found During | File(s) | Impact | Why Out of Scope |
|---|-------|-------------|---------|--------|------------------|
| 1 | SSAO "NormalPass" console error spam | Baseline audit | BattleMap3D.tsx PostProcessingStack | 8+ error lines per render cycle. Non-crashing but noisy. | Investigated in Task #6 — if it can't be fixed there, becomes a gap to revisit. |
| 2 | CombatView pop-out window doesn't support 3D toggle | Baseline audit | CombatView.tsx lines 445-465 | Pop-out WindowFrame renders BattleMap3D but the toggle state isn't synced if user switches while popped out | UX issue, not visual quality |
| 3 | Duplicate React key warnings in console | Baseline audit | Multiple components | "Encountered two children with the same key" — ~26 errors on page load | Pre-existing bug, not related to 3D rendering |
| 4 | Terrain click handler doesn't account for height offset | Baseline audit | TerrainMesh.tsx line 270-271 | `Math.floor(px / TILE_SIZE)` ignores that heightfield displaces vertices, so clicks on steep slopes may register on wrong tile | Functional bug, not visual |
| 5 | ~~BattleMap3D biome detection broken~~ **RESOLVED 2026-06-07** | Baseline audit / 2026-06-07 sweep | BattleMap3D.tsx | BattleMap3D read `mapData.biome` (a field that doesn't exist — generator sets `mapData.theme`), so it ALWAYS fell back to 'forest'. Every biome rendered with forest lighting/fog/sky/apron. **Fixed** to read `mapData.theme`. This was in-scope (it's THE cause of "biomes all look the same") so fixed, not parked. Verified via biome sweep. | — |
| 6 | `preview_screenshot` (MCP preview tools) hang on the 3D scene | 2026-06-07 atmosphere re-pass | preview/tooling, not app code | The preview tab runs backgrounded (`document.hidden===true`), so the browser pauses `requestAnimationFrame`. R3F's render loop is rAF-driven, and the screenshot tool waits for a frame that never comes → 30s timeout on every capture. In-page `__cap()` (toDataURL within rAF) works but is context-heavy. Built `captures/shoot.mjs` (headless Playwright) as the reliable path. | Verification tooling, not visual quality. Workaround in place; a permanent fix (force visibility / `frameloop` fallback / a preview screenshot mode that forces a compositor frame) is its own task. |
| 7 | Quick Start party gen requires Ollama and takes 60-90s | 2026-06-07 capture pipeline | useGameInitialization.handleSkipCharacterCreator | Headless capture must wait for 3 sequential Ollama companion generations; if Ollama is down, no party → can't reach Battle Map Demo. Slows automated capture and makes it environment-dependent. | Dev/test ergonomics, not visual. A deterministic no-AI dev party would make 3D capture fast and offline-safe. |
| 8 | GOAL/Phase-2 premise "terrain flat, no grain" is stale | 2026-06-07 atmosphere re-pass | docs vs `TerrainMesh.tsx` | The goal doc and Phase 2 assessment say terrain has no texture; the code has a full procedural PBR terrain shader. Misleads agents into re-doing existing work. | Doc accuracy. Corrected in TRACKER Phase 2.5 notes; GOAL doc itself should be refreshed against current code. |
| 9 | ~~Open biomes show a hard map-edge into a flat void band~~ **RESOLVED 2026-06-07** | 2026-06-07 desert capture | `BattleMap3D.tsx` (ground apron) | On open biomes the terrain plane ended in a cliff over the sky void. **Fixed:** added a 260×260 biome-fog-colored ground apron at y=-0.15 (BattleMap3D.tsx). apron-desert.png confirms ground now extends to a soft horizon. Was in-scope after all (it gates desert atmosphere) so fixed rather than parked. | — |
| 10 | ~~Stray flagstone on desert sand~~ **NOT A BUG (2026-06-07)** | 2026-06-07 desert capture | `battleMapGenerator.ts` | Re-checked the generator: desert assigns only `sand` / `rock` (noiseValue>0.4), never `floor`. The dark patches in baseline-desert.png are `rock` outcrops (legit desert terrain) overlaid with the neutral GridOverlay lines — not misplaced flagstone. Screenshot misread. No change. | — |
| 11 | ~~Sky/horizon seam after ground apron~~ **RESOLVED 2026-06-07** | 2026-06-07 apron pass | `BattleMap3D.tsx` SkyDome | Fixed by setting each biome's SkyDome `horizon` = its fog color (task 38). apron→horizon→sky now blends. | — |
| 12 | ~~Stray grass in cave/dungeon~~ **NOT A BUG (2026-06-07)** | 2026-06-07 biome sweep | `GridOverlay.tsx` / `GrassLayer.tsx` | Re-checked: `GrassLayer` and `GroundScatter` only render on `terrain==='grass'||'difficult'`; the generator gives cave/dungeon only `wall`/`floor`, so neither renders there. The green tiles in biomefix-cave/dungeon.png are the **valid-move grid highlight** (`GridOverlay` paints valid moves green: vec3(0.13,0.67,0.27)) around the active character in move mode — expected combat UI, not grass. Screenshot misread. No change. | — |
| 17 | (Confirmed OK) Biome decoration mapping correct | 2026-06-07 generator check | `battleMapGenerator.ts` placeObstacles | GOAL #50 cited "swamp renders saguaro cacti (desert assets)." Verified the obstacle config is now biome-correct: forest=trees/bushes/logs, cave=stalagmites, dungeon=pillars, desert=cacti, swamp=mangroves/bushes/stumps. The #50 bug is already resolved. | — (no action; recorded so the GOAL item isn't re-investigated) |
| 13 | ~~Dungeon columns ~10x character height~~ **NOT A BUG (2026-06-07)** | 2026-06-07 biome sweep | `DecorationProps.tsx` createPillarGeometry | Investigated: shaft 6.5 + caps ≈ 7 units, × 0.8-1.2 instance variation = ~5.6-8.4 units vs ~2.3-unit characters = **~2.4-3.7× = within GOAL #48's 3-4× target.** The "towering" look in biomefix-dungeon.png was camera framing (columns extend above the frame), not wrong scale. No change made — verified before acting. | — |
| 14 | ~~Cave/dungeon lack point-light pools~~ **RESOLVED 2026-06-07** | 2026-06-07 biome sweep | `BattleMap3D.tsx` SceneLighting | Added biome accent point-lights: 4 cyan crystal pools (cave) / warm torch pools (dungeon) around map center, decay 2. pools-cave.png (cyan crystal glow + darkness between) and pools-dungeon.png (warm torchlit columns + long shadows) — real underground drama per GOAL #54/#170. | — |
| 15 | Class silhouettes — distance readability (GOAL #2) | 2026-06-07 teams capture; 2026-06-08 code review | `CharacterActor.tsx` archetype models (`:81-330`) | **Cues already exist** (not "every char identical"): `getArchetype` maps class→fighter/caster/rogue, each with distinct geometry — caster = pointed wizard hat + robes + tall staff w/ glowing orb; rogue = hood/cowl + dual daggers; fighter = half-sphere helmet + broadsword + shield. Scale 3.7× makes them prominent. **Residual is subjective tuning only:** at far tactical distance the cues are small and the three can still blur together. | Low-ROI polish: exaggerate the existing cues (taller hat, wider shield/shoulders) or add a thin rim-light/outline so class reads at distance. Not "missing models" — refinement. **2026-06-08: an autonomous before/after attempt was abandoned** — the snap-to-active camera + random seed can't deterministically frame fighter+caster+rogue together at a comparable distance, and forest occludes (silh-before-forest.png). A silhouette-distance change is both subjective and unverifiable with current capture tooling, so per "every change needs before/after" it should NOT be made blind. Needs a human art call and/or a deterministic multi-archetype capture rig. Parked. |
| 16 | ~~Foliage occludes characters at low camera angles~~ **RESOLVED 2026-06-08** | 2026-06-07 teams/rings captures | `EzTreeLayer.tsx` leaf material | In forest the snap-to-active camera often lands under dense canopy that occludes the characters (occl-before-forest.png). **Fixed:** added a BG3-style camera-proximity fade to the leaf canopy material via `onBeforeCompile` — view-space depth (`-mvPosition.z`) drives `diffuseColor.a *= smoothstep(2.5, 7.0, depth)`, so foliage near the camera (the occluders) fades out while distant forest stays opaque. Pure GPU, no per-frame JS, instancing-safe. **Proof:** deterministic same-seed far/near capture — fade-far.png (foliage opaque at distance) vs fade-near.png (near canopy gone, character fully readable). **Residual (minor):** this is a camera-*proximity* fade, not a true camera-to-target cylinder cutaway, so foliage beside the camera also fades; acceptable for the occlusion case. Branch/trunk geometry is not faded (thin, not a wall). | Done. Tune NEAR/FAR (2.5/7.0) if play-testing wants more/less reveal; consider a true between-camera-and-target cutaway only if the proximity fade feels off. |
| 18 | ~~Squad members cluster within a team at spawn (GOAL #61/#62)~~ **RESOLVED in code, verified 2026-06-08** | 2026-06-07 overview capture | `useBattleMapGeneration.ts` getSpawnTiles `spreadTiles` (`:62-95`) | The original 2026-06-07 clumping observation no longer matches the code: `getSpawnTiles` now runs `spreadTiles` with `MIN_SEP = 2` (Chebyshev), claiming a tile only if no already-claimed tile is within 2 tiles, with a fallback fill for tight zones. **Verified:** eyetest-desert.png shows the enemy team spaced across the field with 1–2-tile gaps (a formation, not a clump); cross-team split also holds. Code was ahead of this gap entry. | Done. If play-testing wants tighter front-line/back-line roles, sort `spreadTiles` output by distance-to-enemy-zone; otherwise no change needed. |
| 19 | ~~Open-biome map edge shows a hard cliff/step into the apron~~ **MISDIAGNOSED → real residual RESOLVED 2026-06-08** | 2026-06-08 eye-test re-baseline | `TerrainMesh.tsx` `buildSkirtGeometry` (`:519-609`) + `skirtMaterial` (`:713`) | Root-caused: the terrain is **not** a floating sheet — `TerrainMesh` already renders a perimeter **skirt** (vertical panels from each edge vertex down to y=-1.0 + a bottom cap), so the edge is a solid mesa, which is the intended bounded-battle-map look (BG3-style). The genuine residual was that the skirt used a **uniform near-black** color (`0x2a1e12`) regardless of biome, so under desert sand it read as a dark void band (what I mistook for a cliff). **Fixed:** biome-tinted skirt color (desert→sandstone `0x7a5836`, cave/dungeon→stone-grey, forest/swamp→dark loam/peat). **Before/after:** edge-before-desert.png (dark edge band) → edge-after-desert.png (sandy cliff face blends with the sand). | Done. The bounded vertical edge itself is intended (tabletop combat area on a landscape apron); no further geometry change needed. |
| 20 | EzTreeLayer reads `mapData.biome` (nonexistent) → forest fallback; non-forest `BIOME_TREES` configs are dead | 2026-06-08, found while implementing #16 | `EzTreeLayer.tsx:135`; `battleMapGenerator.ts:95-102` placeObstacles | Same class of bug task 40 fixed in `BattleMap3D` (generator sets `mapData.theme`, not `.biome`); EzTreeLayer:135 reads `mapData.biome ?? 'forest'`. **Confirmed currently INERT:** `placeObstacles` only assigns `decoration==='tree'` for the **forest** biome — cave→stalagmite, dungeon→pillar, desert→cactus, swamp→mangrove (all rendered by `DecorationProps`, not `EzTreeLayer`). So EzTreeLayer only ever runs in forest, and the swamp/desert/cave/dungeon `BIOME_TREES` variant configs are dead code. Fixing the field read produces **no visible change** today. | Low value now: mirror the `m.biome ?? m.theme ?? 'forest'` read for correctness (cheap), OR delete the unused non-forest `BIOME_TREES` entries, OR (if biome-specific trees are wanted) have the generator emit `'tree'` tiles for swamp/etc. No before/after possible until the generator emits non-forest tree tiles. Parked. |
| 21 | Dark biomes (cave/dungeon) lose the battlefield to fog/darkness at overview (max zoom-out) | 2026-06-08 final eye-test sweep | `BattleMap3D.tsx` BIOME_LIGHTING cave/dungeon fogFar + ambient; OrbitControls maxDistance 35 | finaleye-cave.png / finaleye-dungeon.png (OVERVIEW=1, wheeled to ~maxDistance): the frame is almost entirely black — only a faint nameplate + accent point-lights survive. The dark-biome fog/ambient (tuned for the close combat camera, where lit-cave.png/pools-dungeon.png read fine) swallows everything at distance. Forest/desert/swamp stay readable at overview. Edge case — most play is at closer zoom. | Low priority: if overview readability matters, scale ambient/fogFar with camera distance in dark biomes, or clamp maxDistance lower for cave/dungeon. Verify it doesn't wash out the close-zoom mood. Parked per "record, don't fix". |
| 22 | Intermittent React "Maximum update depth exceeded" during BattleMapDemo 3D mount | 2026-06-09 world-context ridge-band pass | unknown — not yet isolated; surfaces in the BattleMapDemo 3D mount path | Appears only on **slow** capture mounts (7–8 `hasBattleMap` retries) *after* the world-context change; **absent** on fast mounts and on stashed-baseline captures. Rendering, camera posing, and screenshots all still succeed, so it is not an actual frozen loop. The code added this pass (DistantTerrain / SkyDome / camera-pose hook) contains no setState, so a deterministic loop from it is unlikely — more consistent with a pre-existing timing/resize-driven effect surfaced by the heavier scene's longer mount. | Needs isolation, not a one-line fix: rerun with React's dev component-stack to find the looping component (a resize handler / ResizeObserver / demo effect). Non-blocking — does not affect the visual output or the world-context proof, so not fixed during this visual pass. |
| 23 | ~~Desert distant ridge reads subtle — gentle dunes, no distinct mesas/buttes~~ **RESOLVED 2026-06-09** | 2026-06-09 world-context ridge-band pass | `terrain/DistantTerrain.tsx` desert `RIDGE_PROFILES.desert` + mesa shaping | The first-pass desert ridge removed the tan-void but the relief was gentle. **Fixed:** added a desert-only `mesa` flag — raised `amp` 10→15, lowered `freq` 0.05→0.034 (fewer/broader landforms), `rise` 0.28→0.24, `inner` 34→36, and a flat-top profile (`0.10 + 0.28*shaped + 0.62*smoothstep(0.46,0.62,shaped)`) that carves a low desert floor with distinct plateaus. **Before/after:** horizon-after-desert.png (gentle dunes) → horizon-mesa-desert.png (flat-topped mesas/buttes on the horizon). Desert-only change; other biomes use the unchanged rolling-hill branch. | — |
| 24 | Race visuals are skin + proportions + small cues only; team-colored armor still dominates the torso | 2026-06-09 race-aesthetics pass | `CharacterActor.tsx` HumanoidModel | The fighter/caster/rogue torso/helmet/legs are team-colored (gold/crimson) for everyone, so race shows only via head/hand skin, overall build, and the beard/horn cue. Reads clearly at close/lineup zoom (task 51 captures) but is subtle at far tactical zoom. **Intentional tradeoff** — keeping armor team-colored preserves the strong friend/foe signal (GOAL #1). | Follow-up polish, not a defect: optionally tint cloth/leather (not plate) toward race, add race-distinct outfit accents, or a thin race-hued rim light. Should be weighed against not muddying team color. |
| 25 | A few enemy creature classes still share the **humanoid body plan** (Ooze/Aberration) | 2026-06-09 race-aesthetics pass; narrowed three times (creature/quadruped/dragon passes) | `CharacterActor.tsx`, enemy gen | **Addressed:** `stats.size` scales the model (task 53), Undead = gaunt bone-pale, goblins small+green (53), **Beast = quadruped** `BeastModel` (55), **Dragon = winged** `DragonModel` (56). **Still open:** Ooze/blob and Aberration have no distinct geometry — they fall back to the upright humanoid. Also race/creature skin reads subtly at far tactical zoom because team-colored armor dominates the torso (see gap #24). | Follow-up: add a blob/ooze plan (and any aberration form) via the established `RaceVisual.form` + dedicated-model pattern. Verify with `CREATURELINEUP=1` + `poseTeam`. |
