import { GameState } from '../types';
import { AppAction } from '../state/actionTypes';
export declare const useTownCrierAnnouncements: (gameState: GameState, dispatch: React.Dispatch<AppAction>) => void;
