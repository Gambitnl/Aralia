import React from 'react';
import { Organization } from '../../types/organizations';
interface OrgOverviewProps {
    organization: Organization;
}
declare const OrgOverview: React.FC<OrgOverviewProps>;
export default OrgOverview;
