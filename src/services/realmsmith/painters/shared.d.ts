export declare const TILE_SIZE = 32;
/**
 * Robust rounded-rectangle path builder with clamping and Safari fallback.
 */
export declare function roundedRect(ctx: CanvasRenderingContext2D, x: number, y: number, w: number, h: number, r: number): void;
