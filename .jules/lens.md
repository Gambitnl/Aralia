## 2025-12-15 - AI Decision Visibility **Learning:** The combat AI (`combatAI.ts`) was a black box. It calculated scores for actions but never logged *why* it chose one action over another, making debugging "stupid" AI behavior impossible without stepping through code. **Action:** Added structured logging to `evaluateCombatTurn` to show considered plans, scores, and the final decision. Future AI logic should log its "reasoning" at debug level.
