## 2024-05-21 - Factory Pattern Efficacy
**Learning:** `src/utils/factories.ts` provides robust mocks (`createMockPlayerCharacter`, `createMockItem`) that handle complex nested structures (like class proficiencies) required for utility testing.
**Action:** Always check `factories.ts` before creating custom mocks to ensure type compliance and reduce boilerplate.
