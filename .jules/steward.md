## 2024-05-24 - [Combat State Decomposition] **Learning:** `useTurnManager` has become a "God Hook" mixing scheduling, engine logic, and AI. **Action:** Decomposed turn ordering into `useTurnOrder`, but `useTurnManager` still holds too much engine state. Future refactors should extract `useCombatEngine` to handle effects, damage, and zones, leaving `useTurnManager` as a pure coordinator.
